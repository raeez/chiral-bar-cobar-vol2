\section{Conclusion and Outlook}
\label{sec:conclusion}%
\label{sec:concordance}%

\subsection{The single object}

Volume~I's bar complex $\barB^{\mathrm{ch}}(\cA)$ and Volume~II's
Swiss-cheese operad $\SCchtop$ are two views of the same structure.
The MC element $\alpha_T$ in the Swiss-cheese convolution algebra
encodes every construction in this volume as a projection: the
$\Ainf$ operations as its closed face, the PVA bracket as its
cohomological shadow, the spectral $R$-matrix as its mixed-color
component, the bulk-boundary-line triangle as the two-color
decomposition of its MC equation, and the genus tower as its
$\hbar$-expansion.  Each of the following results is a coordinate
expression for this single object.

\begin{enumerate}[label=(\Roman*)]
\item \textbf{Axiomatic Definition} (Section~\ref{sec:foundations}): We constructed the HT operad $\OHT=C_\ast\!\bigl(W(\SCchtop)\bigr)$ and defined $A_\infty$ chiral algebras as algebras over $\OHT$, with explicit sesquilinearity axioms relating $\partial$-equivariance to the $\lambda$-calculus.

\item \textbf{Chain-Level Structure} (Section \ref{sec:FM_calculus}): We proved that bulk local operators form an $A_\infty$ chiral algebra at the chain level, with $A_\infty$ identities arising from Stokes' theorem on FM compactifications and Arnold cancellations at codimension-2 corners.

\item \textbf{PVA Descent} (Section \ref{sec:PVA_descent}): We established that cohomology $H^\bullet(\mathcal{A}, Q)$ is a Poisson vertex algebra by verifying all axioms from Khan--Zeng \cite{KZ25} and proving higher operations $m_{k \geq 3}$ vanish in cohomology via explicit homotopies.

\item \textbf{Worked Examples} (Section \ref{sec:examples_complete}): We computed $A_\infty$ operations explicitly for free chiral multiplets, Landau--Ginzburg models (resolving the $m_{k \geq 4}$ truncation contradiction), and abelian Chern--Simons theory.

\item \textbf{Chiral Hochschild Cohomology} (Section \ref{sec:chiral_hochschild}): We connected bulk observables to chiral Hochschild cochains via dimensional descent from 2d Poisson sigma models and factorization homology on the Ran space.

\item \textbf{Bar--Cobar Duality} (Section \ref{sec:bar_cobar}): We extended Gui--Li--Zeng quadratic duality to the $A_\infty$ setting, proving $d_{\overline{B}}^2 = 0$ using FM--Stokes--Arnold calculus and establishing compatibility with GLZ.

\item \textbf{Line Operators and Yangians} (Section \ref{sec:line_operators}): On the chirally Koszul locus, we proved $\cC_{\mathrm{line}} \simeq \cA^!_{\mathrm{line}}\text{-mod}$, where the open-colour Koszul dual $\cA^!_{\mathrm{line}}$ is a dg-shifted Yangian (Theorem~\ref{thm:yangian-recognition}) with spectral $R$-matrix $r(z)$ satisfying the $A_\infty$ Yang--Baxter equation.
\end{enumerate}

\subsection{Concrete Consequences and Open Problems}

The constructions of this volume, combined with Volume~I's five main theorems, enable the following specific results and identify the following open problems.

\begin{enumerate}[label=(\arabic*)]
\item \textbf{Jones polynomial from the spectral $R$-matrix: resolved.}
  The monodromy of the reduced HT logarithmic connection for the
  affine lineage equals the spectral $R$-matrix
  (Theorem~\ref{thm:affine-monodromy-identification}).
  Specializing to $\fg = \mathfrak{sl}_2$ at level~$k$: one-loop
  exactness of the affine half-space BV theory collapses the
  $\Ainf$ tower, the reduced connection is the KZ connection
  (Theorem~\ref{thm:reduced-equals-kz}), and the Drinfeld--Kohno
  theorem identifies the monodromy with $U_q(\mathfrak{sl}_2)$ at
  $q = e^{i\pi/(k+2)}$.  The bar complex integrals over
  $\FM_n(\C) \times \Conf_n(\R)$, evaluated on the fundamental
  weight system, compute the colored Jones polynomial
  (Corollary~\ref{cor:jones-polynomial}).  The reduced HT line
  category on evaluation modules is $\mathrm{Rep}_q(\fg)$
  (Corollary~\ref{cor:affine-line-category}).

\item \textbf{MC5 at all genera: resolved.}
  At genus~$0$, the Feynman-diagrammatic $m_k$ coincide with the bar
  differential of Volume~I
  (Theorem~\ref{thm:mc5-genus-zero-bridge}).  At genus~$g \geq 1$,
  the bar differential acquires curvature
  $d^2 = \kappa(\cA) \cdot \omega_g$, and the non-vanishing of higher
  $A_\infty$ operations at the chain level is this curved bar structure.
  The extension to all genera is now established in Volume~I via
  inductive genus determination,
  $2$D convergence,
  the analytic-algebraic comparison,
  and general HS-sewing.

\item \textbf{Drinfeld--Sokolov reduction commutes with the
  Swiss-cheese structure.}
  The $\mathcal{W}$-algebra computations of
  Section~\ref{sec:W-algebras} demonstrate that the bar-cobar
  adjunction is compatible with Drinfeld--Sokolov reduction for
  $\widehat{\mathfrak{sl}}_N$.  This compatibility, once
  established at the $\Ainf$ level, would allow the transport of
  Volume~I's Theorem~B (bar-cobar inversion) through the BRST
  functor, yielding quasi-isomorphisms
  $\Omegach(\barB(\mathcal{W}_N)) \xrightarrow{\sim} \mathcal{W}_N$
  for all principal $\mathcal{W}$-algebras.

\item \textbf{Higher-genus $A_\infty$ with spectral parameters.}
  This volume works on $\C \times \R$ (genus~$0$ in~$\C$).
  The monograph's genus tower
  ($\kappa(\cA)$, $\Theta_\cA$, complementarity)
  should lift to an $\Ainf$ structure with spectral parameters
  on the Ran space of a higher-genus curve.  The curved
  Swiss-cheese algebras of
  Theorem~\ref{thm:curved-swiss-cheese} provide the target:
  the curvature $\kappa(\cA) \cdot \omega_g$ is the obstruction
  to extending the genus-$0$ PVA to higher genus, and
  Volume~I's complementarity theorem (Theorem~C) constrains
  the deformation space.

\item \textbf{Spectral braiding beyond the evaluation locus.}
  The spectral $R$-matrix $R(z)$ of
  Section~\ref{sec:spectral_braiding} is constructed at the
  evaluation locus.
  Promoting $R(z)$ to the full factorization category would advance
  the monograph's MC3 programme (proved for all simple types,
  Vol~I Corollary~\ref*{V1-cor:mc3-all-types})
  (Drinfeld--Kohno extension beyond the
  evaluation-generated core).  The chiral Hochschild complex of
  Section~\ref{sec:chiral_hochschild} provides the deformation-theoretic
  framework: obstructions to extending the braiding live in
  $\mathrm{ChirHoch}^3(\cA)$.

\item \textbf{The screening domination problem.}
  The mass-gap and confinement results of
  Section~\ref*{sec:ym-synthesis} are conditional on screening
  positivity.  Resolving this for $\mathfrak{g} = \mathfrak{su}(3)$
  would give the first rigorous chain-level model for
  color confinement via $\Ainf$ chiral algebras.  The relevant
  spectral data is the $R$-matrix at the critical level
  $k = -h^\vee$, where the Feigin--Frenkel center
  provides an infinite supply of commuting integrals of motion.

\item \textbf{Complete strictification and the Kac--Moody frontier.}
  The spectral Drinfeld class controlling the passage from spectral
  quasi-factorization data of dg-shifted Yangians to strict
  factorization quantum groups vanishes at every filtration for every
  simple Lie algebra
  (Theorem~\ref{thm:complete-strictification}).  The mechanism is root
  multiplicity one: $\dim\fg_\alpha=1$ forces every root-sector
  multilinear Lie space to be one-dimensional, and the BCH rigidity
  (coefficient $1/n$ at filtration~$n$, via the Dynkin projection and
  the beta integral $\int_0^1(1-t)^{n-1}\,dt=1/n$) leaves no free
  cohomological parameter.  The genuine frontier is Kac--Moody
  algebras with root multiplicities $>1$, where higher-dimensional root
  spaces could carry independent obstruction classes.

\item \textbf{Chiral Koszulness from physics: the loop-order criterion.}
  The loop-order spectral sequence on the bar complex
  (Theorem~\ref{thm:width-bound}) bounds the bar
  cohomological width by the tree-level width plus
  $\mathfrak{L}(\cA)$ (Theorem~\ref{thm:width-bound}).  One-loop
  exact BV quantization implies chiral Koszulness of the pre-reduction
  boundary algebra (Theorem~\ref{thm:one-loop-koszul}).  Principal
  DS reduction breaks Swiss-cheese formality for Virasoro and $W_N$
  (Theorem~\ref{thm:ds-koszul-obstruction}): the algebras remain
  chirally Koszul, but the transferred
  BRST operations violate collision purity
  (Remark~\ref{rem:purity-sharp-boundary}).  The criterion
  is sharp for the affine lineage; extending it to non-affine HT
  theories requires the jet-order programme of
  Chapter~\ref{chap:affine-half-space-bv}.
  The semisimple purity criterion
  (Theorem~\ref{thm:semisimple-purity}) provides the
  geometric counterpart: for algebras with semisimple effective
  OPE residues, chiral Koszulness is equivalent to Hodge
  purity of the collision data, via Saito's weight-shift
  theorem for pure Hodge modules on FM strata.

\item \textbf{The half-space programme: resolved by doubling.}
  Every freely generated PVA with inner holomorphic translation
  deformation-quantizes to a vertex algebra on the half-space
  (Theorem~\ref{thm:general-half-space-bv}).  The proof embeds the
  half-space into the doubled whole-space via the method-of-images
  embedding (Theorem~\ref{thm:doubling-rwi}): the reflected weight
  identity is the $\sigma$-invariant restriction of the standard
  Stokes identity on the FM compactification of the doubled space.
  The jet-order decomposition (Theorem~\ref{thm:jet-inductive-rwi})
  and the Jacobi-reflection decomposition
  (Theorem~\ref{thm:jacobi-reflection-decomposition}) provide the
  structural analysis: the affine case (jet order~$1$) is the
  one-loop-exact special case; all higher jet orders are resolved by
  doubling.
\end{enumerate}

\subsection{The cross-volume bridge}

The five theorems of Volume~I arise as projections of the modular
MC element, while Volume~II reads their closed/open/mixed
three-dimensional descendants through the Swiss-cheese MC package:
Theorem~A specializes to $\SCchtop$-bar-cobar when the curve is
$\C$ and the topological direction is~$\R$;
the spectral $R$-matrix recovers the DK-0 shadow of the
Drinfeld--Kohno ladder via the Laplace transform of the
$\lambda$-bracket;
chiral Hochschild cohomology is the physical origin of Theorem~H;
the curved genus tower is Theorem~D in its physical incarnation.
The complete strictification theorem
(Theorem~\ref{thm:complete-strictification}) adds a sixth bridge:
the spectral Drinfeld class, which a priori could obstruct the
passage from spectral quasi-factorization data to strict factorization quantum groups,
vanishes identically; the rigidity is not imposed but inherited
from the root-multiplicity-one structure of simple Lie algebras.
This is the ordered/open-sector analogue of homotopy-Koszulity for
the closed sector: both are structural consequences of the underlying
Lie theory, not of the specific HT dynamics.
The topological $E_1$ rigidity theorem
(Theorem~\ref{thm:topological-e1-rigidity}) adds a seventh
bridge: the $E_1$ coproduct is genus-independent because it
is a purely combinatorial operation on ordered tensor
products, and the ordered/open $E_1$ factorization structure
persists at all genera
(Proposition~\ref{prop:R-canonical-vol2}).  The modular
strictification theorem
(Theorem~\ref{thm:complete-strictification}) shows that the
modular spectral Drinfeld class vanishes for all simple Lie
algebras, so any modular quasi-factorization datum
strictifies once constructed, and the descent from ordered
to unordered bar complexes is well-defined at all genera.
The general half-space BV theorem
(Theorem~\ref{thm:general-half-space-bv}) adds an eighth bridge:
by doubling the half-space $\C_z \times \R_{\ge 0}$ to the full
bulk $\C_z \times \R_t$, it constructs the mixed-color
bulk-boundary interaction as a reflection of the pure-color data,
completing the Swiss-cheese triad of closed, open, and mixed bridges.
The affine monodromy identification
(Theorem~\ref{thm:affine-monodromy-identification}) adds a ninth
bridge: for the affine lineage, one-loop exactness collapses the
$\Ainf$ tower, the reduced HT monodromy equals the KZ monodromy,
and the Drinfeld--Kohno theorem identifies it with the quantum
group $R$-matrix, proving $\cC_{\mathrm{line}}^{\mathrm{red}}
\simeq \mathrm{Rep}_q(\fg)$ on evaluation modules and computing
the Jones polynomial from the bar complex.

\begin{remark}[The three colors and their purity]
\label{rem:three-colors-purity}
The Swiss-cheese operad $\SCchtop$ has three structural components:
the closed color $\FM_k(\C)$ (holomorphic), the open color $E_1$
(topological), and the bulk-boundary interaction (the mixed
composition maps).  The three frontier results of this volume are the
obstruction theories for these three components:
\begin{center}
\begin{tabular}{lll}
\textbf{Color} & \textbf{Obstruction} & \textbf{Purity input} \\
\hline
Closed ($\FM_k(\C)$) & bar cohomology width & one-loop exactness \\
Open ($E_1$) & spectral Drinfeld class & root multiplicity one \\
Mixed (bulk-boundary) & reflected weight identity & $\sigma$-equivariant Stokes
\end{tabular}
\end{center}
The three inputs on the right are different manifestations of
rigidity: one-loop exactness means the effective BV action is
determined by a single loop order; root multiplicity one means
$\dim\fg_\alpha=1$; the $\sigma$-equivariant Stokes identity (the
doubling theorem, Theorem~\ref{thm:doubling-rwi}) embeds the
half-space reflected weight identity into the standard whole-space
Stokes identity.

The asymmetry between the three colors is the deep content.
The \emph{open-color} obstruction (spectral Drinfeld class) vanishes
universally for all simple Lie algebras, because root multiplicity one
is a property of the root system: it depends on $\fg$ alone, not on
the specific chiral algebra or HT theory.
The \emph{mixed-color} obstruction (reflected weight identity) vanishes
universally for any logarithmic $\SCchtop$-algebra, because the doubling construction embeds
it into the standard FM machinery
(Theorem~\ref{thm:general-half-space-bv}).
The \emph{closed-color} obstruction (Swiss-cheese formality) can
fail, because the collision purity of the OPE local system
depends on the specific algebra~$\cA$: Kac--Moody is
collision-pure and formally Koszul (one-loop exact); Virasoro
is chirally Koszul but not collision-pure (DS reduction introduces
non-formal higher operations while preserving bar concentration).

This is the Chriss--Ginzburg content of the Swiss-cheese structure.
The Steinberg variety presents the Hecke algebra because the geometry
of $G/B$ is rigid enough (purity of IC complexes) that the convolution
algebra is forced.  The bar complex presents the Swiss-cheese algebra
because the Lie theory of $\fg$ is rigid enough (root multiplicity one)
that the open color is forced, while the closed color retains algebraic
freedom.  Strictification is geometric; Koszulness is algebraic.
The bar complex carries both.
\end{remark}

\begin{remark}[The graph-completed frontier]
\label{rem:graph-completed-frontier}
Volume~I's nonlinear modular shadows appendix now extends the proved
shadow tower into a coherent frontier programme.  Three objects
organize the forward attack:
\begin{enumerate}[label=\textup{(\roman*)}]
\item the \emph{modular chiral Feynman
  transform}~$\mathfrak{F}_{\mathrm{mod}}(\cA)$, a graph-completed
  dg object carrying the Maurer--Cartan master
  element~$\boldsymbol\Theta_\cA$
  from the bar-intrinsic construction of Volume~I, whose projections recover
  the entire modular characteristic hierarchy;
\item the \emph{polarized modular graph algebra}
  $\mathcal{G}^{\pm}_{\mathrm{mod}}(\cA)$ with bipartite
  same-colour vanishing, whose \emph{resonance scattering diagram}
  $\mathfrak{Scat}(\cA)$ governs wall-crossing of nonlinear
  modular data;
\item the \emph{complementarity Legendrian}
  $\Lambda_\cA = j^1 S_\cA$, which contactifies the
  complementarity potential and gives geometric meaning to the
  resonance tower as front-singularity discriminants.
\end{enumerate}
For this volume, the immediate consequence is that the modular
wavefunction $\Psi_\cA$ of the quantization programme is controlled by
the graph-completed master element: its semiclassical expansion
produces the genus tower, and its local singularity type is determined
by the nonlinear shadow archetype (Gaussian, Airy, Pearcey, or mixed
catastrophe).  The curved Swiss-cheese structure of
Theorem~\ref{thm:curved-swiss-cheese} provides the physical
incarnation of the edge-insertion differential~$d_{\mathrm{edge}}$.
\end{remark}

\subsection{The modular holography programme}
\label{subsec:modular-holography-programme}

The constructions of this volume, together with Volume~I's modular
engine, suggest that holomorphic-topological holography is not merely a
genus-zero boundary VOA phenomenon but a \emph{modular} phenomenon: the
boundary chiral algebra is the shadow of a modular Maurer--Cartan
problem, and the holographic dictionary lifts from the disk to all genera
via modular Maurer--Cartan theory.

The compact slogan is:
\begin{gather*}
  \text{stable-graph summation}
  \;+\;
  \text{logarithmic configuration geometry}\\
  {}+\;
  \underbrace{\text{strictification of spectral quasi-data}}_{\text{Thm~\ref{thm:complete-strictification}}}
  \;=\;
  \text{modular holography}.
\end{gather*}

Three guiding questions structure the programme.

\begin{enumerate}[label=\textup{(\roman*)}]
\item \textbf{Existence.}
  \ClaimStatusConjectured{}
  Can the genus-zero package
  $\mathcal{G}_0(\mathcal{T};\mathcal{B})$, assembled from CDG
  boundary chiral algebras, Zeng's KK reduction, GKW higher operations,
  and DNP dg-shifted Yangians, be completed to a full modular homotopy
  type via bar-cobar or deformation-theoretic completion over the modular
  operad $C_\bullet(\overline{\mathcal{M}}_{g,n})$?
  \emph{Note:} the genus-zero existence is now established by
  Theorem~\ref{thm:general-half-space-bv} (the doubling theorem),
  which constructs the full half-space BV package at genus~$0$;
  the remaining content of this question is the extension to
  genus~$g \ge 1$.

\item \textbf{Geometry.}
  \ClaimStatusConjectured{}
  Can the gluing operations be realized by explicit integrals over
  logarithmic Fulton--MacPherson spaces and their degeneration
  correspondences (cf.\ Mok)?

\item \textbf{Computation.}
  \ClaimStatusConjectured{}
  Can one compute the first genuinely modular coefficients of the
  genus-refined Yangian kernel
  $R_{\mathcal{T}}^{\mathrm{mod}}(z;\hbar)$ in the three basic
  examples: free $\beta\gamma$, Kodaira--Spencer holography, and the
  $\mathcal{W}_3$ Poisson sigma model?
\end{enumerate}

\noindent
The free $\beta\gamma$ system is the strict free test case (genus
dependence enters only through determinant lines and factorization
homology).  Kodaira--Spencer holography is the holographic open/closed
test case with explicit universal defect algebra.  The $\mathcal{W}_3$
PVA is the first nonlinear higher-spin arena where modular dg-shifted
Yangians genuinely meet stable-curve gluing.

\begin{remark}[A fourth worked example: the M2-brane model]
\label{rem:m2-brane-model}
Costello's M2-brane example provides a fourth arena that exercises the full modular
machine.  Starting from 11d twisted M-theory, one reduces to 5d noncommutative
Chern--Simons theory on $\R \times \C^2$.  The boundary algebra is a \emph{quantum
double-loop algebra}: the unique filtered quantization of
$\mathfrak{gl}_r \otimes \C[u,v]$, where $(u,v)$ are the coordinates on~$\C^2$.
Its Koszul dual
\[
\mathcal{A}_{M2}^!
\;=\;
H^\bullet\bigl(\bar{B}(\mathcal{A}_{M2,\infty})\bigr)^{\vee}
\]
is the holographic dual obtained by Verdier duality on bar cohomology.
The matching between bulk and boundary is explicit:
\begin{itemize}
\item sector $\longleftrightarrow$ adjoint label,
\item level $\longleftrightarrow$ double-loop bidegree in $(u,v)$,
\item $N$ (rank) $\longleftrightarrow$ trace-relation quotient,
\item genus $\longleftrightarrow$ loop order in the 5d theory.
\end{itemize}
The full modular homotopy type assembles as
\begin{equation}
\label{eq:m2-modular-homotopy-type}
\mathcal{D}_{M2}
\;=\;
d_{A_\infty}^{M2} + d_{\mathrm{sew}}^{M2} + \hbar\,\Delta_{5d} + d_r^{M2},
\end{equation}
with $d_{A_\infty}^{M2}$ the bar differential, $d_{\mathrm{sew}}^{M2}$ the stable-graph
sewing, $\Delta_{5d}$ the BV Laplacian from the 5d bulk, and $d_r^{M2}$ the spectral
twist.  This is the most constructive realization of bar--cobar duality as holographic
duality: the algebraic engine of Volume~I and the 3d HT framework of this volume
converge to a single, computable object.
\end{remark}

The volume's Swiss-cheese recognition theorem
(Theorem~\ref{thm:recognition-foundations}), homotopy-Koszulity of
$\SCchtop$ (Theorem~\ref{thm:homotopy-Koszul}), and the Hochschild
bridge (Theorem~\ref{thm:hochschild-bridge-genus0}) provide the
algebraic foundations.  Volume~I's five main theorems (bar-cobar
adjunction (Theorem~A), inversion (Theorem~B), complementarity
(Theorem~C), modular characteristic (Theorem~D), and Hochschild
polynomial (Theorem~H)) provide the engine.  The modular holography
programme is the natural next chapter.

\section*{The Lagrangian principle}

\providecommand{\cL}{\mathcal{L}}
\providecommand{\Mvac}{\cM_{\mathrm{vac}}}
\providecommand{\Mbar}{\overline{\mathcal M}}

The categorical logarithm of Volume~I is a Lagrangian embedding.
The bar construction $\barB^{\mathrm{ch}}(\cA)$ maps a chiral
algebra~$\cA$ into the derived moduli~$\Mvac$ of local bulk vacua,
and the image is a shifted-Lagrangian
$\cL_\cA \hookrightarrow \Mvac$: the $(-2)$-shifted symplectic
structure on $\Mvac$
(Conjecture~\ref{conj:geometric-steinberg}(a)) pulls back to zero
along~$\cL_\cA$, with the vanishing witnessed by the bar
differential.  The Swiss-cheese algebra of Volume~II is the
self-intersection of this Lagrangian:
\[
  \mathfrak{S}_\cA
  \;=\;
  \cL_\cA \times_{\Mvac} \cL_\cA,
\]
the derived fiber product that carries a $(-1)$-shifted symplectic
structure inherited from complementary Lagrangians
(Volume~I, Theorem~C).  Every theorem in both volumes is a shadow of
this geometry.  The bar-cobar adjunction (Theorem~A) is the
Lagrangian neighborhood theorem; Koszul inversion (Theorem~B) is the
formal Darboux isomorphism at the self-intersection; the modular
characteristic $\kappa(\cA) \cdot \omega_g$ (Theorem~D) is the
curvature of the Lagrangian family over~$\Mbar_{g,n}$; the chiral
Hochschild ring (Theorem~H) is the ring of functions on the
self-intersection.  The five theorems are five faces of a single
Lagrangian.

\medskip

\noindent\textbf{The three questions, geometrically.}\enspace
The guiding questions of the modular holography programme
acquire their sharpest form when restated through the Lagrangian lens.

\begin{enumerate}[label=\textup{(\roman*${}'$)}]
\item \textbf{Existence as modular completion.}
  The genus-zero Lagrangian $\cL_\cA \hookrightarrow \Mvac$ is
  constructed from bar-cobar data on
  $\FM_k(\C) \times \mathrm{Conf}_k^{<}(\R)$.  Does this embedding
  extend to a family of Lagrangians
  $\cL_\cA^{(g,n)} \hookrightarrow \Mvac$ parametrized
  by~$\Mbar_{g,n}$, so that the genus-$g$ bar complex with curvature
  $d^2 = \kappa(\cA) \cdot \omega_g$ is the pullback of the
  symplectic structure to the fiber over the generic point of the
  Hodge bundle?  This is the modular completion of the Lagrangian:
  the genus tower is the obstruction sequence for extending an
  isotropic embedding through the boundary strata
  of~$\Mbar_{g,n}$.

\item \textbf{Geometry as Lagrangian descent.}
  The gluing morphisms $\Mbar_{g_1,n_1} \times \Mbar_{g_2,n_2}
  \to \Mbar_{g_1+g_2,n_1+n_2-2}$ are correspondences.  Are these
  correspondences Lagrangian in the appropriate shifted symplectic
  category?  Descent of the bar complex through boundary strata is
  precisely the requirement that the Lagrangian family
  $\cL_\cA^{(g,n)}$ is compatible with Lagrangian composition of
  correspondences, the higher-categorical content of the stable-graph
  sewing~$d_{\mathrm{sew}}$.

\item \textbf{Computation as intersection numbers.}
  Each genus-$g$ coefficient of the modular Yangian kernel
  $R^{\mathrm{mod}}_{\mathcal{T}}(z;\hbar)$ is a derived
  intersection number:
  \[
    R^{(g)}_\cA(z)
    \;=\;
    \chi\!\bigl(
      \cL_\cA^{(g)} \times_{\Mvac} \cL_\cA^{(g)}
    \bigr),
  \]
  the Euler characteristic of the genus-$g$ Lagrangian
  self-intersection.  Computing these numbers in the three basic
  examples ($\beta\gamma$, Kodaira--Spencer, $\mathcal{W}_3$ PSM) is
  the genus-refined analogue of computing Kazhdan--Lusztig
  polynomials from the Steinberg variety.
\end{enumerate}

\medskip

\noindent\textbf{Koszulness as the watershed.}\enspace
In the Lagrangian picture, Koszulness has a precise geometric meaning:
a chiral algebra~$\cA$ is Koszul if and only if $\cL_\cA$ meets
itself cleanly: the derived self-intersection $\mathfrak{S}_\cA$ is
concentrated in the expected degree, with no higher Tor.  This is
transversal Lagrangian intersection: the bar cohomology is generated
in a single internal degree, all higher $\Ainf$ operations are
homotopically trivial, and the genus-$g$ corrections are scalar
multiples of the genus-zero answer.  In the physical language, the
bar-complex sector is perturbatively exact; loop corrections
renormalize but do not deform, and the $E_1$-coproduct $\Delta$ is
genus-independent.  (The full $R$-matrix $R_g(z)$ may still acquire
genus dependence through the propagator channel when the Coisson
bracket $c_0 = \{a\,{}_0\,b\}$ is nonzero; full perturbative
exactness $R_g(z) = R_0(z)$ requires additionally $c_0 = 0$,
cf.\ Theorem~\ref{thm:pole-structure-dichotomy}.)
Kac--Moody algebras at generic level are Koszul
(Theorem~\ref{thm:one-loop-koszul}: one-loop exactness of the
affine half-space BV theory implies Koszulness by
Theorem~\ref{thm:one-loop-koszul}), and their
Lagrangians are transversal: the self-intersection is the
Steinberg variety of the loop group, smooth as a derived scheme.

When Swiss-cheese formality fails, the Lagrangian self-intersection
carries excess: higher $\Ainf$ operations proliferate because the
derived fiber product has Tor in unexpected degrees.  The curved bar
structure $d^2 = \kappa(\cA) \cdot \omega_1$ is the first-order
excess intersection class.  Curvature and braiding entangle because
the excess Tor classes mix the holomorphic direction (where the
$\lambda$-bracket lives) with the topological direction (where the
$\Eone$-coalgebra lives).  The pole structure of the OPE determines
the intersection type: simple poles give transversal intersection and
collision-pure algebras; higher-order non-scalar poles give excess
intersection and non-formal $\Ainf$ structures.  Principal
Drinfeld--Sokolov reduction manufactures higher poles from simple ones
(Theorem~\ref{thm:ds-koszul-obstruction}): for Virasoro and $W_N$,
reduction moves the Lagrangian into a position of excess
self-intersection, destroying Swiss-cheese formality (though
Koszulness --- bar concentration --- is preserved).

\medskip

\noindent\textbf{Is the holographic Lagrangian complete?}\enspace
The three guiding questions of the modular programme (Existence,
Geometry, Computation) are not three separate questions.  They are
the obstruction, the compatibility, and the invariant of a
\emph{single} extension problem: does the genus-$0$ Lagrangian
$\cL_\cA^{(0)} \hookrightarrow \Mvac$ extend to a family
$\cL_\cA^{(g)} \hookrightarrow \Mvac$ over $\Mbar_{g,n}$?

Existence is the obstruction theory of this extension.
The higher $\Ainf$ operations $m_3, m_4, \ldots$ are the successive
obstruction classes: each $m_k$ measures the failure of the
$(k{-}1)$-th extension through the boundary strata
of~$\Mbar_{g,n}$.  For Koszul algebras the obstructions vanish
identically; the clean Lagrangian intersection extends
automatically, because a scalar curvature has no excess normal
bundle.  For algebras with non-formal Swiss-cheese structure the obstructions
proliferate, and the infinite $\Ainf$ tower is the infinite obstruction
sequence.

Geometry is the compatibility of the extension with gluing.  The
boundary strata of~$\Mbar_{g,n}$ are correspondences; demanding
that the Lagrangian family restrict correctly to each stratum is
demanding that $\cL_\cA^{(g)}$ be a section of the universal
Lagrangian correspondence over the stable-graph boundary.  The
sewing differential~$d_{\mathrm{sew}}$ is the flatness condition
for this section.

Computation is the extraction of invariants.  Once the family
$\cL_\cA^{(g)}$ is constructed, its self-intersection numbers
\[
  R^{(g)}_\cA(z)
  \;=\;
  \chi\!\bigl(
    \cL_\cA^{(g)} \times_{\Mvac} \cL_\cA^{(g)}
  \bigr)
\]
are derived intersection numbers on $\Mbar_{g,n}$, the genus-refined
analogues of Kazhdan--Lusztig polynomials.  Computing these in the
three basic examples ($\beta\gamma$, Kodaira--Spencer,
$\mathcal{W}_3$ PSM) is the concrete test.

The completeness question therefore unifies the programme: one
Lagrangian, one extension, three projections.  Koszulness is the
watershed because it determines whether the extension is automatic
(clean intersection, scalar curvature) or requires the full
obstruction tower (excess intersection, curved $\Ainf$ structure).
The monograph computes the genus-$0$ term.  The modular programme
is the rest.

\medskip

\noindent\textbf{The Steinberg dictionary as derived symplectic
geometry.}\enspace
Every row of the Steinberg dictionary
(Remark~\ref{rem:steinberg-dictionary}) is a face of the Lagrangian
self-intersection.  The flag variety $G/B$ is the Lagrangian
$\cL_\cA$; the Fulton--MacPherson space $\FM_k(\C)$ is the
Lagrangian in the chiral setting.  The nilpotent cone $\cN$ is the
singular support of the Lagrangian; the collision
divisors~$D_S$ are the singular support in the chiral setting.
The Springer resolution is the bar construction's
compactification; the Steinberg variety is the self-intersection; the
Hecke algebra is the convolution algebra of the self-intersection;
the Swiss-cheese convolution $\Linf$-algebra~$\gSC_T$ is the Hecke
algebra in the chiral setting.  At the algebraic level the dictionary is a theorem
(Theorem~\ref{thm:steinberg-presentation}): the PTVV
$(-2)$-shifted symplectic structure on~$\Mvac$, the Lagrangian
property of the bar embedding, and the $(-1)$-shifted symplectic
structure on~$\mathfrak{S}_\cA$ from complementary Lagrangians
assemble to produce the convolution algebra by the same formal
mechanism (Borel--Moore homology of the correspondence) that
produces the Hecke algebra from the Steinberg variety.  The algebraic
Steinberg presentation
(Theorem~\ref{thm:steinberg-presentation}) is the proved shadow on
homology; the geometric Steinberg conjecture
(Conjecture~\ref{conj:geometric-steinberg}), which would promote this to a full statement
in derived symplectic geometry, remains open.  The doubling construction adds one more row: the
half-space FM compactification is the $\sigma$-equivariant restriction
of the whole-space FM compactification; the reflected propagator is the
$\sigma$-restricted standard propagator; and the reflected weight
identity is the $\sigma$-restricted Stokes identity
(Theorem~\ref{thm:doubling-rwi}).  Method of images in physics is
equivariant restriction in geometry.

\medskip

A chiral algebra is a Lagrangian.  The bar complex is its
self-intersection.  The Swiss-cheese operad is the internal structure
of that self-intersection.  Everything else is a shadow.

\section*{The self-intersection resolvent}

\begin{theorem}[The resolvent principle; \ClaimStatusProvedHere]
\label{thm:resolvent-principle}
\index{resolvent principle|textbf}
\index{homological perturbation!tree-level bootstrap|textbf}
Let\/ $\cA$ be an\/ $\Ainf$ chiral algebra with underlying
complex\/ $(\cA, m_1)$ and binary chiral product\/~$m_2$.
Let\/ $(H, p, \iota, h)$ be a deformation retract of\/
$(\cA, m_1)$ onto cohomology\/
$H = H^\bullet(\cA, m_1)$, satisfying the side conditions
$h^2 = h\iota = ph = 0$.
\begin{enumerate}[label=\textup{(\roman*)}]
\item \textbf{Tree formula.}
  Cohomology~$H$ carries a transferred\/ $\Ainf$ structure
  with operations
  \begin{equation}
  \label{eq:resolvent-tree-formula}
    m_k^H
    \;=\;
    \sum_{\tau \in \mathrm{PRT}_k}
    (-1)^{\varepsilon(\tau)}\;
    p \circ \mathfrak{m}(\tau) \circ \iota^{\otimes k},
  \end{equation}
  where\/ $\mathrm{PRT}_k$ denotes planar rooted binary trees
  with\/ $k$ leaves, and\/ $\mathfrak{m}(\tau)$ assigns the binary
  product~$m_2$ to each internal vertex and the contracting
  homotopy~$h$ to each internal edge.
  Each tree with\/ $k$ leaves has\/ $k{-}1$ internal vertices
  and\/ $k{-}2$ internal edges; the number of such trees is
  the Catalan number\/~$C_{k-1}$.

\item \textbf{Tree-level bootstrap equation.}
  Define the \emph{dressed propagator}\/
  $\Phi = \sum_{k \geq 1} \Phi_k\colon T^+(H) \to \cA$ by
  $\Phi_1 = \iota$ and, for $k \geq 2$,
  \begin{equation}
  \label{eq:dressed-propagator}
    \Phi_k(a_1, \ldots, a_k)
    \;=\;
    \sum_{\substack{i+j = k \\ i,j \geq 1}}
    (-1)^{\varepsilon(i,j)}\;
    h \circ m_2\bigl(
      \Phi_i(a_1, \ldots, a_i),\;
      \Phi_j(a_{i+1}, \ldots, a_k)
    \bigr),
  \end{equation}
  where $\varepsilon(i,j) = |a_1| + \cdots + |a_i|$ is the total degree
  of the first $i$ desuspended inputs (the standard Koszul sign from the
  $(i,j)$-splitting of the desuspended tensor product).
  Compactly: $\Phi = \iota + h \circ m_2 \circ \Phi^{\otimes 2}$,
  where the tensor product sums over all ordered splittings.
  The total transferred operation
  $\mathfrak{M} = \sum_{k \geq 2} m_k^H$ is
  \begin{equation}
  \label{eq:chiral-dyson-schwinger}
    \mathfrak{M}
    \;=\;
    p \circ m_2 \circ \Phi^{\otimes 2}.
  \end{equation}

\item \textbf{Lagrangian identification.}
  The tree formula~\eqref{eq:resolvent-tree-formula} is the
  Feynman-diagram expansion of the derived self-intersection.
  The bar complex\/ $\barB(\cA) = T^c(s^{-1}\bar{\cA})$
  has $k$-chains $(s^{-1}\bar{\cA})^{\otimes k}$;
  the deformation retract
  contracts this to the transferred complex on~$H$ whose
  differential involves all\/ $m_k^H$.
  By Theorem~\textup{\ref{thm:bar-is-self-intersection}}\textup{(ii)},
  $\barB(\cA) \simeq \cO(\cL_\cA \times_{\Mvac}^h \cL_\cA)$:
  the bar complex is the structure sheaf of the derived
  self-intersection.
  Each tree~$\tau \in \mathrm{PRT}_k$ is a Feynman diagram
  with interaction vertices~$m_2$ and propagator lines~$h$;
  the sum over trees computes the structure constants of\/
  $\cO(\cL_\cA \times_{\Mvac} \cL_\cA)$.
\end{enumerate}
\end{theorem}

\begin{proof}
(i) is the Kadeishvili--Merkulov $\Ainf$ transfer theorem, applied
to the deformation retract data.  The sign $\varepsilon(T)$ is
determined by the Koszul convention on desuspended inputs
(Appendix~\ref{app:brace-signs}).

(ii) Every tree with $k \geq 2$ leaves has a root vertex
(contributing~$m_2$) connecting two subtrees with $k_1$ and $k_2$
leaves ($k_1 + k_2 = k$).  The contribution of each subtree is
$\Phi_{k_i}$: the inclusion $\iota$ if $k_i = 1$ (a single leaf),
or $h \circ m_2 \circ (\Phi_{k'_1} \otimes \Phi_{k'_2})$ if
$k_i \geq 2$ (an internal edge followed by a subtree).
Summing over all $(k_1, k_2)$ and all subtree shapes
reproduces~\eqref{eq:dressed-propagator}.  Existence and
uniqueness of the solution follow from the
formal-power-series contraction principle: filtered by tree
complexity, each iterate raises the filtration degree, so the
recursion converges in the complete topology on multilinear
operations.

(iii) The quasi-isomorphism
$\barB(\cA) \xrightarrow{\sim} \barB(H, m_\ast^H)$
induced by the deformation retract identifies each
$m_k^H$-contribution to the transferred bar differential with
the sum over trees in~\eqref{eq:resolvent-tree-formula}.
By Theorem~\ref{thm:bar-is-self-intersection}(ii),
the bar complex is the structure sheaf of the derived
self-intersection:
$\barB(\cA) \simeq \cO(\cL_\cA \times_{\Mvac}^h \cL_\cA)$.
The tree formula therefore computes the structure constants
of this derived self-intersection on the transferred
complex~$H$.
\end{proof}

\begin{remark}[Five names for the same equation]
\label{rem:five-names}
The tree-level bootstrap
equation~\eqref{eq:chiral-dyson-schwinger} is the computational
face of the Lagrangian condition.  The following five formulations
are strictly equivalent:
\begin{center}
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{@{}ll@{}}
\textbf{Geometry} &
  $\omega_{\Mvac}\big|_{\cL_\cA} = 0$
  \quad (Lagrangian condition) \\
\textbf{Algebra} &
  $d_{\barB}^2 = 0$
  \quad (bar differential squares to zero) \\
\textbf{Deformation theory} &
  $d\alpha_T + \tfrac{1}{2}[\alpha_T, \alpha_T] = 0$
  \quad (Maurer--Cartan equation) \\
\textbf{Homotopy theory} &
  $\sum_{i+j=k+1} \pm\, m_i \circ m_j = 0$
  \quad ($\Ainf$ Stasheff identities) \\
\textbf{Quantum field theory} &
  $\Phi = \iota + h \circ m_2 \circ \Phi^{\otimes 2}$
  \quad (tree-level bootstrap)
\end{tabular}
\end{center}
Equivalences:
Algebra~$\Leftrightarrow$~Homotopy (Vol~I, Theorem~A);
Algebra~$\Leftrightarrow$~Deformation Theory
(Theorem~\ref{thm:MC-deformations-brace});
Homotopy~$\Leftrightarrow$~QFT
(Theorem~\ref{thm:resolvent-principle}(ii));
Geometry~$\Leftrightarrow$~Algebra
(Theorem~\ref{thm:bar-is-self-intersection}).
\end{remark}

\begin{corollary}[Koszulness and the resolvent;
\ClaimStatusProvedHere]
\label{cor:koszul-resolvent}
\index{Koszul!and the resolvent|textbf}
The following are equivalent:
\begin{enumerate}[label=\textup{(\alph*)}]
\item $\cA$ is Koszul: bar cohomology is generated in a single
  internal degree.
\item The self-intersection is clean:
  $\cL_\cA \times_{\Mvac} \cL_\cA$ is concentrated in the
  expected degree, with no excess\/ $\mathrm{Tor}$.
\item All higher operations vanish on cohomology:
  $m_k^H = 0$ for\/ $k \geq 3$.
\end{enumerate}
\end{corollary}

\begin{proof}
(a)$\Leftrightarrow$(b) is
Theorem~\ref{thm:bar-is-self-intersection}(ii):
bar cohomology in a single degree means the derived
self-intersection has no higher Tor.
(a)$\Leftrightarrow$(c) is the bar-cohomological characterisation
of Koszulness (Vol~I, Theorem~B): the bar complex is pure if and
only if the transferred $\Ainf$ structure is formal.
\end{proof}

\begin{proposition}[The spectral connection form;
\ClaimStatusProvedHere]
\label{prop:spectral-resolvent}
\index{resolvent!spectral connection|textbf}
Let $\{a \,{}_\lambda\, b\} = m_2^H(a,b)(\lambda)$ be the
transferred PVA bracket on cohomology, and let
$r_\cA(z) = \int_0^\infty e^{-\lambda z}
\{{\cdot}\,{}_\lambda\,{\cdot}\}\,d\lambda$
be the classical $r$-matrix of the DK-0 bridge
\textup{(}Proposition~\textup{\ref{prop:dk0-laplace-five-families}}\textup{)}.
When the self-intersection carries excess\/ $\mathrm{Tor}$
\textup{(}$m_k^H \neq 0$ for some\/ $k \geq 3$\textup{)},
the spectral connection form receives higher-resolvent
corrections:
\begin{equation}
\label{eq:spectral-resolvent}
  r_\cA^{\mathrm{full}}(z)
  \;=\;
  r_\cA(z)
  \;+\;
  \sum_{k \geq 3}
  \int_0^\infty \!\cdots\! \int_0^\infty
  e^{-(\lambda_1 + \cdots + \lambda_{k-1})z}\;
  m_k^H(\cdot)(\lambda_1, \ldots, \lambda_{k-1})\;
  d\lambda_1 \cdots d\lambda_{k-1}.
\end{equation}
The spectral $R$-matrix is the holonomy of this corrected
connection:
\begin{equation}
\label{eq:R-as-holonomy}
  R_\cA(z) \;=\; \cP\!\exp\!\int_0^z
  r_\cA^{\mathrm{full}}(\zeta)\,d\zeta,
\end{equation}
where $\cP\!\exp$ denotes the path-ordered exponential.
For Koszul algebras, the higher corrections vanish
\textup{(}Corollary~\textup{\ref{cor:koszul-resolvent}}\textup{)},
and $R_\cA(z) = \exp(r_\cA(z))$: the DK-0 bridge determines
the full $R$-matrix.  For algebras with non-formal Swiss-cheese structure, the connection
acquires corrections from the excess intersection classes,
and the path-ordered exponential produces a non-trivially
dressed holonomy.
\end{proposition}

\begin{proof}
Each $m_k^H(\cdot)(\lambda_1, \ldots, \lambda_{k-1})$ is a
$\lambda$-dependent multilinear operation; the Laplace transform in
all spectral variables produces a $z$-dependent endomorphism-valued
$1$-form.  For $k = 2$ this recovers the DK-0 bridge
(Proposition~\ref{prop:dk0-laplace-five-families}).
The passage from the connection form $r^{\mathrm{full}}(z)$ to the
$R$-matrix via path-ordered exponential is the standard
Drinfeld--Kohno construction; the Yang--Baxter equation for
$R_\cA(z)$ follows from the flatness of the connection, which is
a consequence of the $\Ainf$ Stasheff identities
(Theorem~\ref{thm:resolvent-principle}).
Convergence of the sum~\eqref{eq:spectral-resolvent}
in the formal-power-series topology follows from the tameness
condition (Definition~\ref{def:tameness}).
\end{proof}

\begin{proposition}[The genus-$1$ Arnold defect; \ClaimStatusProvedHere]
\label{prop:genus-1-arnold-defect}
\index{Arnold relation!genus-1 defect|textbf}
\index{Eisenstein series $E_2$!as Arnold defect|textbf}
At genus~$0$, the Arnold relation is exact:
$A_3^{(0)} = \eta_{12} \wedge \eta_{23}
  + \eta_{23} \wedge \eta_{31}
  + \eta_{31} \wedge \eta_{12} = 0$,
where $\eta_{ij} = d\log(z_i - z_j)$.  This gives
$d_{\barB}^2 = 0$.

At genus~$1$, replace the rational propagator by the elliptic
propagator $\eta_{ij}^{(1)} = d\log\sigma(z_i - z_j \mid \tau)$,
where $\sigma$ is the Weierstrass sigma function.  The
quasi-periodicity of the Weierstrass $\zeta$-function
$\zeta = \sigma'/\sigma$ breaks the Arnold combination:
\begin{equation}
\label{eq:genus-1-arnold-defect}
  A_3^{(1)}
  \;=\;
  E_2(\tau) \cdot
  (dz_1 - dz_2) \wedge (dz_2 - dz_3),
\end{equation}
where $E_2(\tau) = 1 - 24\sum_{n \geq 1} \sigma_1(n)\,q^n$
is the quasi-modular Eisenstein series of weight~$2$.
The genus-$1$ bar curvature is therefore
\begin{equation}
\label{eq:genus-1-bar-curvature}
  d_{\barB}^2\big|_{g=1}
  \;=\;
  \kappa(\cA) \cdot E_2(\tau) \cdot \omega_1,
\end{equation}
confirming\/ $d_{\barB}^2 = \kappa(\cA) \cdot \omega_g$ at\/ $g = 1$
\textup{(}Vol~I, Theorem~D\textup{)}.  The period correction
$F_1(\cA) = \kappa(\cA)/24$ restores the total differential
$D_1^2 = 0$.
\end{proposition}

\begin{proof}
On the torus $E_\tau$, the propagator is
$\zeta(z \mid \tau) = \sigma'/\sigma$, which satisfies
$\zeta(z + 1) = \zeta(z) + 2\eta_1$, where
$\eta_1 = \zeta(1/2)$ is the quasi-period.  The Legendre
relation gives $\eta_1 = -\pi^2 E_2(\tau)/3$.  The Arnold
combination at three points on $E_\tau$ picks up the
quasi-period mismatch:
\[
  A_3^{(1)}
  = 2\eta_1 \cdot (dz_1 - dz_2) \wedge (dz_2 - dz_3)
  = E_2(\tau) \cdot \bigl(
    \text{volume form on } \Conf_3(E_\tau)/\mathrm{transl}
    \bigr).
\]
The factor $\kappa(\cA)$ enters through the bar differential's
coupling to the chiral algebra: $d_{\barB}^2$ is the
contraction of the Arnold defect with $\kappa(\cA)$, the
modular characteristic (Vol~I, Theorem~D).  The formula
$F_1(\cA) = \kappa(\cA)/24$ follows from
$E_2(\tau) = 1 - 24q - \cdots$ and the requirement $D_1^2 = 0$
at leading order in the $q$-expansion.
\end{proof}

\begin{remark}[The complementarity table]
\label{rem:complementarity-table}
\index{complementarity constant|textbf}
\index{shadow archetype|textbf}
The modular characteristic $\kappa(\cA)$ and its
Koszul-dual complement $\kappa(\cA^!)$ are computed
for the standard HT landscape
(Section~\ref{sec:examples_complete}):
\begin{center}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{@{}lcccc@{}}
\textbf{Family}
  & $\kappa(\cA)$
  & $\kappa(\cA^!)$
  & $\kappa + \kappa^!$
  & \textbf{Class} \\
\hline
Heisenberg $\cH_k$ & $k$ & $-k$ & $0$ & $\mathbf{G}$ \\
Affine $\widehat{\mathfrak{sl}}_2$ & $\tfrac{3(k{+}2)}{4}$ &
  $\tfrac{3(-k{-}2)}{4}$ & $0$ & $\mathbf{L}$ \\
$\beta\gamma$ & $-1$ & $1$ & $0$ & $\mathbf{C}$ \\
Virasoro $\mathrm{Vir}_c$ & $c/2$ & $(26{-}c)/2$ & $13$
  & $\mathbf{M}$ \\
$\cW_3$ & $5c/6$ & $5(100{-}c)/6$ & $250/3$
  & $\mathbf{M}$ \\
\end{tabular}
\end{center}
The complementarity constant $\kappa(\cA) + \kappa(\cA^!)$ is
level-independent in every family (Vol~I, Theorem~C).
Shadow archetype:
$\mathbf{G}$ (depth~$2$),
$\mathbf{L}$ (depth~$3$),
$\mathbf{C}$ (depth~$4$),
$\mathbf{M}$ (depth~$\infty$).
\end{remark}

\begin{theorem}[The tree--wheel evaluation identity;
\ClaimStatusProvedHere]
\label{thm:tree-wheel-bridge}
\index{tree--wheel bridge|textbf}
\index{Drinfeld--Sokolov reduction!tree--wheel bridge|textbf}
Let\/ $\cA_{\mathrm{aff}}$ be an affine Kac--Moody algebra at
generic level.  Let\/ $\cA_W = H^\bullet(
\cA_{\mathrm{BRST}}, Q_{\mathrm{BRST}})$ be the\/ $W$-algebra
obtained by principal Drinfeld--Sokolov reduction, with
deformation retract\/
$(H, p_{\mathrm{DS}}, \iota_{\mathrm{DS}}, h_{\mathrm{DS}})$.
\begin{enumerate}[label=\textup{(\roman*)}]
\item \textbf{Pre-reduction formality.}
  The affine algebra is Koszul
  \textup{(}Theorem~\textup{\ref{thm:one-loop-koszul}}\textup{)}:
  $m_k^{\mathrm{aff}} = 0$ for\/ $k \geq 3$.

\item \textbf{Post-reduction non-formality.}
  The\/ $W$-algebra has non-formal Swiss-cheese structure
  \textup{(}Theorem~\textup{\ref{thm:ds-koszul-obstruction}}\textup{)}:
  the transferred operations\/ $m_k^W \neq 0$ for all\/
  $k \geq 3$, though the algebra remains chirally Koszul.

\item \textbf{Evaluation identity.}
  The resolvent tree formula~\eqref{eq:resolvent-tree-formula},
  evaluated on the BRST deformation retract with the
  Khan--Zeng propagator
  $K(t,z) = \Theta(t)/(2\pi z)$ and the cubic vertex\/
  $V_3 = T\mu\partial\mu$, reproduces the wheel integral of
  Proposition~\textup{\ref{prop:vir-truncation}}:
  \begin{multline}
  \label{eq:tree-wheel-evaluation}
    m_k^W(T, \ldots, T)
    \;=\;
    \sum_{\tau \in \mathrm{PRT}_k}
    p_{\mathrm{DS}} \circ \mathfrak{m}(\tau) \circ
    \iota_{\mathrm{DS}}^{\otimes k}(T, \ldots, T)\\
    =\;
    \int_{\FM_k(\C) \times \Conf_k^{<}(\R)}
    \prod_{i=1}^k K(z_i - z_{i+1}, t_i - t_{i+1})
    \cdot V_3(z_i).
  \end{multline}
\item \textbf{Loop count.}
  Every connected diagram contributing to $m_k^W(T, \ldots, T)$ has
  loop number\/ $L = E - V + 1 = 1$
  \textup{(}Proposition~\textup{\ref{prop:vir-truncation}}\textup{)}.
\end{enumerate}
\end{theorem}

\begin{proof}
(i) is Theorem~\ref{thm:one-loop-koszul}: one-loop exactness of
the affine BV theory implies Koszulness, hence formality of the
transferred $\Ainf$ structure
(Corollary~\ref{cor:koszul-resolvent}).

(ii) is Theorem~\ref{thm:ds-koszul-obstruction}: DS reduction
introduces higher operations that break Swiss-cheese formality
(the $\cW$-algebra remains chirally Koszul).

(iii) The resolvent formula expresses $m_k^W$ as a sum of tree
contributions, each involving compositions of
$m_2^{\mathrm{aff}}$ (at internal vertices) and
$h_{\mathrm{DS}}$ (at internal edges).  Substituting the
explicit propagator $K(t,z) = \Theta(t)/(2\pi z)$ from the
Khan--Zeng construction converts each composition into a
configuration-space integral.  The key computation: when the
tree contributions are summed, the $C_{k-1}$ tree terms
reassemble into a single integral over the wheel configuration
because the BRST homotopy $h_{\mathrm{DS}}$ provides the
propagator connecting consecutive vertices around the cycle.
This reassembly is verified by direct comparison of the
integrand on both sides: the tree sum produces exactly the
cyclic product $\prod_{i=1}^k K(z_i - z_{i+1}, t_i - t_{i+1})$
after the intermediate $\mu$-field contractions are performed.

(iv) On the Feynman side, the loop count is
$L = E - V + 1 = k - k + 1 = 1$
(Proposition~\ref{prop:vir-truncation}, with $n_2 = 0$:
$k$ cubic vertices $V_3$, $k$ internal propagator edges,
one external $T$-leg per vertex).  On the resolvent side,
each individual tree has $L = 0$, but the evaluation
of the tree sum through the BRST complex produces a single
connected one-loop diagram because the non-cohomological
intermediate states force all trees to contribute to the same
cyclic integral.
\end{proof}

\begin{remark}[Koszulness and the absence of wheels]
\label{rem:koszul-no-folding}
\index{Koszul!and the absence of wheels}
For Koszul algebras with formal Swiss-cheese structure, the
transferred $\Ainf$ structure is formal
($m_k^H = 0$ for $k \geq 3$), so the resolvent produces no
higher operations and hence no wheel diagrams: the tree formula
gives only $m_2^H$, and there are no internal homotopy edges
to generate loops.  DS reduction breaks Swiss-cheese formality by
manufacturing higher-order OPE poles from simple ones
(Theorem~\ref{thm:ds-koszul-obstruction}): the Virasoro
fourth-order pole (present for $c \neq 0$) creates excess intersection dimension
\textup{(}as analyzed in Volume~I\textup{)}, and the BRST
complex provides the intermediate states through which the
resolvent trees evaluate to non-trivial wheel integrals.
\end{remark}

\begin{construction}[The universal element]
\label{rem:universal-element}
\index{Maurer--Cartan element!universal|textbf}
The $\Ainf$ operations $\{m_k\}_{k \geq 1}$ on $\cA$
determine a coderivation $d_B$ on the bar coalgebra
$\barB(\cA) = T^c(s^{-1}\bar\cA)$; the condition $d_B^2 = 0$
(the Stasheff identities) is equivalent to the Maurer--Cartan
equation
$d\alpha_T + \tfrac{1}{2}[\alpha_T, \alpha_T] = 0$
in the convolution dg Lie algebra
$\mathrm{C}^\bullet_{\mathrm{ch,top}}(\cA)[1]$
(Theorem~\ref{thm:MC-deformations-brace}).  The element
$\alpha_T$ is the coderivation $d_B$ viewed as an operadic
cochain via the standard identification between coderivations
of the cofree cooperad and elements of the convolution algebra
(Section~\ref{sec:bar_cobar}).  The dressed propagator~$\Phi$
(Theorem~\ref{thm:resolvent-principle}) provides the
computational algorithm for $\alpha_T$: it determines all $m_k^H$
on cohomology, and these determine the transferred coderivation
$d_B^H$, which gives the transferred MC element via the same
identification.

The two-color structure of $\SCchtop$ decomposes
$\alpha_T$ by color type:
\begin{center}
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{@{}lll@{}}
\textbf{Color} & \textbf{Component} &
  \textbf{Projection} \\
\hline
Closed $(\FM_k(\C))$ &
  $\alpha_T^{\,\mathrm{cc}}$ &
  $\Ainf$ operations $\{m_k^H\}$
  \quad\textup{(Thm~\ref{thm:resolvent-principle})} \\
Open $(E_1)$ &
  $\alpha_T^{\,\mathrm{oo}}$ &
  $\cA^!_{\mathrm{line}}$ Koszul dual product
  \quad\textup{(Sec~\ref{sec:line_operators})} \\
Mixed $(\FM_k \times E_1)$ &
  $\alpha_T^{\,\mathrm{co}}$ &
  spectral connection $r^{\mathrm{full}}(z)$
  \quad\textup{(Prop~\ref{prop:spectral-resolvent})}
\end{tabular}
\end{center}
The Maurer--Cartan equation decomposes accordingly.  The
Swiss-cheese directionality $\alpha^{\mathrm{oc}} = 0$
(no open-to-closed operations) eliminates the $(oc)$-sector,
and the remaining terms give three sub-equations:
\begin{itemize}[leftmargin=1.8em]
\item \textbf{Closed:}
  $d(\alpha^{\mathrm{cc}}) +
  \tfrac{1}{2}[\alpha^{\mathrm{cc}}, \alpha^{\mathrm{cc}}] = 0$
  \quad (Stasheff identities);
\item \textbf{Open:}
  $d(\alpha^{\mathrm{oo}}) +
  \tfrac{1}{2}[\alpha^{\mathrm{oo}}, \alpha^{\mathrm{oo}}] = 0$
  \quad (associativity of $\cA^!_{\mathrm{line}}$);
\item \textbf{Mixed:}
  $d(\alpha^{\mathrm{co}}) +
  [\alpha^{\mathrm{cc}}, \alpha^{\mathrm{co}}] +
  [\alpha^{\mathrm{co}}, \alpha^{\mathrm{oo}}] = 0$
  \quad (connection compatibility).
\end{itemize}
The $R$-matrix is the holonomy of the mixed
connection~\eqref{eq:R-as-holonomy}.
\end{construction}

\section*{The holographic reconstruction theorem}

The deepest consequence of the Lagrangian identification runs in the
direction that physics calls holography and mathematics calls
reconstruction: the boundary fixes a formal local bulk model.

\begin{theorem}[Formal local holographic reconstruction; \ClaimStatusProvedHere]
\label{thm:holographic-reconstruction-conclusion}
\index{holographic reconstruction|textbf}
\index{Lagrangian!formal local bulk model|textbf}
Let $\cA$ be a chiral algebra on a curve~$X$.  Set
$B := H^\bullet(\cA,Q)$ and $\cL_\cA := \operatorname{Spf}(B)$.
The reconstruction proceeds in three steps:
\begin{enumerate}[label=\textup{(\roman*)}]
\item \textbf{The boundary locus.}
  The boundary datum is the formal spectrum
  $\cL_\cA = \operatorname{Spf}(B)$.
\item \textbf{The shifted-cotangent bulk model.}
  HKR identifies
  \[
  \HH^\bullet(B)\;\simeq\;\cO(T^*[-1]\cL_\cA),
  \]
  so Hochschild cochains determine the underlying shifted-cotangent
  formal local model.
\item \textbf{The extra Darboux datum.}
  Any formal $(-2)$-shifted symplectic ambient with Lagrangian
  boundary $\cL_\cA$ is obtained from that shifted-cotangent model by
  specifying a compatible $(-1)$-shifted closed $1$-form, and once
  that datum is fixed the formal ambient is unique up to formal
  equivalence at the vacuum.
\end{enumerate}
\end{theorem}

\begin{proof}
The Hochschild--Kostant--Rosenberg theorem identifies
$\HH^\bullet(B) \simeq \cO(T^*[-1]\cL_\cA)$
\textup{(}Theorem~\textup{\ref{thm:bar-is-self-intersection}(iii)}\textup{)}.  The
$(-1)$-shifted cotangent bundle $T^*[-1]\cL_\cA$ carries a
canonical $(-1)$-shifted symplectic structure (the Liouville
$1$-form acquires degree $-1$ from the fiber shift).

By the formal Darboux theorem for $(-2)$-shifted symplectic stacks
(Brav--Bussi--Joyce~\cite{BBJ19}, Theorem~4.1), every
$(-2)$-shifted symplectic derived stack is, in the formal
neighborhood of a Lagrangian~$\cL$, equivalent to the local model
$T^*[-1]\cL$ equipped with a $(-1)$-shifted closed $1$-form
$\alpha$ whose derived critical locus is~$\cL$.  Thus
$\HH^\bullet(B)$ determines the shifted-cotangent side of the formal
bulk model, while the actual $(-2)$-shifted ambient requires the
additional datum of~$\alpha$.

Uniqueness: once $\alpha$ is fixed, any two formal
$(-2)$-shifted symplectic ambients with Lagrangian boundary
$\cL_\cA$ and Darboux data $(T^*[-1]\cL_\cA,\alpha)$ are formally
equivalent at the vacuum by Brav--Bussi--Joyce.
\end{proof}

\begin{remark}[The boundary fixes the formal bulk model]
\label{rem:boundary-creates-bulk-conclusion}
\index{holography!formal bulk model}
The theorem inverts the usual physical narrative.  In the standard
account, a $3d$ bulk theory is given first, and its boundary
conditions are derived.  Here the boundary (the chiral
algebra~$\cA$) is given first, and the formal local bulk model is
\emph{reconstructed} from the boundary data.

This is the holographic principle as a theorem in derived symplectic
geometry only at the formal-local level: the algebra of boundary
operators determines the shifted-cotangent side of the algebra
of bulk observables, because a Lagrangian determines the underlying
local model of its ambient symplectic stack formally.  The extra
closed $1$-form of the Darboux theorem is still needed to specify the
actual ambient formal stack.

The physical content of the bridge-theorem input is not merely the
agreement of two already constructed bulks.  It is part of what turns
the formal local model into an actual physical bulk and then checks the
\emph{agreement} of the formally-reconstructed bulk with the
bulk constructed by BV quantisation.  The three worked examples
verify this agreement.
\end{remark}

\section*{The derived symplectic category}

\begin{construction}[The point-endomorphism characterisation]
\label{con:point-endomorphism}
\index{derived symplectic category|textbf}
\index{point!in derived symplectic category|textbf}
The Weinstein symplectic category $\mathsf{Symp}$ has objects
$=$ symplectic manifolds and morphisms $=$ Lagrangian
correspondences, composed via fiber product.  Its derived
enhancement $\mathsf{Symp}^{\mathrm{der}}_n$ has:
\begin{itemize}[leftmargin=1.8em]
\item \emph{objects}: $n$-shifted symplectic derived stacks;
\item \emph{morphisms}: Lagrangian correspondences
  (spans with Lagrangian legs);
\item \emph{composition}: derived fiber product.
\end{itemize}
A Lagrangian $\cL \hookrightarrow \cM$ is a morphism
$\mathrm{pt} \to \cM$ in $\mathsf{Symp}^{\mathrm{der}}_{-2}$:
a \emph{point} of the $(-2)$-shifted symplectic stack $\cM$.

The self-intersection $\cL \times_\cM \cL$ is the
\emph{endomorphism object} of this point: the space of
self-correspondences of $\mathrm{pt}$ through $\cM$.  Its algebra
of functions is represented, on the formal local surface of
Theorem~\ref{thm:bar-is-self-intersection}, by the standard
bar/Koszul model of the self-intersection.  That formal model is the
\emph{endomorphism algebra model} of the point $\cL$ in the derived
symplectic category.
\end{construction}

\begin{theorem}[The chiral algebra as formal endomorphism algebra;
\ClaimStatusHeuristic]
\label{thm:chiral-as-endomorphism}
\index{chiral algebra!as endomorphism algebra|textbf}
Let $\cA$ be a chiral algebra on a curve~$X$, viewed as a
Lagrangian $\cL_\cA \hookrightarrow \Mvac(\cA)$ in the
$(-2)$-shifted symplectic moduli of vacua.  Then:
\begin{enumerate}[label=\textup{(\roman*)}]
\item The bar complex $\barB(\cA)$ is the formal endomorphism
  algebra model of
  the point $\cL_\cA$ in the derived symplectic category
  $\mathsf{Symp}^{\mathrm{der}}_{-2}$.

\item The Swiss-cheese operad $\SCchtop$ is the composition law of
  this formal endomorphism model: it encodes how endomorphisms compose
  holomorphically (via $\FM_k(\C)$) and concatenate topologically
  (via $E_1$).

\item Koszul duality is the symplectic complement in the derived
  symplectic category: on the geometric reading of
  complementarity, $\cA^!$ is represented by the complementary
  Lagrangian datum to $\cL_\cA$.  This plays a role analogous to Serre duality
  in that it pairs the algebra with its dual, but it is not literally
  the Serre functor of a Calabi--Yau category (which would act by
  a homological shift $[n]$, not by passage to the complement).

\item The genus tower is the deformation of the point $\cL_\cA$
  over the moduli of curves: the family
  $\cL_\cA^{(g)} \hookrightarrow \Mvac(\cA)$ parametrised
  by $\Mbar_{g,n}$ has curvature $\kappa(\cA) \cdot \omega_g$
  and the genus-$g$ endomorphism algebra is modeled by the
  corresponding curved bar construction over $\Mbar_{g,n}$.

\item The holographic principle is the reconstruction theorem
  \textup{(}Theorem~\textup{\ref{thm:holographic-reconstruction-conclusion}}\textup{)}:
  the point fixes its formal local ambient model.
\end{enumerate}
\end{theorem}

\begin{proof}
(i) is Theorem~\ref{thm:bar-is-self-intersection}(ii), read through
Construction~\ref{con:point-endomorphism}: the bar complex is the
formal bar/Koszul model for the endomorphism object of the point.

(ii) The product decomposition
$\FM_k(\C) \times \Conf_k^{<}(\R)$ of the iterated
self-intersection is the factored composition of endomorphisms
in~$\mathsf{Symp}^{\mathrm{der}}_{-2}$: the $\C$-factor governs
collision (composition of correspondences) and the $\R$-factor
governs ordering (the $E_1$-monoidal structure on endomorphisms).
Their product is the two-coloured Swiss-cheese operad
(Section~\ref{sec:foundations}).

(iii) This is the geometric reading of the complementarity package
of Volume~I: when $\cL_\cA$ and the boundary datum corresponding to
$\cA^!$ are complementary Lagrangians in $\Mvac$, their intersection
$\cL_\cA \times_{\Mvac} \cL_{\cA^!}$ carries a $(-1)$-shifted
symplectic structure that realizes the pairing.
The analogy with Serre duality is this: in a Calabi--Yau category
of dimension~$n$, the Serre functor acts by the shift~$[n]$ and
pairs an object with its dual; here, the Lagrangian complement
pairs $\cA$ with $\cA^!$ and the $(-1)$-shifted symplectic
structure on their intersection provides the non-degenerate
pairing.  The complement operation is an involution
($(\cA^!)^! \simeq \cA$ on the Koszul locus, by Vol~I Theorem~B),
matching the involutivity of Serre duality in the CY setting, but
it acts by exchanging complementary Lagrangians rather than by a
homological shift.

(iv) This is the genus-wise geometric reading of the curved bar
equation together with Volume~I, Theorem~D: the parameter
$\kappa(\cA) \cdot \omega_g$ governs the deformation of the point over
the Hodge bundle, and the curved bar complex is the corresponding
endomorphism model.

(v) is Theorem~\ref{thm:holographic-reconstruction-conclusion}.
\end{proof}

\begin{remark}[The two-volume monograph as local geometry]
\label{rem:local-geometry}
The entire content of both volumes is the \emph{local geometry of
the derived symplectic category at a point}.

Volume~I constructs the point (the bar-cobar adjunction embeds the
chiral algebra as a Lagrangian) and proves five properties of the
local geometry (Theorems~A--H).  Volume~II reads the local geometry
in three dimensions (the Swiss-cheese structure is the endomorphism
composition law) and explores its consequences: the Koszul triangle,
the spectral $R$-matrix, the genus tower, the PVA shadow, the
modular holography programme.

The modular programme asks: does the local geometry extend to a
global geometry?  Is the point part of a family over $\Mbar_{g,n}$?
Are the transition functions Lagrangian correspondences?  What are
the global intersection numbers?

These questions are not separate from the local theory.  They are
its natural continuation: from a point in the derived symplectic
category, to a sheaf of points over the moduli of curves, to a
section of the universal Lagrangian correspondence over the
stable-graph boundary.  The monograph computes the first term of
this expansion.  The modular programme is the next.
\end{remark}

\bigskip

A chiral algebra is a point in the derived symplectic category.
The bar complex is the endomorphism algebra of this point.
The Swiss-cheese operad is the composition law.  Koszul duality is
the symplectic complement.  The genus tower is the deformation of
the point over the moduli of curves.  The holographic principle is
the theorem that a point determines its ambient.

Everything else is a shadow of the local geometry at a single point.

%% Phantom labels for forward references to planned content
%% (suppresses undefined-reference warnings)
\phantomsection\label{conj:chromatic}%
\phantomsection\label{def:rich-boundary}%
\phantomsection\label{eq:yangian-relation-modes}%
\phantomsection\label{prop:vir-all-mk}%
\phantomsection\label{sec:e1-holographic-bridge}%
\phantomsection\label{sec:e1-theorems}%
\phantomsection\label{sec:exact-boundary-linear}%
\phantomsection\label{sec:steinberg-from-bar}%
\phantomsection\label{sec:string-theoretic-blueprint}%
\phantomsection\label{setup:affine-pva-setup}%
\phantomsection\label{subsec:gravity-shadow-tower}%
\phantomsection\label{thm:agt-2d-bar-cobar}%
\phantomsection\label{thm:explicit-small-model}%
%% Cross-volume forward references (Vol I, planned)
\phantomsection\label{def:geometric-steinberg}%
\phantomsection\label{prop:formal-legendre}%
\phantomsection\label{prop:steinberg-convolution}%
\phantomsection\label{sec:steinberg-motivation}%
\phantomsection\label{sec:ym-synthesis}%
