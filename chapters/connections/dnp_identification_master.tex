% =========================================================================
% Vol II Master Chapter: The seven faces of r(z) in 3d HT QFT
% =========================================================================

\chapter{The seven faces of $r(z)$ in 3d holomorphic-topological QFT}
\label{ch:dnp-identification-master}

The collision residue
$r(z)=\operatorname{Res}^{\mathrm{coll}}_{0,2}(\Theta_{\cA})$ of an
$\SCchtop$-algebra $\cA$ is the common genus-zero binary datum behind
seven presentations: the bar--cobar twisting morphism, DNP25's
$A_\infty$ Yang--Baxter Maurer--Cartan element on line operators, the
classical $r$-matrix from the boundary PVA, the leading pole of GZ26
commuting Hamiltonians, the Yangian $r$-matrix, the Sklyanin Poisson
bracket, and the Gaudin Hamiltonian.  On the common comparison locus
specified below these presentations identify the same class.  Outside
that locus the needed genus, associator, level, Lie-type, and
normalization hypotheses are theorem data.

\section{The 3d HT collision residue}
\label{sec:dnp-master-collision-residue}

Let $\cA$ be a logarithmic $\SCchtop$-algebra
\textup{(}Definition~\textup{\ref{def:log-SC-algebra}}\textup{)}. The bar complex
$\barB(\cA)$ on the configuration space $\FM_n(\bC)\times\Conf_n(\bR)$
carries a Maurer--Cartan element $\Theta_\cA = D_\cA - d_0$
\textup{(}Vol~I, Theorem~\textup{\ref{V1-thm:mc2-bar-intrinsic}}\textup{)}, whose
binary collision residue is the central object of this chapter:
\begin{equation}\label{eq:dnp-master-collision-residue}
r(z)
\;:=\;
\operatorname{Res}^{\mathrm{coll}}_{0,2}(\Theta_\cA)
\;\in\;
\bar{\cA}\,\widehat{\otimes}\,\bar{\cA}((z^{-1})).
\end{equation}
By the $\mathrm d\log$ absorption (the bar propagator
$\mathrm d\log(z-w)$ absorbs one power of $(z-w)$, so the
collision-residue pole order is one less than the OPE pole order),
the maximal pole order of $r(z)$ is $k_{\max}(\cA) = p_{\max}(\cA) - 1$, where
$p_{\max}$ is the maximal OPE pole among generating fields
\textup{(}Vol~I, Definition~\textup{\ref{V1-def:p-max}}\textup{)}. For the standard
landscape:
\begin{itemize}
\item Heisenberg: $p_{\max}=2$, so $r(z) = k/z$ (single pole).
\item Affine $\widehat{\fg}_k$: $p_{\max}=2$.  The trace-form
 presentation is $r_{\mathrm{tr}}(z)=k\,\Omega/z$.  The KZ/Sugawara
 presentation at non-critical level is
 $r_{\mathrm{KZ}}(z)=\Omega/((k+h^\vee)z)$.  The level-prefixed KZ
 representative is $r_{\mathrm{KZ,lev}}(z)=k\,\Omega/((k+h^\vee)z)$.
 These are $\mathrm{GRT}^{\mathrm{fin}}$-orbit representatives related
 by finite Casimir rescalings on the locus where the displayed scalars
 are defined (Remark~\ref{rem:grt-casimir-fm99}).  At $k=0$ the
 trace-form and level-prefixed representatives vanish; the
 KZ/Sugawara representative does not.  The relation is a tensor
 identity between representatives, not a scalar equality such as
 $k=1/(k+h^\vee)$.
\item $\beta\gamma$: $p_{\max}=1$, so $r(z) = 0$ in the classical sense
 (the simple pole is fully absorbed); the shadow depth $r_{\max}=4$
 arises from composite fields.
\item Virasoro: $p_{\max}=4$, so
 $r(z) = (c/2)/z^3 + 2T/z$ (cubic plus simple poles).
\item $\Walg_3$: $p_{\max}=6$, so $r(z)$ has poles up to $z^{-5}$ in the
 $W$-$W$ channel.
\end{itemize}

\section{Face 1: Swiss-cheese bar-cobar twisting}
\label{sec:face-1-bar-cobar}

The first face of $r(z)$ is the bar-cobar twisting morphism.
The chiral bar-cobar adjunction
\textup{(}Theorem~\textup{\ref{thm:scchtop-bar-cobar-adjunction}}\textup{)} produces a
universal twisting morphism
$\pi_\cA \in \mathrm{Tw}(\barBch(\cA), \cA)$, and the binary collision
residue is the explicit form of this twisting morphism evaluated on the
degree-$2$ component:
\[
r(z)
\;=\;
\pi_\cA\big|_{\mathrm{degree}\,2,\,\mathrm{collision}}.
\]
This is the algebraic definition of $r(z)$ from which all other faces
descend.

\section{Face 2: Line operators as $\cA^!_{\mathrm{line}}$-modules}
\label{sec:face-2-line-operators}

The second face is the elevated identification of line operators with
modules over the Koszul dual. The full statement is
Theorem~\ref{thm:dnp-bar-cobar-identification} of
\S\ref{subsec:dnp-identification} (in chapters/connections/line-operators.tex).
Recalling its three parts:

\begin{enumerate}[label=\textup{(\roman*)}]
\item \textbf{Monoidal identification.} Under the equivalence
 $\cC_{\mathrm{line}} \xrightarrow{\sim}
 \cA^!_{\mathrm{line}}\text{-}\mathbf{mod}$, the meromorphic tensor
 product $\ell_1 \otimes_z \ell_2$ corresponds to the $R$-twisted
 tensor product $V_{\ell_1} \otimes_{R(z)} V_{\ell_2}$.
\item \textbf{Maurer--Cartan identification.} The DNP $A_\infty$
 Yang--Baxter MC element $r(z) \in \cA^! \otimes \cA^!$ is the
 collision residue $\operatorname{Res}^{\mathrm{coll}}_{0,2}(\Theta_\cA)$.
\item \textbf{Non-renormalization $=$ Koszulness.} The one-loop
 exactness of line-operator OPE \textup{(\cite[Thm.\,4.1]{DNP25})} is
 the $E_2$-collapse of the bar spectral sequence (chiral Koszulness).
\end{enumerate}

This face is the 3d HT physical interpretation: the line-operator
category captures $r(z)$ from the boundary perspective.

\begin{remark}[DNP identification: cohomology, strict chains, and Lie type]
\label{rem:dnp-identification-lanes}
\index{Dimofte--Niu--Py!chain-level comparison}
\index{Dimofte--Niu--Py!type scope}
The identification
Theorem~\ref{thm:dnp-bar-cobar-identification} has three distinct
lanes, each used as a hypothesis in
Theorem~\ref{thm:vol2-seven-faces-master}:
\begin{enumerate}[label=\textup{(\roman*)}]
\item \textbf{Cohomological and module lane.}
On the completed perturbative, boundary-linear, chirally Koszul locus
of Theorem~\ref{thm:lines_as_modules}, the line category is equivalent
to modules over $\cA^!_{\mathrm{line}}$, the DNP meromorphic tensor
product is the $R$-twisted tensor product, and the DNP
$A_\infty$ Yang--Baxter Maurer--Cartan element is the binary collision
residue.  This is the cohomological content of
\cite[Thm.\,4.1, Thm.\,5.5, Thm.\,6.1]{DNP25} together with
Theorem~\ref{thm:dnp-bar-cobar-identification}; it uses the
$E_2$-collapse of the bar spectral sequence and needs no associator.
\item \textbf{Strict chain lane.}
The strict identification of the meromorphic-tensor chain model of
\cite{DNP25} with the $R$-twisted ordered-bar chain model requires a
fixed Drinfeld associator
$\Phi\in\mathrm{GRT}_1(\bQ)$.  With this choice, the
Kontsevich--Tamarkin formality
morphism
$\varphi_\Phi : C_*\bigl(\FM_\bullet(\bC)\bigr) \xrightarrow{\sim}
E_2$
transports the chain-level DNP model to the chain-level bar-cobar
model.  Without this choice the canonical object is the
$\infty$-quasi-isomorphism class; strict chain-level equality lives in the
$\mathrm{GRT}_1(\bQ)$-torsor of Remark~\ref{rem:grt-torsor-3d-ht}.
\item \textbf{Lie-type and evaluation lane.}
The perturbative DNP construction is not a type-$A$ statement only: its
3d HT gauge-theory examples are formulated for a Lie algebra
$\fg$ and matter representation $V$.  The type restriction enters
when Face~2 is identified with the standard Drinfeld/Yangian/Gaudin
faces used below.  The theorem uses the type-$A$ evaluation core
\textup{(}\(\mathfrak{gl}_N\), \(\mathfrak{sl}_N\);
Theorem~\ref{thm:recognition-completed-dg-shifted-yangian}\textup{)}
and the affine non-critical KZ/Gaudin comparison.  Simply-laced and
folded classical types require the explicit Dynkin-folding compatibility
between the line-operator OPE and the $R$-twisted coproduct.  Exceptional
types require a Guay--Regelskis--Wendlandt Yangian ambient
\cite{GRW18} together with a DNP OPE comparison, and are not part of the
face-identification clause below.
\end{enumerate}
Thus Face~2 in Theorem~\ref{thm:vol2-seven-faces-master} means the
cohomological/module lane on the completed perturbative boundary-linear
locus, or the strict chain lane after a fixed associator, with the
Drinfeld/Yangian/Gaudin comparison restricted to the Lie-type lanes just
listed.
\end{remark}

\section{Face 3: Classical PVA on the 3d HT boundary
\textup{(}genus~$0$ only\textup{)}}
\label{sec:face-3-classical-pva}

\begin{remark}[Scope restriction inherited from Vol~I]
\label{rem:face-3-genus-zero}
Face~3 is a \emph{genus~$0$} clause. The Vol~I anchor
Theorem~\ref{V1-thm:kz-classical-quantum-bridge} proves the
classical/quantum identification at tree level on $\bP^1$; the
Vol~II master theorem~\ref{thm:vol2-seven-faces-master} therefore
states the PVA comparison only over $\cM_{0,n}$.  A genus-$g\geq 1$
comparison requires the logarithmic Fulton--MacPherson clutching maps
and the KZB/elliptic $r$-matrix comparison as additional theorem data.
\end{remark}

The third face is the classical $r$-matrix from the boundary PVA, via
KZ25's 3d holomorphic-topological Poisson sigma model. The PVA
$\lambda$-bracket on the boundary algebra $\cP$ produces a classical
$r$-matrix
\[
r^{\mathrm{cl}}(z)
\;=\;
\sum_{\alpha,\beta} c_0(e^\alpha,e^\beta)\,e_\alpha\otimes e_\beta\,/\,z
\;+\;\text{higher poles},
\]
via Gui--Li--Zeng quadratic duality \cite{GLZ22}. The full statement is
Vol~I, Theorem~\ref{V1-thm:kz-classical-quantum-bridge}: at tree level
on $\bP^1$ \textup{(}genus~$0$\textup{)}, $r(z) = r^{\mathrm{cl}}(z)$,
and the gauge invariance of the KZ25 sigma model is the
$\lambda$-Jacobi identity, which is equivalent to $d_{\barB}^2 = 0$ via
the Arnold relation.  For genus $g\geq 1$ the corresponding
identification requires a KZB connection, an elliptic or higher-genus
$r$-matrix, and compatibility with clutching in logarithmic
Fulton--MacPherson compactifications; those hypotheses are not included
in this face.

For Heisenberg, affine KM, and Virasoro at \emph{genus~$0$}, the
classical $r$-matrix extracted from the PVA $\lambda$-bracket agrees
with the bar-complex collision residue at leading order; quantum
corrections appear only in class M.

\section{Face 4: Bulk Hamiltonians from the sigma model}
\label{sec:face-4-bulk-hamiltonians}

The fourth face is the leading pole of GZ26's commuting differential
operators on the genus-$0$ moduli space. The full statement is Vol~I,
Theorem~\ref{V1-thm:gz26-commuting-differentials}. The shadow connection
$\nabla^{\mathrm{hol}}_{0,n} = d - \operatorname{Sh}_{0,n}(\Theta_\cA)$
decomposes into commuting Hamiltonians
$\operatorname{Sh}_{0,n}(\Theta_\cA) = \sum_i H_i\,dz_i$, and the
leading $1/z_{ij}$ coefficient in $H_i$ is exactly $r(z_{ij})$ acting on
$V_j$. In the 3d HT setting, this is the dimensional reduction of the
3d sigma-model Hamiltonian to the genus-$0$ moduli space.

The operator order of $H_i$ is $k_{\max}-1$ for $k_{\max}\geq 1$, and
$0$ for $k_{\max}=0$ \textup{(}Theorem~\ref{V1-thm:shadow-depth-operator-order}\textup{)}.
The trichotomy:
\begin{itemize}
\item $k_{\max}=0$: trivial Hamiltonians ($\beta\gamma$).
\item $k_{\max}=1$: scalar/multiplication operators (Heisenberg, KM).
\item $k_{\max}\geq 3$: genuine differential operators of order $k_{\max}-1$ (Virasoro, $\Walg_N$).
\end{itemize}

\begin{remark}[Principal-\(\cW_N\) normalization obligation]
\label{rem:vol2-gz26-wn-normalization}
The existence and commutativity of $\cW_N$ Hamiltonians from
$\Theta_{\cW_N}$ are inherited from
Vol~I, Theorem~\ref{V1-thm:gz26-commuting-differentials}(v). The
term-by-term comparison with the operators of \cite{GZ26} requires an
explicit dictionary between the principal $\cW_N$ generators, the
Calogero--Moser Hamiltonian normalization, and the collision-depth
coefficients of $\Theta_{\cW_N}$.  Face~4 below includes the
existence, commutativity, and leading-collision construction; it does
not include the term-by-term $\cW_N$ identification until that
normalization dictionary is supplied.
\end{remark}

\section{Face 5: The dg-shifted Yangian}
\label{sec:face-5-dg-shifted-yangian}

The fifth face is the dg-shifted Yangian of \cite{DNP25}, with
$r(z)$ as the universal $r$-matrix. In our framework, the dg-shifted
Yangian is the boundary-colour Koszul dual $\cA^!_{\mathrm{line}}$ on the
chirally Koszul locus, with multiplication, coproduct, counit, and
$R$-matrix all derived from the bar complex.

The Yangian quantization parameter is
\[
\hbar
\;=\;
\frac{1}{k+h^\vee}
\]
for affine $\widehat{\fg}_k$ \textup{(}Drinfeld~\cite{Drinfeld85}\textup{)}.
This is the same scalar that appears as the KZ25 sigma-model coupling and
as the collision-residue prefactor: the three-parameter identification
of Vol~I, Theorem~\ref{V1-thm:yangian-sklyanin-quantization}.

\section{Face 6: The Sklyanin Poisson bracket}
\label{sec:face-6-sklyanin}

The sixth face is the Sklyanin Poisson bracket on the dual algebra
$(\fg^!)^*$, defined by Semenov-Tian-Shansky \cite{STS83}. Given a
classical $r$-matrix $r^{\mathrm{cl}}(z)$ satisfying CYBE, the Sklyanin
bracket on $C^\infty((\fg^!)^*)$ is
\[
\{f,g\}_{\mathrm{Skl}}(x)
\;=\;
\langle x,[df,dg]\rangle
\;+\;\langle r^{\mathrm{cl}},df\otimes dg\rangle.
\]
The Yangian $Y_\hbar(\fg)$ is the canonical deformation quantization of
this Poisson structure, with parameter $\hbar = 1/(k+h^\vee)$. This is
the classical limit of the Yangian face above.

\section{Face 7: The Gaudin Hamiltonians}
\label{sec:face-7-gaudin}

The seventh face is the Gaudin Hamiltonians of the integrable system
associated to the Yangian. The full statement is Vol~I,
Theorem~\ref{V1-thm:gaudin-yangian-identification}: for affine
$\widehat{\fg}_k$, the GZ26 commuting Hamiltonians are exactly the
Gaudin Hamiltonians of $Y_\hbar(\fg)$ in the evaluation representation:
\[
H_i^{\mathrm{GZ}}
\;=\;
\frac{1}{k+h^\vee}\,
\sum_{j\neq i}
\frac{\Omega_{ij}}{z_i-z_j}
\;=\;
\frac{1}{k+h^\vee}\,H_i^{\mathrm{Gaudin}},
\]
the standard Gaudin model of Feigin--Frenkel--Reshetikhin
\cite{FFR94}. Higher Gaudin Hamiltonians (collision residues at depth
$k\geq 2$) extend this to the full $A_\infty$ Yangian structure.

\section{The seven-face master theorem in 3d HT}
\label{sec:dnp-master-seven-face}

\begin{theorem}[The seven-face master theorem in 3d HT]
\label{thm:seven-face-master-3d-ht}
\label{thm:vol2-seven-faces-master}
\index{seven-face master theorem!3d HT|textbf}
Let $\cA$ be a logarithmic $\SCchtop$-algebra in the standard 3d HT
landscape, and put
\[
r(z)=\operatorname{Res}^{\mathrm{coll}}_{0,2}(\Theta_\cA).
\]
Assume the following comparison data.
\begin{enumerate}[label=\textup{(H\arabic*)},start=0]
\item $\cA$ lies on the completed perturbative, boundary-linear,
chirally Koszul locus used in Theorems~\ref{thm:lines_as_modules} and
\ref{thm:dnp-bar-cobar-identification}.
\item The PVA, Hamiltonian, and Gaudin comparisons are made over
$\cM_{0,n}$.
\item For affine $\widehat{\fg}_k$ one has $k\neq -h^\vee$, so that
$\hbar=(k+h^\vee)^{-1}$ is defined and the KZ/Sugawara representative
is finite.
\item For strict chain models one fixed associator
$\Phi\in \GRT_1(\bQ)$ is used throughout.  At cohomology level no
associator is chosen.
\item The Drinfeld/Yangian/Gaudin comparison is taken on the
type-$A$ evaluation core, with simply-laced or folded classical types
included only after the line-OPE/folding compatibility of
Remark~\ref{rem:dnp-identification-lanes}.
\item Principal $\cW_N$ terms are interpreted through the
collision-depth construction; a term-by-term comparison with
\cite{GZ26} requires the normalization dictionary of
Remark~\ref{rem:vol2-gz26-wn-normalization}.
\end{enumerate}
Under these hypotheses the binary collision residue admits the
following seven presentations:
\begin{enumerate}[label=\textup{(F\arabic*)}]
\item The bar-cobar twisting morphism $\pi_\cA$
 \textup{(}Vol~I, Theorem~\textup{\ref{V1-thm:collision-residue-twisting}}\textup{)}.
\item The DNP $A_\infty$ Yang--Baxter Maurer--Cartan element on
 $\cA^!_{\mathrm{line}}$
 \textup{(}Theorem~\textup{\ref{thm:dnp-bar-cobar-identification}}\textup{)},
 in the cohomological/module lane, or in the strict chain lane after
 fixing $\Phi$.
\item The classical $r$-matrix from the boundary PVA $\lambda$-bracket
 via Gui--Li--Zeng quadratic duality
 \textup{(}Vol~I, Theorem~\textup{\ref{V1-thm:kz-classical-quantum-bridge}}\textup{)},
 over $\cM_{0,n}$.
\item The leading $1/z_{ij}$ coefficient of the GZ26 commuting
 Hamiltonians on $\cM_{0,n}$
 \textup{(}Vol~I, Theorem~\textup{\ref{V1-thm:gz26-commuting-differentials}}\textup{)};
 for principal $\cW_N$, this means the MC construction and
 commutativity, with term-by-term GZ26 matching only after the
 normalization dictionary in \textup{(}H5\textup{)}.
\item The Yangian $r$-matrix in the evaluation representation
 \textup{(}Drinfeld~\cite{Drinfeld85}\textup{)}; the identification
 holds away from $k = -h^\vee$ where the Yangian specialization
 parameter $\hbar = 1/(k + h^\vee)$ is well-defined.
\item The classical limit of the Yangian commutator equals the Sklyanin
 Poisson bracket on $(\fg^!)^*$
 \textup{(}Semenov-Tian-Shansky~\cite{STS83}\textup{)}.
\item The Gaudin Hamiltonian of the integrable system on $\cM_{0,n}$
 \textup{(}Feigin--Frenkel--Reshetikhin~\cite{FFR94};
 Vol~I, Theorem~\textup{\ref{V1-thm:gaudin-yangian-identification}}\textup{)}.
 The literal rescaling $H_i^{\mathrm{GZ}} = (1/(k+h^\vee))
 H_i^{\mathrm{Gaudin}}$ is singular at $k = -h^\vee$; the
 identification is stated away from the critical level.
\end{enumerate}
On the common locus specified by \textup{(H0)--(H5)}, these seven
presentations are equivalent presentations of the same binary collision
residue $r(z)$.
\end{theorem}

\begin{proof}
Face F1 is the definition of the binary collision residue under the
bar--cobar adjunction.  Face F2 is
Theorem~\ref{thm:dnp-bar-cobar-identification}, with the completed
perturbative boundary-linear hypotheses and the associator convention
specified in \textup{(H0)} and \textup{(H3)}.  Face F3 is Vol~I,
Theorem~\ref{V1-thm:kz-classical-quantum-bridge} restricted to
$\cM_{0,n}$.  Face F4 is Vol~I,
Theorem~\ref{V1-thm:gz26-commuting-differentials}, with the
principal-$\cW_N$ normalization obligation separated in \textup{(H5)}.
Faces F5--F7 are the standard Drinfeld, Semenov-Tian-Shansky, and
Feigin--Frenkel--Reshetikhin identifications at the non-critical
evaluation point $\hbar=(k+h^\vee)^{-1}$.  Each cited construction
maps its face to the displayed element
$\operatorname{Res}^{\mathrm{coll}}_{0,2}(\Theta_\cA)$; therefore the
seven presentations agree after restriction to the common locus.
\end{proof}

\begin{proposition}[Common-codomain commutativity of the seven-face diagram]
\label{prop:seven-face-sasaki-commutativity}
\index{seven-face master theorem!common-codomain commutativity|textbf}
\index{seven-face master theorem!diagram commutativity}
Let $\mathcal{A}$ be an $\SCchtop$-algebra in the standard 3d HT
landscape, restricted to the common locus $\cL$ of
Theorem~\ref{thm:vol2-seven-faces-master}. Write
$\phi_i: \mathrm{F}_i(\cA) \xrightarrow{\sim}
r(z)$ for the seven pairwise identifications
\textup{(}$i\in\{1,\dots,7\}$\textup{)} supplied by
Theorem~\ref{thm:vol2-seven-faces-master}. Then the complete graph
$K_7$ of face-to-face identifications
\[
\phi_{ij} := \phi_j^{-1}\circ\phi_i : \mathrm{F}_i(\cA)
\xrightarrow{\sim}\mathrm{F}_j(\cA),
\qquad i,j\in\{1,\dots,7\},
\]
commutes: for every triple $i,j,k\in\{1,\dots,7\}$,
$\phi_{jk}\circ\phi_{ij} = \phi_{ik}$ as isomorphisms of
$\fg\otimes\fg$-valued meromorphic one-forms on the configuration
space $\mathrm{Conf}_2(\bC)^*$.
\end{proposition}

\begin{proof}
The pairwise identifications $\phi_i$ of
Theorem~\ref{thm:vol2-seven-faces-master} are all maps into the same
codomain: the binary collision residue
$r(z) = \operatorname{Res}^{\mathrm{coll}}_{0,2}(\Theta_\cA)$
computed from the Maurer--Cartan element
$\Theta_\cA\in\mathrm{MC}\bigl(g^{\SCchtop}_\cA\bigr)$. This datum is
unique \textup{(}as a cohomology class in
$H^2\bigl(g^{\SCchtop}_\cA\bigr)$\textup{)} by the
Gui--Li--Zeng quadratic duality
\cite[Thm.~5.2]{GLZ24} once the conformal structure on $\cA$ is fixed,
and its scalar normalization is pinned down on each chart of $\cL$ by
the chosen representative: the affine-KM trace-form representative
$k\Omega/z$ and the level-prefixed KZ representative
$k\Omega/((k+h^\vee)z)$ vanish at $k=0$, whereas the KZ/Sugawara
representative $\Omega/((k+h^\vee)z)$ does not.  The finite
$\GRT^{\mathrm{fin}}$ Casimir rescaling records the change of
representative. Hence every face
$\mathrm{F}_i$ targets a common object, and the star-center
factorization
\[
\phi_{ij} = \phi_j^{-1}\circ\phi_i
\]
makes the $K_7$-diagram a cocycle-free groupoid on seven objects
because any connected diagram of isomorphisms into one chosen codomain
commutes strictly. Explicitly: for
any triangle $(i,j,k)$,
\[
\phi_{jk}\circ\phi_{ij}
\;=\;\phi_k^{-1}\circ\phi_j\circ\phi_j^{-1}\circ\phi_i
\;=\;\phi_k^{-1}\circ\phi_i
\;=\;\phi_{ik},
\]
with each composition evaluated on the common restriction locus
$\cL$. At cohomology level the associator action on the genus-zero
binary datum factors, by Remark~\ref{rem:grt-torsor-3d-ht}, through
the finite Casimir-rescaling quotient
$\mathrm{GRT}^{\mathrm{fin}}\cong\bQ^\times$, and scalar rescalings
commute. For strict cochains the same associator $\Phi$ is fixed in
every face, so no new cocycle appears in the triangle calculation. The
seven-face identification therefore asserts that the 21-edge graph of
face-to-face maps $\{\phi_{ij}\}_{i<j}$ is a commuting groupoid on
$\cL$, with the bar-intrinsic face $\mathrm{F}_1$ as the canonical
terminal vertex. The super-extension
\textup{(}Remark~\ref{rem:grt-torsor-3d-ht}~\textup{(iii)}\textup{)}
replaces $\mathrm{GRT}_1$ by its super-analogue; the cocycle-freeness
argument is unchanged modulo Koszul signs.
\end{proof}

\begin{remark}[Seven representatives and four generators]
\label{rem:seven-vs-four-resolution}
There are seven chosen orbit representatives in the theorem.  Under the
coarser relation that identifies the classical Drinfeld, Sklyanin, and
Gaudin presentations, these representatives are generated by four
independent constructions.  The commutativity proposition says that both
counts describe the same common-codomain groupoid.
\end{remark}

\begin{remark}[The $\mathrm{GRT}$-torsor of seven-face presentations]
\label{rem:grt-torsor-3d-ht}
\label{rem:grt-casimir-fm99}
The seven faces above are seven orbit representatives under the
free transitive action of the Drinfeld
Grothendieck--Teichm\"uller group $\mathrm{GRT}_1(\bQ)$ on the
space of coordinatized presentations of the universal collision
residue (Vol~I standalone \texttt{seven\_faces.tex},
Theorem~\textup{GRT-face-torsor}). Three consequences for the
Vol~II 3d HT statement:

\begin{enumerate}[label=\textup{(\roman*)}]
\item \textbf{Infinite face family.} Each associator
$\Phi\in\mathrm{GRT}_1(\bQ)$ produces a face $\mathrm{F}_\Phi$; the
bar-intrinsic face $\mathrm{F}_1$ is the origin of the torsor; the
seven above are seven specific choices of $\Phi$. Brown's motivic
associator gives $\mathrm{F}_8$ (MZV-valued; weight $\geq 3$
detects class-$M$ non-formality), and Willwacher's graph-cocycle
associator gives $\mathrm{F}_9$ (operadic/graph-complex).

\item \textbf{Casimir rescaling is a tensor identity.} On
affine Kac--Moody the $\mathrm{GRT}$-action factors through
$\mathrm{GRT}^{\mathrm{fin}}\cong\bQ^\times$ on the genus-zero
binary datum.  The trace-form representative
$r_{\mathrm{tr}}(z)=k\,\Omega/z$, the KZ/Sugawara representative
$r_{\mathrm{KZ}}(z)=\Omega/((k+h^\vee)z)$, and the level-prefixed KZ
representative $r_{\mathrm{KZ,lev}}(z)=k\,\Omega/((k+h^\vee)z)$ are
related by finite Casimir rescalings wherever the displayed scalars are
defined.  This is a tensor identity between orbit representatives,
\emph{not} a chained scalar equality $k = 1/(k+h^\vee)$ (which is
false).  At $k=0$ the trace-form and level-prefixed representatives
vanish; the KZ/Sugawara representative does not.

\item \textbf{$\mathrm{F}_7$ disambiguation.} Face F7 in
the list above is $\mathrm{F}_7^{\mathrm{Gaudin}}$: the simple-pole
projection $r_1$, valid for all four shadow classes $G,L,C,M$. The
class-$M$-specific top-$A_\infty$ face $\mathrm{F}_7^{M,\mathrm{top}}$
(the operation $m_3$ detecting non-formality for Virasoro and
$\Walg_N$) is a \emph{separate} face, \emph{not} in the
$\mathrm{GRT}$-orbit of $\mathrm{F}_1$. It vanishes for classes
$G$, $L$, $C$ and is written $\mathrm{F}_7'$ or
$\mathrm{F}_7^{M,\mathrm{top}}$ when ambiguity could arise.

\item \textbf{Super-variant (odd-generator extension).} For chiral algebras
with odd generating fields, the seven-face theorem and the
$\mathrm{GRT}$-torsor statement hold verbatim after replacing
$\otimes$ by the Koszul-signed graded tensor product, $\Omega$ by
the super-Casimir, and $\mathrm{GRT}_1$ by its super-analogue on the
parity-graded pure-braid Lie superalgebra. The
$\mathrm{F}_1\Leftrightarrow\mathrm{F}_2$ identification is
mediated by the super-twist
$\sigma(a\otimes b) = (-1)^{|a||b|}b\otimes a$.
\end{enumerate}

On the affine-KM binary datum the motivic and operadic associator
directions project to the same finite Casimir-rescaling quotient; higher
MZV and graph-complex contributions are invisible in this finite
projection.  They can reappear only in higher collision depth or
non-formal class-$M$ data.
\end{remark}

\section{The Drinfeld--Kohno four-path comparison}
\label{sec:dnp-master-dk-four-path}

The Drinfeld--Kohno comparison (DK-0) relates the KZ monodromy,
quantum-group Casimir eigenvalues, the Yangian evaluation
specialization, and the Verlinde truncation on the integrable
$\widehat{\mathfrak{sl}}_2$ locus.

\begin{theorem}[The four-path Drinfeld--Kohno comparison]
\label{thm:dk-four-path}
\index{Drinfeld-Kohno!four-path comparison|textbf}
For affine $\widehat{\mathfrak{sl}}_2$ at integrable non-critical level
$k\in\bZ_{>0}$ and spins $0\leq j_i\leq k/2$, the following four
constructions give the same root-of-unity comparison datum: the first
three give the same rank-$1$ braiding eigenvalue ratio, and the
Verlinde construction gives the corresponding allowed fusion channels:
\begin{enumerate}[label=\textup{(\arabic*)}]
\item \textbf{KZ monodromy.} The monodromy of the Knizhnik--Zamolodchikov
 connection $\nabla^{\mathrm{KZ}} = d - (1/(k+2))\sum_{i<j}\Omega_{ij}\,d\log(z_i-z_j)$
 on $V_{j_1}\otimes\cdots\otimes V_{j_n}$ around the discriminant
 divisor. (This is the bar-coefficient realization; the connection
 $1$-form is $\sum r_{ij}\,dz_{ij}$.)
\item \textbf{Quantum Casimir eigenvalues.} The eigenvalues of
 $q^{C_2}$ on irreducible summands, where
 $q = \exp(\pi i/(k+2))$ and $C_2$ is the second Casimir.
\item \textbf{Yangian $R$-matrix at Drinfeld specialization.} The
 Yang $R$-matrix $R^Y(u) = u\cdot I + i P$ evaluated at the spectral
 parameter $u_D = \cot(\pi/(k+2))$, the \emph{Drinfeld
 specialization}.
\item \textbf{Verlinde truncation.} The Verlinde fusion ring at
 level $k$, with truncation at the Weyl chamber boundary determining
 which fusion channels survive.
\end{enumerate}
\end{theorem}

\begin{proof}
The agreement of paths \textup{(1)} and \textup{(2)} is the
Drinfeld--Kohno theorem for the KZ connection with
parameter $q=\exp(\pi i/(k+2))$.  For path \textup{(3)}, Yang's
rank-$1$ evaluation $R$-matrix has eigenvalue ratio
\[
\frac{u+i}{u-i}.
\]
At the Drinfeld specialization $u_D=\cot(\pi/(k+2))$ this ratio is
\[
\frac{\cot\theta+i}{\cot\theta-i}=e^{2i\theta},
\qquad \theta=\frac{\pi}{k+2},
\]
which is the same $q^2$ phase entering the KZ braiding eigenvalue
ratio.  Path \textup{(4)} gives the surviving channels by the
$\widehat{\mathfrak{sl}}_2$ Verlinde formula, i.e. by the same
root-of-unity truncation at the Weyl alcove boundary.  Thus the
braiding eigenvalue ratio and the fusion support agree on the stated
integrable locus.
\end{proof}

\begin{remark}[Why the Drinfeld specialization is real]
\label{rem:dnp-master-drinfeld-real}
A common confusion is that the Drinfeld specialization parameter
$u_D = \cot(\pi/(k+2))$ should be imaginary, by analogy with the
quantum group parameter $q = \exp(\pi i/(k+2))$. In fact, $u_D$ is
\emph{real} because the Yang $R$-matrix has eigenvalue ratio
$(u+i)/(u-i)$, and at the specialization
$u_D = \cot(\theta)$ this becomes
$(\cot\theta + i)/(\cot\theta - i) = e^{2i\theta}$,
which is the desired phase $q^2 = e^{2\pi i/(k+2)}$.
The braiding eigenvalues (Hecke generators $q$ and $-q^{-1}$) are
\emph{distinct} from the KZ monodromy eigenvalues
($q^{1/2}$ and $-q^{-3/2}$); the DK comparison matches monodromy
with the quantum Casimir, not with the Hecke generator.
\end{remark}

\section{Fusion rules from the $R$-twisted coproduct}
\label{sec:dnp-master-fusion}

The Verlinde fusion rules for $\widehat{\mathfrak{sl}}_2$ at level $k$
arise from the $R$-twisted coproduct on the evaluation modules of the
ordered bar complex.  On the integrable locus
$0\leq \lambda,\mu,\nu\leq k$ the bar-coproduct description has the
same fusion support as the standard Verlinde formula
\[
N^\lambda_{\mu\nu}
\;=\;
\sum_{\sigma}
\frac{S_{\mu\sigma} S_{\nu\sigma} \overline{S_{\lambda\sigma}}}{S_{0\sigma}}.
\]

\section{The Virasoro spectral $R$-matrix in closed form}
\label{sec:dnp-master-virasoro-r}

\begin{computation}[Virasoro spectral $R$-matrix on primary states]
\label{comp:dnp-master-virasoro-r-matrix}
\index{Virasoro!spectral $R$-matrix closed form}
For the Virasoro algebra at central charge $c$ acting on a primary state
of conformal weight $h$, the spectral $R$-matrix admits the closed form
\begin{equation}
\label{eq:dnp-master-virasoro-r-closed}
R(z)
\;=\;
z^{2h}\,\exp\!\left(-\frac{c}{4z^2}\right)
\;=\;
z^{2h}\sum_{k=0}^{\infty}\frac{(-c/4)^k}{k!\,z^{2k}}.
\end{equation}
After factoring out $z^{2h}$, the first four even coefficients are
$1$, $-c/4$, $c^2/32$, and $-c^3/384$.
Only even powers of $1/z$ appear (bosonic parity). The non-terminating
character is the genus-$0$ signature of class~M (infinite shadow depth).
\end{computation}

This closed form is the 3d HT analogue of the rank-$1$ Yangian $r$-matrix
$R^Y(u) = u\cdot I + iP$ in the affine KM setting; the Virasoro
exponential structure replaces the linear-in-$u$ structure because the
underlying OPE has higher pole order.

\section{The $\Walg_3$ commuting Hamiltonians: rank-$2$ calculation}
\label{sec:dnp-master-w3-hamiltonians}

For $\Walg_3$ with $p_{\max} = 6$ and $k_{\max} = 5$, the GZ26 commuting
Hamiltonians are differential operators of order $4 = k_{\max} - 1$ on
the conformal block space. The collision-depth expansion gives
\[
H_i^{(\Walg_3)}
\;=\;
\sum_{j\neq i}
\sum_{k=1}^{5}
\frac{c_k^{(\Walg_3)}(V_j,\partial_{z_j})}{z_{ij}^k},
\]
where the coefficient operators $c_k^{(\Walg_3)}$ are determined by the
$\Walg_3$ structure constants.  In the primary-state normalization used
in this lane the relevant coefficient identities are:
\begin{itemize}
\item Depth-$1$ residue: $\partial_{z_j}/z_{ij}$
\item Depth-$2$ residue: $h_j/z_{ij}^2$ (conformal weight)
\item Depth-$3$ residue: $w_j/z_{ij}^3$ (spin-$3$ charge)
\item Depth-$4$ residue: $0$ (the depth-$4$ contribution vanishes)
\item Depth-$5$ residue: $(c/3)/z_{ij}^5$ (trivial on primaries)
\end{itemize}

The $\Lambda$ composite field on primaries (a key ingredient of the
$\Walg_3$ algebra) acts as
\[
\Lambda_0|h\rangle
\;=\;
(h^2 - 3h/5)|h\rangle,
\]
with roots at $h = 0$ and $h = 3/5$, an algebraic identity
independent of $c$.

\section{The first rank-$2$ $\mathfrak{sl}_3$ Yangian $R$-matrix}
\label{sec:dnp-master-sl3-yangian}

\begin{computation}[The $\mathfrak{sl}_3$ Yangian $R$-matrix from the
ordered bar]
\label{comp:dnp-master-sl3-yangian}
\index{Yangian!$\mathfrak{sl}_3$ from ordered bar|textbf}
For affine $\widehat{\mathfrak{sl}}_3$ at level $k$, the Yang
$R$-matrix on the fundamental representation $V = \bC^3$ takes the form
\[
R^{\mathfrak{sl}_3}(u)
\;=\;
u\cdot I + P,
\]
where $P$ is the permutation operator. The Casimir
$\Omega = (1/2)\sum_a \lambda_a\otimes\lambda_a$ in the Gell-Mann basis
acts on $V\otimes V = \mathbf{6}\oplus\mathbf{3}^*$ with eigenvalues
$1/3$ (symmetric) and $-2/3$ (antisymmetric). The level-prefixed KZ
representative of the collision residue from the bar complex on
$\Conf_2^{\mathrm{ord}}(\bC)$ gives
\[
r^{\mathfrak{sl}_3}(z)
\;=\;
\frac{k\Omega}{(k+3) z},
\]
which matches Yang's $R$-matrix on the fundamental evaluation
representation in the rank-$2$ case.  The trace-form representative is
$k\Omega/z$, while the KZ/Sugawara representative is
$\Omega/((k+3)z)$.
\end{computation}

\begin{remark}[The fundamental--adjoint distinction]
\label{rem:dnp-master-fund-adj}
The Yang form $R(u) = u\cdot I + \Omega$ satisfies the spectral
parameter Yang--Baxter equation \emph{only} in the fundamental
representation, where $\Omega = P - I/N$ for $\mathfrak{sl}_N$. In the
adjoint representation, the Casimir has eigenvalues $\{2,0,-3,-6\}$
with multiplicities $\{27,20,16,1\}$ matching the decomposition
$\mathbf{8}\otimes\mathbf{8} = \mathbf{27}\oplus\mathbf{10}\oplus
\mathbf{10}^*\oplus\mathbf{8}\oplus\mathbf{8}\oplus\mathbf{1}$, and the
naive Yang form does not satisfy YBE. The full universal $R$-matrix of
$Y(\mathfrak{sl}_3)$ is required. This is the first rank where the
distinction between the naive Yang $R$ and the full Yangian $R$ is
visible.
\end{remark}

\section{Cross-volume bridge}
\label{sec:dnp-master-cross-volume}

This chapter is the Vol~II realization of the seven-face structure.
The mirror chapters in the other volumes are:
\begin{itemize}
\item \textbf{Vol~I}: the holographic-datum master chapter
 (seven-face structure for chiral algebras on curves), in the
 Vol~I \emph{Seven Faces of $r(z)$} part. The proved theorems
 live there: gz26 commuting differentials, KZ classical-quantum
 bridge, Gaudin--Yangian identification, Yangian--Sklyanin
 quantization, shadow-depth--operator-order. The Vol~I faces
 F1--F7 are in bijection with the present F1--F7 via the 3d HT
 boundary reduction: Vol~I's chiral-algebra-on-curve face
 $F_i^{\mathrm{V1}}$ equals the boundary-algebra face
 $F_i^{\mathrm{V2}}$ of the 3d HT slab whose boundary algebra is
 $\cA$.
\item \textbf{Vol~III}: the CY-holographic-datum master chapter
 (seven-face structure for Calabi--Yau quantum vertex chiral groups;
 includes the Maulik--Okounkov $R$-matrix as face~$4$ for $K3\times
 E$, the affine super Yangian $Y(\widehat{\mathfrak{gl}}_1)$ as
 face~$5$ for toric CY3, and the elliptic Sklyanin bracket as
 face~$6$ for toroidal CY).
\end{itemize}

The three chapters are connected by the master concordance, the
trilingual dictionary between the three realizations.

The genus-$1$ extension of the seven-face structure, proving the
identification of the KZB connection with the elliptic $r$-matrix
and the elliptic Gaudin model for affine KM, is in the Volume~I
genus-1 seven-faces chapter.

\section{Standing hypotheses: associator, Koszul dual, scope, slab}
\label{sec:dnp-master-hypotheses}

The seven-face master theorem in 3d HT carries four standing
conditions. Each is part of the theorem data; none weakens
Theorem~\ref{thm:vol2-seven-faces-master} or
Proposition~\ref{prop:seven-face-sasaki-commutativity} on the common
locus, but each pins the precise content under examination.

\begin{remark}[Associator dichotomy on the Sasaki diagram]
\label{rem:dnp-master-associator-dichotomy}
Proposition~\ref{prop:seven-face-sasaki-commutativity} establishes
strict commutativity of the seven-face groupoid on the common
locus $\cL$, with cocycle-freeness following from the
$\GRT^{\mathrm{fin}}$ collapse of the
$\GRT_1(\bQ)$-torsor at the genus-zero binary datum. At the
\emph{cochain} level, however, each face-to-face transport
$\phi_{ij}$ is associator-dependent: fixing a Drinfeld
associator $\Phi\in \GRT_1(\bQ)$ at face $F_i$ produces a strict
representative $F_i^{\Phi}$ whose composition with $F_j^{\Phi}$ is
strict only because the same $\Phi$ is fixed throughout the
diagram. The cohomological face commutativity (which the
proposition proves) is associator-independent;
the cochain-level face commutativity is associator-dependent
and lives in the $\GRT_1(\bQ)$-torsor of
Remark~\ref{rem:grt-torsor-3d-ht}.
\end{remark}

\begin{remark}[$\cA^{!}_{\mathrm{line}}$ is an
$(\SCchtop)^{!}$-algebra, not an $\SCchtop$-algebra]
\label{rem:dnp-master-koszul-dual-operad}
Face F2 of Theorem~\ref{thm:vol2-seven-faces-master} identifies the
line operator category $\cC_{\mathrm{line}}$ with
$\cA^{!}_{\mathrm{line}}$-modules, where $\cA^{!}_{\mathrm{line}}$ is
the boundary Koszul dual produced by the chiral bar-cobar adjunction.
The object $\cA^{!}_{\mathrm{line}}$ is an algebra over the
\emph{Koszul-dual} operad
$(\SCchtop)^{!} \simeq (\Lie, \Ass, \text{shuffle-mixed})$, which is
\emph{not} self-dual to $\SCchtop$. The bona fide $\SCchtop$-datum of
the 3d HT slab is the pair
$(\cZ^{\mathrm{der}}_{\mathrm{ch}}(\cA), \cA)$ -- bulk acting on
boundary -- not the boundary $\cA^{!}_{\mathrm{line}}$ alone. The DNP
identification of F2 is therefore the identification of
$\cC_{\mathrm{line}}$ with modules over an algebra in the Koszul-dual
operad, not modules over an $\SCchtop$-algebra in disguise.
\end{remark}

\begin{remark}[Concrete vs.\ abstract construction across the nine
specialisation fibres]
\label{rem:dnp-master-concrete-vs-abstract}
The seven-face master statement
Theorem~\ref{thm:vol2-seven-faces-master} is proved on the common
chirally Koszul comparison locus with the F2 type and chain-level
conditions above. It is not an abstract theorem for every
$\SCchtop$-algebra in the standard 3d HT landscape. The \emph{concrete}
construction of all seven face-to-$r(z)$ identifications, with
explicit generators and relations matching standard literature
presentations, is presently complete for two anchor cases:
$\fsl_{2}$-Yangian (rational limit) and affine Kac--Moody $V_k(\fg)$
at non-critical level. The remaining seven fibres of the unified
chiral quantum group are concrete only on the evaluation-generated
core; the elliptic case contains the KZB connection and classical
$r$-matrix, while full quantum-$R$ quasi-triangularity at finite
$\hbar$ requires an additional construction; the toroidal case is
recorded here only at the
formal-disk level (Schiffmann--Vasserot CoHA, Miki). See
Remark~\ref{rem:concrete-vs-abstract-scope} of
the unified-chiral-quantum-group chapter.
\end{remark}

\begin{remark}[Slab is a bimodule, not a Swiss-cheese disk]
\label{rem:dnp-master-slab-bimodule}
The Dimofte $D|T|N$ slab, into which all seven faces of $r(z)$ are
threaded as boundary or line-operator data, is a \emph{bimodule}
over a pair of boundary chiral algebras $(\cA_{\mathrm{L}},
\cA_{\mathrm{R}})$, \emph{not} a Swiss-cheese disk with a single
boundary component. The bulk $\cZ^{\mathrm{der}}_{\mathrm{ch}}(\cA)$
acts on both boundaries, and the slab fiber functor of Dimofte
$D|T|N$ computes the relative bar
$B(\cA_{\mathrm{L}}, \cZ^{\mathrm{der}}_{\mathrm{ch}}(\cA),
\cA_{\mathrm{R}})$ through the bimodule. The seven faces of $r(z)$
arise on the diagonal $\cA_{\mathrm{L}} = \cA_{\mathrm{R}} = \cA$
(self-bimodule case), where the relative bar collapses to
$B(\cA)\otimes_{\cZ^{\mathrm{der}}_{\mathrm{ch}}(\cA)} B(\cA)$ and
the binary collision residue
$r(z) = \Res^{\coll}_{0,2}(\Theta_{\cA})$ extracts the
diagonal-bimodule $r$-matrix. Off-diagonal slabs (with distinct
$\cA_{\mathrm{L}} \neq \cA_{\mathrm{R}}$) carry an $r$-matrix in
$\cA_{\mathrm{L}}^{!} \otimes \cA_{\mathrm{R}}^{!}[\![z^{-1}]\!]$
that mediates the categorified bimodule structure; this is the
3d HT setting for the seven faces and is the natural ambient
for the universal-holography master theorem.
\end{remark}

\begin{remark}[Cross-references to the climax]
\label{rem:dnp-master-climax-anchors}
The seven-face master Theorem~\ref{thm:vol2-seven-faces-master}
sits at the bar-intrinsic apex of three structural anchors:
\begin{itemize}
\item[(a)] \emph{Universal holography master.} On the chosen
 boundary-linear / physics-bridge prefactorization locus, the
 seven-face master is the F1 face of the universal-holography functor
 $\Phi_{\mathrm{hol}}\colon \cA \mapsto T_{\cA}$
 (Theorem~\ref{thm:universal-holography-master}):
 $r(z) = \Res^{\coll}_{0,2}(\Theta_{\cA})$ is the boundary-to-bulk
 obstruction class on the slab bimodule of
 Remark~\ref{rem:dnp-master-slab-bimodule}, restricted to the
 diagonal $\cA = \cA_{\mathrm{L}} = \cA_{\mathrm{R}}$. The five
 rung corollaries (Corollaries~\ref{cor:rung-heisenberg},
 \ref{cor:rung-affine-km}, \ref{cor:rung-virasoro},
 \ref{cor:rung-w-N}, \ref{cor:rung-w-infty}) produce the
 corresponding seven-face data at each rung of the
 $E_n$-topologisation ladder.
\item[(b)] \emph{Universal conductor.} The Volume~I
 universal-conductor theorem computes the ghost-charge
 $K = c + c^{!}$ across the unified family; specialised to F1 of
 the present master, the conductor produces the canonical
 Casimir-rescaling-invariant scalar that pins the absolute
 normalisation of $r(z)$ on each $\GRT^{\mathrm{fin}}$-orbit. The
 conductor formula is the $\GRT^{\mathrm{fin}}$-invariant shadow of
 F1.
\item[(c)] \emph{Volume~III $\Phi$ functor.} The Volume~III
 CY-to-chiral functor $\Phi$ produces seven-face data at every CY
 target; K3 (via the Mukai-orthogonal Yangian
 $Y_{\hbar}(\mathfrak{so}(4,20))$, with Hodge-parity refinement
 requiring a separate proof for $Y_{\hbar}(\mathfrak{so}(4|20))$,
 scope $n=2$);
 $\bC^{3}$ (via affine Yangian
 $\Yhbar(\widehat{\fgl_{\infty}})$ as F5 of the
 Schiffmann--Vasserot CoHA $r$-matrix); toric CY${}_{3}$ (via the
 Maulik--Okounkov $r$-matrix as F4, scope $n=1$). The Volume~III
 master concordance carries the corresponding seven-face data at
 each $\Phi$-target.
\end{itemize}
\end{remark}
