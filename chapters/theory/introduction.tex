\chapter*{Introduction}
\addcontentsline{toc}{chapter}{Introduction}

\providecommand{\cH}{\mathcal{H}}
\providecommand{\Mbar}{\overline{\mathcal M}}
\providecommand{\fg}{\mathfrak{g}}
\providecommand{\Ydg}{Y^{\mathrm{dg}}}
\providecommand{\Zder}{\operatorname{Z}_{\mathrm{der}}}
\providecommand{\Abulk}{A_{\mathrm{bulk}}}
\providecommand{\Bbound}{B_{\partial}}
\providecommand{\dCrit}{\operatorname{dCrit}}
\providecommand{\RHom}{\operatorname{RHom}}
\providecommand{\Perf}{\operatorname{Perf}}
\providecommand{\cL}{\mathcal{L}}
\providecommand{\Tr}{\operatorname{Tr}}
% Vol III two-stage factorisation (cross-volume macros)
\providecommand{\PhiFA}{\Phi^{\mathrm{FA}}}
\providecommand{\SpCh}{\mathrm{Sp}_{\mathrm{Ch}}}
\providecommand{\HolFA}{\mathrm{HolFA}}
\providecommand{\EdHolFA}{E_d\text{-}\HolFA}
\providecommand{\EnHolFA}{E_n\text{-}\HolFA}
\providecommand{\intSigma}{\textstyle\int_{\Sigma}}
\providecommand{\hCS}{\mathrm{hCS}}

\section*{Boundary actions and physical realization}

A boundary operator can compose with other boundary operators and can
receive an action from a bulk insertion. These operations have to agree
when several insertions compose or collide. The derived chiral centre
is the algebraic target for coherent chiral actions. A comparison with
physical observables must be constructed from a field theory and its
boundary condition.

Fix a smooth projective complex curve $X$ and a chirally Koszul chiral
algebra $A$ with conformal vector $\omega$ and boundary linearisation
$\mathrm{BL}$. In Theorem~\ref{thm:universal-holography-master}, the
additional datum $\Xi$ supplies a holomorphic--topological field theory
$\mathcal F_A$, a boundary equivalence, and a bulk comparison:
\[
 \mathrm{Obs}^{\partial}(\mathcal F_A)\xrightarrow{\simeq}A,
 \qquad
 Z^{\mathrm{der}}_{\mathrm{ch}}(A)
 \longrightarrow\mathrm{Obs}^{\mathrm{bulk}}(\mathcal F_A).
\]
The theorem concerns the assignment
$(A,\omega,\mathrm{BL},\Xi)\mapsto\mathcal F_A$ on morphisms
preserving this datum. Its conditional equivalence statement retains the
specified analytic and coherence hypotheses. The construction of $\Xi$
for an independently defined physical theory is a separate problem.

The original chain complex and a weight completion can support different
comparison statements. The class-$\mathbf M$ results use their stated
weight-completed or pro category. An $E_3$-topological structure further
requires coherent primitives for holomorphic translation. These
requirements determine the operations and the domain of the comparison.

The non-logarithmic standard landscape is class-dependent. Classes
$\mathbf G$, $\mathbf L$, and $\mathbf C$ have ordinary-chain
realisations on their stated non-critical loci. Class~$\mathbf M$
is proved in the weight-completed / pro ambient, with raw
direct-sum descent governed by finite propagation. Schellekens
$c=24$ rows and Monster $V^\natural$ require finite-orbifold BV
descent and Dijkgraaf--Witten anomaly trivialisation in their
strata. Irrational cosets enter through the VSKR+BGG tempered
criterion.
Logarithmic $W(p)$ tempering requires the regular
Adamovi\'c--Milas $TW/WW$ character-amplitude bound and the logarithmic
boundary estimate of
Conjecture~\ref{conj:tempered-stratum-contains-wp}: Gurarie 1993
and Flohr 1996 exhibit logarithmic-CFT amplitudes with unbounded
Massey products despite finite-dimensional Zhu, so the
Zhu-bounded-Massey route does not close $W(p)$.

\paragraph{Boundary algebras and topological refinements.}
\begin{enumerate}
\item The Heisenberg boundary $\cA=\cH_k$ gives the abelian
Chern--Simons comparison with $E_3^{\mathrm{top}}$ structure in
Corollary~\ref{cor:rung-heisenberg}.
\item The affine boundary $\cA=\widehat{\fg}_k$, with
$k\ne-h^\vee$, has the $E_3^{\mathrm{top}}$ comparison of
Corollary~\ref{cor:rung-affine-km}.
\item The Virasoro boundary $\cA=\mathrm{Vir}_c$, with nonzero
non-critical $c$, has the completed $E_3^{\mathrm{top}}$ comparison
of Corollary~\ref{cor:rung-virasoro}.
\item For $\cA=W_{N,c}$ obtained by principal Drinfeld--Sokolov
reduction, the Casimir tower supplies the conditional
$E_{N+1}^{\mathrm{top}}$ refinement of
Corollary~\ref{cor:rung-w-N}.
\item For $\cA=W_\infty[\lambda]$ at generic $\lambda$, the
$E_\infty^{\mathrm{top}}$ refinement uses bounded-weight limits
of the $E_n^{\mathrm{top}}$ structures. It retains hypotheses
$\hypProchazka$, $\hypCKL$, $\hypPRSh$, and $\hypYamada$
of Corollary~\ref{cor:rung-w-infty}.
\end{enumerate}
Each comparison is a corollary of $\Phi_{\mathrm{hol}}$ after its
boundary input supplies the stated HT and antighost data.
For Virasoro, the Brown--Henneaux central charge provides a boundary
symmetry datum. Part~\ref{part:gravity} asks for its realization by the
physical currents, states, and gluing of a gravitational theory.
The $W_\infty[\lambda]$ refinement instead concerns the conditional
bounded-weight operadic limit of
Theorem~\ref{thm:climax-w-infty-endpoint}.

\section*{The universal stage chain (reading discipline)}

\noindent
Every working object in the volume occupies one of five stages of
the universal chain
\[
\boxed{\;
\underbrace{\mathsf{P}}_{\substack{\text{primitive}}}
 \xrightarrow{\;\alpha\;}
\underbrace{\mathsf{C}}_{\substack{\text{chart}}}
 \xrightarrow{\;\beta\;}
\underbrace{\mathsf{S}}_{\substack{\text{shadow}}}
 \xrightarrow{\;\gamma\;}
\underbrace{\mathsf{Z}}_{\substack{\text{centre} \\ = \text{bulk}}}
 \xrightarrow{\;\delta\;}
\underbrace{\mathsf{A}}_{\substack{\text{acting} \\ \text{object}}}
\;}
\]
with arrows $\alpha$ \textup(chart-projection\textup), $\beta$
\textup(comparison / functor / natural transformation\textup),
$\gamma$ \textup(ambient declaration\textup), $\delta$
\textup(endpoint hypothesis / convergence\textup), and an
ambient effectiveness type $\epsilon$
\textup(orientation / non-degeneracy\textup) sitting outside the
chain but conditioning every other arrow. Cross-stage collapse ---
identifying objects at different stages without naming the
licensing arrow --- is the master pattern \emph{shadow $=$ object}
that the licensing discipline forbids.

The volume's primitive object lives at level $\mathsf{P}$: the
bicoloured primitive package
$\primPkgBicolour$
on the curve--line pair $(C, \R)$
\textup(\cref{def:bicoloured-primitive-package},
\cref{thm:bicoloured-primitive-universality}\textup).
Choosing a boundary vacuum $b$ projects $\mathsf{P} \to \mathsf{C}$
to the chart algebra $\bdyAlg = A_b = \mathrm{End}_{\openFactCat}(b)$
\textup(licensing $\alpha$\textup). The chiral bar coalgebra
$\BarTwc{A_b}$ at level $\mathsf{S}$ is the twisting-coupling
shadow of $A_b$ \textup(licensing $\beta$\textup); it is
\emph{not} the bulk. The level-$\mathsf{Z}$ object is the algebraic
derived chiral centre
$\Zderch(A_b) \simeq \bulkChirHoch{A_b} = \mathrm{ChirHoch}^\bullet(A_b, A_b)$
\textup(\cref{thm:chd-ds-hochschild}, the Hochschild concentration theorem\textup);
its physical bulk reading requires the scoped HT open/closed
comparison.
Line operators on $A_b$ live at level $\mathsf{A}$, the acting
object on the centre. Each step from $\mathsf{P}$ to $\mathsf{A}$
requires a named licensing datum; this is the reading discipline
of the volume.

The seven heptagon faces of $\SCchtop$
\textup(\cref{rem:heptagon-licensing-duality}\textup) and the
five licensing types $\alpha / \beta / \gamma / \delta / \epsilon$
are mutually dual: face $=$ stage-pair to be licensed; type $=$
data needed to license the stage-pair. Throughout the volume,
every theorem statement carries the relevant licensing tags
inline.

The cyclic-Hochschild four-object discipline
\textup(\cref{thm:cyclic-hochschild-stage-stratification}\textup)
places $\mathrm{ChirHoch}^\bullet$, $\mathrm{ChirHoch}_\bullet$,
$\mathrm{HC}^{\mathrm{cyc}}$, $\mathrm{HC}^-$ at distinct levels
of the chain with their respective $\mathsf{S} \to \mathsf{Z}$ or
$\mathsf{S} \to \mathsf{A}$ promotions; only
$\mathrm{ChirHoch}^\bullet$ is the bulk, the others are shadows
requiring further licensing. The $\kappa$-tuple
$\kappaTuple{\cA}$ is the level-$\mathsf{S}$ characteristic
\textup(no scalar collapse;
\cref{thm:kappa-tuple-primitivity-orthogonal-shimura}\textup):
the per-row constructions are independent functions on the
period domain. Universal Holography $\Phihol$ is the universal
stage chain made physical
\textup(\cref{cor:universal-holography-via-bicoloured-universality}\textup):
the $3$d HT QFT image of an $\SCchtop$-algebra is the universal
stage chain action on its bicoloured primitive package presentation.

\section*{Volume II contributions}

\noindent
The volume contributes the following Vol~II-native theorems beyond
the Vol~I five-theorem core
\textup(A bar--cobar inversion; B chiral Positselski; C derived-centre
complementarity; D obstruction-tower universality; H chiral Hochschild
concentration\textup):
\begin{enumerate}
\item \emph{$\SCchtop$ heptagon}: seven canonical presentations of
the bicoloured operadic primitive of 3d HT QFT
\textup(\cref{ch:sc-chtop-heptagon}\textup), with the heptagon-face /
licensing-type duality
\textup(\cref{rem:heptagon-licensing-duality}\textup) realising the
seven faces as dual to the five licensing types $\alpha / \beta /
\gamma / \delta / \epsilon$ of the universal stage chain.
\item \emph{Bicoloured-primitive universality}
\textup(\cref{thm:bicoloured-primitive-universality}\textup): every
$\SCchtop$-algebra on $(C, \R)$ is canonically presented by its
bicoloured nine-tuple. Universal Holography
$\Phihol$ factors as
$\Phi^{\mathrm{HT}}_{\mathrm{bic}} \circ \mathsf{Prim}^{\mathrm{bic}}$
\textup(\cref{cor:universal-holography-via-bicoloured-universality}\textup).
\item \emph{Curved-Dunn $H^2 = 0$ at $g \ge 2$}
\textup(\cref{thm:curved-dunn-H2-vanishing-all-genera}\textup):
chain-level closure of modular operad composition under the
modular-bootstrap acyclicity and degree-$2$ comparison
hypotheses; $g = 1$ governed separately by
\cref{prop:genus1-twisted-tensor-product}.
\item \emph{Chiral higher Deligne}
\textup(\cref{thm:chiral-higher-deligne}\textup): $\bulkOf{A_b} =
\bulkChirHoch{A_b}$ is the universal $E_3$-chiral algebra acted on
by $\SCchtop$; \cref{thm:chd-ds-hochschild} establishes
$\ChirHoch^\bullet(W_k(\fg)) \simeq H^\bullet_{\mathrm{DS}}(
\ChirHoch^\bullet(V_k(\fg)))$ at chain level.
\item \emph{Universal celestial holography}
\textup(\cref{thm:uch-main}\textup): $\SCchtop$ on
$(A^{\mathrm{cel}}, \bulkOf{A^{\mathrm{cel}}})$ + celestial OPE $=$
chiral factorisation homology on $\mathbb{P}^1_{\mathrm{cel}}$.
Coverage: self-dual gauge \textup(KM\textup); gauge $+$ matter
\textup(DS\textup); gravity \textup(Virasoro $+ w_{1+\infty}$\textup);
YM \textup(Beem--Rastelli $\chi$-functor\textup).
\item \emph{Periodic-CDG on admissible Kazhdan--Lusztig}
\textup(\cref{V1-thm:periodic-cdg-is-koszul-compatible} +
extension chapter\textup): admissible-level closure for
$\widehat{\fsl}_2$; $\fsl_3$ Koszulness frontier item~F4.
\item \emph{Unified $Q^{k, f, \mu}_\fg$ chiral quantum group}
\textup(\cref{thm:Koszul_dual_Yangian}\textup): nine specialisation
fibres covering the non-gauge-theoretic landscape
\textup(Yangian, finite $\cW$, affine $\cW$, truncated-shifted BFN
Coulomb branch, orthogonal-coideal RTT\textup).
\item \emph{$\beta_N$ harmonic closed form}
\textup(\cref{conj:beta-N-harmonic-closed-form}\textup):
$\beta_N = 12 (H_N - 1) = \sum_{s = 2}^N 12/s$, non-integral for
$N \ge 5$ (first $\beta_5 = 77/5$).
\item \emph{$\kappa$-tuple primitivity}
\textup(\cref{thm:kappa-tuple-primitivity-orthogonal-shimura},
\cref{thm:kappa-tuple-primitivity-class-B}\textup): the K3$\times E$
counterexample upgrades the additive-failure tabulation to a
structural impossibility \textup(disjoint-moduli factorisation\textup);
extended to Class B via the BCOV row.
\item \emph{Cyclic-Hochschild stage stratification}
\textup(\cref{thm:cyclic-hochschild-stage-stratification}\textup):
the four chiral-Hochschild objects partition the universal stage
chain levels with explicit licensing for each $\mathsf{S} \to
\mathsf{Z}$ or $\mathsf{S} \to \mathsf{A}$ promotion.
\item \emph{Construction Problem level stratification}
\textup(\cref{thm:construction-problem-stage-stratification}\textup):
the four open Construction Problems CP1--CP4 occupy distinct
level-promotions on the chain across the open / CY / trace lanes.
\item \emph{Chiral Positselski cross-volume synthesis}
\textup(\cref{thm:chiral-positselski-cross-volume}\textup): Quillen
equivalence on the strict Mittag--Leffler weight-completed surface;
two specialisations \textup(discrete curve recovers Positselski
2011; quadratic chiral recovers Gui--Li--Zeng MC bijection\textup);
residual gap at four obstruction loci constituting Construction
Problem~4.
\end{enumerate}
The Vol~III bridge is the two-stage CY-to-chiral functor
$\Phi_d = \SpCh \circ \PhiFA_d$
\textup(\cref{lem:heptagon-two-stage-CY-to-chiral}\textup); the
mixed-HT-strings bridge is the holomorphic de~Rham obstruction
$\hypHTGlobalDR$; the igusa bridge is Construction Problem~1
\textup(the operator $\mathfrak D_X\in
\operatorname{End}_{\mathrm{ChirHoch}}(H_X)$ on $K3 \times E$ with
$\iota_{\mathrm{aut}}(\protectedPfaff{\mathfrak D_X}) = \Deltafive$\textup).

\section*{From algebraic geometry to physics}

Volume~I constructs $E_n$-chiral algebras on curves: the ordered
bar complex $B^{\mathrm{ord}}(\cA) = T^c(s^{-1}\bar\cA)$ with
deconcatenation coproduct; the derived chiral centre
$\cZ^{\mathrm{der}}_{\mathrm{ch}}(\cA)$ carrying $E_2$ structure
by chiral Deligne, and $E_3$ structure only after topologisation;
the $\SCchtop$ pair
$(\cZ^{\mathrm{der}}_{\mathrm{ch}}(\cA),\, \cA)$.
Volume~II identifies the physics: under the HT prefactorisation
and exact-sector hypotheses, the chiral derived centre coincides
with the 3d holomorphic-topological bulk observables
(Reshetikhin--Turaev topological bulk is a distinct construction
and appears only where explicitly invoked),
\[
 \underbrace{\cZ^{\mathrm{der}}_{\mathrm{ch}}(\cA)}_{\text{chiral Hochschild cochains}}
 \;=\;
 \underbrace{\text{bulk algebra of 3d HT QFT}}_{\text{Costello--Gaiotto BV observables}},
 \qquad
 (\cZ^{\mathrm{der}}_{\mathrm{ch}}(\cA),\, \cA)
 \;=\;
 \text{boundary-bulk system},
\]
After the Sugawara/antighost topologisation, the $E_3$-topological
algebra is the local-operator algebra of the corresponding 3d
topological factorisation theory.

\paragraph{Paradigm case: three-dimensional gravity.}
Einstein gravity on AdS$_3$ with cosmological constant
$\Lambda = -1/\ell^2$ is $SL(2, \R) \times SL(2, \R)$
Chern--Simons theory (Ach\'ucarro--Townsend, Witten) with
Brown--Henneaux boundary symmetry algebra
$\mathrm{Vir}_c \otimes \overline{\mathrm{Vir}}_c$ at central
charge $c = 3\ell/(2G_N)$. Volume~I's modular Koszul duality of
$\mathrm{Vir}_c$ packages into
\[
 \Theta_{\mathrm{Vir}_c} \;\in\; \mathrm{MC}\bigl(B^{\mathrm{ord}}(\mathrm{Vir}_c)\bigr),
 \qquad
 \Theta_{\mathrm{Vir}_c}|_{\overline{\cM}_g}
 \;=\;
 \text{genus-$g$ perturbative partition function around empty AdS}_3.
\]
The shadow tower of $\Theta_{\mathrm{Vir}_c}$ classifies the
nonlinear complexity of the gravitational dynamics; its five
structural properties are the five main theorems of Volume~I.
\textbf{Volume~II identification.}
Primitive: open factorization dg-category $\cC$ on the
gravitational boundary. Bulk observables:
$\cZ^{\mathrm{der}}_{\mathrm{ch}}(\cA_b)$ for any chart
$A_b = \End_\cC(b)$. Line operators: modules over the Koszul
dual. Modularity: trace + clutching on $\cC$. Family
substitution: $\mathrm{Vir}_c \to \cW_N$ gives higher-spin
gravity (Rung~$N$); $\mathrm{Vir}_c \to \widehat{\fg}_k$ gives
gauge theory (Rung~$2$). The MC element, bar complex, and
Swiss-cheese structure persist uniformly.

\paragraph{Mechanism: holomorphic-topological decomposition of
spacetime.}
On $\C_z \times \R_t$ the factorization structure splits into
two operations:
\[
 \underbrace{\FM_k(\C_z)}_{\text{holomorphic collisions}}
 \;\xrightarrow{\;d_{\barB}\;}\;
 \text{bar differential},
 \qquad
 \underbrace{\Conf_k(\R_t)}_{\text{topological splittings}}
 \;\xrightarrow{\;\Delta\;}\;
 \text{deconcatenation coproduct}.
\]
Together: $\barB(\cA)$ is a dg coassociative coalgebra with
$E_1$-chiral structure on $\C_z$ and symmetric deconcatenation
on $\R_t$. The $\SCchtop$ datum is the pair
$(C^\bullet_{\mathrm{ch}}(\cA, \cA),\, \cA)$ of bulk Hochschild
cochains acting on boundary algebra; this is the universal
$\SCchtop$-algebra (the $\SCchtop$ structure lives on the pair,
not on $\cA$ itself). The differential records which
operators can collide; the coproduct records which operators
can be separated.

\paragraph{Primitive object: the open-sector factorization
dg-category.}
\[
 \cC \in \mathrm{FactCat}(\widetilde{X}_D),
 \qquad
 \Ob(\cC) = \{\text{boundary conditions}\},
 \qquad
 \Hom_\cC(b, b') = \{\text{open-string states}\}
\]
on the bordified curve $\widetilde{X}_D$
(Definition~\ref{def:oc-factorization-category}).
Each vacuum $b \in \cC$ produces $A_b := \End_\cC(b)$ and
$\barB(A_b)$. The Morita class of $\cC$, not the
quasi-isomorphism class of $A_b$, is the invariant
(Theorem~\ref{thm:boundary-algebra-from-vacuum}).

\paragraph{Primitive hierarchy.}
\[
\cC_{\mathrm{op}}
\;\xrightarrow{\;\End(b)\;}\;
A_b
\;\xrightarrow{\;\barB\;}\;
\barB(A_b)
\;\xrightarrow{\;\Theta\;}\;
\Theta_\cA
\;\xrightarrow{\;+\mu^{M_j}\;}\;
\Theta^{\mathrm{oc}}_\cA.
\]
Each arrow is lossy with named kernel.
$\End(b)$: Morita class preserved, chart $b$ forgotten.
$\barB$: algebra $\to$ coalgebra; deconcatenation recovers $A_b$.
$\Theta$: coalgebra $\to$ MC element on $\mathrm{MC}(\barB)$.
$+\mu^{M_j}$: adjoins module couplings on five marked-point
geometries $M_j$ of $\SCchtop$.
Volume~I constructs arrows 1--4. Volume~II constructs arrow~5,
identifies its 3d output, and proves that
$\Theta^{\mathrm{oc}}_\cA$ is the full open/closed package: bulk
algebra + boundary algebra + module couplings + trace + clutching.

\paragraph{Three independent objects from $\cC$.}
\emph{Universal bulk}: the chiral derived centre
$\cZ^{\mathrm{der}}_{\mathrm{ch}}(\cC) =
\RHom_{\operatorname{Fun}(\cC,\cC)}(\operatorname{Id},
\operatorname{Id})$, computed in any chart by chiral Hochschild
cochains $C^\bullet_{\mathrm{ch}}(A_b, A_b)$.
\emph{Line-side / open-colour Koszul dual algebra}
$A^!_{\mathrm{line}}$: extracted from the Verdier dual
$D_{\Ran}(\barB(A_b)) \simeq \barB(A_b^!)$ (Theorem~A
intertwining); on the Koszul locus, its cohomology recovers
$A^!_{\mathrm{line}}$.
\emph{Twisting morphism classifier}: the bar complex
$\barB(A_b)$, representing universal couplings between $A_b$
and $A_b^!$ via Vol~I Theorem~A. Three different objects from three different functors solving
three different problems: the bar complex is not the bulk.

\paragraph{Bar as Lagrangian self-intersection.}
The bar complex carries a geometric interpretation as a
formal-local Koszul model for a Lagrangian self-intersection
$\cL \times_{\cM}^h \cL$ in a $(-2)$-shifted symplectic stack
$\cM$ (Theorem~\ref{thm:bar-is-self-intersection}). The
differential is the Koszul resolution of the diagonal; the
coproduct is the groupoid diagonal. The chiral algebra
determines a formal local bulk model: the boundary fixes the
shifted-cotangent side
(Theorem~\ref{thm:holographic-reconstruction}), but recovering
the actual formal neighborhood requires the formal Darboux
theorem for $(-2)$-shifted symplectic stacks. The holographic
reconstruction on this surface is precisely the formal Darboux
comparison.

\paragraph{Invariants of the $(-2)$-shifted symplectic geometry.}
\emph{Curvature of the Lagrangian embedding.}
$\kappaChHodge(\cA)$ controls the $d_{\barB}^2$ defect:
\[
 d_{\barB}^2 \;=\; \kappaChHodge(\cA) \cdot \omega_g,
\]
at $g = 1$ by the Hodge class $\omega_1$. For $g \ge 2$ the
chain-level continuation requires exactly two inputs:
modular-bootstrap $H^2$ acyclicity and the degree-$2$
curved-Dunn comparison map
(Theorem~\ref{thm:curved-dunn-H2-vanishing-all-genera},
Chapter~\ref{ch:curved-dunn-higher-genus}).

\emph{Complementarity (Vol~I, Thm~C).} The decomposition
\[
 Q_g(\cA) + Q_g(\cA^!)
 \;=\;
 H^*\bigl(\overline{\cM}_g, Z(\cA)\bigr)
\]
lifts to the bulk-boundary-line triangle as a Lagrangian
splitting of the self-intersection complex, under perfectness
and chain-level nondegeneracy hypotheses. All standard families
satisfy these hypotheses; outside them the obstruction is the cone
of the induced pairing map on the self-intersection complex.

\paragraph{Three structure theorems from Volume~I.}
\begin{description}[leftmargin=1.6em,itemsep=2pt,topsep=1pt]
 \item[Algebraicity (Riccati).]
 $H(t)^2 = t^4\, Q_L(t)$ with $Q_L$ quadratic; growth rate
 $\rho$ of the shadow obstruction tower determines convergence
 of the genus expansion.
 \item[Obstruction-depth identification.]
 Shadow obstruction tower equals the $L_\infty$ obstruction tower
 at all degrees. Vanishing of the whole positive-depth tower is
 $L_\infty$ formality; finite non-zero termination is finite
 obstruction depth.
 \item[Complementarity holomorphic-topological split.]
 The $(-1)$-shifted symplectic structure on the
 self-intersection complex $C_g(\cA)$ realises the Lagrangian
 decomposition $C_g(\cA) = Q_g(\cA) + Q_g(\cA^!)$.
\end{description}

\paragraph{Shadow depth classification as bulk-boundary
taxonomy.}
The critical discriminant
$\Delta = 8\kappaChHodge S_4$ is the mixed-shell scalar; the
contact flag is a separate invariant.  Here
$\chi_{\mathrm{ct}}$ denotes the finite contact shell and
$\mu_{\mathrm{mix}}$ denotes the mixed Virasoro wheel.
\[
 \begin{array}{l|l|l}
 \text{class} & \text{depth datum} & \text{geometry} \\
 \hline
 \mathbf{G} & \Delta = 0,\ \chi_{\mathrm{ct}}=0,\ \mu_{\mathrm{mix}}=0
 & \text{formal; bulk classical; clean self-intersection; $m_k = 0$ for $k \ge 3$} \\
 \mathbf{L} & \Delta = 0,\ S_3\ne0,\ \chi_{\mathrm{ct}}=0
 & \text{Lie-transverse; finite cubic transfer} \\
 \mathbf{C} & \chi_{\mathrm{ct}}=1,\ \mu_{\mathrm{mix}}=0
 & \text{finite contact excess; rank-one contact layer} \\
 \mathbf{M} & \mu_{\mathrm{mix}}=1,\ \Delta\ne0
 & \text{mixed Virasoro wheel; infinite $\Ainf$ obstruction tower}
 \end{array}
\]
Recovery: $A_b$ from genus-$0$ closed data of $\Theta_\cA$;
higher-genus shadow data from descent through the shadow
obstruction tower.

\section*{Tensor products and the bar complex}

Over a field $k$, the bar of a tensor product can retain mixed classes.
For the polynomial augmentations $x\mapsto0$, $y\mapsto0$, the
variables $x$ and $y$ in $k[x,y]$ give the
bar cycle $[x|y]-[y|x]$. Its two boundary terms cancel because
$xy=yx$. The tensor comparison below shows why this mixed class
survives, and specifies the algebraic maps that a geometric comparison
must extend.

For an ordinary unital augmented associative
$k$-algebra $R$, write $I_R=\ker(\varepsilon_R\colon R\to k)$.
Its normalized, coaugmented bar complex is
\[
 B_n(R)=I_R^{\otimes n},\qquad B_0(R)=k,\qquad
 B(R)=\bigoplus_{n\geq0}B_n(R).
\]
In this comparison, degrees are homological: a word
$[r_1|\cdots|r_n]$ has degree $n$, and the differential lowers degree.
The equivalent cohomological convention places this word in degree
$-n$. No geometric form degree is included in this notation. Set
\begin{equation}\label{eq:intro-ordinary-bar-differential}
 d[r_1|\cdots|r_n]
 =\sum_{i=1}^{n-1}(-1)^i
 [r_1|\cdots|r_ir_{i+1}|\cdots|r_n].
\end{equation}
Associativity cancels the two orders of adjacent multiplication, so
$d^2=0$. The coproduct cuts a word into an initial and a final segment,
including the two empty segments. The coaugmentation sends $1$ to the
empty word $[\,]$, and the counit projects onto $B_0(R)$.
These cuts make $B(R)$ a conilpotent differential graded coalgebra:
the reduced iterated coproduct of a word of length $n$ vanishes after
more than $n$ nonempty factors.
The coderivation identity follows by matching a merger and a cut
with the same merger in the segment that contains it.

For augmented algebras $A,A'$, put $C=A\otimes_k A'$ with
augmentation $\varepsilon_A\otimes\varepsilon_{A'}$. Define
$\iota(a)=a\otimes1$ and $\jmath(b)=1\otimes b$.
A $(p,q)$-shuffle interleaves $p$ letters of the first kind and $q$
letters of the second kind while preserving their separate orders.
Let $N(\sigma)$ count pairs in which a second-kind letter precedes a
first-kind letter. Since every suspended letter has degree one, the
Koszul sign is $(-1)^{N(\sigma)}$. Define the shuffle map by
\begin{equation}\label{eq:intro-bar-shuffle}
 \operatorname{sh}([a_1|\cdots|a_p]\otimes[b_1|\cdots|b_q])
 =\sum_{\sigma\in\operatorname{Sh}(p,q)}(-1)^{N(\sigma)}
 [\sigma(\iota(a_1),\ldots,\iota(a_p);
                 \jmath(b_1),\ldots,\jmath(b_q))].
\end{equation}
The notation in brackets means the interleaved word. Empty lists are
allowed. For the reverse map, use the canonical vector-space splitting
\[
 I_C=(I_A\otimes1)\oplus(1\otimes I_{A'})
                  \oplus(I_A\otimes I_{A'}).
\]
The Alexander--Whitney map $\operatorname{AW}$ retains words consisting
of first-kind letters followed by second-kind letters:
\begin{equation}\label{eq:intro-bar-aw}
 \operatorname{AW}
 [\iota(a_1)|\cdots|\iota(a_p)|\jmath(b_1)|\cdots|\jmath(b_q)]
 =[a_1|\cdots|a_p]\otimes[b_1|\cdots|b_q].
\end{equation}
It vanishes on every other word in the displayed direct-sum
decomposition, including any word with a letter in $I_A\otimes I_{A'}$.
Extend both maps $k$-linearly.

\begin{proposition}[The ordinary bar tensor comparison]
\label{prop:intro-bar-tensor-comparison}
For ordinary augmented associative algebras over a field, the maps
\[
 B(A)\otimes_k B(A')
 \mathrel{\mathop{\rightleftarrows}^{\operatorname{sh}}_{\operatorname{AW}}}
 B(A\otimes_k A')
\]
are natural chain homotopy equivalences, with
$\operatorname{AW}\operatorname{sh}=\operatorname{id}$.
The tensor differential is
$d(u\otimes v)=du\otimes v+(-1)^{|u|}u\otimes dv$.
The shuffle map preserves the coaugmentation, counit, and coproduct,
where the tensor coproduct is
\[
 \Delta(u\otimes v)
 =\sum(-1)^{|u_{(2)}||v_{(1)}|}
 (u_{(1)}\otimes v_{(1)})\otimes(u_{(2)}\otimes v_{(2)}).
\]
Here $\Delta u=\sum u_{(1)}\otimes u_{(2)}$, and similarly for $v$.
The Alexander--Whitney map need not preserve the coproduct.
If $A,A'$ are commutative, both bars have the signed shuffle product.
Then $\operatorname{sh}$ is a morphism of differential graded
bialgebras, and $\operatorname{AW}$ preserves the shuffle product.
All tensor products and total complexes in this statement use direct sums.
\end{proposition}

\begin{proof}
In $d\operatorname{sh}$, multiplication of adjacent letters of
different kinds cancels with the term having their order reversed.
Their products agree in $C$, while their shuffle signs differ by $-1$.
For adjacent first-kind letters $a_i,a_{i+1}$, let $j$ second-kind
letters precede them. Their multiplication has sign $(-1)^{i+j}$.
Merging them reduces the shuffle inversion count by $j$.
The resulting sign is therefore $(-1)^i$ times the shorter shuffle
sign, as in $\operatorname{sh}(du\otimes v)$.
For adjacent second-kind letters $b_j,b_{j+1}$ with $i$ first-kind
letters before them, the inversion count decreases by $p-i$.
Their total sign is $(-1)^{p+j}$ times the shorter shuffle sign.
This is the sign in $(-1)^p\operatorname{sh}(u\otimes dv)$.
Thus $\operatorname{sh}$ is a chain map.

Products involving a letter in $I_A\otimes I_{A'}$ remain in that
summand. Multiplication of different kinds also lands there.
An out-of-order word cannot become an ordered word by a same-kind
merger. Consequently, the only terms surviving $\operatorname{AW}d$
are the internal mergers in the two blocks of
\eqref{eq:intro-bar-aw}. Their signs are $(-1)^i$ and $(-1)^{p+j}$,
respectively. This proves the chain-map identity for
$\operatorname{AW}$. Exactly one shuffle has all first-kind letters
first, and its sign is $+1$. Hence
$\operatorname{AW}\operatorname{sh}=\operatorname{id}$.

For the other homotopy, an explicit free resolution avoids any
assumption about the homology of the bars. For an augmented algebra
$R$, set $P_n(R)=R\otimes I_R^{\otimes n}$ and write its elements
as $r[r_1|\cdots|r_n]$. In positive degrees its differential is
\[
 \partial(r[r_1|\cdots|r_n])
 =rr_1[r_2|\cdots|r_n]
 +\sum_{i=1}^{n-1}(-1)^i
 r[r_1|\cdots|r_ir_{i+1}|\cdots|r_n].
\]
The differential vanishes on $P_0(R)$.
Let $\epsilon_P\colon P(R)\to k$ be $\varepsilon_R$ in degree zero
and zero in positive degrees. Let $\eta_P(1)=1[\,]$ and
$e_R=\eta_P\epsilon_P$. The $k$-linear map
\[
 h_R(r[r_1|\cdots|r_n])
 =1[r-\varepsilon_R(r)1|r_1|\cdots|r_n]
\]
satisfies $\partial h_R+h_R\partial=\operatorname{id}-e_R$.
For $n>0$, its first face gives
$(r-\varepsilon_R(r)1)[r_1|\cdots|r_n]$.
The next face and the first face of $h_R\partial$ together give
$\varepsilon_R(r)1[r_1|\cdots|r_n]$. All later faces cancel in pairs.
In degree zero, $\partial h_R(r[\,])=(r-\varepsilon_R(r)1)[\,]$.
Thus $P(R)$ resolves the
augmentation module by free left $R$-modules. Tensoring with the
right augmentation module gives $k\otimes_R P(R)=B(R)$.

Set $P=P(C)$ and $Q=P(A)\otimes_kP(A')$. The latter is a complex
of free left $C$-modules, with
$Q_n=\bigoplus_{p+q=n}C\otimes I_A^{\otimes p}\otimes I_{A'}^{\otimes q}$.
Its contraction onto $k$ is
\[
 h_Q(u\otimes v)=h_Au\otimes v+(-1)^{|u|}e_Au\otimes h_{A'}v.
\]
The tensor differential gives
$\partial h_Q+h_Q\partial=\operatorname{id}-e_A\otimes e_{A'}$.
Extend the shuffle to a $C$-linear map $F\colon Q\to P$ by
\[
 F(a_0[a_1|\cdots|a_p]\otimes b_0[b_1|\cdots|b_q])
 =(a_0\otimes b_0)\operatorname{sh}([a_1|\cdots|a_p]
                                      \otimes[b_1|\cdots|b_q]).
\]
The preceding cancellation proves the internal-face identity.
Shuffles beginning with a first-kind letter give the first face
of $P(A)$. Those beginning with a second-kind letter have $p$
additional inversions and give $(-1)^p$ times the first face of
$P(A')$. Hence $F$ is a chain map lifting $\operatorname{id}_k$.

Construct a $C$-linear reverse map $G\colon P\to Q$ recursively on
the free generator spaces $I_C^{\otimes n}$:
\[
 G_0(1[\,])=1[\,]\otimes1[\,],\qquad
 G_n(1[w])=h_QG_{n-1}\partial(1[w])\quad(n\geq1).
\]
Extend each formula $C$-linearly after evaluating $h_Q$.
Induction shows that $G_{n-1}\partial(1[w])$ is a cycle with zero
augmentation. The contraction identity therefore gives
$\partial G_n=G_{n-1}\partial$.

For completeness, the same recursion constructs the comparison
homotopies. Let $T$ be $FG-\operatorname{id}_P$ or
$GF-\operatorname{id}_Q$, and use $h_P=h_C$ or $h_Q$, respectively.
Put $K_{-1}=0$. For every element $e=1\otimes v$ of the canonical
free generator space in degree $n$, set
\[
 K_n(e)=h\bigl(T_n(e)-K_{n-1}\partial e\bigr),
\]
then extend $C$-linearly. The expression in parentheses is a cycle:
this follows from the chain-map identity for $T$ and the preceding
inductive homotopy identity. Its degree-zero augmentation vanishes.
Thus $\partial K+K\partial=T$. These formulas are finite in every
degree and prove that $F$ and $G$ are chain homotopy inverses.
After tensoring over $C$ with $k$, the map $F$ is
$f=\operatorname{sh}$. Write $g$ for the induced reverse map and
$K$ for the induced homotopy
$dK+Kd=fg-\operatorname{id}$ on $B(C)$.
Since $\operatorname{AW}f=\operatorname{id}$, the map
$(\operatorname{id}-f\operatorname{AW})K$ satisfies
\[
 d\bigl((\operatorname{id}-f\operatorname{AW})K\bigr)
 +\bigl((\operatorname{id}-f\operatorname{AW})K\bigr)d
 =f\operatorname{AW}-\operatorname{id}.
\]
This supplies the claimed homotopy for the displayed maps themselves.

A cut of an interleaving specifies cuts of the two original words,
an interleaving on each side, and the pairs crossing the cut.
If the initial pieces have lengths $i,j$, there are $(p-i)j$
crossing inversions. This is exactly the sign in the tensor coproduct.
It proves $\Delta\operatorname{sh}
=(\operatorname{sh}\otimes\operatorname{sh})\Delta$.
Empty words give preservation of coaugmentation and counit.
However, for $a\in I_A$ and $b\in I_{A'}$,
$\operatorname{AW}[\jmath(b)|\iota(a)]=0$, while its middle
coproduct cut maps to
$(1\otimes[b])\otimes([a]\otimes1)$, which can be nonzero.
Thus $\operatorname{AW}$ is not generally a coalgebra map.

When $R$ is commutative, signed interleaving defines a product
$\star_{\mathrm{sh}}$ on $B(R)$. Associativity follows by identifying both
iterated products with interleavings of three ordered lists.
Interchanging the lists contributes $(-1)^{pq}$, and the empty
word is the unit. Mixed adjacent mergers cancel by commutativity,
so $d$ is a derivation. The cut argument proves compatibility
with the coproduct. Interleaving four lists shows that
$\operatorname{sh}$ preserves products. For $\operatorname{AW}$,
a product can survive only if both input words have the block form
\eqref{eq:intro-bar-aw}. The surviving interleavings give the two
factor shuffle products with the tensor-product sign $(-1)^{qr}$,
where $q$ is the second-block length of the first input and $r$
is the first-block length of the second input.
For a noncommutative algebra the obstruction already appears as
$d([a]\star_{\mathrm{sh}}[b])=-[ab]+[ba]$. No shuffle-product compatibility
is asserted in that case. Every construction commutes with maps of
augmented algebras, which proves naturality.
\end{proof}

Writing $B_+(R)=\bigoplus_{n\geq1}B_n(R)$, restriction to positive
degrees gives the equivalence of complexes
\[
 B_+(A\otimes A')\simeq B_+(A)\oplus B_+(A')
                   \oplus\bigl(B_+(A)\otimes B_+(A')\bigr).
\]
The homotopies preserve these positive-degree subcomplexes.
The additive statement occurs in degree one. Equation
\eqref{eq:intro-ordinary-bar-differential} gives
$H_1B(A)=I_A/I_A^2$. Since $I_A\otimes I_{A'}\subset I_C^2$,
the unit inclusions and the two augmentation projections induce
mutually inverse maps
\[
 H_1B(A\otimes A')\cong H_1B(A)\oplus H_1B(A').
\]
This degree-one identity does not identify the full bar complexes.
For the counital convention, $B(k)=k$, whereas $B(k)\oplus B(k)$
has dimension two in degree zero.

The positive-length convention also retains mixed homology.
For $A=k[x]$, $A'=k[y]$, augmented by evaluation at zero,
the shuffle of $[x]\otimes[y]$ is
$z=[x\otimes1|1\otimes y]-[1\otimes y|x\otimes1]$.
The functional on $B_1(k[x])$ that extracts the coefficient of $x$
vanishes on products of augmentation-ideal elements. Extended by zero
in other degrees, it is a chain map to $k$ in degree one.
Use the corresponding $y$-functional on $B(k[y])$.
Their tensor product, composed with $\operatorname{AW}$, takes $z$
to $1$, so $z$ is not a boundary. On the other hand,
$0\to k[x]\xrightarrow{x}k[x]\to k\to0$ is a free resolution.
It gives $H_nB(k[x])=0$ for $n\geq2$, and similarly for $k[y]$.
Thus even after removing the length-zero summand, the bar of the
tensor product cannot be replaced by the direct sum of the two bars.

\paragraph{The geometric tensor comparison.}
A chiral extension must construct maps on configuration-space
coefficients as well as on field labels. For this geometric question,
work over $\mathbb C$. On a smooth complex curve
$X$, fix a geometric model with coefficient complexes $K_n$ on the
ordered Fulton--MacPherson spaces $\FM_n(X)$ for $n\geq1$.
For the empty word, put $\FM_0(X)=\mathrm{pt}$ and $K_0=\mathbb C$.
For an ordered partition
$I\sqcup J=\{1,\ldots,p+q\}$, let $\pi_I,\pi_J$ forget the
complementary points. Each shuffle requires a coefficient map
\[
 \pi_I^*K_p\otimes\pi_J^*K_q\longrightarrow K_{p+q},
\]
together with unit inclusions of the two field-label complexes into
their specified tensor product. In a logarithmic de~Rham model, the
candidate coefficient map is pullback followed by wedge product.
The pullbacks, tensor product, and suspensions must belong to that
chosen model. For right differential modules $L_1,\ldots,L_n,M$,
chiral operations are morphisms
\[
 j_*j^*(L_1\boxtimes\cdots\boxtimes L_n)\longrightarrow\Delta_*M
 \quad\text{of right $\mathcal D_{X^n}$-modules}.
\]
Here $j\colon\operatorname{Conf}_n(X)\hookrightarrow X^n$ is the
distinct-point inclusion and $\Delta\colon X\hookrightarrow X^n$
is the small diagonal
\cite[\S3.1.1--3.1.2]{BD04}. A permutation of vector-space labels
does not supply this differential-module morphism.

The maps involving $K_0$ must be the unit maps.
The resulting signed sum must commute with the total differential,
including every collision residue and higher operation. It must also
respect cuts, units, nested collisions, and any specified completion.
In particular, the cancellation of mixed adjacent multiplications in
the proof above needs a geometric counterpart on mixed collision
divisors. Proving a quasi-isomorphism then requires a contraction or
comparison in this coefficient category. These maps and identities
constitute the chiral tensor-comparison problem. The ordinary theorem
does not identify its direct-sum carrier with a completed geometric bar.

\section*{The differential: holomorphic factorisation}

The logarithmic form $\eta_{ij}=d\log(z_i-z_j)$ records a
collision divisor. Its logarithmic character concerns geometric
coefficients and is separate from the tensor comparison above.

\begin{remark}[$E_1$ primacy]
\label{rem:vol2-e1-primacy}
The natural primitive of the algebraic engine is the
$E_1$/ordered story, not the modular/symmetric one. The bar
differential lives on \emph{ordered} configuration spaces
$\FM_k(\C)$; the deconcatenation coproduct preserves linear
ordering on $\Conf_k(\R)$. The averaging map
$\mathrm{av}\colon \mathfrak{g}^{E_1}_\cA \to
\mathfrak{g}^{\mathrm{mod}}_\cA$ is the
$\Sigma_n$-coinvariant projection
($T^c \to \Sym^c$). Vol~I's Theorems~A--D$+$H are the invariants
that survive: $\kappaChHodge(\cA)$ at degree~$2$
(equal to $\mathrm{av}(r(z))$ for abelian algebras,
$\mathrm{av}(r(z)) + \dim(\fg)/2$ for non-abelian Kac--Moody,
where the second summand is the Sugawara shift), and the shadow
obstruction tower $(\Theta_\cA^{\le r})_{r \ge 2}$.
Part~\ref{part:e1-core} is the \emph{primary} layer; the
remaining Parts describe the modular/symmetric framework that is
its $\mathrm{av}$-image.
See Vol~I, \S\textup{E$_1$ as primitive}.
\end{remark}

\begin{remark}[Dimensional reduction across the trilogy]
\label{rem:vol2-dimensional-reduction}
This volume operates at Level~2 of the dimensional reduction
hierarchy (Remark~\ref*{V1-rem:three-volume-dimensional-reduction}):
the 3d theory on $\C_z \times \R_t$, with bar differential as
the holomorphic colour and coproduct as the topological colour.
\end{remark}

\paragraph{Residues with coefficients.}
For states $a,b$ of a complex vertex algebra, fix a formal
coordinate $t=z-w$ and write the full vertex operator
as $Y(a,t)b=\sum_{m\in\mathbb Z}(a_{(m)}b)t^{-m-1}$.
For fixed states the singular part is finite. A Laurent polynomial
$f(t)=\sum_j f_jt^j$ defines the relative residue test
\begin{equation}\label{eq:intro-residue-test}
 R_f(a,b)=\Res_{t=0}f(t)Y(a,t)b\,dt
         =\sum_j f_j a_{(j)}b.
\end{equation}
Indeed, the exponent $j-m-1$ equals $-1$ precisely when $j=m$.
The sum is finite, and $f=t^m$ extracts the $m$th mode.
Polynomial coefficients test the singular modes; negative powers
of $t$ can test the regular operator product. In particular,
\[
 \Res Y(a,t)b\,d\log t=a_{(-1)}b,\qquad
 \Res t^{m+1}Y(a,t)b\,d\log t=a_{(m)}b.
\]
Multiplying the singular part alone by $d\log t=dt/t$ gives
zero residue: it increases each pole order by one. Thus a
logarithmic normal form and an OPE coefficient are distinct data.

For the Heisenberg current $J(t)J(0)\sim k t^{-2}\mathbf1$,
polynomial tests give
\[
 R_f(J,J)=k f'(0)\mathbf1,\qquad
 R_1(J,J)=0,\qquad R_t(J,J)=k\mathbf1.
\]
The level survives on a coordinate disc at genus zero. Differentiating
the current gives $R_f(\partial J,J)=-k f''(0)\mathbf1$,
since its singular product is $-2k t^{-3}\mathbf1$.
For the bosonic $\beta\gamma$ convention
$\beta(t)\gamma(0)\sim t^{-1}\mathbf1$ and
$\gamma(t)\beta(0)\sim-t^{-1}\mathbf1$, the same tests give
\[
 R_f(\beta,\gamma)=f(0)\mathbf1,\qquad
 R_f(\gamma,\beta)=-f(0)\mathbf1,
 \qquad R_f(\beta,\beta)=R_f(\gamma,\gamma)=0.
\]
These formulas concern polynomial $f$. For example,
$R_{t^{-1}}(J,J)=J_{(-1)}J$ uses the regular product.
They compute local operations, not cycles or global cohomology.

\paragraph{The chiral Lie coefficient complex.}
A collision changes the support of its coefficient. To include
its target, let $X$ be a smooth separated complex algebraic curve
and let $\cA$ be a strict differential graded chiral Lie algebra
of quasi-coherent right $\mathcal D_X$-modules. Its degree-zero
operation is a morphism
\[
 \mu:j_{2,*}j_2^*(\cA\boxtimes\cA)\longrightarrow\Delta_*\cA,
\]
where $j_2$ includes the complement of the diagonal and
$\Delta:X\hookrightarrow X^2$. It is graded antisymmetric,
$d_{\cA}$ is a derivation of $\mu$, and the chiral Jacobi
identity is an equality of the three iterated operations from
$j_{3,*}j_3^*(\cA^{\boxtimes3})$ to the small-diagonal
direct image of $\cA$. Closed-embedding direct image here is
the differential-module direct image, including normal differential
operators. Formula~\eqref{eq:intro-residue-test} is a local relative
de Rham test of this operation, not its whole target module.

Put $V=\cA[1]$, with $V^m=\cA^{m+1}$ and $|sa|=|a|-1$.
For a finite nonempty label set $I$, a partition $\pi$ has block
set $T$ and a diagonal $\Delta_\pi:X^T\hookrightarrow X^I$.
Let $j_T:\Conf_T(X)\hookrightarrow X^T$ include the locus
of distinct block coordinates. Define
\begin{equation}\label{eq:intro-chiral-lie-carrier}
 C_I(\cA)=\bigoplus_{\pi\in\mathcal P(I)}
          \Delta_{\pi,*}j_{T,*}j_T^*(V^{\boxtimes T}).
\end{equation}
Each partition occurs once, with the Koszul symmetry on $V$.
This is a finite sum in each arity, with algebraic localization
and no completion. It retains the full coefficient $\cA$.
For a unital vertex algebra, the vacuum is not central on all
meromorphic tests: $R_{t^{-1}}(\mathbf1,a)=a$.
Consequently quotienting out the vacuum does not by itself define
a chiral Lie coefficient complex.

Here is the collision map on a summand. For blocks $r,s\in T$,
let $T'=T/(r=s)$ and let $W_{rs}\subset X^T$ allow this pair
to collide while all other pairs remain distinct. Write
$k:W_{rs}\hookrightarrow X^T$ and
$\ell:\Conf_T(X)\hookrightarrow W_{rs}$.
Apply $\mu$ to the selected pair with identities on the spectators
over $W_{rs}$, then push forward by $k$. If
$\delta:X^{T'}\hookrightarrow X^T$ merges the pair, the result is
the degree-one map
\begin{equation}\label{eq:intro-chiral-collision-map}
 j_{T,*}j_T^*(V^{\boxtimes T})
 \longrightarrow
 \delta_*j_{T',*}j_{T'}^*(V^{\boxtimes T'})[1].
\end{equation}
The source identification is $k_*\ell_*=j_{T,*}$; the target
uses $\delta^{-1}W_{rs}=\Conf_{T'}(X)$ and composition of
direct images. Applying $\Delta_{\pi,*}$ gives a map on $X^I$.
Ordinary restriction of the fully localized source, by tensoring
its underlying $\mathcal O_{X^T}$-module with the structure sheaf
of the collision diagonal, would instead be zero. It cannot
replace this differential-module construction.

The differential $Q=Q_1+Q_2$ has suspended coefficients
\[
 q_1(sa)=-s(d_{\cA}a),\qquad
 q_2(sa,sb)=(-1)^{|a|}s\mu(a,b).
\]
The second expression denotes the morphism
\eqref{eq:intro-chiral-collision-map}, including its meromorphic
coefficient. On ordered suspended factors $v_1,\ldots,v_r$,
the unary term on $v_i$ has sign
$(-1)^{\sum_{h<i}|v_h|}$. For $i<j$, move $v_i,v_j$ to the
front, apply $q_2$, and put the merged block first, with all
spectators in their previous order. The sign for this move is
\[
 (-1)^{\chi_{ij}},\qquad
 \chi_{ij}=|v_i|\sum_{h<i}|v_h|
       +|v_j|\sum_{\substack{h<j\\h\ne i}}|v_h|.
\]
Antisymmetry of $\mu$ gives
$q_2(sa,sb)=(-1)^{(|a|-1)(|b|-1)}q_2(sb,sa)$.
Thus Koszul symmetry identifies all block orderings; no extra
sign representation is inserted.

\begin{proposition}[The coefficient differential]
\label{prop:intro-chiral-lie-square-zero}
For this strict differential graded chiral Lie input, $Q^2=0$
on $C_I(\cA)$ in every finite arity.
\end{proposition}
\begin{proof}
Two unary operations on the same factor give $d_{\cA}^2=0$;
on distinct factors they cancel as odd maps. A unary operation
and a disjoint binary contraction cancel for the same reason.
On two inputs the
unary--binary terms, with tensor signs, are
\[
 (-1)^{|a|}s\bigl(-d\mu(a,b)+\mu(da,b)
                         +(-1)^{|a|}\mu(a,db)\bigr)=0.
\]
Disjoint binary contractions also cancel by exchanging two
degree-one maps. For three original blocks with homogeneous
coefficients $x,y,z$ of unsuspended degrees $a,b,c$, the remaining
terms have the common factor
$(-1)^b s$ and coefficient
\[
 \mu(\mu(x,y),z)-(-1)^{bc}\mu(\mu(x,z),y)
                  +(-1)^{a(b+c)}\mu(\mu(y,z),x)=0.
\]
This is chiral Jacobi on the fully localized ternary source.
Spectator identities give the same cancellation in every arity.
After merging a pair, that original pair is absent from the block
set, so no same-pair endomorphism remains to square.
These possibilities exhaust the composite.
\end{proof}

For degree-zero $\cA$, the two-point complex has
$C_2^{-2}=j_{2,*}j_2^*(\cA\boxtimes\cA)$ and
$C_2^{-1}=\Delta_*\cA$. A Heisenberg contraction with coefficient
$t$ therefore lands in the diagonal vacuum state $k\mathbf1$,
not in an empty tensor word. The scalar Arnold relation among
logarithmic forms supplies neither this coefficient map nor
chiral Jacobi. A logarithmic realization further requires
coefficient extensions across collisions, their diagonal
identifications, and compatibility of residues with those maps.

\section*{The coproduct: topological factorisation along $\R$}

The ordinary augmented associative bar $B(R)$ defined above
carries the following strict coproduct:
\begin{equation}\label{eq:intro-delta}
\Delta[a_1|\cdots|a_n]
\;=\;
\sum_{i=0}^{n}
[a_1|\cdots|a_i]
\;\otimes\;
[a_{i+1}|\cdots|a_n].
\end{equation}
Both iterated coproducts sum over the same pairs of cuts
$0\leq i\leq j\leq n$, proving coassociativity. With the
homological differential~\eqref{eq:intro-ordinary-bar-differential},
\[
 \Delta d=(d\otimes\id+\id\otimes d)\Delta,
 \qquad (\id\otimes d)(u\otimes v)=(-1)^{|u|}u\otimes dv.
\]
To check this identity, a merger followed by a cut corresponds
to a cut followed by a merger inside one of its two segments.
If the right segment starts after $i$ letters, its tensor sign
$(-1)^i$ combines with its internal merger sign $(-1)^{j-i}$
to give the original sign $(-1)^j$.
Thus the ordinary bar is a counital coaugmented differential
graded coalgebra. Its underlying graded coalgebra is cofree
among conilpotent coaugmented graded coalgebras. This universal
property concerns maps to the specified deconcatenation coalgebra;
it is not uniqueness of all coproducts on the underlying vector space.

For two letters $a,b\in I_R$, the three cuts give
\[
 \Delta[a|b]=[\,]\otimes[a|b]+[a]\otimes[b]
                         +[a|b]\otimes[\,].
\]
Since $d[a|b]=-[ab]$ and $d[a]=d[b]=0$, both sides of the
coderivation identity are
$-[\,]\otimes[ab]-[ab]\otimes[\,]$.
An attempted scalar-valued contraction behaves differently.
If $d[J|J]=k[\,]$ and $d[J]=d[\,]=0$, then
\[
 \Delta d[J|J]=k[\,]\otimes[\,],\qquad
 (d\otimes\id+\id\otimes d)\Delta[J|J]
                 =2k[\,]\otimes[\,].
\]
At nonzero level these disagree. The diagonal vacuum coefficient
in $C_2(\cH_k)$ cannot be replaced by the coaugmentation of an
ordinary bar. Nor does $J_{(0)}J=0$ imply independence of
half-line observables: the test $R_t(J,J)=k\mathbf1$ remains.

To interpret cuts geometrically, set
$S_n=\FM_n(X)\times\Conf_n^<(\R)$ and choose a coefficient
complex $\mathscr G_n$ on $S_n$. For $p,q>0$ with $p+q=n$,
let $U_{p,q}\subset S_n\times\R$ impose
$t_p<t_*<t_{p+1}$. Write $u:U_{p,q}\to S_n$ for forgetting
the cut and $\pi_L,\pi_R$ for retaining the first $p$ and last
$q$ insertions. A geometric coproduct needs coefficient maps
\begin{equation}\label{eq:intro-geometric-cut-map}
 u^*\mathscr G_{p+q}\longrightarrow
 \pi_L^*\mathscr G_p\otimes\pi_R^*\mathscr G_q
\end{equation}
in a specified coefficient category. They must commute with the
total differential, agree for nested cuts, and include compatible
counit maps for empty segments. Passing to a global chain complex
also requires the corresponding descent and tensor comparison
maps, with any support or completion conditions stated.
The space $\Conf_n^<(\R)$ is convex and hence contractible;
this fact alone constructs none of these coefficient maps or
their compatibilities with holomorphic collisions.
The comparison between an ordered geometric bar and the chiral
Lie complexes~\eqref{eq:intro-chiral-lie-carrier} remains a
construction problem on these coefficients.

\section*{The Swiss-cheese comparison}

The separate holomorphic and topological parameters form
\[
 \FM_n(\C) \times \Conf_n^<(\R):
 \qquad
 \underbrace{\FM_n(\C)}_{\text{holomorphic configurations}}
 \;\times\;
 \underbrace{\Conf_n^<(\R)}_{\text{ordered positions}}.
\]
This parameter space does not specify the mixed operation spaces
or their compositions. In the coupled model of
Definition~\ref{def:SC}, closed-output operations have space
$\FM_k(\C)$, mixed open-output operations have space
$\FM_{k,m}(\mathbb H)$, and a closed output admits no open input.
Here $\mathbb H$ is the upper half-plane. The mixed space
compactifies configurations of $k$ interior and $m$ boundary
points, modulo real translations and positive real dilations.
The closed operadic space similarly uses complex translations
and positive real dilations. These operadic normalizations
are distinct from the absolute configuration spaces in $S_n$.
An action requires maps on the chosen coefficients for every
allowed colour profile, compatible with units, permutations,
and closed and mixed insertions.

The constructions above give a strict coalgebra on the ordinary
ordered bar and a square-zero differential on the chiral Lie
coefficient complex. An $E_1$ chiral coalgebra interpretation
requires the geometric cut maps~\eqref{eq:intro-geometric-cut-map}
and their collision compatibility. The derived-centre action on
$(C^\bullet_{\mathrm{ch}}(\cA,\cA),\cA)$ and the chiral
bar--cobar comparison require, in addition, their own coefficient,
operadic, and Koszul hypotheses. These comparisons are the
subject of Section~\ref{sec:foundations}; they do not follow from
the product of parameter spaces or from the local residue tests.

The self-intersection and physical comparisons below require
additional geometric identifications and a compatible complete
two-coloured action. Their hypotheses are independent of the
local residue and ordinary cut calculations above.
The formal-local model for the derived self-intersection is the
object whose fundamental class is the Maurer--Cartan element
$\alpha_T$ in the Swiss-cheese convolution algebra
$\gSC_T = \Hom_\Sigma(\mathbf B(\SCchtop), \End_{(\Bbound,
\cC_{\mathrm{line}})})$ (of which Vol~I's closed-colour MC element
$\Theta_\cA$ is the closed-component $\alpha_T^{\mathrm{cl}}$);
each direct closed/open/mixed projection of $\alpha_T$ extracts
a different convolution datum. The direct faces are: boundary
$\Ainf$ operations, open-colour line-category operations, the
PVA $\lambda$-bracket, the spectral $R$-matrix, the mixed
bulk-to-boundary map, the genus tower. The bulk-boundary-line
triangle is then assembled from these faces together with the
boundary-linear derived-centre theorem and (on the chirally
Koszul locus) the line-module and chiral-Hochschild
identifications. For the affine lineage on the reduced
evaluation comparison surface this convolution yields the
quantum-group line package:
$\cC_{\mathrm{line}}^{\mathrm{red}}|_{\mathrm{eval}} \simeq
\operatorname{Rep}_q(\fg)$ on evaluation modules; the bar
complex computes the coloured Jones polynomial
(Theorem~\ref{thm:affine-monodromy-identification},
Corollary~\ref{cor:jones-polynomial}).

The product $\C \times \R$ decomposes the self-intersection into
two colours: the closed colour from holomorphic collisions in
$\C$ (fibre direction); the open colour from topological cuts
in $\R$ (base direction). Open-to-closed operations are empty:
representations restrict from $G$ to $B$, not conversely. At
genus $g \ge 1$, the chiral $\Ainf$ structure acquires curvature
($d^2_{\mathrm{fib}} = \kappaChHodge \cdot \omega_g$,
chain-level under the curved-Dunn degree-$2$ comparison hypotheses,
Theorem~\ref{thm:curved-dunn-H2-vanishing-all-genera}) but the
$E_1$ coproduct remains flat
(Theorem~\ref{thm:topological-e1-rigidity}).

\paragraph{Three bar constructions and their inputs}
(see \S\ref{sec:bar_cobar}; master statement
Theorem~\ref{thm:two-color-master}). When the corresponding
coefficient constructions and Koszul hypotheses are available,
the notation distinguishes:
\[
 \begin{array}{l|l|l}
 \text{bar} & \text{content} & \text{Koszul dual} \\
 \hline
 \barB^{\mathrm{FG}}(\cA) & \text{localized chiral Lie operation on partitions} & \text{chiral Lie dual} \\
 \barB^{\Sigma}(\cA) & \text{factorization coefficients and symmetric descent} & \cA^! \\
 \barB^{\mathrm{ord}}(\cA) & \text{ordered coefficients and compatible cuts} & \cA^!_{\mathrm{line}}
 \end{array}
\]
Comparisons $\barB^{\Sigma} \leftrightarrow\barB^{\mathrm{ord}}$
require maps on these coefficients; ordering alone does not
construct them. They are studied in
Part~\ref{part:e1-core} (\S\ref{ch:ordered-associative-chiral-kd}).

\section*{The support-packet principle}
\label{sec:pole-order-principle}
\index{support-packet principle}

Suppose an ordered chiral bar with compatible collision and cut
maps has been constructed, together with its bar-intrinsic
support packet. Write
\[
\Theta_\cA=\sum_{r\ge2}\Theta_{\cA,r},
\qquad
r^\Theta_{\max}(\cA)=\sup\{r\ge2\mid\Theta_{\cA,r}\ne0\},
\]
after the chosen generator package has been closed under
normal-ordered products. OPE pole orders are input data for the
construction of \(\Theta_\cA\); they are not, by themselves, the
classification invariant.
\[
 \begin{array}{l|l|l|l|l}
 \text{class} & \text{support source} & \text{algebra} & m_k \text{ profile} & r^\Theta_{\max} \\
 \hline
 \mathbf{G} & \text{double, central coeff.} & \text{Heisenberg} & m_k = 0\ \forall k \ge 3 & 2 \\
 \mathbf{L} & \text{double, non-central residues} & \text{affine Kac--Moody} & m_3 \ne 0,\ m_4 = 0\ \text{(coh.)} & 3 \\
 \mathbf{C} & \text{simple generators; closed contact envelope} & \beta\gamma\text{ system} & \text{finite contact layer},\ m_{k\ge 5} = 0\ \text{(coh.)} & 4 \\
 \mathbf{M} & \text{mixed Virasoro wheel} & \text{Virasoro},\ \cW_N & m_k \ne 0\ \forall k \ge 3 & \infty \\
 \end{array}
\]

\textbf{Class $\mathbf{G}$ (Gaussian).}
$\Ainf$ structure trivial; bar complex formal.

\textbf{Class $\mathbf{L}$ (Lie).}
$m_3$ is the Lie--Jacobi transfer. The tower is finite, and it is
strictly formal only when this transfer class vanishes.

\textbf{Class $\mathbf{C}$ (contact).}
First nonlinear correction beyond Lie; finitely generated.

\textbf{Class $\mathbf{M}$ (mixed).}
The Virasoro wheel is detected first by the quartic
$T(z)T(w)$ channel, but the invariant is the non-terminating
support packet.  Borcherds identity converts the mixed channel
to the ternary cocycle \(m_3\); on the analytic SDR surface the
Kac determinant controls the surviving higher wheel classes.
The support packet is infinite; the perturbative hierarchy of
3d quantum gravity unfolds on that mixed shell.

\paragraph{Class transport via Drinfeld--Sokolov reduction.}
Sugawara:
$T(z) = \frac{1}{2(k + h^\vee)} \sum_a {:}J^a J^a{:}(z)$,
$c = k \dim \fg/(k + h^\vee)$. Double Wick contraction in
$T(z) T(w)$ produces a quartic pole proportional to $c$. DS
reduction is a BRST functor transporting the finite $\Ainf$
tower of $\widehat{\fg}_k$ to the infinite tower of the Virasoro
quotient (Theorem~\ref{thm:ds-hpl-transfer}):
$\mathbf{L} \xrightarrow{\;\mathrm{DS}\;} \mathbf{M}$ (class
transport, manufactured not postulated).

Canonical illustrations: Heisenberg ($\mathbf{G}$); affine
Kac--Moody ($\mathbf{L}$); $\beta\gamma$ ($\mathbf{C}$);
Virasoro and $\cW_N$ ($\mathbf{M}$); DS as L-to-M bridge. Full
development: \S\ref{def:gravity-shadow-depth-class}.

\paragraph{Spectral-depth gap migration.}
Quantitative refinement: each $T$-dependent monomial in $m_n$
carries derivative weight $w$ and spectral depth $d$,
constrained by $w + d = n - 1$. A depth-$d$ term requires a
weight-$(n - 1 - d)$ field; when no such field exists, that
depth is absent from the expansion. Virasoro has no weight-$1$
primary, so depth $n$ is absent from every $m_n$: gap at
$d_{\mathrm{gap}} = n$. For principal $\cW_N$, under the
principal DS/KZ sector hypotheses, the T-propagated binding-sector gap migrates:
$d_{\mathrm{gap}}^{\mathrm{bind}}(\cW_N, n) = 2N + n - 4$.
At $N = 2$: $d_{\mathrm{gap}} = n$ recovered. Full development:
\S\ref{sec:3d-gravity}, Theorem~\ref{thm:gap-migration};
ordered-sector refinement:
\S\ref{ch:ordered-associative-chiral-kd}.

\paragraph{Common combinatorial source.}
Scalar shadow at odd degree $k = 2r + 1$ factors as
\[
 |s_r| \;=\;
 \underbrace{C_{r-1}}_{\substack{\text{planar binary trees} \\ \text{Stasheff}\ K_r\ \text{on}\ \Conf_k(\R)}}
 \;\times\;
 \underbrace{\frac{(2r+1)!}{2}}_{\substack{L_0\text{-eigenvalues of } \partial^w T \\ \text{on}\ \FM_k(\C)}}.
\]
One factor from each Swiss-cheese colour: topological count
(Catalan, universal) $\times$ holomorphic weight (conformal
weight dependent). Gap migration: DS changes the holomorphic
factor, not the topological. Full development:
\S\ref{sec:3d-gravity}, Remark~\ref{rem:sesquilinearity-two-colour}.

\section*{The Heisenberg}

\paragraph{One generator, one pole.}
\[
 \cH_k\colon \quad
 \text{single generator } J,
 \quad
 J(z)\, J(w) \;\sim\; \frac{k}{(z-w)^2},
 \quad
 \text{no composite fields}.
\]
One generator, one pole order, no composites. Every step of the
3d machine checkable by hand.

The bar complex is $\barB(\cH_k) = T^c(s^{-1}\C \cdot J)$, with
cogenerator projection
$m_2(J, J) = k \cdot \mathbf{1}$
and all higher operations $m_{k \ge 3} = 0$ (no higher
singularities, no composites). The coproduct is deconcatenation.
Together (differential from $\FM_2(\C)$, coproduct from
$\Conf_k(\R)$, no higher interactions) the bar complex is an
$E_1$ dg coassociative coalgebra, formal: the $\R$-factorisation
is exact.

Passing to cohomology: on $H^\bullet(\cH_k, Q) = \C[J]$, the
binary $m_2$ decomposes. Regular part: commutative product.
Singular part: $\lambda$-bracket
\[
\{J \,{}_\lambda\, J\} \;=\; k\lambda
\]
(the $\lambda$-bracket encodes the singular OPE in
generating-function form
$\{a {}_\lambda b\} = \sum_{n \ge 0} (a_{(n)} b)\, \lambda^{(n)}$,
divided-power $\lambda^{(n)} = \lambda^n/n!$). Together: a
$(-1)$-shifted Poisson vertex algebra. Jacobi vacuous (single
generator); Leibniz is $\partial$-derivation; skew-symmetry is
OPE antisymmetry. Entire physical content in level $k$.

The Laplace transform of the $\lambda$-bracket gives the
pre-\textup{\(d\log\)} coefficient and the Fock scalar transport:
\[
r^L(z) \;=\; \sum_{n \geq 0} \frac{k\,\delta_{n,1}}{z^{n+1}}
\;=\; \frac{k}{z^2},
\qquad
R(z) \;=\; \exp\!\Bigl(\frac{k\hbar}{z}\Bigr).
\]
The bar-theoretic collision residue is
$r^{\mathrm{coll}}_{\cH}(z)=k\,\Omega_{\cH}/z$; its
rank-one coefficient is $k/z$ (one pole order lower than the
Laplace coefficient, by $d\log$ absorption). $R(z)$
satisfies YBE because $\cH_k$ is abelian.

The chiral Koszul dual is
$\cA^!_{\mathrm{ch}} = \mathrm{Sym}^{\mathrm{ch}}(V^*)$. The
open-colour dual is the abelian Yangian
$\cA^!_{\mathrm{line}} = Y(\mathfrak{u}(1))$, with semisimple
Fock-module line category. As a topological algebra
$Y(\mathfrak{u}(1))$ has $\kappaChHodge = -k$ (same as
$\cH_{-k}$, but the two are distinct algebraic objects: the
Yangian is the ordered Koszul dual, and $\cH_k^! \neq \cH_{-k}$
despite sharing $\kappaChHodge$). Braiding scalar,
$R(z) = \exp(k\hbar/z)$. The BBL
triangle collapses:
\[
\cH_k
\;\simeq\;
Z_{\mathrm{der}}(\cH_k)
\;\simeq\;
\HH^\bullet_{\mathrm{ch}}(Y(\mathfrak{u}(1))),
\]
$\cH_k$ is the boundary VOA; $Y(\mathfrak{u}(1))$ is the
open-colour dual modelling the line side.

At genus~$1$, the Arnold relation acquires a defect from the
elliptic period matrix. Bar differential acquires curvature:
$d^2 = k \cdot \omega_1$. The \emph{modular characteristic}
$\kappaChHodge(\cA)$ (Vol~I, Thm~D) is the genus-$1$
curvature coefficient; chain-level continuation
$d_{\barB}^2 = \kappaChHodge(\cA) \cdot \omega_g$ for
$g \ge 2$ requires modular-bootstrap acyclicity plus the
degree-$2$ curved-Dunn comparison
(Theorem~\ref{thm:curved-dunn-H2-vanishing-all-genera}).
$\omega_g = c_1(\lambda) \in H^2(\overline{\cM}_g, \Z)$ is the
first Chern class of the Hodge bundle. For $\cH_k$:
$\kappaChHodge = k$. For $\widehat{\fg}_k$ with simple
$\fg$: $\kappaChHodge = \dim\fg \cdot (k + h^\vee)/(2h^\vee)$.

The scalar genus-$1$ free energy is $F_1=k/24$. The determinant-line
shadow is the section $\eta(\tau)^{-k}$ of the inverse determinant
character over $\overline{\cM}_1$
(Computation~\ref{comp:heisenberg-line-bundle}).
$\kappaChHodge = k$ is the Euler class of the cyclic
$A_\infty$ structure on $\cH_k$
(Proposition~\ref{prop:kappa-euler-class}). The bar construction
$\barB(\cH_k)$ classifies twisting morphisms into $\cH_k$
(Computation~\ref{comp:heisenberg-classifying-space}), realising
the representability theorem
(Theorem~\ref{thm:bar-representability}) in the simplest case.

\paragraph{Shadow depth classification (Vol~I).}
The shadow-depth function is defined on the chirally Koszul locus:
\[
A\in\operatorname{Kosz}_{\mathrm{ch}}
\Longrightarrow r_{\mathrm{sh}}(A)\in\{2,3,4,\infty\}.
\]
For the finite Koszul classes we also record
$d_{\mathrm{alg}}=r_{\mathrm{sh}}-2$; for class~$\mathbf M$ both
invariants are infinite. Four Koszul classes plus one wild boundary:
\[
 \begin{array}{c|c|c|l}
 \text{class} & r_{\mathrm{sh}} & d_{\mathrm{alg}} & \text{examples} \\
 \hline
 \mathbf{G}\ \text{(Gaussian)} & 2 & 0 &
 \text{free theories; Heisenberg} \\
 \mathbf{L}\ \text{(Lie-transverse)} & 3 & 1 &
 \text{affine Kac--Moody} \\
 \mathbf{C}\ \text{(Contact)} & 4 & 2 &
 \text{first nonlinear correction; } \beta\gamma \\
 \mathbf{M}\ \text{(Mixed)} & \infty & \infty &
 \text{Virasoro, } \cW_N \\
 \mathbf{W}\ \text{(Wild)} & \text{undef.} & \text{undef.} &
 \text{Kronecker } K_{m \ge 3}\text{; DT replaces shadow tower}
 \end{array}
\]
All four Koszul classes (G, L, C, M) are chirally Koszul; the
classification measures complexity within the Koszul world.
$\mathbf{W}$ is the non-Koszul boundary
(Vol~I Remarks~\ref{V1-rem:wild-quiver-class},
\ref{V1-rem:wild-quiver-boundary}). If
$A\notin\operatorname{Kosz}_{\mathrm{ch}}$ and
$E_r(B^{\mathrm{ord}}(A))$ does not collapse to $E_\infty$ for any
finite~$r$, then $r_{\mathrm{sh}}(A)$ is undefined. The open problem
is not the existence of infinite depth: Virasoro and $\cW_N$ are
class~$\mathbf M$. The open problem is the existence of genuine
logarithmic $\SCchtop$-algebras whose bar spectral sequence refuses
the Koszul shadow altogether.
Total depth: $d = 1 + d_{\mathrm{arith}} + d_{\mathrm{alg}}$,
$d_{\mathrm{arith}} \ge 1$ from cusp-form contributions.
Shadow metric $Q_L(t)$: quadratic form on the obstruction tower;
discriminant $\Delta$ and growth rate $\rho$ control
genus-expansion convergence.

The shadow metric $Q_L(t) = (2k)^2$ is constant for Heisenberg:
class $\mathbf{G}$ (Gaussian), critical discriminant $\Delta = 0$,
algebraic depth $d_{\mathrm{alg}} = 0$. Depths $d \geq 5$ are
purely arithmetic.

The six projections of the Heisenberg MC element, the shadow
classification, the PVA, and the genus-$1$ curvature are all
controlled by the single scalar $k$. This is the full machine
at minimal complexity.

\section*{The 3d MC element}

Each volume has one generating datum:
\[
\begin{array}{l|l|l}
 & \text{Volume~I} & \text{Volume~II} \\
\hline
\text{MC element} & \Theta_\cA & \alpha_T \\
\text{Ambient} & \gAmod\ \text{(modular)} &
 \gSC_T\ \text{(Swiss-cheese)} \\
\text{Parameter space} & \Mbar_{g,n} &
 \FM_k(\C) \times \Conf_k^<(\R) \\
\text{Projects to} & \text{every theorem} &
 \text{closed/open/mixed data}
\end{array}
\]
(Definition~\ref{def:sc-convolution},
Theorem~\ref{thm:3d-universal-mc}.) Reading the
closed/open/mixed operadic datum recovers: boundary $\Ainf$
operations, line-sector operations, PVA bracket, spectral
$R$-matrix, genus-$1$ curvature. The bulk-boundary-line triangle
assembles from these projections together with the
boundary-linear derived-centre theorem and (on the chirally
Koszul locus) the line-module and chiral-Hochschild
identifications.

$\alpha_T$ is the fundamental class of the chiral Steinberg
self-intersection $\cL \times^h_{\cM} \cL$
(Theorem~\ref{thm:steinberg-presentation}). At genus $g$:
$Z_g(\cC) = \Tr_\cC^{\circ g}(\Theta_\cC^{(g)})$ is $g$-fold
clutching-trace
(Theorem~\ref{thm:genus-g-traces},
Construction~\ref{constr:cyclic-structure}).

For the Heisenberg, the Swiss-cheese convolution algebra is
abelian (Theorem~\ref{thm:rosetta-3d-mc}), and
\[
\alpha_{\cH_k}
\;=\;
\underbrace{m_2}_{\text{double-pole OPE}}
\;+\;
\hbar\,\underbrace{k \cdot \eta \otimes \Lambda}_{\text{genus-}1
\text{ self-loop}},
\]
with $\eta$ the unique holomorphic 1-form on $E_\tau$ normalised
so that $\int_{a\text{-cycle}}\eta = 1$ and $\Lambda = c_1(\lambda)
= \omega_1$ the Hodge class on $\overline{\cM}_{1,1}$ (the distinct
role of $\eta$ here from $\eta_{ij} = d\log(z_i - z_j)$ at
genus~$0$ is the elliptic specialisation of Arnold's logarithmic form).
Six projections (Corollary~\ref{cor:rosetta-heisenberg-projections}):
\begin{center}
\small
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{ll}
\textbf{Projection} & \textbf{Result} \\
\hline
closed face & $m_2(J,J;\lambda) = k\lambda$, $\;m_{k\ge3} = 0$ \\
open face & trivial open-colour line-sector operations on Fock category \\
spectral braiding & $r_{\cH}(z)=k\Omega_{\cH}/z$, $\mathcal R(z)=z^k$ \\
bulk-to-boundary & $\Abulk = \Zder(\cH_k) = \cH_k$ \\
genus face & $\Theta_{\cH_k} = k\cdot\eta\otimes\Lambda$ \\
PVA descent & $\{J_\lambda J\} = k\lambda$
\end{tabular}
\end{center}
The single scalar $k$ controls every entry.
Section~\ref{sec:rosetta-stone} carries out this computation in
full detail. The Heisenberg MC element is the answer for
class $\mathbf{G}$. For class $\mathbf{M}$ (Virasoro, $\cW_N$,
3d gravity) the MC element has infinitely many independent
projections.

\section*{The completeness question}

\providecommand{\Mbar}{\overline{\mathcal M}}
\providecommand{\Mvac}{\cM_{\mathrm{vac}}}

The genus-$0$ structures (closed/open $\Ainf$ operations, PVA
bracket, spectral $R$-matrix, mixed bulk-to-boundary) are
projections of the MC element $\alpha_T$. The corrected Koszul
triangle is assembled from this projection data together with
the scoped comparison theorems. But $\alpha_T$ lives at
genus~$0$. The central question of modular Koszul duality:

\medskip
\begin{center}
\emph{Is the holographic Lagrangian complete?}
\end{center}
\medskip

The bar construction embeds the chiral algebra as a Lagrangian
$\cL_\cA \hookrightarrow \Mvac$ in the $(-2)$-shifted symplectic
moduli of local vacua
(Theorem~\ref{thm:bar-is-self-intersection}). At genus~$0$ this
Lagrangian is constructed from bar-cobar data; its
self-intersection $\cL_\cA \times_{\Mvac} \cL_\cA$ is the
Swiss-cheese algebra. Completeness asks whether the genus-$0$
Lagrangian $\cL_\cA^{(0)}$ extends to a family
$\cL_\cA^{(g)} \hookrightarrow \Mvac$ over $\Mbar_{g,n}$,
compatible with gluing along boundary strata, with computable
self-intersection numbers.

Koszulness stratifies the extension problem:
\[
\begin{array}{l|l|l|l}
 & \textbf{Strict formal} & \textbf{Finite non-formal}
 & \textbf{Infinite non-formal} \\
\hline
\text{Self-intersection} & \text{clean} &
 \text{finite excess Tor} & \text{infinite excess tower} \\
\text{Curvature} &
 d^2 = \kappaChHodge\cdot\omega_g &
 \text{finite Lie/contact corrections} &
 \text{Virasoro wheel} \\
\text{Extension to } \Mbar_{g,n} & \text{scalar lane} &
 \text{finite obstruction equations} & \text{infinite tower} \\
\text{Examples} & \text{Heisenberg} &
 V_k(\fg),\ \beta\gamma & \text{Virasoro, } \cW_N
\end{array}
\]
Scalar curvature extends automatically: the obstruction lies in
the excess normal bundle, and a scalar curvature has no excess.

For a non-formal algebra the non-zero transferred operations are
Massey products of the obstruction class. Classes $\mathbf{L}$ and
$\mathbf{C}$ have finitely many such operations; the Virasoro class
has the whole wheel. Coherent vanishing of these operations is
formality, not finite depth.

Koszul shadow depth $r_{\mathrm{sh}}(\cA)$ counts obstruction
classes on the standard atlas:
\[
\begin{array}{c|c|l|l}
\text{class} & r_{\mathrm{sh}} & \text{intersection} & \text{example} \\
\hline
\mathbf{G} & 2 & \text{clean} & \text{Heisenberg, lattice VOAs} \\
\mathbf{L} & 3 & \text{Lie-transverse} &
 \text{affine } V_k(\fg)\ (\text{Jacobi kills quartic}) \\
\mathbf{C} & 4 & \text{contact} & \beta\gamma \\
\mathbf{M} & \infty & \text{infinite excess tower} &
 \text{Virasoro, } \cW_N
\end{array}
\]
The $\mathbf{G}$/$\mathbf{L}$ discriminant at the initial residue layer is
centrality of the double-pole residue
$c_0 = \{a_{(0)}b\}$: central $\Rightarrow \mathbf{G}$;
Lie-bracket $\Rightarrow \mathbf{L}$.

Strata of a single invariant, the shadow metric $Q_\cA(t)$:
\[
\begin{array}{c|l|l}
\text{class} & Q_\cA(t) & \text{branching} \\
\hline
\mathbf{G} & \text{constant} & - \\
\mathbf{L} & \text{linear} & - \\
\mathbf{C} & \text{quadratic} & \text{finite} \\
\mathbf{M} & \text{quadratic} & \text{infinite} \\
\mathbf{W} & \text{undefined outside }\operatorname{Kosz}_{\mathrm{ch}} &
\text{DT replaces tower}
\end{array}
\]
$\mathrm{disc}_t(Q_\cA)$ controls the entire obstruction tower.
$\mathbf{W}$: non-Koszul boundary
(Vol~I Remark~\ref{V1-rem:wild-quiver-boundary}).
Stasheff cancellation
(Theorem~\ref{thm:stasheff-cancellation}) explains quadraticity:
$\Ainf$ relations cancel all Kac-table denominator factors
beyond those of $Q_\cA$, taming representation-theoretic
infinity into algebraic finiteness.

(The total depth is
$d = 1 + d_{\mathrm{arith}} + d_{\mathrm{alg}}$; for standard
examples at genus~$0$, $d_{\mathrm{arith}} = 1$, giving
$d = d_{\mathrm{alg}} + 2$.) Shadow-depth values
$r_{\mathrm{sh}} = 2, 3, 4$ are computed in Vol~I; geometric
interpretations proved for $r_{\mathrm{sh}} = 2$ and
$r_{\mathrm{sh}} = 3$; for $r_{\mathrm{sh}} = 4$ and
$r_{\mathrm{sh}} = \infty$ the
remaining datum is a moduli-stratification construction, not a
geometric classification theorem. Atlas:
\S\ref{sec:shadow-tower-atlas}.

The worked examples of Part~\ref{part:examples} exhibit a sharp
dichotomy (Remark~\ref{rem:platonic-shadow-dichotomy}):
\[
\begin{array}{l|l|l}
 & \textbf{Current algebras} & \textbf{$\cW$-algebras} \\
\hline
\text{Class} & \mathbf{L} & \mathbf{M} \\
\text{Tower} & \text{finite} & \text{infinite} \\
\kappaChHodge(\cA) + \kappaChHodge(\cA^!) & 0\ (\text{KM, free fields}) &
 \rho_K \neq 0\ (\text{Vir: }13,\text{ family-specific}) \\
\text{Physics} & \text{CS, M2} &
 \text{3d gravity, } \cW_3, \text{ M5}
\end{array}
\]
The Drinfeld--Sokolov functor is the shadow-depth escalator:
$\mathbf{L} \rightsquigarrow \mathbf{M}$.

On a mixed primary deformation line, the infinite tower
($d = \infty$) is controlled by the quadratic shadow metric
$Q_L(t) = (2\kappaChHodge + 3\alpha\,t)^2 + 2\Delta\,t^2$
on each primary deformation line, where $S_r$ is the degree-$r$
shadow coefficient (weight-$r$ projection of $\Theta_\cA$ in the
cyclic deformation complex), $\alpha := S_3$ is the cubic shadow
coefficient and $\Delta = 8\kappaChHodge S_4$ is the
critical discriminant. For Virasoro ($\alpha = 2$,
$\kappaChHodge = c/2$):
$Q_L(t) = (c + 6t)^2 + 80\,t^2/(5c{+}22)$. The weighted
generating function $H(t)^2 = t^4 Q_L(t)$, so the shadow
obstruction tower is algebraic of degree~$2$. Each coefficient
has poles only at $c = 0$ and $c = -22/5$ (Lee--Yang):
$S_r = \varepsilon_r P_r(c)/[c^{r-3}(5c{+}22)^{\lfloor(r-2)/2\rfloor}]$
for $r \ge 4$, with no new factors at any degree.

Existence, Geometry, Computation are three aspects:
\[
\begin{array}{l|l|l}
\text{Aspect} & \text{Question} & \text{Invariant} \\
\hline
\text{Existence} & \cL_\cA^{(0)}\ \text{extends?} &
 \text{obstruction} \\
\text{Geometry} & \text{compatible with boundary correspondences?} &
 \text{compatibility} \\
\text{Computation} & \chi(\cL_\cA^{(g)} \times_{\Mvac} \cL_\cA^{(g)}) = ? &
 R^{(g)}_\cA(z)
\end{array}
\]
One Lagrangian, one extension, three projections.
Part~\ref{part:examples} develops the machinery; $\beta\gamma$,
Kodaira--Spencer, $\mathcal{W}_3$ PSM test it.

\section*{Construction of the Swiss-cheese operad $\SCchtop$}

The structures above (differential, coproduct, $\Ainf$
operations, $R$-matrix, Koszul triangle) are governed by a
single two-coloured operad.

\paragraph{Colours and spaces of operations.}
Colours $\{\mathsf{ch}, \mathsf{top}\}$: closed (holomorphic)
and open (topological).

\emph{Closed-to-closed.}
$\SCchtop(\mathsf{ch}^k;\,\mathsf{ch}) = \FM_k(\C)$, the
Fulton--MacPherson compactification of $k$ labelled points in
$\C$. Smooth manifold with corners of real dimension
$2k - 3$ (for $k \ge 2$, after quotienting by translations and
dilations); codimension-1 strata $D_S$ ($|S| \ge 2$): collision
divisor with internal cluster configuration in $\FM_{|S|}(\C)$
and cluster position in $\FM_{k - |S| + 1}(\C)$:
\begin{equation}
\label{eq:intro-FM-fibration}
\FM_{|S|}(\C) \;\hookrightarrow\; D_S \;\twoheadrightarrow\;
\FM_{k - |S| + 1}(\C).
\end{equation}
Convention: $\FM_0(\C) = \FM_1(\C) = \mathrm{pt}$.

\emph{Mixed-to-open.}
$\SCchtop(\mathsf{ch}^k, \mathsf{top}^m;\,\mathsf{top}) =
\FM_{k,m}(\mathbb H)$, the Fulton--MacPherson--Voronov
compactification of $k$ interior points of the closed upper
half-plane $\mathbb H$ and $m$ boundary points on
$\partial\mathbb H = \R$, modulo boundary-preserving affine maps.
Its depth-zero associated graded is the decoupled product
$\FM_k(\C) \times \Eone(m)$ with the little-intervals operad
$\Eone(m) = \Conf_m^{<}(\R)$: what locality forces is this
splitting of the associated graded --- a local observable
factorising holomorphically in $z$ and locally constant in $t$
depends, away from the boundary-incidence strata, only on
collision data in $\C$ and time-ordering in $\R$ --- while the
incidence strata of $\FM_{k,m}(\mathbb H)$ record the
bulk-to-boundary coupling that the product forgets.

\emph{Open-to-closed (empty).}
$\SCchtop(\ldots, \mathsf{top}, \ldots;\,\mathsf{ch}) = \varnothing$.
Operadic expression of the physical principle: bulk
interactions restrict to boundaries, not conversely.

\paragraph{Composition.}
By substitution of compactified configurations.

\emph{Closed-into-closed.}
$\gamma_{\mathrm{ch}}\colon \FM_k(\C) \times \prod_i
\FM_{\ell_i}(\C) \to \FM_{\sum_i \ell_i}(\C)$ via FM blow-up
(Fulton--MacPherson~\cite{FM94}, Getzler--Jones~\cite{GJ94}).

\emph{Closed-into-open.}
Closed operations are substituted at interior points of
$\FM_{k,m}(\mathbb H)$; no open operation may be substituted into a
closed input slot. Forced at three levels: geometrically (no
configuration space interpolates real-line data to holomorphic
collision data); D-module level (!-extension support vanishing
from codim-$1$ boundary); physically (the factorized singular
parametrix has separate holomorphic and topological factors, so
topological inputs do not contribute to holomorphic outputs).

\emph{Mixed-into-open.}
Mixed operations are substituted at boundary points, the boundary
point carrying the transverse position of the inserted
configuration; at depth zero this reduces to the componentwise
product of $\gamma_{\mathrm{ch}}$ with the standard $\Eone$
composition.

\begin{center}
\emph{Composition is associative, equivariant, unital by
inheritance from the Fulton--MacPherson insertion of compactified
configurations
(Definition~\textup{\ref{def:SC-composition}}).}
\end{center}

\begin{observation}[Helicity as Swiss-cheese directionality]
\label{obs:helicity-directionality}
The two $\SCchtop$ colours are, in the celestial reading, the
two helicities of scattering amplitudes. Positive-helicity
insertions are holomorphic (closed colour, on $\FM_k(\C)$,
producing $\omega_k \in \Omega^\bullet_{\log}(\ov{\FM}_k(\C))$
generating the bar differential). Negative-helicity insertions
are topological (open colour, on $\Conf_m^{<}(\R)$, producing
the $\Eone$ coproduct). The no-open-to-closed rule models the
helicity selection rule: negative-helicity gluons cannot produce
positive-helicity gluons by OPE alone. The MHV amplitude is the
minimal open--closed coupling under $\SCchtop$ once the comparison
data below are supplied; all-plus and single-minus amplitudes vanish
for the same form-type obstruction reason on that comparison surface.
Self-dual sector identification is proved via
$\barBch(\cA)$ in
Theorem~\textup{\ref{thm:ch-core-helicity-splitting}(i)}
(Part~\textup{VII}).  The anti-self-dual, mixed-helicity, and MHV
clauses of Theorem~\textup{\ref{thm:ch-core-helicity-splitting}} are
conditional on the Verdier-helicity comparison and the tree-level
Mellin/Parke--Taylor propagator comparison stated there.  The remaining
datum is the form-type comparison for the mixed-helicity twisting
morphism; it is not an unconditional consequence of \(\SCchtop\) alone.
\end{observation}

\section*{The $\Ainf$ chiral operations $m_k$}

Let $\bar a:=|a|-1$ denote the suspended degree. The reduced chiral
bar coalgebra is
\[
\barBch(\cA)=T^c(s^{-1}\overline{\cA})
=\bigoplus_{n\ge0}(s^{-1}\overline{\cA})^{\otimes n}.
\]
An $\Ainf$ chiral structure is a degree-$1$ coderivation $D_\cA$ of
$\barBch(\cA)$. Its cogenerator projections are the chiral operations
\[
m_k \colon A^{\otimes k} \longrightarrow
A((\lambda_1))\cdots((\lambda_{k-1})),
\qquad k \geq 1,\quad \deg m_k=2-k,
\]
with spectral parameters $\lambda_i=z_i-z_k$. Explicitly, for
homogeneous inputs,
\begin{multline}
\label{eq:intro-ainfty-coderivation}
D_\cA\bigl[s^{-1}a_1|\cdots|s^{-1}a_n\bigr]  \\
=
\sum_{\substack{k\ge1\\1\le i\le n-k+1}}
(-1)^{\epsilon(i,k)}
\bigl[s^{-1}a_1|\cdots|
s^{-1}m_k(a_i,\ldots,a_{i+k-1})|\cdots|s^{-1}a_n\bigr],
\qquad
\epsilon(i,k)=\sum_{r<i}\bar a_r .
\end{multline}
The deconcatenation coproduct satisfies
\[
\Delta D_\cA=(D_\cA\otimes\id+\id\otimes D_\cA)\Delta.
\]
The whole $\Ainf$ chiral equation is $D_\cA^2=0$; the displayed
Stasheff relation below is the arity-$n$ cogenerator projection after
translating \eqref{eq:intro-ainfty-coderivation} into the desuspended
coordinate convention fixed in Definition~\ref{def:ainfty-relations}.

\paragraph{The $\Ainf$ relations as boundary decompositions of
$\FM_n(\C)$.}
\begin{multline}
\label{eq:intro-ainfty}
\sum_{\substack{i+j = n+1 \\ i,j \geq 1}} \;\sum_{s=0}^{n-j}
(-1)^{|a_1| + \cdots + |a_s|}\;
m_i\bigl(a_1, \ldots, a_s,\,
m_j(a_{s+1}, \ldots, a_{s+j};\, \lambda_{s+1}, \ldots,
\lambda_{s+j-1}) \;\big|\;\\
\Lambda_{[s+1,\ldots,s+j]},\,
a_{s+j+1}, \ldots, a_n;\, \bm\lambda^{\mathrm{out}}\bigr) = 0,
\end{multline}
$\Lambda_{[s+1,\ldots,s+j]} = \lambda_{s+1} + \cdots +
\lambda_{s+j-1}$ effective spectral parameter of fused block.
The relation is Stokes theorem on $\FM_n(\C)$, term-by-term:
\[
\begin{array}{l|l}
\text{Algebraic datum} & \text{Geometric meaning} \\
\hline
\text{divisor } D_S,\ |S|\ge 2 &
 \text{cluster of } |S| \text{ points colliding} \\
\text{inner } m_{|S|} & \text{residue along } D_S \\
\text{outer } m_{n-|S|+1} &
 \text{fused cluster interacting with remainder} \\
(-1)^{|a_1|+\cdots+|a_s|} &
 \text{Koszul sign (coderivation past first } s\text{)} \\
\lambda_{\mathrm{inner}} \mapsto
 \Lambda_{[s+1,\ldots,s+j]} &
 \text{momentum conservation (fused cluster)}
\end{array}
\]

For $n = 2$: $m_1 \circ m_2 + m_2 \circ
(m_1 \otimes \id + \id \otimes m_1) = 0$, $Q = m_1$ a graded
derivation of $m_2$.

For $n = 3$: codim-1 strata of $\FM_3(\C)$ are $D_{\{1,2\}}$,
$D_{\{2,3\}}$, $D_{\{1,3\}}$. In ordered $A_\infty$ convention,
only consecutive blocks contribute to the bar coderivation:
$D_{\{1,2\}}$ and $D_{\{2,3\}}$ give the two
$m_2(m_2(-,-),-)$ terms; $D_{\{1,3\}}$ (non-consecutive) does
not contribute at the bar-complex level, but contributes to the
PVA Jacobi identity after passing to the unordered shadow
(see the final Arnold-boundary identity in
Theorem~\ref{thm:stokes_arnold}). The relation
reads:
\begin{equation}
\label{eq:intro-n3}
m_2(m_2(a,b;\lambda_1), c;\, \lambda_1 + \lambda_2) \;-\;
m_2(a, m_2(b,c;\lambda_2);\, \lambda_1) \;+\;
m_1 m_3(a,b,c) \;+\; m_3(Q\cdot) \;=\; 0.
\end{equation}
On cohomology, $m_3$ terms vanish and the surviving identity is
the Jacobi identity for the $\lambda$-bracket. At chain level,
$m_3$ is the homotopy for failure of strict associativity.

\paragraph{Resolvent principle: one equation for all $m_k$.}
$\{m_k\}_{k \ge 2}$ are not independent. With $(H, p, \iota, h)$
a deformation retract of $(\cA, m_1)$ onto cohomology with
$h^2 = h\iota = ph = 0$, the dressed propagator
$\Phi\colon T^+(H) \to \cA$ satisfies
\begin{equation}
\label{eq:intro-resolvent}
\Phi \;=\; \iota \;+\; h \circ m_2 \circ \Phi^{\otimes 2}
\end{equation}
the tree-level bootstrap (Theorem~\ref{thm:resolvent-principle}).
Solution by iteration: $m_k^H$ as a sum over $C_{k-1}$ planar
binary trees (equivalently, binary trees with $n = k - 1$
internal vertices and $k$ leaves), with $m_2$ at vertices and
$h$ at edges. One binary input, one homotopy, all higher
operations.

\paragraph{Regular/singular decomposition of $m_2$.}
\begin{equation}
\label{eq:intro-reg-sing}
m_2(a,b)(\lambda) \;=\;
\underbrace{m_2^{\mathrm{reg}}(a,b)}_{\in\, A[[\lambda]]}
\;+\;
\underbrace{m_2^{\mathrm{sing}}(a,b)}_{\in\, \lambda^{-1}
A[\lambda^{-1}]}.
\end{equation}
Regular: normally ordered product (commutative multiplication).
Singular: $\lambda$-bracket (OPE residue):
$a \cdot b := m_2^{\mathrm{reg}}(a,b)|_{\lambda=0}$,
$\{a \,{}_\lambda\, b\} := m_2^{\mathrm{sing}}(a,b)$.
Together they form a Poisson vertex algebra on
$H^\bullet(A, Q)$.


\section*{PVA descent: from $\Ainf$ to Poisson vertex algebra}

\paragraph{The product.}
On $Q$-closed $a, b$, the regular part $a \cdot b$ is
well-defined commutative product (Vacuum: if $a = Qc$, $m_2(Qc,
b) = Q m_2(c,b) \pm m_2(c, Qb)$; regular at $\lambda = 0$ is
$Q$-exact). Commutativity from $\sigma\colon \FM_2(\C) \to
\FM_2(\C)$ exchanging the two points: $\lambda \to -\lambda - \partial$.

\paragraph{The $\lambda$-bracket.}
Singular part satisfies sesquilinearity (chain-level
sesquilinearity of $m_2$, geometric translation covariance on
$\FM_2(\C)$): $\{\partial a {}_\lambda b\} = -\lambda \{a {}_\lambda b\}$,
$\{a {}_\lambda \partial b\} = (\partial + \lambda)\{a {}_\lambda b\}$.
Skew-symmetry: $\sigma$ on $\FM_2(\C)$ sends
$\lambda \to -\lambda - \partial$;
$\{a {}_\lambda b\} = -\{b {}_{-\lambda - \partial} a\}$.
Leibniz: $n = 2$ $\Ainf$ relation with product yields
$\{c {}_\lambda (a \cdot b)\} = \{c {}_\lambda a\} \cdot b +
a \cdot \{c {}_\lambda b\}$.

\paragraph{Higher operations and the PVA shadow.}
The PVA descent does not require the unconditional vanishing of
$m_k$ for $k\ge3$. The higher operations are chain-level homotopies
and obstruction classes; they disappear from the binary PVA shadow,
but they vanish on cohomology only on a formal or compatibly
nullhomotopic locus. In Section~\ref{sec:PVA_descent} this extra
input is named as a compatible topological bounding-chain datum.

\paragraph{Jacobi identity.}
Jacobi requires $n = 3$ $\Ainf$ relation and Stokes on
$\FM_3(\C)$. Apply~\eqref{eq:intro-n3} on $Q$-closed $a, b, c$;
the $m_3$ contribution appears as a $Q$-boundary, and $m_1 = Q$
vanishes on the inputs. Surviving:
$m_2(m_2(a,b;\lambda_1), c;\, \lambda_1 + \lambda_2) =
m_2(a, m_2(b,c;\lambda_2);\, \lambda_1)$. Singular parts give
LHS and RHS of Jacobi identity; mixed terms cancel by Arnold
relation $\omega_{12}\wedge\omega_{23} + \omega_{23}\wedge\omega_{31}
+ \omega_{31}\wedge\omega_{12} = 0$ on $\Conf_3(\C)$ via Stokes
on boundary strata of $\FM_3(\C)$. Full verification:
Theorem~\ref{thm:cohomology_PVA}, Section~\ref{sec:PVA_descent}.

\begin{maintheorem}[Cohomological descent to PVA;
Theorem~\textup{\ref{thm:cohomology_PVA}}]
\label{thm:intro-PVA-descent}
Let $\cA$ be a logarithmic $\SCchtop$-algebra
\textup{(}Definition~\textup{\ref{def:log-SC-algebra}}: an
$\SCchtop$-algebra whose structure maps extend to logarithmic
differential forms on the FM compactification, i.e.\ propagator
is a log $1$-form with at worst simple poles along boundary
divisors\textup{)}. Then $H^\bullet(\cA, Q)$ carries the
structure of a $(-1)$-shifted Poisson vertex algebra
($\lambda$-bracket has cohomological degree $-1$; underlying Lie
bracket lives on $A[1]$):
\begin{enumerate}[label=\textup{(\roman*)}]
\item commutative product is the regular part of $m_2$;
\item $\lambda$-bracket is the singular part of $m_2$;
\end{enumerate}
PVA axioms (sesquilinearity, skew-symmetry, Jacobi, Leibniz,
vacuum) are proved by Stokes on $\FM_n(\C)$. Higher-operation
vanishing is equivalent to the compatible topological
bounding-chain datum of Proposition~\ref{prop:m3_vanish}: the
classes $[m_k]\in H^\bullet(\operatorname{End}_{\SCchtop}(\cA))$
are killed by explicit homotopies $h_k$.
\end{maintheorem}

The PVA is the classical shadow: it remembers the commutative
product and singular OPE, but forgets the chain-level homotopies
$m_3, m_4, \ldots$.

\section*{The spectral $R$-matrix from the $\lambda$-bracket}

The $\lambda$-bracket encodes the classical braiding of line
operators. Bridge from PVA to Yang--Baxter: Laplace transform.

\paragraph{The classical $r$-matrix.}
\begin{equation}
\label{eq:intro-laplace}
r(z) \;=\; \int_0^\infty e^{-\lambda z}\,
\{{\cdot}\,{}_\lambda\,{\cdot}\}\, d\lambda.
\end{equation}
Convergent for $\operatorname{Re}(z) > 0$ under the
physical-realization hypotheses of
Theorem~\ref{thm:physics-bridge}; meromorphic on $\C^\times$.
Algebraically: if $\{a {}_\lambda b\} = \sum_{n \ge 0} c_n \lambda^n / n!$,
then $r(z)(a \otimes b) = \sum_{n \ge 0} c_n / z^{n+1}$.

\emph{PVA Jacobi and CYBE.} The Laplace transform sends the PVA
Jacobi identity to a spectral kernel identity. For a non-dynamical
constant collision tensor this identity is the classical Yang--Baxter
equation:
\begin{equation}
\label{eq:intro-CYBE}
[r_{12}(z_{12}),\, r_{13}(z_{13})] +
[r_{12}(z_{12}),\, r_{23}(z_{23})] +
[r_{13}(z_{13}),\, r_{23}(z_{23})] \;=\; 0.
\end{equation}
$\mu = \lambda_2$, $\lambda = \lambda_1$ becomes
$z_{13} = z_{12} + z_{23}$ under Laplace. For field-dependent PVA
kernels the same transform is the PVA Jacobiator in spectral
coordinates, not an ordinary non-dynamical CYBE.

\paragraph{The quantum $R$-matrix.}
$R(z)$ from bulk-to-boundary OPE of line operators: parallel
boundary lines $L_1(z_1), L_2(z_2)$ in 3d HT theory on
$\C \times \R_{\ge 0}$ with braiding
\[
R_{L_1, L_2}(z) \colon L_1 \otimes L_2
\;\xrightarrow{\;\sim\;}\; L_2 \otimes L_1,
\qquad z = z_1 - z_2,
\]
meromorphic, computed by configuration integrals over
$\FM_2(\C) \times \Eone(2)$
(Definition~\ref{def:spectral-braiding}). Semiclassical limit
$R(z) = \id + \h\, r(z) + O(\h^2)$ recovers $r(z)$.

\emph{Yang--Baxter.}
\begin{equation}
\label{eq:intro-YBE}
R_{12}(z_{12})\, R_{13}(z_{13})\, R_{23}(z_{23})
\;=\;
R_{23}(z_{23})\, R_{13}(z_{13})\, R_{12}(z_{12}),
\end{equation}
proved by Stokes on $\FM_3(\C)$. Three codim-1 strata contribute
the three double products on each side; vanishing of the
boundary integral
$\int_{\partial \FM_3(\C)} \omega_3^{\partial} = 0$ forces
equality. $D_{\{1,3\}}$ does not contribute independently:
spectral parameter $z_{13} = z_{12} + z_{23}$.

\emph{Classical limit.} CYBE is the semiclassical limit of YBE;
both follow from Stokes on $\FM_3(\C)$.

\section*{The bulk--boundary--line Koszul triangle}

A 3d HT theory on $\C \times \R$ produces three algebraic
objects:
\begin{enumerate}[label=(\roman*)]
\item \textbf{bulk chiral algebra} $\Abulk$: local operators on
 $\C \times \R$;
\item \textbf{boundary algebra} $\Bbound$: local operators on
 $\C \times \{0\}$;
\item \textbf{line category} $\cC_{\mathrm{line}}$: line
 operators on $\{z_0\} \times \R_t$.
\end{enumerate}
Strongest proved relation, scoped on the chirally Koszul
locus and under compact-generation / derived-centre hypotheses:
\begin{equation}
\label{eq:intro-triangle}
\Abulk \;\simeq\;
\Zder(\Bbound) \;\simeq\;
\Zder(\cC_{\mathrm{line}}) \;\simeq\;
\HH^\bullet_{\mathrm{ch}}(\cA^!_{\mathrm{line}}),
\qquad
\cC_{\mathrm{line}} \;\simeq\; \cA^!_{\mathrm{line}}\text{-}\mathbf{mod}.
\end{equation}
Unconditionally: bulk $\simeq$ chiral Hochschild cochains of the
boundary algebra. Primitive datum is the open-sector
factorization category $\cC$
(Definition~\ref{def:oc-factorization-category}); $B_\partial =
\End_\cC(b)$ and $\cO_{\mathrm{bulk}} = Z_{\mathrm{der}}(\cC)$
are derived invariants depending only on the Morita class of
$\cC$.

\paragraph{Construction of the three vertices.}
\emph{Bulk algebra.} $\Abulk$ is the factorisation algebra of
local observables on $\C \times \R$, with differential
$Q_{\mathrm{BRST}}$ from BV master equation. On cohomology
$H^\bullet(\Abulk, Q)$ is the $(-1)$-shifted PVA of
Theorem~\ref{thm:intro-PVA-descent}.

\emph{Boundary algebra.} A boundary condition
$b\colon \C \times \{0\} \hookrightarrow \C \times \R_{\ge 0}$
defines $\Bbound = \End_{\cC_{\mathrm{line}}}(M_b)$, with
boundary differential $Q_{\partial}$ and full $\Ainf$ chiral
structure from restriction of Swiss-cheese operations to the
half-space.

\emph{Line category.} A line $L$ at $z_0$ defines topological
quantum mechanics on $\R_t$ with state space $V_L$. Bulk
operators near the line act on $V_L$ through the open colour of
$\SCchtop$, the mixed operations
$C_\ast(\FM_k(\C) \times \Eone(m)) \otimes \Abulk^{\otimes k}
\otimes V_L^{\otimes m} \to V_L$. On the chirally Koszul locus
the comparison theorem
\textup{(}Theorem~\textup{\ref{thm:lines_as_modules}}\textup{)}
models this by $\cA^!_{\mathrm{line}}$-modules: equivalence
$\Phi\colon \cC_{\mathrm{line}} \to
\cA^!_{\mathrm{line}}\text{-}\mathbf{mod}$.

\paragraph{Derived centre identification.}
$\Abulk \simeq \Zder(\Bbound)$ proved in the exact
boundary-linear sector
(Theorem~\ref{thm:boundary-linear-bulk-boundary}). Derived
centre $\Zder(\Bbound) := \RHom_{\Bbound^e}(\Bbound, \Bbound)$.
Identification uses derived critical locus technology: $\Abulk =
\dCrit(W)$ (derived zero locus of $dW$ in $T^*X$) for
holomorphic potential $W$; derived centre identified with
$T^*[-1]\dCrit(W)$. Morita invariance:
$\Zder(\Bbound) \simeq \Zder(\cC_{\mathrm{line}}) \simeq
\HH^\bullet_{\mathrm{ch}}(\cA^!_{\mathrm{line}})$.

\paragraph{Homotopy coherence.}
Triangle~\eqref{eq:intro-triangle} is an equivalence of
homotopy-coherent objects
(Remark~\ref{rem:triangle-homotopy-first}); each vertex is
$\Ainf$ or $\Linf$. Premature strictification at any corner
destroys the equivalence.

\paragraph{The $(\infty, 2)$-adjunction on
$\overline{\mathcal A_2}$.}
Chain-level and $(\infty, 1)$-categorical ambients for bar--cobar
are not two parallel statements of the same theorem; they are
two $1$-cells of one $(\infty, 2)$-adjunction on the Siegel
parameter base $\overline{\mathcal A_2}$. The compatibility
data between the two ambients is the $\infty$-groupoid
\[
 \mathrm{Data}(\cA)
 \;\simeq\;
 B\,\mathrm{Aut}^h_{\Linf}\bigl(\mathfrak g^{\infty,1}_\cA\bigr),
\]
with $\mathfrak g^{\infty,1}_\cA$ the
$(\infty,1)$-categorical chiral deformation dg Lie algebra of
$\cA$. Specialised to the K3 Hall boundary object
$\mathbf H_{\Delta_5}$ of the universal trace identity, the
$\infty$-groupoid collapses to
$\widehat{\mathfrak{grt}}_1 / \mathrm{ob}^{\mathrm{GN}}$, the
Grothendieck--Teichm\"uller completion modulo the Gan--Ginzburg
obstruction subalgebra. The $\SCchtop$ coloured coherence on the
chiral derived centre pair is the $2$-cell data of this
adjunction, not an auxiliary rectification. Every
$\SCchtop$-algebra on the Koszul locus, read simultaneously at
chain level and at $(\infty, 1)$-categorical level, is a single
$(\infty, 2)$-functorial datum on $\overline{\mathcal A_2}$.

\paragraph{Chiral Kontsevich formality.}
Kontsevich's admissible-graph $\Linf$-quasi-isomorphism
$T^{\mathrm{poly}} \xrightarrow{\sim} \mathrm{Hoch}^\bullet$
on $\R^d$ (Kontsevich 2003) admits a chiral upgrade:
\[
 T^{\mathrm{poly}}_{\mathrm{ch}}
 \;\xrightarrow{\;\sim\;}\;
 \mathrm{ChirHoch}^\bullet,
\]
transported through the Francis--Gaitsgory
factorisation-$\infty$-operadic lane and the Tamarkin operadic
formality lane simultaneously. The admissible graphs carry
$\eta_{ij}$-weights instead of Kontsevich's hyperbolic
propagator; the obstruction for coherence between the two
transport lanes is the Vol I modular characteristic
$\kappaChHodge(\cA)$. The chiral Kontsevich
quasi-isomorphism is the Vol II bridge identifying the classical
shadow (PVA, Theorem~\ref{thm:intro-PVA-descent}) with the
chain-level chiral Hochschild cochains; it is what lets Theorem
H of the trilogy inherit its chain-level concentration range
from $(\infty, 1)$-categorical Tamarkin vanishing.

\section*{The dg-shifted Yangian}

On the chirally Koszul locus, $\cA^!_{\mathrm{line}}$ carries
two layers of structure: $\SCchtop$-algebra structure from
operadic self-duality (the algebra half), bialgebra structure
from the monoidal category of line operators (the coalgebra
half).

\noindent\textsc{Definition}
\textit{(dg-shifted Yangian; Definition~\textup{\ref{def:dg_Yangian}}).}
A \emph{dg-shifted Yangian} $\mathcal{Y}$ is an $\Ainf$ algebra
with:
\begin{enumerate}[label=(\roman*)]
\item translation automorphism
 $\tau_z = \exp(zT)\colon \mathcal{Y} \to \mathcal{Y}[[z]]$;
\item spectral $R$-matrix
 $r(z) \in \mathcal{Y} \widehat{\otimes}\, \mathcal{Y}[[z^{-1}, z]]$;
\item twisted coproduct $\Delta_z\colon \mathcal{Y} \to
 \mathcal{Y} \otimes_{r(z)} \mathcal{Y}[[z^{-1}, z]]$;
\item counit $\varepsilon\colon \mathcal{Y} \to \C$;
\end{enumerate}
	satisfying counit axioms, translation invariance,
	weak co-commutativity $(\tau_w\otimes\tau_w)r(z)=r(z)$,
	the normalized skew relation $r(-z)=-r_{21}(z)$ only when the
	unitary exchange normalization is part of the comparison surface,
	and the quantum $\Ainf$ Yang--Baxter relation
	$r_{23}(z) + (\id \otimes \Delta_z)(r(z+w)) =
	r_{12}(w) + (\Delta_w \otimes \id)(r(z))$.

The Koszul duality
$(\SCchtop)^! = (\mathsf{Lie}, \mathsf{Ass}, \text{shuffle-mixed})$
(\S\ref{subsec:dg-yangian-operadic}) gives
$\cA^!_{\mathrm{line}}$ a $(\SCchtop)^!$-algebra structure;
since $\mathsf{Ass}$ is self-dual the open colour preserves its
$E_1$-algebra structure. The monoidal structure on line
operators on the chirally Koszul locus
($\cC_{\mathrm{line}} \simeq \cA^!_{\mathrm{line}}\text{-}\mathbf{mod}$)
provides the coproduct and $R$-matrix (the bialgebra half).
$\cA^!_{\mathrm{line}}$ carries the dg-shifted Yangian package
(Theorem~\ref{thm:yangian-recognition}), and the full two-colour
Koszul duality involution returns $\cA$
(Theorem~\ref{thm:duality-involution}).

In the affine lineage, the ordered/open sector sharpens the
abstract Yangian package to the familiar Drinfeld and RTT
presentations: $T(u) = \sum T_{ij}(u)\, e_{ij}$ satisfies
$R_{12}(u-v)\, T_1(u)\, T_2(v) = T_2(v)\, T_1(u)\, R_{12}(u-v)$;
RTT is the spectral rewriting of the F2 line-operator-braiding face
of the $r(z)$ $\mathrm{GRT}_1(\Q)$-torsor developed in
Part~\ref{part:char-datum}.

\section*{The recognition theorem and homotopy-Koszulity}

\begin{maintheorem}[Recognition under logarithmic $\SCchtop$ hypotheses;
Theorem~\textup{\ref{thm:recognition}}]
\label{thm:intro-recognition}
Let $\cA$ be a logarithmic $\SCchtop$-algebra
\textup{(}Definition~\textup{\ref{def:log-SC-algebra})}. Then
$\cA$ carries a $C_\ast(W(\SCchtop))$-algebra structure, where
$W(\SCchtop)$ is the Boardman--Vogt $W$-construction (replaces
strict operadic composition by homotopy-coherent composition,
inserting intervals $[0,1]$ at internal edges). Bar
differential $d_{\barB}$ is holomorphic factorisation; bar
coproduct $\Delta$ is topological factorisation.
\end{maintheorem}

Proof (Section~\ref{sec:foundations}) reduces to
$\FM_k(\C) \times \Conf_k(\R)$ decomposition + retract
argument, given holomorphic factorisation of Vol~I and
logarithmic $\SCchtop$ structure
(Definition~\ref{def:log-SC-algebra}). Physical realizations:
Theorem~\ref{thm:physics-bridge}.

\begin{maintheorem}[Homotopy-Koszulity at the associated-graded
level; Theorem~\textup{\ref{thm:homotopy-Koszul}}]
\label{thm:intro-homotopy-Koszul}
The depth-zero associated graded $\gr\,\SCchtop$ (the decoupled
operad with mixed components $C_\ast(\FM_k(\C))\otimes C_\ast(E_1(m))$)
is homotopy-Koszul: the canonical
$\Omega \mathbf{B}(\gr\,\SCchtop) \xrightarrow{\,\sim\,} \gr\,\SCchtop$ is
a quasi-isomorphism of dg operads. The coupled statement for
$\SCchtop$ itself is Conjecture~\ref{conj:coupled-sc-koszul}:
Livernet's non-formality of the Swiss-cheese operad \cite{Liv15}
obstructs the formality route to it.
\end{maintheorem}

Three facts enter. (1) The decoupled homology operad, built
from individually Koszul $\Etwo$ ($\Etwo(k) = H_\ast(\mathrm{Conf}_k(\C))$,
governing Gerstenhaber algebras) and $\Ass = \Eone$ via the
componentwise distributive law, is Koszul (Ginzburg--Kapranov~\cite{GK94});
the coupled homology Swiss-cheese operad is also Koszul
(Hoefel~\cite{Hoe09}, Hoefel--Livernet~\cite{HL12}), but only the
decoupled statement is used. (2)
Kontsevich formality: $\Phi\colon \gr\,\SCchtop \to \mathsf{sc}^{\mathrm{gr}}$
quasi-isomorphism of two-coloured dg operads (identity on open
colour, formality $C_\ast(\FM_k(\C)) \to \Etwo(k)$ on closed
colour); no such map exists out of the coupled $\SCchtop$
\cite{Liv15}. (3) Bar/cobar preserve qi between augmented operads
(Berger--Moerdijk~\cite{BM03}); two-out-of-three gives
$\Omega \mathbf{B}(\gr\,\SCchtop) \simeq \gr\,\SCchtop$.

Bar-cobar adjunction $(\Omega, \mathbf{B})$ on $\SCchtop$-algebras
is a Quillen equivalence
(Theorem~\ref{thm:scchtop-bar-cobar-adjunction}). Filtered version
(Theorem~\ref{thm:filtered-koszul}) extends beyond quadratic.
$\SCchtop$ is not self-dual: the duality functor is an
involution, but the operad and its dual differ in dimensions.
$(\SCchtop)^! = (\mathsf{Lie}, \mathsf{Ass},
\text{shuffle-mixed})$.

\paragraph{Colour decomposition of the MC element.}
$\alpha_T = \alpha_T^{\mathrm{cl}} + \alpha_T^{\mathrm{mix}} +
\alpha_T^{\mathrm{op}}$. MC equation
$D\alpha_T + \tfrac{1}{2}[\alpha_T, \alpha_T] = 0$ decomposes:
\begin{alignat}{2}
\label{eq:intro-mc-closed}
&D\alpha_T^{\mathrm{cl}}
 + \tfrac{1}{2}[\alpha_T^{\mathrm{cl}},
 \alpha_T^{\mathrm{cl}}] &&= 0, \\
\label{eq:intro-mc-mixed}
&d_{\Theta}\,\alpha_T^{\mathrm{mix}} &&= 0, \\
\label{eq:intro-mc-open}
&d_{\Theta}\,\alpha_T^{\mathrm{op}}
 + [\alpha_T^{\mathrm{mix}}, \alpha_T^{\mathrm{op}}]
 + \tfrac{1}{2}[\alpha_T^{\mathrm{mix}},
 \alpha_T^{\mathrm{mix}}]_{\mathrm{op}}
 + \tfrac{1}{2}[\alpha_T^{\mathrm{op}},
 \alpha_T^{\mathrm{op}}] &&= 0,
\end{alignat}
$\Theta_\cA := \alpha_T^{\mathrm{cl}}$ is the closed-colour
component, $d_\Theta = D + [\Theta_\cA, -]$ the
$\Theta_\cA$-twisted differential.
\eqref{eq:intro-mc-closed}: Vol~I modular MC equation.
\eqref{eq:intro-mc-mixed}: classical $r$-matrix and PVA bracket
are $\Theta_\cA$-twisted cocycles in $\gSC_T$.
\eqref{eq:intro-mc-open}: line-category structure closes under
combined twisting.

Two worked examples:
\[
\begin{array}{l|l|l}
 & \textbf{Kac--Moody } \widehat{\fg}_k &
 \textbf{Virasoro}_c \\
\hline
\alpha_T^{\mathrm{cl}} & \Theta_{\widehat{\fg}_k}\ (\text{affine MC}) &
 \Theta_{\mathrm{Vir}_c}\ (\text{infinite-depth MC}) \\
\alpha_T^{\mathrm{mix}} & k\,\Omega_{\mathrm{tr}}/z\ (\text{level-weighted Casimir}) &
 (c/2)/z^4 + 2T/z^2 + (\partial T)/z \\
\text{Mixed cocycle} &
 \text{CYBE for } r(z) = k\Omega_{\mathrm{tr}}/z &
 \text{Laplace Jacobi / collision-residue CYBE} \\
\alpha_T^{\mathrm{op}} &
 \text{Yangian coproduct on } Y(\fg) &
 \text{Virasoro fusion kernel} \\
\text{Open equation} &
 \text{RTT: } R_{12}T_1T_2 = T_2T_1R_{12} &
 \text{BPZ crossing kernel}
\end{array}
\]
On the affine comparison surface, the mixed MC component along
spectral parameter identifies with KZ
$\nabla = d - \frac{1}{k+h^\vee}\,\Omega\,d\log z$. On Virasoro,
the collision residue $(c/2)/z^3 + 2T/z$ has pole orders
one lower than $\alpha_T^{\mathrm{mix}}$ by $d\log$ absorption
(the standard collision-vs-Laplace gap);
quartic contact $\mathfrak{Q} = 10/[c(5c+22)]$, whose large-$c$
asymptotic is $2/c^2$ (not $2/(5c^2)$), reappears as
self-coupling
$[\alpha_T^{\mathrm{mix}},\alpha_T^{\mathrm{mix}}]_{\mathrm{op}}$
in~\eqref{eq:intro-mc-open}.

\section*{Curved Swiss-cheese at genus $g$}

Vol~I Theorem~D: $\kappaChHodge(\cA)$ scalar invariant
controls genus-$1$ curvature and chain-level continuation to
higher genus through the modular-bootstrap $H^2$ acyclicity and
degree-$2$ curved-Dunn comparison map
(Theorem~\ref{thm:curved-dunn-H2-vanishing-all-genera}).

At genus~$1$, and for $g \ge 2$ after those two comparison inputs:
\begin{equation}
\label{eq:intro-curvature}
\dfib^{\,2} \;=\; \kappaChHodge(\cA) \cdot \omega_g,
\end{equation}
$\omega_g = c_1(\lambda) \in H^2(\ov{\cM}_g, \Z)$ first Chern
class of Hodge bundle. Coproduct $\Delta$ remains coassociative:
curvature is central in the coalgebra. Total corrected
differential $\Dg{g} = \dfib + \delta_g$ restores flatness over
$\ov{\cM}_g$ at the cost of coupling $\R$-ordering to Hodge
bundle.

The genus tower $\{\barB^{(g)}(\cA), \dfib, \Delta\}_{g \ge 0}$
is controlled by the $\hat{A}$-genus generating function
($\hat{A}(x) = (x/2)/\sinh(x/2) = 1 - x^2/24 + 7x^4/5760 - \cdots$):
\[
\sum_{g \geq 0} \h^{2g-2} F_g(\cA)
\;=\;
\sum_{g \geq 0} \h^{2g-2}
\int_{\ov{\cM}_g} \hat{A}(\kappaChHodge(\cA) \cdot \omega_g).
\]
At genus~$0$: $\omega_0 = 0$, Swiss-cheese flat. At genus~$1$:
$\omega_1 = c_1(\lambda) \in H^2(\overline{\cM}_1, \Z)$,
$d^2 = \kappaChHodge(\cA) \cdot \omega_1$, corrected
scalar value $F_1=\kappaChHodge(\cA)/24$. The determinant-line
character has leading logarithm
$-\kappaChHodge(\cA)\log\eta(\tau)$.

Full development: Section~\ref{sec:bar_cobar},
Part~\ref{part:examples} (Section~\ref{ch:modular-pva-quantization}).
Curved-Dunn $H^2$ vanishing at all $g$ is the theorem obtained from
the modular-bootstrap-to-curved-Dunn bridge hypotheses
(Theorem~\ref{thm:curved-dunn-H2-vanishing-all-genera}); MC5
analytic at all genera; BV $=$ bar coderived for all shadow
classes (Vol~I, Theorem~\ref*{V1-thm:bv-bar-coderived});
genuswise chain-level BV/BRST/bar identification on the original
direct-sum complex reduces to the finite-propagation obstruction
of F3 (class~M without completion is false).

\paragraph{Modular bar complex and Feynman transform.}
Genus-$g$ bar complex is one fibre of the complete modular bar
coalgebra $\Bmod(C)$
(Definition~\ref{def:complete-modular-bar},
\S\ref{ch:modular-pva-quantization}):
\[
\Bmod(C)
\;=\;
\prod_{[\Gamma] \in \StGraph^{\mathrm{conn}}}
\bigl(C[\Gamma]\bigr)_{\Aut(\Gamma)},
\]
over connected stable graphs (genus labelling
$g\colon V(\Gamma) \to \Z_{\ge 0}$ and stability
$2g(v) - 2 + \mathrm{val}(v) > 0$). Differential
$D = D_{\mathrm{int}} + D_{\mathrm{exp}}$ squares to zero by
codim-2 cancellation
(Theorem~\ref{thm:modular-bar}). Decomposes into
genus-preserving $D_0$ and genus-raising $D_1$:
$D_0^2 = 0$, $D_0 D_1 + D_1 D_0 = 0$, $D_1^2 = 0$.
Genus-raising $D_1 = D_{\mathrm{nsep}}$: nonseparating one-edge
expansions, the algebraic shadow of first loop correction.

The modular deformation dg Lie algebra $L_{\mathrm{mod}}(C) =
\Coder(\Bmod(C))[-1]$ (coderivations form Lie under commutator;
$[-1]$ shift makes MC $d(x) + \tfrac{1}{2}[x,x] = 0$) is a
complete filtered dg Lie algebra whose genus-zero associated
graded \emph{is} the chiral convolution dg Lie algebra of
Vol~I. Total positive-genus correction
$\delta_\cA := D - D^{(0)}$ is canonical MC element in
positive-genus ideal
(Theorem~\ref{thm:coderivation-MC-synthesis}):
$\partial\delta_\cA + \tfrac{1}{2}[\delta_\cA, \delta_\cA] = 0$.
Genus expansion $D = \sum_{g \ge 0} \hbar^g D^{(g)}$ is the
Feynman transform of $\{\overline{\cM}_{g,n}\}$;
$D^2 = 0$ decomposes into sewing relations
$\sum_{p+q=g} D^{(p)} D^{(q)} = 0$, each graph carrying symmetry
factor $1/|\Aut(\Gamma)|$
(Theorem~\ref{thm:formal-genus-expansion}).

\paragraph{Genus completion and chiral convolution.}
$L_{\mathrm{mod}}(C)$ has complete genus filtration with strong
filtration axiom $\mu_r(F^{i_1}, \ldots, F^{i_r}) \subset
F^{i_1 + \cdots + i_r}$, whose genus-$0$ associated graded is
the chiral convolution dg Lie algebra
$\mathfrak{g}^{\mathrm{ch}}_C = \Coder(\Bch(C))[-1]$
(Theorem~\ref{thm:genus-completion}):
\[
\renewcommand{\arraystretch}{1.3}
\begin{array}{@{}c|cc@{}}
 & \text{coalgebra} & \text{Lie algebra} \\
\hline
\text{genus } 0
 & \Bch(C) = \bar{B}^{(0)}(C)
 & \mathfrak{g}^{\mathrm{ch}}_C = \Coder(\Bch)[-1]
 \\
\text{all genera}
 & \Bmod(C) = \mathrm{FT}_{\cM\!od}(C)
 & L_{\mathrm{mod}}(C) = \Coder(\Bmod)[-1]
\end{array}
\]
The Lie bracket on $L_{\mathrm{mod}}$ is a Chriss--Ginzburg
correspondence product, defined by clutching maps
($\xi_{\mathrm{sep}}\colon \ov{\cM}_{g_1,n_1+1} \times
\ov{\cM}_{g_2,n_2+1} \to \ov{\cM}_{g,n}$ separating;
$\xi_{\mathrm{ns}}\colon \ov{\cM}_{g-1,n+2} \to \ov{\cM}_{g,n}$
non-separating;
Remark~\ref{rem:convolution-as-correspondence}).
$\delta_\cA$ factorises along boundary strata of
$\overline{\cM}_{g,n}$
(Proposition~\ref{prop:factorisation-boundary-strata}):
$\xi_{\mathrm{sep}}^*(\delta^{(g)}) = \sum_{g_1+g_2=g}
\delta^{(g_1)} \star \delta^{(g_2)}$;
$\xi_{\mathrm{ns}}^*(\delta^{(g)}) =
\Delta_{\mathrm{cyc}}(\delta^{(g-1)}) +$ lower-genus terms.
In Beilinson--Drinfeld language: chiral convolution is
factorisation homology $\int_{\Ran(X)} \End_\cA$; modular
convolution is $\int_{\cM\!od} \End_\cA$.

\paragraph{Half-space and the doubling principle.}
PVA-to-VA bridge through the half-space. PV sigma model of
Khan--Zeng on $\C \times \R_{\ge 0}$ with Neumann boundary
produces a BV theory whose boundary algebra is the sought
vertex algebra. For affine: mixed BF/Chern--Simons, one-loop
exact (Theorem~\ref{thm:affine-half-space-bv}). For nonlinear
PVAs, all loop orders are the theorem. The shifted-cotangent action
is classical, and the classical master equation is equivalent to the
PVA Hamiltonian condition. The quantum problem is the finite-quotient
recursion for counterterms: the obstruction at loop order~$\ell$ is
closed, the harmonic projection must vanish on every jet--weight
quotient, and the SDR solves
$d I_{\ell}+\mathrm{Ob}_{\ell}=0$ compatibly in the inverse limit. The
doubling principle reduces the reflected identity to an image-choice
Stokes theorem on the doubled $\C \times \R$, but the nonlinear
all-loop boundary vertex algebra requires the KZ analytic SDR package of
Assumption~\ref{assum:kz-analytic-sdr-package}
(Theorem~\ref{thm:general-half-space-bv}).

Logical chain closed for named examples: affine PV sigma model
satisfies physics bridge hypotheses
(Proposition~\ref{prop:affine-is-log-SC}); DS preserves
$\SCchtop$ (Proposition~\ref{prop:ds-preserves-SC}); Virasoro
and $\cW_N$ inherit the full bulk--boundary--line datum
(Corollary~\ref{cor:virasoro-wn-are-SC}). The five-component
datum (bulk factorization, boundary VA, scoped derived-centre,
scoped line category, spectral $R$-matrix) assembles into a
single theorem
(Theorem~\ref{thm:bulk-boundary-line-factorization}).

\section*{The topologisation theorem}

The $\SCchtop$ output admits a further structure when the
boundary algebra has conformal vector at non-critical level:
topologisation. Sugawara
$T(z) = \frac{1}{2(k+h^\vee)}\sum_a {:}J^a J^a{:}(z)$ renders
$\C$-translations $Q$-exact on \(Q_{\mathrm{CS}}\)-cohomology, makes the closed
chiral colour topological on cohomology, and produces an
$E_3$-topological algebra on
$H^\bullet_{Q_{\mathrm{CS}}}\cZ^{\mathrm{der}}_{\mathrm{ch}}(\cA)$. Dunn additivity then combines
$\Etwo^{\mathrm{top}} \times \Eone^{\mathrm{top}} =
E_3^{\mathrm{top}}$: a 3d TQFT, independent of complex structure
on $\C$ after passage to \(Q\)-cohomology. The strict raw-chain-level
lift is a separate statement.

\begin{maintheorem}[Topologisation for affine Kac--Moody;
Theorem~\textup{\ref{thm:E3-topological-km}}]
\label{thm:intro-e3-km}
For $\widehat{\fg}_k$ at non-critical $k \neq -h^\vee$ with
simple $\fg$,
$H^\bullet_{Q_{\mathrm{CS}}}\cZ^{\mathrm{der}}_{\mathrm{ch}}(\widehat{\fg}_k)$
carries $E_3$-topological structure. The 3d HT theory is
holomorphic Chern--Simons (Costello--Li, by dimensional
reduction from 6d holomorphic gauge theory).
\end{maintheorem}

\begin{maintheorem}[Topologisation via Drinfeld--Sokolov;
Theorems~\textup{\ref{thm:E3-topological-DS}}
and~\textup{\ref{thm:E3-topological-DS-general}}]
\label{thm:intro-e3-ds}
For every DS reduction covered by the Costello--Gaiotto boundary
condition, with a good DS grading and non-critical level
(principal and the stated hook/subregular non-principal families),
the chiral derived centre carries $E_3$-topological structure after
passing to the cohomology of the total DS differential
\(Q_{\mathrm{DS}}\), conditional on the DS BRST primitive package
\(\hypDSBRST\).  The 3d theory comes from Costello--Gaiotto; the
remaining input is the normalised cohomological identity
\[
T_{\mathrm{DS}}=[Q_{\mathrm{DS}},G'_{\mathrm{DS}}],
\]
not the false statement that Cartan currents are
\(Q_{\mathrm{CS}}\)-exact in the unreduced holomorphic
Chern--Simons bulk.
\end{maintheorem}

\begin{maintheorem}[Topologisation for free PVAs;
Theorem~\textup{\ref{thm:E3-topological-free-PVA}},
under the KZ analytic SDR and topological-lift hypotheses]
\label{thm:intro-e3-free-PVA}
For finite-type freely generated finite-jet PVAs with conformal vector
at non-critical level, the chiral derived centre admits
$E_3$-topological structure after the KZ analytic SDR package and the
quantized topological lift $T=[Q_{\mathrm{tot}},G]$ are supplied. The
3d theory is the Khan--Zeng 3d Poisson sigma model; beyond the affine
and DS gauge-theoretic lanes, its all-loop boundary quantization is the
KZ analytic SDR plus image-choice Stokes package of
Theorem~\ref{thm:E3-topological-free-PVA}.
\end{maintheorem}

Heisenberg and lattice VOAs reach $E_3$-topological as abelian
specialisations of Theorem~\ref{thm:E3-topological-km}, via abelian
Sugawara $T = (1/(2k))\!:JJ:$ and abelian Chern--Simons. The same
objects also lie on the free-PVA lane, where the general nonlinear
statement requires the same KZ analytic SDR and topological-lift
package.

At critical level $k = -h^\vee$ the Sugawara denominator
vanishes; topologisation fails; centre jumps to Feigin--Frenkel
oper centre $\mathfrak{z}(\widehat{\fg})$; pair stays at
$\SCchtop$.

The modular operad $\SCchtop_{\mathrm{mod}}$ has composition at
integral level
(Theorem~\ref{thm:affine-composition-associativity}),
braided-annular equivariance under the quasi-triangular comparison,
YBE braid coherence, pure-braid descent, and inverse-orientation datum
(Proposition~\ref{prop:qt-equivariance}), and
unit-normalised annular unitality
(Proposition~\ref{prop:modular-operad-unitality})
proved at all genera. Genus-$1$ curvature unconditional via
twisted K\"unneth on $\FM_k(C_\tau)$
(Proposition~\ref{prop:genus1-twisted-tensor-product}). The
continuation to $g \ge 2$ requires the
modular-bootstrap-to-curved-Dunn degree-\(2\) comparison
(Theorem~\ref{thm:curved-dunn-H2-vanishing-all-genera});
irregular-singular KZB regularity at generic non-integral level
supplies boundary composition only under the boundary formal-type
and mixed Stokes hypotheses
(Theorem~\ref{thm:irregular-singular-kzb-regularity}). Higher-genus
boundary formal-type existence at $\overline{\cM}_{g,n}$ is the
Stokes-sector obstruction of F5.


\section*{Universal holography}

Three pillars prove the ambient-qualified holographic equivalence on
the non-logarithmic standard landscape. Let $\cA$ be chirally Koszul
at generic parameter:
\[
\{\,\mathrm{Vir}_c,\; W_{N, c}\ (N \ge 2),\;
 \text{Schellekens } c = 24,\;
 \mathrm{Com}(B, C)\ \text{irrational coset}\,\}.
\]
The logarithmic $W(p)$ triplets are excluded here; F1 records the
tempering problem.

\begin{maintheorem}[Universal holography;
Theorem~\textup{\ref{thm:programme-climax}},
with class-dependent chain ambient]
\label{thm:intro-programme-climax}
There exists a 3d HT gauge theory $T_\cA$ on
$X \times \R$ with
\begin{enumerate}[label=\textup{(\roman*)}]
\item $\mathrm{Obs}^\partial(T_\cA) \simeq \cA$ as
 $E_1$-chiral factorisation algebras on $X$;
\item $\mathrm{Obs}^{\mathrm{bulk}}(T_\cA) \simeq
 \cZ^{\mathrm{der}}_{\mathrm{ch}}(\cA)$ as $E_3$-topological
 factorisation algebras on $X \times \R$ in the appropriate chain
 ambient: original complex for finite-propagation strata, and
 weight-completed / KS-$\rho$ Banach ambient for class~M;
\item Banach completion $\mathcal{B}_\rho(\cA)$ below the ordinary
 shadow radius gives explicit chain-level
 quasi-isomorphism to $\cZ^{\mathrm{der}}_{\mathrm{ch}}(\cA)$,
 with
 \[
 \beta_\cA \;=\;
\begin{cases}
6 & \cA = \mathrm{Vir}_c, \\
12(H_N - 1) & \cA = W_{N, c}\ \text{(principal, harmonic law)}, \\
6 \cdot |c_K(k)| & \cA = \mathrm{Com}(H_k, V_k(\mathfrak{sl}_2)),
\end{cases}
\]
 the Arnold coupling $\beta_\cA$ being the coefficient of the
 leading $d\log$-singularity in the $m_2$-part of $\Theta_\cA$ at
 the binary-collision residue
($H_N = \sum_{j=1}^N 1/j$; specialises to $\beta_{W_2} = 6$ and
$\beta_{W_3} = 10$ under the harmonic law; the naive
Fateev--Lukyanov polynomial
$(N+1)(N+2)/2$ agrees through $N = 3$ but overestimates at
 $N \ge 4$).
\end{enumerate}
\end{maintheorem}

\paragraph{Three pillars.}

\smallskip\textbf{(1) Convergence (Stirling dominance).}
Shadow-tower coefficients satisfy
$\limsup_{r \to \infty}(|S_r(\cA)|/r!)^{1/r} = 0$
(Theorem~\ref{thm:tempered-stratum-contains-virasoro},
Theorem~\ref{thm:wn-tempered-all-N},
Theorem~\ref{thm:irrational-coset-tempered}). Uniform proof:
$(r!)^{1/r} \sim r/e$ grows without bound, finite Arnold coupling
$\beta_\cA$ forces $|S_r(\cA)|^{1/r} \le \beta_\cA/|c|$, quotient
$\to 0$. Stirling dominates polynomial growth; ordinary
generating series $\sum_r S_r(\cA)\,\rho^r$ converges absolutely
for $\rho < \rho_*(\cA)$. Finite principal $\cW_N$ tempering does not
use the exact value of $\beta_N$; the harmonic law
$\beta_N = 12(H_N - 1)$ is
Conjecture~\ref{conj:beta-N-harmonic-closed-form} and supplies
the exact radius only when the harmonic law is assumed.

\smallskip\textbf{(2) Rigidity (finite amplitude data + refinement).}
$\mathrm{Zhu}(\cA)$ records the finite algebraic shadow on
rational/lisse rows; it does not by itself bound Massey products
through all bar degrees.  Tempering is proved only where a
family-specific arity-growth bound is supplied: finite-depth transfer,
Virasoro recurrence, finite-envelope estimates, or orbifold BV descent.
The logarithmic triplet $\cW(p)$ remains conjectural until the regular
and logarithmic amplitude bounds of
Conjecture~\ref{conj:tempered-stratum-contains-wp} are proved.
Refinement is necessary: witness $K(\mathfrak{sl}_2, \sqrt{2})$
has infinite Zhu yet tempered shadow growth
(Proposition~\ref{prop:zhu-unbounded-tempered-nontrivial});
Heisenberg branching of coset modules polynomial in charge
(Proposition~\ref{prop:heisenberg-branching-polynomial}).
Correct criterion: \emph{VSKR + BGG}
(Corollary~\ref{cor:tempered-criterion-refined}), strictly weaker
than $C_2$-cofiniteness. Unbounded Zhu is not an obstruction to tempering
(Proposition~\ref{prop:zhu-unbounded-tempered-nontrivial}):
the converse of the tempered-unbounded-Zhu implication fails.

\smallskip\textbf{(3) Gauge-theoretic (Costello--Gwilliam +
finite-orbifold BV descent).}
Costello--Li factorisation envelope extended by
Costello--Gwilliam orbifold BV. For a cyclic lattice-VOA orbifold
the finite-group obstruction is the Dijkgraaf--Witten class
$[\alpha_{\mathrm{orb}}]\in H^3(B\Z/n;\, U(1))$; the determinant and
fixed-lattice data are evidence in the type-specific computation, not
a universal descent theorem. Monster $V^\natural$ uses the separate
input $[\alpha_{\mathrm{orb}}]=0$
(Proposition~\ref{prop:monster-alpha-explicit-zero}) together with
Frenkel--Lepowsky--Meurman twisted Jacobi associativity. Representatives
of the $71$ Schellekens $c = 24$ rows enter through the three-stratum
partition
\[
71 \;=\; 24_{\mathrm{Niemeier}} + 1_{V_1=0} + 46_{\mathrm{nonlat}}
\]
of pure Niemeier rows, the $V_1=0$ row represented by
$V^\natural$, and non-lattice Leech deep-hole rows
(van Ekeren--M\"oller--Scheithauer 2020 level-matching); their
$E_3$ descent is invoked only after the corresponding anomaly and
BV-descent inputs:
Theorem~\ref{thm:schellekens-71-all-alpha-zero}.

\smallskip\textbf{Intersection.}
\[
\begin{array}{l|l}
\text{Pillar} & \text{Contribution} \\
\hline
\text{Convergence} &
 \text{Banach completion } \cB_\rho(\cA) \\
\text{Rigidity} &
 \text{chain-level } E_3\text{-brace on } \cB_\rho(\cA)
 \text{ (bounded Massey)} \\
\text{Gauge-theoretic} &
 \cB_\rho(\cA) = \text{bulk factorisation algebra of
 Costello--Li 3d theory}
\end{array}
\]
Intersection: chain-level equivalence
$\mathrm{Obs}^{\mathrm{bulk}}(T_\cA) \simeq
\cZ^{\mathrm{der}}_{\mathrm{ch}}(\cA)$ on the original complex for
finite-propagation strata and on the weight-completed / pro ambient
for class~$\mathbf{M}$.

\paragraph{Class~$\mathbf{M}$ global triangle.}
DS--Hochschild compatibility gap resolved: chiral higher Deligne
identification
\[
\mathrm{ChirHoch}^\bullet(W_k(\fg))
\;\simeq\;
H^\bullet_{\mathrm{DS}}\bigl(\mathrm{ChirHoch}^\bullet(V_k(\fg))\bigr)
\]
(Theorem~\ref{thm:chd-ds-hochschild}) transports chain-level
$E_2$-chiral Gerstenhaber from affine through DS reduction.
Combined with the tempered-stratum argument, the global triangle
$\Abulk \simeq \cZ^{\mathrm{der}}_{\mathrm{ch}}(\Bbound) \simeq
\cZ^{\mathrm{der}}_{\mathrm{ch}}(\cC_{\mathrm{line}}) \simeq
\HH^\bullet_{\mathrm{ch}}(\cA^!_{\mathrm{line}})$ is proved on the
original complex for finite-propagation strata and in the
weight-completed / pro ambient for class~$\mathbf{M}$
(Corollary~\ref{cor:universal-holography-class-M}). Raw direct-sum
closure for class~$\mathbf{M}$ at positive genus remains the
finite-propagation problem, not a consequence of DS--Hochschild
compatibility alone.

\paragraph{Five-archetype landscape.}
The averaging map
$\mathrm{av}\colon \mathfrak g^{E_1}_\cA \to
\mathfrak g^{\mathrm{mod}}_\cA$ partitions the standard
landscape, augmented by the Borcherds BKM lineage, into five
archetypes G/L/C/M/B with their distinguishing invariants. Rows
G/L/C/M record the complementarity sum $\kappa_{ch} + \kappa_{ch}^!$;
row B carries the separate Mukai/conductor ceiling
$K^{\kappaChHodge}$ (a different invariant, not a complementarity
sum):
\[
\renewcommand{\arraystretch}{1.3}
\begin{array}{l|l|l|l}
\text{archetype} & \text{representative} & \text{invariant} &
 \text{witness} \\
\hline
\mathbf{G} & \cH_k & \kappa_{ch} + \kappa_{ch}^! = 0 &
 \text{abelian self-dual Heisenberg} \\
\mathbf{L} & \widehat{\fg}_k\ (\fg = \mathfrak{sl}_2) &
 \kappa_{ch} + \kappa_{ch}^! = 0 &
 \text{trace-form symmetry } k \leftrightarrow -k \\
\mathbf{C} & \beta\gamma_\lambda &
 \kappa_{ch} + \kappa_{ch}^! = 0 &
 \text{curved $\cA$-module self-duality, } \kappa_{ch}^! = -\kappa_{ch} \\
\mathbf{M} & \mathrm{Vir}_c & \kappa_{ch} + \kappa_{ch}^! = 13 &
 \text{Feigin--Fuchs pair } c \leftrightarrow 26 - c \\
\mathbf{B} & \mathbf H_{\Delta_5} &
 \kappa_{ch} + \kappa_{ch}^! = 0;\ K^{\kappaChHodge} = 8 &
 \text{Hodge sum on K3$\times E$; Mukai doubling } K^{\kappaChHodge}
 = 2c_+(\mathrm{II}_{4,20}),
 \text{Bruinier--Heegner}
\end{array}
\]
The archetype $\mathbf{B}$ is the Borcherds crown: the K3 Hall
boundary object $\mathbf H_{\Delta_5}$ on the Mukai-enhanced
lattice $\mathrm{II}_{4,20}$, with $K^{\kappaChHodge} = 8$ forced by the
Bruinier 2002 \emph{LNM}~1780 Prop~5.1 Heegner--Chern reciprocity
$K^{\kappaChHodge} = 2 c_+(L)$ applied to K3's positive-signature count
$c_+(\mathrm{II}_{4,20}) = 4$. The Vol~II three-faces identity
$\hbar^2 K^{\kappaChHodge} = -1$ specialises to
$(\hbar^2, K^{\kappaChHodge}) = (-1/8, 8)$ at
$\mathbf{B}$; the chiral conductor
${\kappaChHodge}^{\mathrm{Heis}}(\mathrm{K3}\times E) = 3$
(rank of the surviving Heisenberg Cartan after the second reduction)
is a \emph{separate} Cartan-rank invariant and does not enter
$\kappa + \kappa^!$.
On the $\mathbf{M}$ row, principal $\cW_3$ at dual levels yields
$\kappa + \kappa^! = 250/3$, a sub-row value specialising
Virasoro's $\mathbf{M}$-archetype $13$; the Bershadsky--Polyakov
$\kappa$-sum is open — the BP central-charge conductor
$c_{\mathrm{BP}}(k) + c_{\mathrm{BP}}(-k-6) \equiv 50$ is a
self-contraction of central charges, not a $\kappa$-sum, and no
derivation of $\kappaChHodge(\mathrm{BP}_k)$ exists.
The computed complementarity spectrum is $\{0, 13, 250/3\}$:
the $\mathbf{G}/\mathbf{L}/\mathbf{C}$ rows and the
$\mathbf{B}$-row Hodge sum give $0$, the $\mathbf{M}$ row gives
$13$ and $250/3$, and the BP slot is open. The $\mathbf{B}$-row
Mukai conductor $K^{\kappaChHodge}=8$ is a distinct sixth
invariant, not a complementarity sum; the spectrum together with
the Mukai conductor forms the combined value set of the trilogy
landscape.

\paragraph{Bar--cobar octachotomy at codim $3$.}
The strict chain-level bar--cobar closure on
$\overline{\mathcal A_2} \setminus \bigcup_n H_n$ passes through
eight ambient $(\infty,1)$-categories (fibrewise generic;
single-monodromy $H_n$; bi-unipotent Malcev; tri-unipotent Malcev
at $(3,4,7)$; weight-completed coderived; $\Ainf$-corrected;
$(\infty,1)$-$\mathrm{Perf}(\overline{\mathcal A_2})$;
chiral-Kontsevich-formal on the Koszul locus), each carrying the
Maurer--Cartan closure $\mathrm{MC}(\overline B^{(g)}(\cA))$ on
distinct Humbert-divisor intersection data. The universal closure
depth is
\[
 k_{\max}(g)
 \;=\;
 \frac{g(g+1)}{2}.
\]
The octachotomy proves closure of the shadow obstruction tower
at universal degree $k_{\max}(g)$ for every chirally Koszul
$\cA$ simultaneously. At $g = 1$ one recovers $k_{\max} = 1$,
coinciding with the one-loop exactness of
Section~\ref{sec:foundations}; at $g = 2$ one recovers
$k_{\max} = 3$, the three-loop bound governing Siegel
$\Phi_{10}$ Fourier coefficients; the general bound
$g(g+1)/2$ is the triangular number that also counts rigid
Humbert divisors on $\overline{\mathcal A_g}$.

\paragraph{Universal ratio-of-levels law.}
On the Fricke-reflective signature-$(2,n)$ rows
$\{\mathrm{II}_{2,2}, \mathrm{II}_{10,2}, \mathrm{II}_{18,2},
\mathrm{II}_{26,2}\}$ and the $24 A_1$ Niemeier, for pairs
$(X, L_X)$ and $(Y, L_Y)$ of archetype representatives sharing
the same shadow profile at $g = 0$ and admitting a Fricke
involution on $L_X, L_Y$, the ratio of characteristic levels
satisfies
\[
 \frac{\ell_X}{\ell_Y}
 \;=\;
 \frac{c_+(L_X)}{c_+(L_Y)},
\]
where $c_+(L)$ is the positive-signature count of the
even lattice $L$ (the universal ratio-of-levels identity).
Positive-definite rows; Leech and Conway $V^{s\natural}$
on $\Lambda_{24}$; admit no Fricke involution; the ratio is
undefined there. The law
harmonises the affine level $k$, the Virasoro central charge
$c$, and the Borcherds weight $\mathrm{wt}(\Delta_5) = 5$ in a
single homogeneous scale. For $\widehat{\fg}_k$ and
$\mathrm{Vir}_c$ at $\mathbf L$-to-$\mathbf M$ transport via
Drinfeld--Sokolov, $\ell_{\mathrm{Vir}} / \ell_{\widehat\fg} =
c/2 \cdot 2h^\vee / (\dim\fg (k + h^\vee))$.

\section*{The iterated-Sugawara endpoint: $E_3 \to E_\infty$}

Theorem~\ref{thm:intro-programme-climax} places the 3d theory on
a single rung. The first $k$ commuting inner stress tensors
$T^{(2)},\ldots,T^{(k+1)}$ make
$\cZ^{\mathrm{der}}_{\mathrm{ch}}(\cA)$ $E_{k+2}$-topological
on $Q_{\mathrm{tot}}$-cohomology via iterated-Sugawara
(Theorem~\ref{thm:iterated-sugawara-construction}). Endpoint:
$W_\infty[\lambda]$ at generic $\lambda$.

\begin{maintheorem}[Infinite-rung ladder climax;
Theorem~\textup{\ref{thm:climax-w-infty-endpoint}},
on bounded conformal-weight windows]
\label{thm:intro-climax-w-infty}
For every bounded conformal-weight window and every
$\lambda \in \Lambda_{\mathrm{gen}} \subset \C$:
\[
\cZ^{\mathrm{der}}_{\mathrm{ch}}(W_\infty[\lambda])
\;\in\;
E_\infty\text{-}\mathrm{top}\text{-}\mathrm{Alg}
\;=\;
\varprojlim_n E_n\text{-}\mathrm{top}\text{-}\mathrm{Alg},
\]
and $T_{W_\infty[\lambda]}$ is the bounded-window
$N \to \infty$ limit of $T_{W_{N,c(N,\lambda)}}$.
\end{maintheorem}

Three convergence mechanisms agree on common threshold
$N_0(w_{\max}) = 2w_{\max} - 1$: Prochazka triangular truncation;
Creutzig--Kanade--Linshaw parafermion; Pope--Romans--Shen with
Bakas wedge algebra. Each disjoint machinery
(vertex-algebraic Miura + triangular filtration;
$\mathfrak{sl}_\infty$ parafermion; classical symplectic Moyal
quantisation) yields the same threshold; the agreement is
structural
(Proposition~\ref{prop:uniform-threshold-2wmax-minus-1}).

\paragraph{Climax as shadow.}
Part~\ref{part:gravity} (3d quantum gravity) is the $N = 2$
shadow of the $3d{+}\infty$ topological endpoint. Virasoro
captures spin-two; full tower captures spin-$n$ for every $n$.
Pure 3d gravity admits spin-ladder refinement with infinite
tower at $W_\infty[\lambda]$; the Brown--Henneaux boundary
$\mathrm{Vir}_c$ at $c = 3\ell/(2G_N)$ is the first rung climbing
to $E_\infty$.

\paragraph{Three-factor universal trace identity
(cross-volume bridge).}
On the Koszul-self-dual subcategory of objects admitting a
BRST resolution with Calabi--Yau target supporting a Borcherds
product,
\[
 \underbrace{\mathrm{tr}_{\mathrm{ghost}}(Q_{\mathrm{BRST}}^2)}_{\text{Vol~I ghost-scope}}
 \;=\;\underbrace{\mathrm{tr}_{\mathrm{Pentagon}}}_{\text{Vol~II MC5 sewing, single-colour closed $\SCchtop$}}
 \;=\;\underbrace{\kappa_{\mathrm{BKM}}(\Phi_N)}_{\text{Vol~III Borcherds weight}}
 \;=\;c_N(0)/2
\]
(Conjecture~\ref{conj:universal-trace-identity-volii-bridge},
\S\ref{sec:universal-trace-identity}).
The weak Jacobi index-$1$ input lifts to one paramodular ladder
on the CHL index $N \in \{1,2,3,4,6\}$: the additive
(Gritsenko~$1999$ Thm.~$1.2$, Cl\'ery--Gritsenko atlas) and
multiplicative (Borcherds product) lifts of the same seed produce
the CHL forms $\Delta_1^{(N)}$ with weights
$\mathrm{wt}(\Delta_1^{(N)}) = c_N(0)/2 \in (5, 3, 2, 3/2, 1)$,
where $c_N(0) = (10, 6, 4, 3, 2)$ is the Fourier zero-coefficient
of the seed (Jatkar--Sen; Govindarajan--Krishna for $N = 4, 6$;
the $N = 4$ weight $3/2$ is half-integral, with square-root
multiplier system). The canonical Borcherds family $\Phi_N$ of the
K3-fibered Class~A rows carries
$\kappa_{\mathrm{BKM}}(\Phi_N) = c_N(0)/2$
(\cref{conj:universal-trace-identity-volii-bridge}); the
three-factor trace identity above uses this family, whose Fourier
zero-coefficient $c_N(0)/2$ equals the trace.
At $N = 1$ the three
factors converge on the K3 Heisenberg witness with common value
$5$: ghost, Pentagon, Borcherds all equal $5$. Scope: the
identity holds on Class~A, the K3-fibered stratum of CY-$3$
(Calabi--Yau threefolds fibred over $\P^1$ with generic K3
fibre: the eight diagonal orbifolds and the STU model;
Proposition~\ref{V3-prop:bkm-weight-universal} in Vol~III).
Class~B (non-K3-fibered CY-$3$, controlled by the
Bershadsky--Cecotti--Ooguri--Vafa invariant
$\kappa_{\mathrm{BCOV}} = \chi(X)/24$ and shadow depth) carries
no Borcherds input and lies outside. The additive split
$\kappa_{\mathrm{BKM}} = \kappaChHodge
+ \chi(\mathcal{O}_{\mathrm{fiber}})$ is false at every
$N \in \{1, 2, 3, 4, 6\}$ and does not appear: the universal
form is $c_N(0)/2$ from the input weak Jacobi form, not an
additive breakdown. The remaining construction datum is the
cross-volume comparison map identifying the Vol~I conductor, the
Vol~II trace functional, and the Vol~III Borcherds zero-coefficient.

\paragraph{The K3 specialisation.}
On the K3 row the three-factor identity is the Hall--Hochschild--
Borcherds comparison. The boundary algebra is the
ordered Hall datum with its Drinfeld coproduct; the bulk algebra is
the derived centre carrying the Siegel denominator; the open/closed
comparison is the averaging map from the smooth Ran locus of the
$24$-node degeneration to the Siegel parameter space. The chiral
Hochschild comparison theorem packages these as three models of one
$\SCchtop$ object, so the Drinfeld,
holomorphic-twist, and
class-$\mathcal{S}$/AGT readings
\cite{Drinfeld90,CostelloGaiotto2020,AGT09,SchiffmannVasserot13}
are boundary-bulk presentations of the same K3 datum. The comparison
maps are the Ran collision morphism, the ordered Hall coproduct, and
the singular-theta pushforward; these are three projections of one K3
datum. More precisely, the datum is already bi-based:
chiral factorisation lives on $\mathrm{Ran}(E^{\mathrm{nod,sm}}_{24})$
with nearby cycles at the $24$ Kodaira nodes, parameter
factorisation lives on $\overline{\mathcal{A}_2}$ in the
Francis--Gaitsgory $\infty$-category of holonomic
$\mathcal{D}$-modules with regular singularities along the Humbert
divisor $H_1\cup H_4$, and the averaging morphism
\[
\mathrm{av}
\;=\;
\mathrm{Torelli}_{K3}\circ\mathrm{KugaSatake}\circ j_{\mathrm{Kodaira}}
\]
is the precise boundary-to-bulk map. The factorisation-homology
centre $\int_{S^1\times\mathrm{Ran}(X)}\cA$ carries the
universal-centre supertrace
\[
K(\cA)=\operatorname{tr}_{\int_{S^1\times\mathrm{Ran}(X)}\cA}
(\mathfrak{K}_{\cA}),
\]
and the K3 Hall--Borcherds source-recognition problem asks for an
object $\mathbf{H}_{\Delta_5}$ and a denominator-character map for
which
\[
K(\mathbf{H}_{\Delta_5})
\;=\;
\kappa_{\mathrm{BKM}}(\mathfrak{g}_{\Delta_5})
\;=\;
c_{\Lambda^{3,2}}(0)/2
\;=\;
5
\;=\;
\mathrm{wt}(\Delta_5),
\]
so $\mathbb{C}\cdot\Delta_5$ is the classifying line and its weight
is the Heegner characteristic of the K3 Hall boundary. This line fixes
the target invariant; it does not by itself construct
$\mathbf{H}_{\Delta_5}$. The Mukai-self-dual partner satisfies
$K(\mathcal{H}_{\mathrm{Muk}})=0$ under the Borcherds
singular-theta pushforward.

On the Costello--Gaiotto holomorphic-topological side, the same K3
pair is the Omega-deformed twisted-$11$D supergravity configuration
on $K3\times T^2$ with $24$ M5-branes wrapped on the Kodaira $I_1$
fibres; the one-loop partition function is paramodular with Borcherds
weight $\kappa_{\mathrm{BKM}}(\Delta_5) = c_{\phi_{0,1}^{K3}}(0)/2
= 5 = \mathrm{wt}(\Delta_5)$, the Fourier zero-coefficient of the
Gritsenko family at $N=1$ (the Borcherds lift is
multiplicative, not additive, so $\kappa_{\mathrm{BKM}}$ admits no
decomposition as $\kappaChHodge + \chi(\cO_{\mathrm{fiber}})$,
consistent with the paragraph above).
Harvey--Moore's heterotic frame, the Dijkgraaf--Verlinde--Verlinde
IIB frame, the Dijkgraaf--Moore--Verlinde--Verlinde IIA frame, and
Witten's M-theory frame on $K3\times T^3$ all return the same
$\Delta_5$. The Beem--Rastelli $4d/2d$ shadow of the same datum is
the class-$\mathcal{S}$ $A_1$ theory on $\Sigma_{0,24}$ with
first-principles Shapere--Tachikawa values $c_{4d} = 107/6$,
$a_{4d} = 403/24$, Coulomb rank $21$, and
$c_{2d} = -12 c_{4d} = -214$ (Shapere--Tachikawa 2008
\texttt{arXiv:0804.1957} \S 3 applied to Chacaltana--Distler 2010
eq.~(5.14); BLLPRvR 2013 eq.~(3.14)); its
$M_{24}$-invariant Schur sector is required to map to
$\phi_{0,1}$, whose Borcherds lift is $\Delta_5$. Along the Humbert
walls the K3 Hall--Drinfeld-double comparison has target
\[
H^2(\mathfrak{g}_{\Delta_5})^{\mathbb{Z}/2,\,K(1)}
\;\cong\;
\mathbb{C}\cdot \Delta_5,
\]
with source-recognition and gauge-comparison maps still required.
The Igusa cusp form is therefore the Heegner-characteristic target:
candidate obstruction class, automorphic output, and conditional
Vol~III answer to the Vol~II composition problem. This is the point
where Volume~I's shadow lens, Volume~II's composition machine, and
Volume~III's origin story have a common classifying line.

\paragraph{$\Omega$-background, Nekrasov self-dual point, and
K3-AGT.}
The Beem--Rastelli $(4d/2d)$ shadow admits a Nekrasov
$\Omega$-deformation refinement. On the class-$\mathcal{S}$ side
$\mathcal{T}[A_1,\Sigma_{0,24}]$ one turns on
$(\epsilon_1,\epsilon_2)$ along the two rotation planes of
$\mathbb{R}^4$ and computes the instanton partition function
$Z^{\mathcal{T}[A_1,\Sigma_{0,24}]}_{\mathrm{inst}}(\epsilon_1,\epsilon_2;
\vec a;m)$ by equivariant integration on the moduli of framed
rank-$2$ instantons with $24$ punctures carrying $\mathfrak{su}(2)$
flavour holonomy \cite{Nekrasov2003,NekrasovOkounkov2006}.
Conditional on a Hall--Borcherds source-recognition theorem and an
AGT Schur comparison theorem, the Schur limit is the
unrefined-locus character of
$\mathbf{H}_{\Delta_5}$; refinement restores the full Siegel
modular variable $Z \in \mathbb{H}_2$ via the
Alday--Gaiotto--Tachikawa dictionary \cite{AGT09,AldayTachikawa10}.
The $\hbar^2 = -1/8$ specialisation fixed by the $K(1)$ Lusztig
$u_\zeta$ root-of-unity condition
corresponds to the Nekrasov--Shatashvili self-dual point
\cite{NekrasovShatashvili2009} via
\[
 \hbar^2 \;=\; \epsilon_1\,\epsilon_2,
 \qquad
 \epsilon_1 = -\epsilon_2 = \epsilon,
 \qquad
 \epsilon^2 \;=\; 1/8,
 \qquad
 \epsilon \;=\; \tfrac{1}{2\sqrt{2}}.
\]
The self-dual phase $\epsilon_1 = -\epsilon_2$ is the $\Omega$
fixed-point locus on which the instanton moduli carries a genuine
holomorphic symplectic structure. After the same source-recognition
and AGT comparison theorems, the K3 chiral bialgebra
$\mathbf{H}_{\Delta_5}$ is required to appear as the Bethe-subalgebra
of its quantum integrable system, with Humbert walls $H_1, H_4$
matching the wall-crossing loci of the Nekrasov--Shatashvili Bethe
equations.

\paragraph{K3-AGT input.}
Let $\mathcal{T}_{6d}^{(2,0)}[A_1]$ denote the 6d $(2,0)$ theory
of type $A_1$ and $\mathbb{H}^1_{\mathrm{K3}}(\tau,Z)$ the
holomorphic-anomaly partition function of its $\Omega$-deformed
compactification on $\mathbb{R}^4_{\epsilon_1,\epsilon_2}\times
\Sigma_{0,24}$. The K3-AGT comparison problem asks for the equality
\[
 \mathbb{H}^1_{\mathrm{K3}}(\tau,Z)\big|_{\epsilon_1=-\epsilon_2=1/(2\sqrt{2})}
 \;=\;
 \chi_{\mathbf{H}_{\Delta_5}}(\tau,Z)
 \;=\;
 \bigl(\Phi_{10}^{\mathrm{un}}(Z)\bigr)^{-1}
\]
on the $M_{24}$-invariant Coulomb-branch locus, with
$\Phi_{10}^{\mathrm{un}}=\Delta_5^2$ the unnormalised Igusa cusp
form of weight $10$. The required comparison datum is the map between
the $\Omega$-deformed $6d$ compactification trace and the Borcherds
product character of $\mathbf H_{\Delta_5}$; the displayed equality
is a comparison problem, not a theorem, until that trace comparison is
supplied.
Central charge matches by
anomaly inflow: $c_{6d}^{(2,0)}[A_1] = 1$, which upon K3
compactification contributes $\chi(\mathrm{K3})\cdot c_{6d}/24 =
1$ per boundary node, summed with the Beem--Rastelli shift
$-12\cdot c_{4d} = -12 \cdot 107/6 = -214$ to produce the effective
$-214 + \dim(\mathfrak{su}(2)^{24})\cdot 0$ for the chiral sector
via the Chacaltana--Distler pants decomposition
\cite{AldayTachikawa10,CordovaGaiotto2016}. Under this comparison
the unrefined Schur limit recovers the two-step arrow
$\mathcal{I}_{\mathrm{Schur}}\xrightarrow{M_{24}\text{-avg}}
\phi_{0,1}\xrightarrow{\mathrm{Borch}}\Delta_5$ \cite{EguchiOoguriTachikawa2011}.

\paragraph{Maulik--Okounkov origin.}
The Maulik--Okounkov comparison is conditional on the same
Hall--Borcherds source-recognition theorem. The chiral quantum group
undergirding the BKM/Siegel pair is to be compared with the
Maulik--Okounkov quantum group
\[
 Y^{\mathrm{MO}}_\hbar\bigl(\mathrm{Hilb}(\mathrm{K3})\bigr)
\]
at $\hbar^2 = -1/8$ \cite{MaulikOkounkov12,SchiffmannVasserot13}.
The comparison requires the stable-envelope $R$-matrix to match the
Siegel-elliptic dynamical $R_{\mathrm{Sieg,dyn}}$ of the
$\mathbf{H}_{\Delta_5}$ presentation, and the Atiyah--Bott
localisation character on $\mathrm{Hilb}^n(\mathrm{K3})$ to reproduce
the Borcherds product expansion of
$(\Phi_{10}^{\mathrm{un}})^{-1}$ \cite{Borcherds1995,Borcherds1998}.
In this conditional form the BKM denominator identity is the target
localisation of the Maulik--Okounkov character to the rank-$2$ torus
fixed-point locus; the imaginary-simple-root multiplicities are the
$\phi_{0,1}$ Fourier coefficients; and the Humbert-wall monodromy
$\mathrm{ord}(H_1)=8$ is the target order of the Maulik--Okounkov
stable-envelope braid-group representation at $\hbar^2 = -1/8$.

\paragraph{Three-volume junction.}
\[
\renewcommand{\arraystretch}{1.3}
\begin{array}{l|l|l}
 & \text{Lens} & \text{Key content} \\
\hline
\text{Volume~I} & \text{algebraic} &
 \text{bar-cobar, Fuchsian ODE on 4 Laurent tiers;} \\
 & & \text{arithmetic duality locates Kummer-irregular primes} \\
\text{Volume~II} & \text{physical} &
 \text{tempered stratum, Schellekens 71, } V^\natural, \\
 & & \text{irrational cosets, } W_\infty \text{ endpoint} \\
\text{Volume~III} & \text{categorical} &
 \Phi_d = {\SpCh}_{\Sigma_{d-1},C}\circ\PhiFA_d\colon CY_d\text{-Cat} \to E_n\text{-ChirAlg};
 \text{ six-routes pentagon;} \\
 & & \rho^{R_i}\text{-stratification (distinct from } \kappaChHodge\text{);}
 \text{ super-Riccati; } CY_4\ p_1\text{-twist}
\end{array}
\]
The three descriptions agree on the same chain complex. The two
stages of $\Phi_d$ (stage~1 canonical $E_d$-holomorphic
factorisation via Kontsevich--Tamarkin plus
Costello--Gwilliam--Li; stage~2 specialisation
${\SpCh}_{\Sigma_{d-1},C}$ via factorisation-homology pushforward)
are the closed and open colours of $\SCchtop$ at $d = 2, 3$
(Remark~\ref{rem:heptagon-two-stage-CY-to-chiral}).

\paragraph{Theorem H and the CY-to-chiral comparison.}
For the curve chart $\cA=\End_\cC(b)$, a family datum
$H_H(\cA;S_\cA)$ gives
\[
 \ChirHoch^n(\cA)\cong H^n(K_{\cA,S_\cA}),
 \qquad
 \operatorname{Supp}\ChirHoch^\bullet(\cA)\subseteq S_\cA.
\]
For a CY$_d$ category~$\cC$, the two-stage functor
$\Phi_d={\SpCh}_{\Sigma_{d-1},C}\circ\PhiFA_d$ produces the chiral
chart $A_\cC=\Phi_d(\cC)$.  HKR and the Serre pairing compute the
categorical Hochschild object $\HH^\bullet(\cC)$ in the CY ambient.
The bridge between these objects is a cochain map
\[
 q_{\Phi,C}\colon
 \HH^\bullet(\cC)\longrightarrow
 \ChirHoch^\bullet(A_\cC).
\]
The family datum $H_H(A_\cC;S_{A_\cC})$ computes the target
support.  Compatibility with the CY source means that for every
closed categorical cochain $x$ of degree $n\notin S_{A_\cC}$ one
constructs a chart cochain $h_x$ satisfying
$q_{\Phi,C}(x)=dh_x$.  A degree-$d$ Serre pairing on the source
induces a degree-$d$ chiral pairing only after $q_{\Phi,C}$ is shown
to preserve the pairing.  The Borcherds weight
$\kappaBKM(\Delta_5)=5$ is the scalar trace at the terminal stage;
its comparison with any chiral cohomology group requires a named
trace functional.

\paragraph{The CY-$4$ HK-restricted $\Phi_4$ comparison problem.}
Hyperk\"ahler CY-$4$ ($d = 4$ with full $\mathrm{Sp}(2)$
holonomy) admits a conjectural scope-restricted CY-to-chiral
candidate $\Phi_4^{\mathrm{HK}}$ via the twistor $S^1$-fibration
$\mathcal X_{\mathrm{HK}} \to \mathbb P^1$, descent to the 3d
$\mathcal N = 4$ sigma model on the twistor fibre, and lift to
Costello--Gaiotto 11d supergravity on
$\mathbb R^4 \times \mathcal X_{\mathrm{HK}}$
(the CY-$4$ hyper-K\"ahler instance of the \(\Phi_d\) programme once
the twistor descent and 11d lift are constructed). The test case is
$\mathcal X = \mathrm{K3}_1 \times \mathrm{K3}_2$ with its
natural HK structure. The candidate pairing formula is
\[
 \langle \Phi_4^{\mathrm{HK}}(\mathcal X), \Phi_4^{\mathrm{HK}}(\mathcal X) \rangle
 \;=\;
 4 \cdot \mathrm{Vol}(\mathrm{K3}_1) \cdot
 \mathrm{Vol}(\mathrm{K3}_2) \cdot (2\pi i)^4,
\]
with three independent comparison targets: the DT-$4$ MacMahon
moonshine product on $\mathrm{K3} \times \mathrm{K3}$
(geometric); the Nekrasov $8$d instanton partition function at
$\epsilon_1 \epsilon_2 \epsilon_3 \epsilon_4 = 0$ with
self-dual phase on two planes (gauge-theoretic); the
Dijkgraaf--Moore--Verlinde--Verlinde product formula for the
second-quantised K3 elliptic genus at weight $\mathrm{wt} = 4$
(automorphic). The factor $4 = \chi(\mathrm{K3})/6$ at
$g = 2$, the factor $(2\pi i)^4$ from the twistor-$S^1$ periodic
cycle, and the product structure of the volumes reflect the
holomorphic-$2$-form K\"unneth decomposition of
$\Omega^2_{\mathrm{HK}}$. These routes are evidence and verification
targets, not a proof, until \(\Phi_4^{\mathrm{HK}}\) is constructed at
the HK locus; non-HK CY-$4$ lies outside the current scope.


\section*{Families: the standard landscape}

Five families beyond Heisenberg, ordered by complexity.

\paragraph{Free multiplet.}
Generated by free boson $\phi$ with OPE
$\phi(z) \phi(w) \sim -\log(z - w)$, $m_{k \geq 3} = 0$. Bar
strictly coassociative; Swiss-cheese formal (qi to cohomology
as operad algebra); $\R$-factorisation exact. PVA polynomial:
$\{\phi {}_\lambda \phi\} = \lambda$. Spectral $R$-matrix
$R(z) = \exp(\hbar/z)$ universal abelian.

\paragraph{Landau--Ginzburg: first genuine $\Ainf$ correction.}
LG with cubic superpotential $W = \phi^3/3$:
\begin{itemize}
\item $m_1 = Q = \dbar + dW \wedge$, Dolbeault twisted by
 superpotential;
\item $m_2$ with $\{\phi {}_\lambda \phi\} = \lambda + \phi^2$;
\item $m_3 \neq 0$ from cubic vertex of BV action as tree-level
 Feynman integral on $\FM_3(\C) \times \Conf_3^{<}(\R)$;
\item $m_{k \geq \deg W + 1} = 0$: $\Ainf$ structure finite,
 truncation by superpotential degree
 (Proposition~\ref{prop:truncation_criterion}).
\end{itemize}
Transferred operations on $H^\bullet = \C[\phi]/(dW)$ are
tree-level Feynman sums.

\paragraph{Abelian Chern--Simons: braided atom.}
$U(1)$ CS at level $k$ with boundary on $\C \times \{0\}$:
\begin{itemize}
\item Boundary VA $\widehat{\mathfrak{u}(1)}_k$ (Heisenberg at
 level $k$);
\item $\{J {}_\lambda J\} = k\lambda$;
\item Pre-\textup{\(d\log\)} Laplace/OPE coefficient $k/z^2$;
 the collision/bar tensor kernel is
 $r^{\mathrm{coll}}_{\cH}(z)=k\,\Omega_{\cH}/z$, with rank-one
 coefficient $k/z$;
\item Quantum $R(z) = \exp(k\hbar/z)$, YBE trivial (abelian);
\item Lines: $\cC_{\mathrm{line}} \simeq Y(\mathfrak{u}(1))\text{-mod}
 \simeq \widehat{\mathfrak{u}(1)}_{-k}\text{-mod}$, semisimple
 Fock sector.
\end{itemize}

\paragraph{Affine $\widehat{\fg}_k$: non-abelian test.}
$\widehat{\mathfrak{su}(2)}_k$:
\begin{itemize}
\item $\{J^a {}_\lambda J^b\} = f^{ab}_c J^c + k \delta^{ab}\lambda$;
\item $m_3 \neq 0$ from cubic Casimir Jacobi structure;
\item Classical $r$-matrix $r(z) = k\,\Omega_{\mathrm{tr}}/z$ (trace-form
 convention, vanishing at $k = 0$); KZ equivalent
 $r(z) = \Omega/((k+h^\vee)z)$ diverges at $k = -h^\vee$;
\item Quantum $R$-matrix on reduced evaluation surface: agrees
 with $U_q(\fg)$ at $q = e^{i\pi/(k+h^\vee)}$
 (Theorem~\ref{thm:affine-monodromy-identification});
\item Genus-$1$ curvature
 $\kappaChHodge = \dim\fg\,(k+h^\vee)/(2h^\vee)$
 (at $k = 0$, $\kappaChHodge = \dim\fg/2$ from the
 Sugawara shift, not zero);
\item Chiral Koszul dual
 $\cA^!_{\mathrm{ch}} = \mathrm{CE}^{\mathrm{ch}}(\widehat{\fg}_{-k-2h^\vee})$
 (chiral CE at FF dual level); line dual
 $\cA^!_{\mathrm{line}} = \Ydg(\fg)$.
\end{itemize}

\paragraph{Virasoro and $\mathcal{W}_3$: nonlinear tests.}
Virasoro $\{T {}_\lambda T\} = \partial T + 2T\lambda + (c/12)\lambda^3$
(Kac convention $\{a_\lambda b\} = \sum a_{(n)}b\,\lambda^n/n!$;
$T_{(3)}T = c/2$ contributes $(c/2)/3! = c/12$; the shadow
invariant $S_2 = \kappaChHodge = c/2$ is independent of
this divided-power coefficient choice). $\cW_3$
analogous with weight-$3$ generator $W$:
\begin{itemize}
\item OPE poles order $4$ (Vir) and $6$ ($\cW_3$); bar
 coderivations non-vanishing all degrees; $\Ainf$ genuinely
 infinite;
\item Recursive $m_k$ from BV master
 (Theorem~\ref{thm:vir-recursive}): tree-level Feynman sums on
 $\FM_k(\C) \times \Conf_k^{<}(\R)$ with vertices from
 Khan--Zeng 3d HT PSM interaction $\eta \Pi^{ij}(\partial) \eta$;
\item Classical YBE: the Vir $\lambda$-bracket gives the pre-\(d\log\)
 Laplace kernel
 $r^L(z) = (c/2)/z^4 + 2T/z^2 + (\partial T)/z$; the
 bar-theoretic collision residue
 $r^{\mathrm{coll}}(z) = (c/2)/z^3 + 2T/z$ is one pole lower and is
 the object entering the CYBE computation
 (Computation~\ref{comp:vir-CYBE});
\item One-loop Virasoro complementarity constant
 $\alpha_{\mathrm{Vir}} = 26$
 (Theorem~\ref{thm:central-charge-shift}), shift $\Delta c = 13$;
 with Vir duality $\operatorname{Vir}_c^! = \operatorname{Vir}_{26 - c}$:
 self-duality at the Virasoro-specific value $c = 13$, not at
 the matter-ghost critical value $c = 26$;
\item $\cW_N$ Koszul involution
 (Theorem~\ref{thm:wn-koszul-involution}):
 $\cW_N(c)^! = \cW_N(\alpha_N - c)$,
 $\alpha_N = 2(N-1)(2N^2 + 2N + 1)$, $\alpha_2 = 26$. Self-dual
 $c^* = \alpha_N/2$ (Vir: $c^* = 13$; $\cW_3$: $c^* = 50$).
 Critical string $c_{\mathrm{crit}} = \alpha_N$ (matter-ghost
 cancellation; Vir: $c_{\mathrm{crit}} = 26$; $\cW_3$:
 $c_{\mathrm{crit}} = 100$). $c_{\mathrm{crit}} = 2c^*$ for all
 $N$: self-dual and critical-string points always distinct.
\end{itemize}

\paragraph{Three-dimensional Einstein gravity.}
In the Brown--Henneaux boundary chart for $\Lambda < 0$, one oriented
$\mathrm{SL}(2,\R)$ Chern--Simons factor has level
$k = \ell/(4G_N)$ and boundary algebra
$\operatorname{Vir}_{c_{\mathrm{ch}}}$ with
$c_{\mathrm{ch}}=6k=3\ell/(2G_N)$. Laplace kernel
$r^L(z) = (c/2)/z^4 + 2T/z^2 + (\partial T)/z$ reads as graviton
propagator in AdS$_3$. The operations
$m_3, m_4, \ldots$ form the perturbative graviton hierarchy.
Genus expansion of curved bar tracks loop expansion of effective
scalar partition function. The Virasoro algebraic closed-sector vertex
is the chiral Hochschild object
$\Zderch(\operatorname{Vir}_c) = \bulkChirHoch{\operatorname{Vir}_c}$.
The physical three-dimensional gravity reading passes through the
Brown--Henneaux comparison; its scalar gravitational coordinate is the Gel'fand--Fuchs
central-extension line $\C[[c]]$, specialised by Brown--Henneaux to
$c = 3\ell/(2G_N)$. Absence of local bulk gravitons is the vanishing
of propagating cohomology in the Einstein deformation complex, not an
isomorphism from the Hochschild object to its scalar central-charge
coordinate. Principal DS reduction
(Theorem~\ref{thm:ds-hpl-transfer}), realised as homological
perturbation through BRST SDR, transfers
affine dg-shifted Yangian data to Virasoro side. Transferred
$\Ainf$ products genuinely infinite (quartic pole forces $m_3$,
then $m_4, \ldots$); transferred coproduct primitive on the
principal DS ghost-zero generators after the signed ghost-defect
condition is verified
(Theorem~\ref{thm:gravitational-primitivity}). Gravitational
dynamics then resides in the \(r\)-matrix twist and the transferred
products, not in independent generator-level coproduct vertices.

Three further gravitational results emerge from the bar complex.

\smallskip\textbf{(i) Infinite soft-graviton tower}
(Theorem~\ref{thm:gravity-infinite-soft-tower}). Degree-$r$
operation $m_r$ controls soft graviton identity at order
$p = r - 2$:
\[
\begin{array}{c|l}
r & \text{Soft identity} \\
\hline
2 & \text{Weinberg leading} \\
3 & \text{Cachazo--Strominger subleading} \\
r\ge 4 & \text{sub}^{r-2}\text{leading}
\end{array}
\]
The full hierarchy is the $\Ainf$ relations in the gravitational
frame.

\smallskip\textbf{(ii) Wick-rotation symmetry breaking.}
Genus tower measures: $F_g \neq 0$ at every genus iff
$\kappa_{\mathrm{eff}} = (c{-}26)/2 \neq 0$; critical string
$c = 26$ unique locus of $S$-invariance. Under the
line-identification and Cardy saddle hypotheses, the entropy
$S_{\mathrm{Cardy}} = 2\pi\sqrt{ch/6}$
(Conjectures~\ref{conj:gravity-line-identification},
\ref{conj:gravity-cardy}) is the high-temperature shadow via
$S$-transform composed with saddle-point.

\smallskip\textbf{(iii) Genus-1 Eisenstein weighting}
(Theorem~\ref{thm:genus-1-eisenstein}). Curved differential on
elliptic surface:
\[
d_{\barB,\tau} = d_{\barB}^{(0)} + \sum_{k \geq 2}
G_{2k}(\tau)\, d_{\barB}^{(2k)},
\]
$G_{2k}(\tau)$ Eisenstein series, $d_{\barB}^{(2k)}$
weight-$2k$ components of bar coderivation on torus. Genus-$1$
avatar of Vol~I Theorem~D Hodge deformation: $\tau$ enters
through period matrix; $G_{2k}$ are Fourier coefficients of
torus propagator.


\section*{Volume~I in three dimensions: the mapping table}

Each Vol~I theorem acquires a 3d reading through the
Swiss-cheese lens.

\begin{center}
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{p{0.06\textwidth}p{0.25\textwidth}p{0.56\textwidth}}
\textbf{Thm} & \textbf{Volume~I} &
 \textbf{Three-dimensional reading} \\
\hline
\textbf{(A)} & Bar-cobar adjunction
 & Swiss-cheese presentation; Quillen equivalence for
 $\SCchtop$-algebras. The bar-cobar adjunction
 $(\Omega, \mathbf{B})$ on 2-coloured algebras is a Quillen
 equivalence by homotopy-Koszulity.
 (\S\ref{sec:foundations}, \S\ref{sec:bar-cobar}) \\[3pt]
\textbf{(B)} & Bar-cobar inversion
 & Raviolo VA descent: restriction to time-slices
 $\{t = \text{const}\}$ recovers raviolo vertex algebras via
 the bar-cobar counit $\Omegach(\barBch(\cA)) \to \cA$. The
 boundary vertex algebra is the cobar.
 (Appendix~\ref{sec:raviolo}) \\[3pt]
\textbf{(C)} & Complementarity
 & The Koszul triangle inherits the
 $(-1)$-shifted symplectic structure of complementarity:
 the bulk--boundary pairing is non-degenerate.
 $Q_g(\cA) + Q_g(\cA^!) = H^*(\ov{\cM}_g, Z(\cA))$.
 (\S\ref{sec:ht-bulk-boundary-line},
 \S\ref{sec:ambient-complementarity-chain-level}) \\[3pt]
\textbf{(D)} & Modular characteristic
 & Curved Swiss-cheese over $\ov{\cM}_g$:
 $\dfib^2 = \kappaChHodge(\cA) \cdot \omega_g$. The
 genus-$1$ obstruction controls $F_1$.
 (\S\ref{sec:bar-cobar},
 \S\ref{ch:modular-pva-quantization}) \\[3pt]
\textbf{(H)} & Hochschild ring
 & Algebraic closed sector $\simeq$ chiral Hochschild cochains;
 physical bulk only after the scoped HT comparison.
 The brace algebra on $\FM(\C) \times \Conf(\R)$ is the
 deformation algebra of the Swiss-cheese structure.
 (\S\ref{sec:chiral_hochschild_expanded},
 \S\ref{sec:chiral-Hochschild})
\end{tabular}
\end{center}

The Vol~II theorems F (Universal Holography) and G (Infinite
Fingerprint) raise the count to seven; see preface \S II.

The following table refines the dictionary for each of six
standard families:

\begin{center}
\renewcommand{\arraystretch}{1.25}
{\small
\begin{tabular}{p{0.12\textwidth}p{0.11\textwidth}p{0.11\textwidth}p{0.10\textwidth}p{0.11\textwidth}p{0.11\textwidth}p{0.18\textwidth}}
& \textbf{Heis.} & \textbf{Free} &
 \textbf{LG} & \textbf{CS} &
 \textbf{$\widehat{\fg}_k$} &
 \textbf{$\operatorname{Vir}_c$} \\
\hline
$m_{\geq 3}$ & $= 0$ & $= 0$ & finite & $= 0$ &
 $\neq 0$ & $\neq 0$, $\infty$ \\
PVA type & poly & poly & poly & poly &
 Lie & nonlinear \\
$r^L(z)$\,$^*$ & $k/z^2$ & $1/z^2$ & n/a & $k/z^2$ &
 $k\,\Omega_{\mathrm{tr}}/z$ &
 $\frac{c/2}{z^4} + \frac{2T}{z^2} + \frac{\partial T}{z}$ \\
$R(z)$ & $z^k$ & $z$ & trivial & $z^k$ &
 Drinfeld & heuristic model via fusion kernel \\
$\kappaChHodge$ & $k$ & $1$ & $0$ & $k$ &
 $\frac{(k{+}h^\vee)\dim\fg}{2h^\vee}$ & $c/2$ \\
$\cA^!_{\mathrm{ch}}$ & $\mathrm{Sym}^{\mathrm{ch}}(V^*)$ & $\mathrm{Sym}^{\mathrm{ch}}(V^*)$ & $\cA^!_W$ &
 $\mathrm{Sym}^{\mathrm{ch}}(V^*)$ &
 $\widehat{\fg}_{-k-2h^\vee}$ &
 $\operatorname{Vir}_{26 - c}$ \\
$\cA^!_{\mathrm{line}}$ & $Y(\mathfrak{u}(1))$ & n/a & n/a &
 $Y(\mathfrak{u}(1))$ &
 $\Ydg(\fg)$ &
 heuristic model via $\operatorname{Vir}_{26 - c}$ \\
Lines & Fock & $\Vect$ & fin.\ dim. &
 Fock &
 $\Ydg(\fg)\text{-mod}$ &
 heuristic model via $\operatorname{Vir}_{26 - c}\text{-mod}$
\end{tabular}
}
\end{center}
\noindent{\footnotesize ${}^*$Laplace kernel
$r^L(z) = \int_0^\infty e^{-\lambda z}\{{\cdot}{}_\lambda{\cdot}\}\,d\lambda$
(OPE generating function). Bar-theoretic collision residue
$r^{\mathrm{coll}}(z)$ has pole orders one lower: for Virasoro,
$r^{\mathrm{coll}}(z) = (c/2)/z^3 + 2T/z$.}\smallskip

\smallskip\noindent
The table distinguishes two Koszul duals:
\[
\renewcommand{\arraystretch}{1.3}
\begin{array}{l|l|l}
 & \cA^!_{\mathrm{ch}}\ (\text{chiral, F--G sense}) &
 \cA^!_{\mathrm{line}}\ (\text{line-side, } E_1) \\
\hline
\text{Source} &
 \barBch(\cA)\ (\text{chiral symmetric}) &
 \barB^{\mathrm{ord}}(\cA)\ (\text{ordered}) \\
\text{Duality} &
 D_{\mathrm{Ran}}(\barBch(\cA)) \simeq \barBch(\cA^!) &
 \text{Theorem~\ref{thm:Koszul_dual_Yangian}} \\
 & (\text{Volume~I, Thm~A}) & \\
\widehat{\fg}_k &
 \mathrm{CE}^{\mathrm{ch}}(\widehat{\fg}_{-k-2h^\vee}) &
 \Ydg(\fg) \\
\cH_k &
 \mathrm{Sym}^{\mathrm{ch}}(V^*)\ (\ne \cH_{-k}) &
 Y(\mathfrak{u}(1))\ (\kappaChHodge = -k) \\
\operatorname{Vir}_c &
 \operatorname{Vir}_{26-c} &
 \text{heuristic Vir-module model}
\end{array}
\]
For affine Kac--Moody, $\cA^!_{\mathrm{ch}}$ is the chiral CE
algebra at the Feigin--Frenkel dual level, not
$\widehat{\fg}_{-k-2h^\vee}$ itself. For the Heisenberg, the line side gives modules forming
the semisimple Fock sector. On the chirally Koszul locus, the
line category is modeled by $\cA^!_{\mathrm{line}}$-modules
(Theorem~\ref{thm:lines_as_modules}). The Virasoro line-side
entry is heuristic.

\section*{Strict models and homotopy-coherent invariants}

Every strict dg structure (dg Lie algebras, dg categories, bar
and cobar complexes) is one chart on a larger
homotopy-coherent object. The strict model is for computation;
the homotopy-coherent package is the invariant. Five
manifestations:
\begin{enumerate}[label=\textup{(\roman*)}]
\item $\SCchtop$-algebra structure is $C_\ast(W(\SCchtop))$-algebra,
 not strict operad algebra. Rectification
 (Theorem~\ref{thm:rectification}) identifies when strict and
 homotopy-coherent presentations coincide (requires
 \emph{tameness} (truncation $m_k=0$ for $k\gg0$) or completion
 of cofree coalgebra; Definition~\ref{def:tameness}).
\item Chiral Hochschild cochains form a brace algebra (operations
 $M\{a_1,\ldots,a_n\}$ whose antisymmetrisation gives the
 Gerstenhaber bracket
 $[a,b] = a\{b\} - (-1)^{|a||b|} b\{a\}$;
 Section~\ref{sec:chiral-Hochschild}), Gerstenhaber bracket =
 binary Taylor coefficient of filtered $\Linf$-structure
 governing deformations.
\item BBL triangle invariant only up to derived/Morita and
 $\Linf$ equivalence; false if any vertex artificially
 strictified.
\item Spectral $R$-matrix is component of homotopy-coherent
 braiding on $\Ainf$-module category; YBE one coherence among
 many.
\item Genus tower of curved Swiss-cheese over $\ov{\cM}_g$ is
 genus-filtered $\Linf$-object; strict dg model at each genus is
 a truncation whose higher coherences source the modular
 corrections from Vol~I Theorem~D.
\end{enumerate}

\noindent
Convention~\ref{conv:vol2-strict-models}: every strict dg
structure is one chart on an underlying homotopy deformation
object.

\paragraph{Boundary of control.}
Vol~I's algebraic engine controls everything inside the modular
Koszul locus. The boundary is four independent tests:
\[
\renewcommand{\arraystretch}{1.3}
\begin{array}{l|l|l}
\text{Test} & \text{Barrier} & \text{Available result} \\
\hline
\text{(i) DK-3 categorical} &
 \text{full quantum group} &
 r\text{-matrix only} \\
\text{(ii) Lattice arithmetic} &
 \text{automorphy} &
 \text{symmetric-power moments only} \\
\text{(iii) Chirally Koszul geometric} &
 \text{Koszulness detection} &
 \text{works inside Koszul world only} \\
\text{(iv) Shadow-radius analytic} &
 \text{summation when } \rho \ge 1 &
 \text{finite-degree coefficients only}
\end{array}
\]
This volume operates inside the same four-test boundary; the
Swiss-cheese, PVA, and Yangian structures are constructed for
all logarithmic $\SCchtop$-algebras satisfying chiral
Koszulness; the frontier material of Part~\ref{part:frontier}
probes the boundary.

\paragraph{Five exact proof obligations.}
(F1) Logarithmic $W(p)$ triplet tempering requires a direct
regular Adamovi\'c--Milas character-amplitude bound and a logarithmic
boundary estimate, since the Zhu-bounded-Massey route fails per
Gurarie 1993 and Flohr 1996.
(F2) Periodic CDG for non-simply-laced admissible levels
($\mathfrak{sl}_2$ closed via Kubota class; the obstruction for
$\mathfrak{sl}_{N \ge 3}$ is the Brylinski--Deligne reciprocity
class in the twisted Satake comparison).
(F3) Original-complex chain-level $E_3$-topological for
class~$\mathbf{M}$ (weight-completed proved; chain-level on
the original complex is the finite-propagation obstruction).
(F4) Super-complementarity canonical pairing
($\kappaChHodge + {\kappaChHodge}^! = \max(m,n)$ for
super-Yangians on sub-Sugawara line; supertrace versus Berezinian
pairings coexist).
(F5) Irregular-singular KZB regularity at $\overline{\cM}_{g,n}$
boundaries (Stokes-sector composition follows from
Theorem~\ref{thm:irregular-singular-kzb-regularity} once the
higher-genus boundary formal type and clutching compatibility are
constructed).

\section*{Plan of the volume}

The volume has eight parts. Part~VIII records the closure mechanisms
whose hypotheses are stated in the chapters where they are used.

\paragraph{Part~\ref{part:swiss-cheese}: The Open Primitive}
(\S\ref{sec:foundations}--\S\ref{sec:Ainfty-to-PVA}).
Structural heart. $\SCchtop$ defined
(Section~\ref{sec:HT-operad}), bar complex shown to be $E_1$
chiral coassociative coalgebra whose two-coloured
configuration-space parametrisation recovers $\SCchtop$
operation spaces (Section~\ref{sec:foundations}). $\Ainf$
chiral axioms extracted (Section~\ref{sec:axioms-Ainfty-chiral})
and equivalent to $W(\SCchtop)$-algebra structure
(Section~\ref{sec:equivalence}). FM calculus
(Section~\ref{sec:FM_calculus}) supplies Stokes-on-strata proofs
($\Ainf$ relations + explicit $m_2, m_3$ formulas;
Propositions~\ref{prop:m2_formula},
\ref{prop:m3_formula}). BV-BRST construction
(Section~\ref{sec:BV_construction}): BV quantisation of 3d HT
field theory on $\C \times \R$ produces these bar complexes
(Theorem~\ref{thm:Obs-is-SC}). Cohomological descent yields the
$(-1)$-shifted PVA (Theorem~\ref{thm:cohomology_PVA}), with
regular/singular decomposition of $m_2$
(Section~\ref{sec:PVA_descent},
Section~\ref{sec:Ainfty-to-PVA}).

\paragraph{Part~\ref{part:e1-core}: The $E_1$ Core}
(\S\ref{sec:bar_cobar}--\S\ref{chap:dg-shifted-factorization}).
Ordered bar coalgebra $B^{\mathrm{ord}}(\cA) = T^c(s^{-1}\bar\cA)$
is the native object of the Swiss-cheese open colour. Chiral
bar-cobar review (Section~\ref{sec:bar_cobar}) places GLZ
quadratic duality in context. Line operators carry modules for
$\cA^!_{\mathrm{line}}$. Ordered associative side carries its
own bar-cobar theory, diagonal bicomodule, open trace.
Homotopy-Koszulity proved at the associated-graded level
(Theorem~\ref{thm:homotopy-Koszul}; coupled case
Conjecture~\ref{conj:coupled-sc-koszul}); Quillen equivalence for
$\gr$-algebras
(Theorem~\ref{thm:scchtop-bar-cobar-adjunction}). On chirally Koszul
locus, line side modeled by $\cA^!_{\mathrm{line}}$-modules
(Theorem~\ref{thm:lines_as_modules}); spectral $R$-matrix
constructed (Definition~\ref{def:spectral-braiding}) and YBE
proved (Theorem~\ref{thm:YBE}); open-colour Koszul dual carries
dg-shifted Yangian package
(Theorem~\ref{thm:yangian-recognition}).
Spectral Drinfeld class controlling strictification vanishes at
every filtration for every simple Lie algebra
(Theorem~\ref{thm:complete-strictification}), via
root-multiplicity-one rigidity and Dynkin projection. Loop-order
spectral sequence on bar gives width bound for bar cohomology
(Theorem~\ref{thm:width-bound}); one-loop exactness implies
chiral Koszulness before DS (Theorem~\ref{thm:one-loop-koszul});
principal DS turns finite Lie transfer into the Virasoro wheel tower for
Vir and $W_N$ via homological transfer
(Theorem~\ref{thm:ds-koszul-obstruction}). The unified chiral
quantum group $Q_g^{k,f,\mu}$ (Yangian + W + RTT + coideal as a
single $E_1$-core synthesis) and super chiral Yangian extend the
theory.

\paragraph{Part~\ref{part:topologisation-ladder}: The
Topologisation Ladder}
(\S\ref{sec:e-infinity-topologization}--\S\ref{ch:fractional-ghost-chain-level}).
The chart functor of Part~\ref{part:e1-core} carries an iterated
Sugawara dynamics. A conformal vector $\omega \in A_b$ at
non-critical level identifies $A_b$ as $E_1$-chiral promoted to
$E_3^{\mathrm{top}}$ on $Q$-cohomology via
$T = [Q_{\mathrm{tot}}, G]$. Each inner higher-spin Casimir
$T^{(n)} = [Q_{\mathrm{tot}}, G^{(n)}]$ supplies the next
topological $E_1$ direction on $Q$-cohomology; Dunn additivity
then combines the constructed directions, building the ladder
$E_1 \to E_3^{\mathrm{top}} \to \cdots \to E_{N+1}^{\mathrm{top}}$
at $A_b = \cW_N$, with inverse-limit endpoint
$E_\infty^{\mathrm{top}}$ at $\cW_\infty[\lambda]$ conditional on
$\hypProchazka, \hypCKL, \hypPRSh, \hypYamada$
(Theorem~\ref{thm:e-infinity-topologization-ladder},
Theorem~\ref{thm:climax-w-infty-endpoint}). Curved-Dunn
$H^2 = 0$ at $g \ge 2$ kills the algebraic degree-$2$ twisting
obstruction under the modular-bootstrap and comparison hypotheses
(Theorem~\ref{thm:curved-dunn-H2-vanishing-all-genera}); boundary
composition still uses the formal-type input. The original-complex
dichotomy on class~$\mathbf{M}$ separates the
tempered locus from non-tempered exceptions; the strict
Mittag--Leffler weight-completed ambient $\hypAmbientWtCpl$
supplies finiteness of the $\cW_N$ tempering closure
$\beta_N = 12 \sum_{s = 2}^N 1/s$
(Conjecture~\ref{conj:beta-N-harmonic-closed-form}).

\paragraph{Part~\ref{part:char-datum}: The Characteristic Datum}
(\S\ref{sec:rosetta-stone}--\S\ref{ch:ht-boundary}).
Collision residue
$r(z) = \mathrm{Res}^{\mathrm{coll}}_{0,2}(\Theta_\cA)$ in seven
guises plus two motivic spokes: DK associator, spectral
$R$-matrix satisfying YBE, Sklyanin classical $r$-matrix,
Koszul-dual line-side modelling algebra, celestial OPE,
dg-shifted Yangian, holographic boundary-to-bulk map; F8 Brown
motivic, F9 Willwacher operadic. The set
$\operatorname{Face}(\cA)$ is a torsor over $\mathrm{GRT}_1(\Q)$.
The five-row $\kappa$-tuple
$\kappaTuple{\cA} =
(\kappaCat, \kappaChHodge, \kappaChHeis, \kappaBKM, \kappaFiber)$
is the genuine modular characteristic; the K3$\times E$
counterexample
(Theorem~\ref{thm:kappa-tuple-primitivity-orthogonal-shimura})
witnesses primitivity. Theorem~C ceiling
$\rho_K(\cA) = \kappaChHodge(\cA) + \kappaChHodge(\cA^!) \in
\{0, 13, 250/3\}$ is a family-wise scalar ceiling: the
Gaussian, affine, and contact Verdier lanes and the Mukai--K3
\(\mathsf B\)-row Hodge sum have value \(0\), the Virasoro and
\(W_3\) lanes have \(13\) and \(250/3\), and the
Bershadsky--Polyakov slot is open. The \(\mathsf B\)-row Mukai
conductor \(K^{\kappaChHodge} = 2c_+(\mathrm{II}_{4,20}) = 8\)
is a distinct sixth invariant, not a \(\rho_K\) value.
Each algebra family is a test case.
Heisenberg Rosetta Stone
(Section~\ref{sec:rosetta-stone}) translates every new object
(Swiss-cheese operations, brace, PVA, $R$-matrix, lines, curved
genus-$1$, Hochschild duality) into simplest concrete language
verifying each Vol~I theorem
(Theorem~\ref{thm:rosetta-e1-coalgebra},
Proposition~\ref{prop:rosetta-pva},
Theorem~\ref{thm:rosetta-curved},
Proposition~\ref{prop:rosetta-bar-hochschild},
Theorem~\ref{thm:rosetta-3d-mc}). Free multiplet:
$m_{k \geq 3} = 0$, formal
(Proposition~\ref{prop:free_cohomology_complete}). LG:
nontrivial $m_3$, truncation by degree
(Theorem~\ref{thm:LG_truncation},
Proposition~\ref{prop:LG_m3_formula}). Abelian CS with boundary:
explicit spectral $R$-matrix YBE
(Theorem~\ref{thm:CS_boundary}). $\widehat{\mathfrak{su}(2)}_k$:
non-abelian machine: $\lambda$-bracket, $m_3$ from Jacobi,
truncation, PVA, Yangian, genus-$1$ curvature
$\kappaChHodge = \dim\fg\,(k+h^\vee)/(2h^\vee)$
(Propositions~\ref{prop:su2-lambda-bracket}--\ref{prop:su2-curvature}).
$\mathcal{W}$-algebra chapter
(Section~\ref{sec:W-algebras}): Vir and $\cW_3$ as nonlinear
tests: recursive $m_k$ from BV master
(Theorem~\ref{thm:vir-recursive},
Theorem~\ref{thm:w3-recursive}); PVA Jacobi kernels from
$\lambda$-bracket Jacobi
(Computation~\ref{comp:vir-CYBE} for Vir,
Theorem~\ref{thm:w3-CYBE} for $\cW_3$); one-loop central-charge
shift (Theorem~\ref{thm:central-charge-shift}). Chiral Hochschild
cohomology (Section~\ref{sec:chiral_hochschild_expanded})
computes the algebraic closed-sector Hochschild complex; the physical
bulk identification is the scoped HT prefactorisation/exact-sector
comparison, bridging 2d PSM to 3d HT via dimensional descent
(Theorem~\ref{thm:2d_PSM_Hochschild}); chiral Hochschild
cochains form brace algebra on $\FM(\C) \times \Conf(\R)$
(Section~\ref{sec:chiral-Hochschild}). Modular PVA quantisation
(Section~\ref{ch:modular-pva-quantization}): stable-graph modular
bar (Theorem~\ref{thm:modular-bar}); universal genus-$1$
obstruction (Theorem~\ref{thm:Ob1}); higher-genus recursion
(Theorem~\ref{thm:Obg}); variational modular class
(Theorem~\ref{thm:modclass}). Curved-Dunn $H^2$ vanishing at all
$g$, under the modular-bootstrap acyclicity and degree-$2$
comparison inputs
(Theorem~\ref{thm:curved-dunn-H2-vanishing-all-genera});
$\beta_N = 12(H_N - 1)$ harmonic law
(Conjecture~\ref{conj:beta-N-harmonic-closed-form}); tempered
stratum (three chapters). Physical origins chapter connects to
4d/2d correspondences, gauge theories, D-brane algebras.

\paragraph{Part~\ref{part:holography}: The Standard HT Landscape}
(Chapters~\ref{ch:ym-boundary-theory}--\ref{ch:ym-instanton-screening}
and Appendix~\ref{app:anomaly-completed-topological-holography}).
Yang--Mills boundary theory with instanton completion and
screening (Chapters~\ref{ch:ym-boundary-theory}--\ref{ch:ym-instanton-screening});
celestial and twisted holography made modular
(Section~\ref{sec:modular-twisted-celestial-first-principles});
logarithmic HT monodromy from shifted KZ connections and $\Ainf$
collision theory (Section~\ref{chap:log-ht-monodromy},
Theorem~\ref{thm:synthesis}, Theorem~\ref{thm:master-curvature};
five structural levels in
Convention~\ref{conv:five-levels-log-ht});
anomaly-completed topological holography
(Section~\ref{app:anomaly-completed-topological-holography}).
For affine lineage on the reduced evaluation comparison surface
the monodromy of the reduced HT logarithmic connection is
compared with the corresponding quantum-group braid
representation, and the spectral $R$-matrix agrees with the
quantum group $R$-matrix
\textup{(}Theorem~\textup{\ref{thm:affine-monodromy-identification}}\textup{)};
one-loop exactness removes the independent wheel/contact tower; reduced
connection identifies with KZ
(Theorem~\ref{thm:reduced-equals-kz});
$\cC_{\mathrm{line}}^{\mathrm{red}}|_{\mathrm{eval}} \simeq
\mathrm{Rep}_q(\fg)$ at $q = e^{i\pi/(k+h^\vee)}$
(Corollary~\ref{cor:affine-line-category}); bar complex computes
coloured Jones polynomial
(Corollary~\ref{cor:jones-polynomial}). Holographic
reconstruction and modular bootstrap supply the boundary-linear
comparison used by the theorem.

\paragraph{Part~\ref{part:gravity}: Three-Dimensional Quantum
Gravity (CLIMAX).}
The Virasoro $\lambda$-bracket
$\{T_\lambda T\} = \partial T + 2T\lambda + (c/12)\lambda^3$
generates the full gravitational theory. The quartic pole forces
the infinite $\Ainf$ tower, the Koszul involution
$c \mapsto 26 - c$, the curvature $\kappaChHodge = c/2$, and
the genus expansion. 3d gravity provides the first test of the
full obstruction tower. Critical string dichotomy at $c = 26$ is
ghost-anomaly cancellation; it does not truncate the Virasoro
$\Ainf$ tower forced by the quartic pole. Perturbative scalar
finiteness belongs to the protected scalar lane and its
Faber--Pandharipande decay, not to a disappearance of the tower.
Soft graviton theorems, symplectic polarisation, and the
gravitational Yangian complete the gravitational algebra.
The part also contains chiral higher Deligne
(Chapter~\ref{ch:chiral-higher-deligne}), Universal Holography
functor and master theorem (Chapter~\ref{ch:programme-climax-platonic}),
celestial holography, the Monster finite-orbifold anomaly criterion
(Theorem~\ref{thm:monster-chain-level-e3-top}), the Schellekens
71 finite-orbifold classification criterion, the iterated-Sugawara
$E_\infty$ tower (Theorem~\ref{thm:climax-w-infty-endpoint},
Theorem~\ref{thm:e-infinity-topologization-ladder}).

\paragraph{Part~\ref{part:frontier}: The Frontier.}
The four irreducible fronts and the four operator-level
Construction Problems CP1--CP4
(\cref{rem:four-construction-problems},
\cref{thm:construction-problem-stage-stratification}). The
fronts are: curved-Dunn $H^2$ vanishing at $g \ge 2$
\textup(\cref{thm:curved-dunn-H2-vanishing-all-genera}\textup);
class-$\mathbf{M}$ DS--Hochschild compatibility
\textup(\cref{thm:chd-ds-hochschild}\textup); periodic-CDG at
admissible level
\textup(Vol~I~\cref{V1-thm:periodic-cdg-is-koszul-compatible}\textup);
chain-level chiral Deligne--Tamarkin after associator fix
\textup(\cref{thm:chd-deligne-tamarkin}\textup). The Construction
Problems are operator-level lifts: $\mathfrak D_X\in
\operatorname{End}_{\mathrm{ChirHoch}}(H_X)$ on $K3 \times E$ with
$\iota_{\mathrm{aut}}(\protectedPfaff{\mathfrak D_X})=\Deltafive$
\textup(CP1\textup);
gravity-line operator algebra with Pentagon-face scalar trace
$\Phitenun = \Deltafive^2$ \textup(CP2\textup); unified
PVA-quantum HT theory \textup(CP3\textup); chiral Positselski
extending Vol~I Theorem~B at chiral generality
\textup(CP4,
\cref{thm:chiral-positselski-cross-volume}\textup). The
classical half-space BV datum is constructed for finite-type
freely-generated finite-jet KZ inputs; the all-loop deformation
quantisation requires the KZ analytic SDR package
\textup(\cref{thm:general-half-space-bv}\textup). Closing section
\emph{Closures and re-openings} \textup(folded from former
Part~VIII\textup) records the Koszulness moduli
$\mathcal{M}_{\mathrm{Kosz}}$ \textup(fourteen-chart atlas with
$\mathrm{GRT}_1(\Q)$-torsor action\textup) and the Infinite
Fingerprint Classification
\textup(\cref{chap:infinite-fingerprint},
\cref{thm:fingerprint-complete-ch}, Vol~II~Theorem~G\textup), where
the six slots are reconstructive only after the appropriate
branchwise datum \(\mathfrak R_c\) is supplied.
No earlier part depends on this part.

\paragraph{Appendices.}
Raviolo vertex algebras (Appendix~\ref{sec:raviolo}) supply
comparison with Garner--Williams: restriction to time-slices
recovers raviolo VAs via bar-cobar counit
(Proposition~\ref{prop:raviolo-VA},
Theorem~\ref{thm:raviolo-PVA}). Expanded raviolo restriction
(Appendix~\ref{sec:raviolo-expanded}) develops the
\v{C}ech/Thom--Sullivan model and coinvariants. Further
appendices: brace-algebra sign conventions, FM orientation
conventions, expanded PVA proofs.

\section*{Reading this volume}

\noindent\textbf{Three entry points.}
\begin{enumerate}[label=\textup{(\alph*)}]
\item \emph{Via the master theorem.}
 Universal holography (Chapter~\ref{ch:programme-climax-platonic}) $\to$
 Foundations (\S\ref{sec:foundations}) $\to$
 Heisenberg as Rung 1 (\S\ref{sec:rosetta-stone}) $\to$
 3d quantum gravity (Part~\ref{part:gravity}).
\item \emph{Via the Heisenberg.}
 Rosetta Stone (\S\ref{sec:rosetta-stone}) $\to$
 Foundations (\S\ref{sec:foundations}) $\to$
 PVA descent (\S\ref{sec:PVA_descent}) $\to$
 worked examples (\S\ref{sec:examples_complete}).
\item \emph{Complete theory.}
 Part~\ref{part:swiss-cheese} linearly, then
 Part~\ref{part:e1-core} ($E_1$ core), then
 Parts~\ref{part:bbl-core}--\ref{part:examples} (faces of
 $r(z)$, examples, modularity), then Parts~\ref{part:holography}
 and \ref{part:gravity} (HT landscape, gravity climax).
\item \emph{Physics entry.}
 BV construction (\S\ref{sec:BV_construction}) $\to$
 FM calculus (\S\ref{sec:FM_calculus}) $\to$
 Rosetta Stone (\S\ref{sec:rosetta-stone}) $\to$
 YM synthesis (Chapters~\ref{ch:ym-boundary-theory}--\ref{ch:ym-instanton-screening}).
\end{enumerate}

\smallskip
\noindent\textbf{Part summary.}
\[
\renewcommand{\arraystretch}{1.3}
\begin{array}{l|l}
\text{Part} & \text{Content} \\
\hline
\ref{part:swiss-cheese} &
 \text{Swiss-cheese operad identification; classical PVA shadow} \\
\ref{part:e1-core} &
 E_1\text{ core: ordered bar, line operators, dg-shifted Yangian} \\
\ref{part:bbl-core} &
 \text{Faces of } r(z)\text{ as } \mathrm{GRT}_1(\Q)\text{-torsor:} \\
 & \text{bulk--boundary--line, celestial, half-space BV, planted forest} \\
\ref{part:examples} &
 \text{Five families; chiral Hochschild; modular operad; curved Dunn} \\
\ref{part:holography} &
 \text{HT landscape: YM, celestial, log monodromy, anomaly} \\
\ref{part:gravity} &
 \text{3d quantum gravity (CLIMAX): Universal Holography, } W_\infty \\
\ref{part:frontier} &
 \text{Extensions and proof obligations (independent)} \\
\ref{part:frontier-to-theorem} &
 \text{From Frontier to Theorem: } \mathcal{M}_{\mathrm{Kosz}}\text{, Infinite Fingerprint}
\end{array}
\]

\section*{The K3 base point}

The K3 base point uses the class-$\mathcal S$ \(A_1\) Schur-sector
candidate on \(\Sigma_{0,24}\) with canonical central charges
$(c_{4d}, c_{2d}) = (107/6, -214)$, Coulomb rank $21$, flavour
$\mathfrak{su}(2)^{24}$, via
$c_{4d}(A_1, \Sigma_{0,n}, \text{all max}) = (5n-13)/6$
(Chacaltana--Distler 2010 \S5 eq.~(5.14);
Shapere--Tachikawa 2008 eq.~(2.20), Table~4;
Tachikawa 2013 eq.~(13.6); BLLPRvR 2013 eq.~(3.14)),
cross-verified at $n = 3$ ($c_{4d} = 1/3$) and $n = 4$
($c_{4d} = 7/6$). The \(\SCchtop\) landing statement is conditional:
after Hall--Borcherds source recognition and the Schur comparison
theorem, the chiral algebra of the Schur sector has target the
$\mathbf H_{\Delta_5}$-module category; the $3$D HT QFT framing becomes
the bi-based Ran topological field theory on
$(\mathrm{Ran}(E^{\mathrm{nod,sm}}_{24}),\,\bar{\mathcal A}_2)$
\cite{Gaiotto2009,BeemRastelli2015}.
Under these hypotheses, the five theorems A--D, H specialise to this
base:
(A) bar--cobar for the recognized source $\mathbf H_{\Delta_5}$;
(B) chiral Positselski at $\hbar^2 = -1/8$;
(C) derived-centre with Mukai conductor $K^{\kappaChHodge} = 8$
 on the Mukai-enhanced K3 Heisenberg witness (a distinct sixth
 invariant beside the complementarity spectrum
 $\{0, 13, 250/3\}$ with open BP slot; the $\mathsf{B}$-row
 Hodge sum itself is $0$; via Bruinier--Heegner reciprocity);
(D) obstruction tower with $\widetilde M_{24}$ shift $m_\sigma$;
(H) chiral Hochschild support computed by
$H_H(\mathbf H_{\Delta_5};S_{\Delta_5})$, with the
chiral-specialisation comparison $q_{\Phi,C}$ carrying categorical
K3 classes to the curve chart.
The universal trace identity
$\hbar^2 K^{\kappaChHodge} = -1$ (Bruinier--Heegner
$K^{\kappaChHodge} = 2c_+(L)$)
specialises at the K3 row to
$\hbar^2 = -1/8$, $K^{\kappaChHodge} = 8$, with
$\kappaChHodge = 3$
the separate post-reduction Cartan-rank invariant
\cite{Mukai1987,Lusztig1990,ChengDuncanHarvey2014}.
Volume~III Chapter~\ref{V3-ch:k3-chiral-bialgebra-platonic}
constructs the corresponding CY-to-chiral image.
