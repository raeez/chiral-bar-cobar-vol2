\providecommand{\cL}{\mathscr{L}}
\providecommand{\Steinb}{\mathfrak{S}}
\providecommand{\cZ}{\mathcal{Z}}
\providecommand{\cH}{\mathcal{H}}
\providecommand{\fg}{\mathfrak{g}}
\providecommand{\cW}{\mathcal{W}}
\providecommand{\gSC}{\mathfrak{g}^{\mathrm{SC}}}
\providecommand{\gAmod}{\mathfrak{g}^{\mathrm{mod}}_\cA}
\providecommand{\Ydg}{Y^{\mathrm{dg}}}
\providecommand{\Zder}{\operatorname{Z}_{\mathrm{der}}}
\providecommand{\Abulk}{A_{\mathrm{bulk}}}
\providecommand{\Bbound}{B_{\partial}}
\providecommand{\mc}{\operatorname{MC}}
\providecommand{\Res}{\operatorname{Res}}
\providecommand{\RHom}{\operatorname{RHom}}

\chapter*{Preface}
\addcontentsline{toc}{chapter}{Preface}
\markboth{Preface}{Preface}

\vspace{1em}

\section*{I.\quad Two directions}

\noindent
A chiral algebra is a point in the derived symplectic category: a Lagrangian
$\cL_\cA \hookrightarrow \cM_{\mathrm{vac}}(\cA)$.  The bar complex is the
endomorphism algebra of this point.  The Swiss-cheese operad is the local
composition law.  Koszul duality is the symplectic complement.  The genus tower
is the deformation of the point over the moduli of curves.  The holographic
principle is the theorem that a point determines its ambient.  The entire content
of both volumes is the local geometry of the derived symplectic category at this
single point.

The Heisenberg algebra is the simplest instance: every entry can be computed by hand.  We begin there, then build the general machine.

\medskip
\noindent
Two operators $a(z_1, t_1)$ and $b(z_2, t_2)$ on $\C \times \R$
interact in two independent directions.

In the \emph{holomorphic direction} ($z_1 \to z_2$), collision produces the OPE residue~$a_{(n)}b$.  Volume~I built the entire modular Koszul theory from this mechanism: the bar differential
\[
d_{\barB}[s^{-1}a \,|\, s^{-1}b]
\;=\;
\Res_{z_1 = z_2}\!\bigl[
  a(z_1)\,b(z_2)\cdot\eta_{12}
\bigr],
\qquad
\eta_{12} = d\log(z_1 - z_2),
\]
the Arnold relation ($\eta_{12} \wedge \eta_{23} + \eta_{23} \wedge \eta_{31} + \eta_{31} \wedge \eta_{12} = 0$) forcing $D_\cA^2 = 0$ at genus~$0$, the curvature $d_{\mathrm{fib}}^2 = \kappa(\cA) \cdot \omega_g$ at higher genus, and the universal Maurer--Cartan element $\Theta_\cA = D_\cA - d_0$ whose projections are the five theorems.

In the \emph{topological direction}, the operators separate: cut $\R$ at a point $t_*$ between $t_1$ and $t_2$.  The observables on each half-line depend only on their own operators.  The \emph{deconcatenation coproduct} records this separation:
\begin{equation}\label{eq:preface2-delta}
\Delta[s^{-1}a_1|\cdots|s^{-1}a_n]
\;=\;
\sum_{i=0}^{n}
[s^{-1}a_1|\cdots|s^{-1}a_i]
\;\otimes\;
[s^{-1}a_{i+1}|\cdots|s^{-1}a_n].
\end{equation}
The two directions are independent:
\begin{equation}\label{eq:preface2-coderivation}
\Delta \circ D_\cA
\;=\;
(D_\cA \otimes \id \;+\; \id \otimes D_\cA) \circ \Delta.
\end{equation}
The differential is a coderivation of the coproduct.  Collisions in $\C$ commute with cuts in $\R$.

\medskip
\noindent\textbf{The Steinberg principle.}\enspace
The bar complex presents the Swiss-cheese algebra, as the Steinberg variety presents the Hecke algebra.  The Steinberg variety $\widetilde{\cN} \times_{\fg} \widetilde{\cN}$ is one geometric object; its Borel--Moore homology, its $K$-theory, and its derived category extract three different algebraic invariants: the Hecke algebra, the Springer correspondence, and the Kazhdan--Lusztig basis.  The bar complex $\barB(\cA)$ on $\FM_k(\C) \times \Conf_k^{<}(\R)$ is one coalgebra; three functors extract three objects: cobar $\Omega(\barBch(\cA)) \simeq \cA$ recovers the original algebra (bar-cobar inversion), Verdier duality $\mathbb{D}_{\mathrm{Ran}}(\barBch(\cA)) \simeq \barBch(\cA^!)$ produces the line-side Koszul dual, and chiral Hochschild cochains $C^\bullet_{\mathrm{ch}}(\cA,\cA)$ compute the bulk observables.  Every chapter of this volume is a different projection of this single object.  We elaborate the parallel precisely in \S IX below; the reader may take the analogy as the organising principle for everything that follows.

\medskip

The independence is geometric.  The bar complex is a formal Koszul model of the Lagrangian self-intersection $\cL \times_{\mathscr{M}}^h \cL$ in a $(-2)$-shifted symplectic stack~$\mathscr{M}$ (Theorem~\ref{thm:bar-is-self-intersection}).  The holomorphic direction is the Koszul differential of the Lagrangian embedding; the topological direction is the groupoid diagonal.  The coderivation property says the diagonal is a groupoid morphism.  Both structures descend from the single fact that the bar complex computes a derived fiber product.  The chiral algebra fixes a formal local bulk model: HKR for Lagrangian embeddings recovers $T^*[-1]\cL_\cA$ from boundary data; the actual ambient stack requires the Darboux $1$-form (Theorem~\ref{thm:holographic-reconstruction}).

The open-colour dual $\cA^!_{\mathrm{line}}$ inherits an algebra structure from the topological direction (deconcatenation dualises to concatenation), and on the chirally Koszul locus this algebra is a dg-shifted Yangian: an $\Ainf$ algebra with spectral $R$-matrix, twisted coproduct, and Yang--Baxter equation (the last being $d^2 = 0$ on the degree-$3$ ordered bar complex).  Strictification of the quasi-factorisation package to a spectral factorisation quantum group is controlled by a single coefficient: $1/n = \int_0^1(1-t)^{n-1}\,dt$ at filtration~$n$, the coefficient of the right-normed Lie monomial in the Dynkin projection $D_n(X_1\cdots X_n) = \tfrac{1}{n}\,[X_1,[X_2,\ldots,X_n]\cdots]$.  Root multiplicity one for simple Lie algebras forces every spectral Drinfeld obstruction into a one-dimensional space where this coefficient determines everything (Theorem~\ref{thm:complete-strictification}).

\medskip
\noindent\textbf{The Vol~I engine.}\enspace
Volume~I is Koszul duality on a curve: the bar construction for chiral algebras on an algebraic curve~$X$, five main theorems proved at all genera.  Three structure theorems organise the representation-theoretic content.  \emph{Algebraicity}: $H(t)^2 = t^4 Q_L(t)$ for a computable quadratic~$Q_L$, so the shadow generating function is algebraic of degree~$2$.  \emph{Formality identification}: the shadow obstruction tower agrees with the $L_\infty$ formality obstruction tower at all arities (Theorem~\ref{thm:shadow-formality-identification}), and the discriminant $\Delta = 8\kappa S_4$ classifies algebras into four depth classes~(G/L/C/M).  \emph{Complementarity}: under perfectness and chain-level nondegeneracy (satisfied for all standard families; conditional in general), factorisation homology decomposes as $H^*(\overline{\mathcal{M}}_g, Z(A)) \simeq Q_g(A) \oplus Q_g(A^!)$, a Lagrangian splitting.

Volume~II lifts this from one to three dimensions.  The bar differential~$D_\cA$ is holomorphic factorisation on~$\C$; the deconcatenation coproduct~$\Delta$ is topological factorisation on~$\R$; together they form the $\mathrm{SC}^{\mathrm{ch,top}}$-coalgebra on $\FM_k(\C) \times \Conf_k^<(\R)$.  The modular characteristic~$\kappa(\cA)$ determines the curved Swiss-cheese deformation at genus $g \ge 1$, complementarity lifts to the bulk-boundary-line triangle, and the discriminant~$\Delta$ classifies the complexity of bulk-boundary pairs.  Classical Koszul duality is genus~$0$ on a point; Volume~I is genus~$g$ on a curve; Volume~II is the dimensional lift to $\C \times \R$.

\medskip
\noindent\textbf{The open primitive.}\enspace
The bar complex is not the fundamental object.  It is the coalgebraic shadow of a more primitive datum: the open/closed factorization dg-category~$\cC$ on the bordified curve $\widetilde{X}_D$ (\S\ref{subsec:categorical-open-sector}).  This category's objects are boundary conditions; its morphisms are open-string states.  A choice of vacuum boundary condition~$b$ produces a boundary algebra $A_b = \End_\cC(b)$ as a \emph{chart}; the bar complex $\barB(A_b)$ encodes how closed-sector operations twist the open-sector composition.  The Morita class of~$\cC$, not the quasi-isomorphism class of~$A_b$, is the invariant.  Three objects are independently constructed from~$\cC$ and must never be conflated:
\begin{itemize}[nosep]
\item the \emph{bar complex} $\barB(A_b)$ classifies twisting morphisms (couplings between~$A_b$ and its line-side/open-colour Koszul dual~$A^!_{\mathrm{line}}$);
\item the \emph{chiral derived center} $\cZ^{\mathrm{der}}_{\mathrm{ch}}(\cC) = \RHom_{\operatorname{Fun}(\cC,\cC)}(\operatorname{Id}, \operatorname{Id})$ is the universal bulk;
\item the \emph{line-side/open-colour Koszul dual algebra} $A^!_{\mathrm{line}} = D_{\operatorname{Ran}}(\barB(A_b))$, which later models the line side on the chirally Koszul locus.
\end{itemize}
Modularity is not an additional axiom.  It is trace plus clutching on the open sector: the cyclic trace on $\End(b)$ seeds a Calabi--Yau structure; the annulus identification $\int_{S^1} \cC \simeq \HH_\bullet(\cC)$ is the first modular shadow; and clutching (composing along shared boundary circles via nodal degeneration) raises genus.  The genus expansion of the modular Maurer--Cartan element $\Theta^{\mathrm{oc}}_\cA$ is the algebraic modular tower.  The shadow archetypes~G/L/C/M classify the $A_\infty$ complexity of the boundary algebra; in the gravitational examples below, each archetype determines a distinct perturbative structure.

\section*{II.\quad The Heisenberg on $\C \times \R$}

Take $\cA = \cH_k$: one generator $J$, one OPE
$J(z)\,J(w) \sim k/(z-w)^2$.  Two operators sit at $(z_1, t_1)$
and $(z_2, t_2)$ with $t_1 < t_2$.  Their bar element is
$[s^{-1}J \,|\, s^{-1}J]$.

\medskip
\noindent\textbf{The holomorphic direction.}\enspace
The bar differential is the codimension-$1$ boundary operator
on $\FM_k(\C)$, the Fulton--MacPherson compactification (the iterated real blowup of $\C^k$ along all diagonal strata).  For two insertions, the boundary stratum
$E_{12} \cong S^1$ of the FM blowup extracts the mode products
$J_{(n)}J$ from the OPE.  The only nonvanishing mode is
$J_{(1)}J = k$ (the level), so
\[
d_{\barB}[s^{-1}J \,|\, s^{-1}J]
\;=\;
J_{(1)}J
\;=\; k.
\]
There is no simple-pole OPE ($J_{(0)}J = 0$), so the bracket
component of the bar differential vanishes.  The entire
holomorphic interaction is the scalar~$k$.

\medskip
\noindent\textbf{The topological direction.}\enspace
Cut $\R$ at each possible gap:
\begin{align*}
\Delta[s^{-1}J \,|\, s^{-1}J]
&\;=\;
[\,] \otimes [s^{-1}J | s^{-1}J]
&&\text{(cut before $t_1$)}
\\
&\;+\;
[s^{-1}J] \otimes [s^{-1}J]
&&\text{(cut between $t_1$ and $t_2$)}
\\
&\;+\;
[s^{-1}J | s^{-1}J] \otimes [\,]
&&\text{(cut after $t_2$)}.
\end{align*}
The middle term places one field on each half-line.  Since
$J_{(0)}J = 0$, the two separated fields do not interact: the
factorisation is exact.

\medskip
\noindent\textbf{The coderivation property.}\enspace
Apply $D_\cA$ to the left-hand side:
$D_\cA[s^{-1}J | s^{-1}J] = k$, then
$\Delta(k) = k \otimes [\,] + [\,] \otimes k$.
Apply $(D_\cA \otimes \id + \id \otimes D_\cA)$ to the
right-hand side: only the first and third summands contribute
(the middle term $[s^{-1}J] \otimes [s^{-1}J]$ gives zero because
$D_\cA(s^{-1}J) = 0$), producing $[\,] \otimes k + k \otimes [\,]$.
Both sides agree.  The collision in $\C$ and the cut in $\R$
commute.

\medskip
\noindent\textbf{The configuration-space product.}\enspace
The holomorphic collision lives on $\FM_2(\C)$.  The topological
cut lives on $\Conf_2^<(\R) = \{t_1 < t_2\} \cong \R_{>0}$,
which is contractible.  The bar element
$[s^{-1}J | s^{-1}J]$ is parametrised by the product
\[
\FM_2(\C) \times \Conf_2^<(\R).
\]
The differential sees the first factor.  The coproduct sees the
second.  The coderivation property is the independence of the two
factors.  The same decomposition holds at degree~$n$:
\[
\bar{B}^{\mathrm{ch},n}(\cA)
\;\subset\;
(s^{-1}\bar\cA)^{\otimes n}
\otimes
\Omega^\bullet\bigl(\FM_n(\C) \times \Conf_n^<(\R)\bigr).
\]
The ordered configuration space $\Conf_n^<(\R) \cong
\R^{n-1}_{> 0}$ is contractible; its $E_1$-coalgebra structure
contracts to the strict coassociative coproduct: deconcatenation.
No higher homotopies survive.  The coproduct is the unique
coassociative coproduct on $T^c(V)$, determined by the
cogenerators.


\medskip\noindent\textbf{The Feynman transform and 't~Hooft expansion.}\enspace
Volume~I organises every genus-tower structure through the
convolution principle: the modular convolution dg Lie algebra
$\gAmod := \Hom_\Sigma(F\!\Com,\, \End_{\barB(\cA)})$
carries a dg Lie bracket from operadic composition, and
its universal MC element
$\Theta_\cA := D_\cA - d_0$ encodes the full genus tower.
For the Heisenberg algebra~$\cH_k$, the MC element collapses to a
scalar: $\Theta_{\cH_k} = k \cdot \eta \otimes \Lambda$, and its
genus-$g$ projection is
$F_g(\cH_k) = k \cdot \lambda_g^{\mathrm{FP}}$.
Here $F\!\Com$ is the Feynman transform of the commutative
modular operad $\Com$ (with $\Com(g,n) = \Bbbk$ for all stable $(g,n)$), generated by stable graphs with edge-contraction
differential ($D^2 = 0$ from $\partial^2 = 0$ on
$\overline{\cM}_{g,n}$).

The associative variant
${\gAmod}^{E_1} := \Hom_\Sigma(F\!\Ass,\,
\End_{\barB^{E_1}(\cA)})$
($F\!\Ass$ is the Feynman transform of the associative modular operad, summing over ribbon graphs rather than stable graphs)
replaces stable graphs by \emph{ribbon graphs} (fatgraphs)
and is the algebraic form of the 't~Hooft $1/N$ expansion:
$\hbar = 1/N^2$, the MC element is
$\sum_g N^{2-2g}F_g(\lambda)$, planar diagrams dominate at
large~$N$, and the coinvariant projection
$\Theta^{E_1} \mapsto \Theta$ is the passage from
colour-ordered to full amplitudes.

Volume~II promotes this to a two-coloured convolution algebra: $\SCchtop$ couples the $\C$-direction (Feynman transform, holomorphic) to the $\R$-direction (coproduct, topological), and the MC element $\alpha_T \in \MC(\gSC_T)$ encodes both.

\section*{III.\quad The Swiss-cheese operad $\SCchtop$}

The product $\FM_k(\C) \times \Conf_k^<(\R)$ is the space of
operations of a two-coloured operad:
\begin{alignat*}{2}
&\text{closed-to-closed:}\quad
&&\SCchtop(\mathsf{ch}^k;\,\mathsf{ch})
\;=\; \FM_k(\C), \\
&\text{mixed-to-open:}\quad
&&\SCchtop(\mathsf{ch}^k, \mathsf{top}^m;\,\mathsf{top})
\;=\; \FM_k(\C) \times E_1(m), \\
&\text{open-to-closed:}\quad
&&\SCchtop(\ldots, \mathsf{top}, \ldots;\,\mathsf{ch})
\;=\; \varnothing.
\end{alignat*}
Closed colour: holomorphic collisions on $\FM_k(\C)$.  Open colour:
topological orderings on $E_1(m) = \Conf_m^<(\R)$.  Mixed: the
product.  Composition is FM substitution on the closed factor,
interval insertion on the open factor.  The empty open-to-closed
component is directionality: bulk restricts to boundary, not
conversely.

The bar complex $(\barBch(\cA), D_\cA, \Delta)$ is a coalgebra
over $\SCchtop$: the differential is the closed-colour
co-structure and the coproduct is the open-colour co-structure.
By the Koszul self-duality $(\SCchtop)^! \simeq \SCchtop$
(Theorem~\ref{thm:SC-self-duality}), the open-colour dual
$\cA^!_{\mathrm{line}}$ inherits an $\SCchtop$-\emph{algebra}
structure.  The verification
for the Heisenberg algebra is the four-step proof of
Theorem~\ref{thm:rosetta-swiss-cheese}, carried out in the Rosetta
Stone chapter.

The closed colour is the Koszul direction (holomorphic collisions on~$\C$); the open colour is the groupoid direction (topological cuts along~$\R$).  Directionality (the empty open-to-closed component) reflects the Springer pattern: representations restrict from $G$ to $B$, not conversely.

\medskip
\noindent\textbf{Homotopy-Koszulity.}\enspace
The classical Swiss-cheese operad $\mathsf{SC}$ ($E_2$ closed, $E_1$ open) is Koszul (Livernet).
Kontsevich formality gives a quasi-isomorphism
$\SCchtop \xrightarrow{\sim} \mathsf{SC}$.  Bar-cobar transfer
gives
\[
\Omega\mathbf{B}(\SCchtop) \;\xrightarrow{\;\sim\;}\; \SCchtop.
\]
The bar-cobar adjunction on $\SCchtop$-algebras is a Quillen
equivalence (Theorem~\ref{thm:homotopy-Koszul}).


\section*{IV.\quad The $\Ainf$ chiral operations}

The bar differential $D_\cA$ is a coderivation of the cofree
coalgebra $T^c(s^{-1}\bar\cA)$.  A coderivation on a cofree
coalgebra is determined by its cogenerator projections: a sequence
of multilinear maps
\[
m_k \colon A^{\otimes k} \longrightarrow
A((\lambda_1))\cdots((\lambda_{k-1})),
\qquad k \ge 1, \quad \deg m_k = 2 - k,
\]
where $\lambda_j = z_j - z_k$ are spectral parameters recording
holomorphic separations.  The identity $D_\cA^2 = 0$ expands into
the $\Ainf$ Stasheff relations: for each~$n$,
\begin{equation}\label{eq:preface2-stasheff}
\sum_{\substack{i+j = n+1 \\ i,j \ge 1}}\;\sum_{s=0}^{n-j}
\pm\; m_i\bigl(a_1, \ldots, m_j(a_{s+1}, \ldots, a_{s+j}),
\ldots, a_n\bigr) \;=\; 0,
\end{equation}
with spectral substitution $\lambda_s \mapsto \Lambda_I$ when a
consecutive block $I$ collapses.

Each relation is Stokes' theorem on $\FM_n(\C)$.

\subsection*{The $n = 1$ relation: $m_1^2 = 0$}

$\FM_1(\C)$ is a single point (one marked point, no moduli).
There is no boundary: $\partial\FM_1(\C) = \varnothing$.
The Stasheff relation at $n = 1$ is
\[
m_1(m_1(a)) \;=\; 0,
\]
which says that $m_1 = Q$ is a differential: $Q^2 = 0$.  This is
the BRST condition.

\subsection*{The $n = 2$ relation: the Leibniz rule}

$\FM_2(\C) = \{(z_1, z_2) : z_1 \ne z_2\}/\text{translation}$
is a point (after modding out translation, the single parameter
$z_{12} = z_1 - z_2$ is fixed by scaling).  The boundary
$\partial\FM_2(\C)$ is a single collision stratum $z_1 = z_2$.
The Stasheff relation at $n = 2$ is
\[
m_1(m_2(a,b)) + m_2(m_1(a),b) +
(-1)^{|a|} m_2(a,m_1(b)) \;=\; 0,
\]
which says that $m_1 = Q$ is a graded derivation of~$m_2$:
the binary product is a chain map.

\subsection*{The $n = 3$ relation: the homotopy-associativity
identity}

The Stasheff associahedron $K_3$ is a closed interval $[0,1]$,
the quotient of $\FM_3(\C)$ by the affine group.
Its two endpoints are the two maximal collision strata:
$D_{12}=\{z_1\to z_2\}$ at one end,
$D_{23}=\{z_2\to z_3\}$ at the other.
The $n=3$ Stasheff relation is:
\[
\underbrace{m_2(m_2(a,b),c) - m_2(a,m_2(b,c))}_{%
  \text{associativity defect}}
\;=\;
\underbrace{m_1(m_3(a,b,c)) + m_3(m_1(a),b,c)
\pm\cdots}_{n=3:\;m_3\text{~interpolates}}\,.
\]
The binary product $m_2$ is associative up to the homotopy~$m_3$.
The operation $m_3$ is the integral over $K_3$:
it interpolates between the two boundary contributions
$D_{12}$ and $D_{23}$.  When $m_3=0$ (Heisenberg, free
fermion), the relation reduces to strict associativity.
When $m_3\ne 0$ (Kac--Moody, Virasoro), the
homotopy is the Jacobiator on $K_3$, and its nonvanishing is the failure of the OPE
to be strictly associative.

\subsection*{Higher $n$: the full Stasheff hierarchy}

At $n=4$, the associahedron $K_4$ is a compact polygon
(the Stasheff pentagon, five boundary faces from the
collision strata $D_{12}, D_{23}, D_{34}, D_{123}, D_{234}$).
The Stasheff relation involves $m_2$, $m_3$, and $m_4$,
with $m_4$ arising as the integral over the interior of~$K_4$.  For the
$\mathcal{W}_3$ algebra, $m_4$ is nonzero and encodes the quartic
contact interaction with coefficient $32/(22+5c)$.

The general pattern: $\FM_n(\C)/\mathrm{Aff}(\C)$, the Fulton--MacPherson
space modulo translations, dilations, and rotations (the full complex
affine group), is a compact manifold with corners of real
dimension $2n - 4$.%
\footnote{The quotient by the full complex affine group
$\mathrm{Aff}(\C) = \C \rtimes \C^*$ gives real dimension
$2(n{-}1) - 2 = 2n{-}4$.  See Remark~\ref{rem:FM-convention}
for the three FM conventions used in this volume
(full: $\dim_\R = 2(n{-}1)$; reduced: $\dim_\R = 2n{-}3$;
affine: $\dim_\R = 2n{-}4$).
The Stasheff associahedron $K_n$ is a real $(n{-}2)$-dimensional
submanifold-with-corners inside $\FM_n(\C)/\mathrm{Aff}(\C)$,
obtained by restricting to the real locus $\FM_n(\R)$.}
Its boundary strata are
indexed by all possible collision patterns (subsets
$S \subset \{1, \ldots, n\}$ with $|S| \ge 2$).  Each stratum
$D_S$ contributes $m_{|S|}$ composed with $m_{n - |S| + 1}$ to
the Stasheff relation, and $m_n$ itself is the integral over the
top stratum of $\FM_n(\C)$.  The identity $\partial^2 = 0$ on
$\FM_n(\C)$ (every codimension-$2$ face is reached by exactly two
codimension-$1$ faces with opposite orientations) is the
combinatorial identity that makes $D_\cA^2 = 0$ hold, and hence
that makes the $\Ainf$ Stasheff relations consistent.


\section*{IV$'$.\quad The resolvent principle}

The $\Ainf$ operations $\{m_k\}_{k \ge 2}$ are determined by
a single datum: the binary product~$m_2$.  The higher
operations are not independent; they are the unique solution
of a fixed-point equation.

Let $(H, p, \iota, h)$ be a deformation retract of $(\cA, m_1)$
onto cohomology $H = H^\bullet(\cA, m_1)$, with $h^2 = h\iota =
ph = 0$.  Define the \emph{dressed propagator}
$\Phi \colon T^+(H) \to \cA$ by
\[
\Phi \;=\; \iota \;+\; h \circ m_2 \circ \Phi^{\otimes 2}.
\]
This is the \emph{tree-level bootstrap equation}: the full
propagator from boundary data~$H$ into the bulk algebra~$\cA$ is
determined self-consistently from the free embedding~$\iota$ and
the interaction vertex~$m_2$.  Its solution by iteration gives the
\emph{tree formula}
(Theorem~\ref{thm:resolvent-principle}):
\[
m_k^H
\;=\;
\sum_{T \in \mathrm{PRT}_k}
p \circ \mathfrak{m}(T) \circ \iota^{\otimes k},
\]
where $\mathrm{PRT}_k$ is the set of planar rooted binary trees
with $k$~leaves ($|\mathrm{PRT}_k| = C_{k-1}$, the Catalan
number), and $\mathfrak{m}(T)$ assigns $m_2$ to each internal
vertex and~$h$ to each internal edge.

One equation, one binary input, Catalan many trees.  The
Stasheff relations are the consistency of the iteration;
the tree formula is the perturbative expansion of the Lagrangian self-intersection.  Wheel diagrams in the Virasoro
(Section~VII) are evaluations of this tree sum using the BRST
homotopy of Drinfeld--Sokolov reduction
(Theorem~\ref{thm:tree-wheel-bridge}).


\section*{V.\quad First computation: the Heisenberg}

\noindent\textbf{Operations.}\enspace
$m_1 = 0$ (no internal differential).
$m_2(J,J;\lambda) = k\lambda$ (the double-pole residue).
$m_k = 0$ for $k \ge 3$ (no composites, no higher poles).
The algebra is formal: the $\Ainf$ structure is concentrated in
$m_2$.

\smallskip
\noindent\textbf{PVA on cohomology.}\enspace
On $H^\bullet(\cH_k) = \C[J]$, the binary operation decomposes
into a commutative product and a $\lambda$-bracket:
\[
\{J_\lambda J\} = k\lambda.
\]
This is a Poisson vertex algebra (PVA) of rank~$1$: a commutative differential algebra equipped with a $\lambda$-bracket $\{a_\lambda b\}$ satisfying sesquilinearity, skew-symmetry, the Jacobi identity, and the Leibniz rule.

\smallskip
\noindent\textbf{Classical $r$-matrix.}\enspace
The bar complex uses the propagator $d\log(z-w)$, which absorbs one pole order from the OPE (AP19): the OPE $J(z)J(w)\sim k/(z-w)^2$ has a double pole, and the $d\log$ kernel reduces this to a simple-pole collision residue.  The classical $r$-matrix is therefore
\[
r(z) \;=\; \frac{k}{z}.
\]
(The formal Laplace transform of the full $\lambda$-bracket $\{J_\lambda J\}=k\lambda$ gives $\int_0^\infty e^{-\lambda z}k\lambda\,d\lambda = k/z^2$, which is the position-space OPE kernel; the collision residue is the simple-pole coefficient $k/z$ after the $d\log$ absorption, in the same way the Kac--Moody section below separates $\Omega/z$ from the $kK_\fg/z^2$ term.)
The quantum $R$-matrix: $R(z) = \exp(k\hbar/z)$.  Yang--Baxter
holds trivially (the exponential of a scalar commutes with itself).

\smallskip
\noindent\textbf{Line operators.}\enspace
The chiral Koszul dual is
$\cA^!_{\mathrm{ch}} = \mathrm{Sym}^{\mathrm{ch}}(V^*)$
(Vol~I, Theorem~\ref*{V1-thm:heisenberg-bar}; note
$\cH_k^!\neq\cH_{-k}$).  The open-colour dual is
$\cA^!_{\mathrm{line}} = Y(\mathfrak{u}(1)) \simeq \cH_{-k}$.
The line side is modeled by modules for
$\cA^!_{\mathrm{line}} = Y(\mathfrak{u}(1)) \simeq \cH_{-k}$:
the category of Fock spaces $\mathcal{F}_\mu$, $\mu \in \C$,
semisimple.  The braiding on Fock modules is
$R_{\mu_1,\mu_2}(z) = \exp(k\mu_1\mu_2\hbar/z)$.

\smallskip
\noindent\textbf{Bulk-boundary-line.}\enspace
$\Abulk \simeq \Zder(\cH_k) = \cH_k$ (abelian algebras are their
own derived centres).  The triangle collapses:
$\cH_k \simeq \Zder(\cH_k) \simeq \HH^\bullet_{\mathrm{ch}}(\cH_{-k})$
(the triangle degenerates because $\cH_k$ is commutative;
the chiral Hochschild corner is computed from the open-colour dual
$\cH_{-k} \simeq Y(\mathfrak{u}(1))$; note
$\cH_k^! = \Sym^{\mathrm{ch}}(V^*) \not\simeq \cH_{-k}$).

\smallskip
\noindent\textbf{Genus~$1$.}\enspace
The cyclic trace on the open sector, composed with the
clutching map on $\overline{\cM}_{1,1}$, produces the
curvature $d^2 = k \cdot \omega_1$: the bar differential
inherits a curved structure as the coalgebraic shadow of
this open-sector data.  The modular characteristic is
$\kappa(\cH_k) = k$: the same scalar that governs the OPE, the
$\lambda$-bracket, the $R$-matrix, and the genus-$1$ obstruction.
The free energy $F_1 = -k\log\eta(\tau)$.


\section*{VI.\quad Second computation: Kac--Moody at level~$k$}

\noindent\textbf{Operations.}\enspace
$m_1 = 0$.
$m_2(J^a, J^b;\lambda) = f^{ab}{}_c\,J^c + k\,\delta^{ab}\lambda$
(the simple-pole bracket plus the double-pole metric).
$m_3 \ne 0$: the Jacobiator homotopy on $\FM_3(\C)$.  The three
codimension-$1$ boundary strata of $\FM_3(\C)$ contribute the
three terms $m_2(m_2(-,-),-)$ in the Stasheff identity; their
failure to cancel is the Jacobi identity defect, corrected by
$m_3$.  Higher $m_k = 0$ by dimension counting on $\FM_k(\C)$.

\smallskip
\noindent\textbf{PVA on cohomology.}\enspace
$\{J^a_\lambda J^b\} = f^{ab}{}_c\,J^c + k\,\delta^{ab}\lambda$.
This is the affine PVA at level~$k$.

\smallskip
\noindent\textbf{Classical $r$-matrix.}\enspace
The Laplace transform of the $\lambda$-bracket gives the
position-space OPE kernel:
\[
\int_0^\infty e^{-\lambda z}\,
  \{J^a{}_\lambda J^b\}\, d\lambda
\;\otimes\; J_a \otimes J_b
\;=\;
\frac{f^{ab}{}_c\,J^c}{z}\,J_a\otimes J_b
  + \frac{k\,\delta^{ab}}{z^2}\,J_a\otimes J_b
\;=\;
\frac{\Omega}{z} + \frac{k\,K_{\fg}}{z^2},
\]
where $\Omega = \sum_a J^a \otimes J_a$ is the split Casimir
and $K_{\fg} = \sum_a J_a \otimes J^a$ is the Killing form
tensor.  The scalar term $k\,K_{\fg}/z^2$ is ad-invariant
and contributes trivially to the CYBE; the collision residue
r-matrix is $r(z) = \Omega/z$.
The KZ connection $\nabla_{\mathrm{KZ}} = d - \hbar\,\Omega/z$ is
the flat connection on conformal blocks on the affine Kac--Moody
comparison surface.  Its monodromy is compared by the affine
Drinfeld--Kohno theorem with the corresponding braided
tensor-product representation of $\mathrm{Rep}_q(\fg)$.
The CYBE for $r(z) =
\Omega/z$ is the Lie-algebra Jacobi identity for $\fg$: the
Arnold relation on $\FM_3(\C)$ applied to the affine OPE.

\smallskip
\noindent\textbf{Line operators.}\enspace
The two-coloured operad $\SCchtop$ has a chiral closed colour
and an $E_1$ open colour.  Koszul duality on each colour
produces two dual algebras.  The \emph{chiral} Koszul dual is
$\cA^!_{\mathrm{ch}} = \mathrm{CE}^{\mathrm{ch}}(\widehat{\fg}_{-k-2h^\vee})$
(the chiral CE algebra at the Feigin--Frenkel dual level;
same~$\kappa$, categorically distinct from $\widehat{\fg}_{-k-2h^\vee}$).
The \emph{open-colour} Koszul dual, the
$E_1$-projection of the $\SCchtop$-dual, is the dg-shifted
Yangian $\cA^!_{\mathrm{line}} = \Ydg(\fg)$
\textup{(}Theorem~\textup{\ref{thm:Koszul_dual_Yangian}}\textup{)}.
On the chirally Koszul locus, modules for $\Ydg(\fg)$ model
the line category
\textup{(}Theorem~\textup{\ref{thm:lines_as_modules}}\textup{)}.  On
the reduced evaluation comparison surface, Drinfeld--Kohno duality
identifies the reduced braided line category on evaluation modules
with the corresponding quantum group category.

\smallskip
\noindent\textbf{Genus~$1$.}\enspace
$\kappa(\widehat{\fg}_k) = \dim\fg\,(k + h^\vee)/(2h^\vee)$.
Feigin--Frenkel anti-symmetry for affine KM: $\kappa(\widehat{\fg}_k) + \kappa(\widehat{\fg}_{-k-2h^\vee}) = 0$.


\section*{VII.\quad Third computation: $\mathcal{W}_3$ and
the birth of nonlinearity}

The $\mathcal{W}_3$ algebra is the first family where the $\Ainf$
structure is genuinely infinite.

The $\mathcal{W}_3$ algebra has two generators: the stress tensor
$T$ of weight~$2$ and a spin-$3$ current $W$ of weight~$3$.  The
$TT$ OPE has poles up to order~$4$; the $TW$ OPE up to order~$3$;
and the $WW$ OPE up to order~$6$, with a composite field
\[
\Lambda \;=\; :(TT): \;-\; \tfrac{3}{10}\,\partial^2 T
\]
of weight~$4$ appearing at the second-order pole.  The coefficient
of $\Lambda$ in the $WW$ OPE is $32/(22 + 5c)$: it diverges at
$c = -22/5$ (the Lee--Yang value).

\smallskip
\noindent\textbf{Operations.}\enspace
$m_2(T,T;\lambda) = (c/12)\lambda^3 + 2T\lambda + \partial T$
(the full $TT$ OPE in divided-power parametrisation; the coefficient
$c/12$ matches the mode $T_{(3)}T = c/2$ via $\lambda^{(3)} = \lambda^3/6$).
The $WW$ operation is
\[
m_2(W,W;\lambda) \;=\; \frac{c}{360}\lambda^5
+ \frac{1}{3}T\lambda^3
+ \frac{1}{2}(\partial T)\lambda^2
+ \Bigl(\frac{3}{10}\partial^2 T + \frac{32}{22+5c}\,\Lambda\Bigr)\lambda
+ \frac{1}{15}\partial^3 T + \frac{16}{22+5c}\,\partial\Lambda,
\]
where $\Lambda = {}\!:(TT): - \tfrac{3}{10}\,\partial^2 T$ is the quasi-primary composite field (the first non-primitive operation).  $m_3 \ne 0$: the Jacobiator
homotopy, as for Kac--Moody, but now involving $\Lambda$.
$m_4 \ne 0$: the quartic operation from the $\FM_4(\C)$ boundary
decomposition, with the coefficient $32/(22+5c)$ entering through
the $WW\Lambda$ channel.  The $\Ainf$ structure does not terminate.

\smallskip
\noindent\textbf{PVA on cohomology.}\enspace
$\{T_\lambda T\} = (c/12)\lambda^3 + 2T\lambda + \partial T$
(Virasoro PVA in divided-power notation $\lambda^{(n)} = \lambda^n/n!$).
$\{T_\lambda W\} = 3W\lambda + \partial W$.
$\{W_\lambda W\}$ involves $\Lambda$ with coefficient
$32/(22+5c)$ and $\partial\Lambda$ with coefficient $16/(22+5c)$.  This is the Zamolodchikov $\mathcal{W}_3$ PVA\@.

\smallskip
\noindent\textbf{Cumulant coalgebra.}\enspace
The Koszul dual coalgebra cannot be built on $T^*, W^*$ alone:
$\Lambda$ forces a new cogenerator $\Lambda^*$ with nonprimitive
coproduct $\Delta(\Lambda^*) = T^* \otimes T^* -
(3/10)(\partial^2 T^* \otimes 1 + 1 \otimes \partial^2 T^*)$.
The completed dual is the cumulant coalgebra
$\operatorname{Cum}_c(\mathcal{W}_3) =
\hat{T}^c(sQ(\mathcal{W}_3)^\vee)$, with primitive generating
series $G_{\mathcal{W}_3}(t) = (t + t^2 + t^3)/(1 - t)$ and
completion entropy $h_K(\mathcal{W}_3) \approx 0.772$.
(The \emph{primitive generating series} $G_\cA(t) = \sum_n (\dim Q_n)\, t^n$
counts cogenerators of the dual coalgebra by conformal weight;
the \emph{completion entropy} $h_K = \lim_{n\to\infty} n^{-1}\log\dim Q_n$
measures exponential growth.)
This is
the first family where kinematic complexity exceeds the Virasoro
baseline.

\smallskip
\noindent\textbf{Line operators.}\enspace
Beyond the affine lineage, the line-side package for
$\mathcal{W}_3$ remains conjectural on the live manuscript surface
\textup{(}Remark~\textup{\ref{comp:line-op-w3}}\textup{)}.  One
expects a nonabelian dg-shifted $\mathcal{W}_3$-Yangian package
whose modules are expected to model the line category, with genuinely matrix-valued spectral
kernel and Yang--Baxter constraint.  Correspondingly, the
classical $r$-matrix $r(z)$ should encode the $\mathcal{W}_3$
Knizhnik--Zamolodchikov system for three-point conformal blocks.


\section*{VIII.\quad The 3d MC element $\alpha_T$}

Volume~I has one MC element $\Theta_\cA$ in the modular
convolution algebra $\gAmod$.  Volume~II has one MC element
$\alpha_T$ in the Swiss-cheese convolution algebra
\[
\gSC_T
\;:=\;
\Hom_\Sigma\!\bigl(
  \mathbf{B}(\SCchtop),\;
  \End_{(\Bbound,\,\cC_{\mathrm{line}})}
\bigr).
\]
Its $L_\infty$-brackets are indexed by two-coloured stable trees:
closed vertices labelled by $\FM_k(\C)$-chains, open vertices by
$E_1(m)$-chains, mixed vertices by products.  The MC element is
\[
\alpha_T
\;=\;
\sum_{\Gamma \in \mathsf{Gr}^{\mathrm{SC}}}
\frac{1}{|\operatorname{Aut}(\Gamma)|}
\;W_\Gamma \otimes \Phi_\Gamma^T
\;\in\;
\mc(\gSC_T).
\]
It exists because $\SCchtop$ is homotopy-Koszul
(Theorem~\ref{thm:homotopy-Koszul}), hence
$\mathbf{B}(\SCchtop)$ has $\partial^2 = 0$.

\medskip
\noindent\textbf{Six direct faces.}\enspace
The basic closed/open/mixed data of the volume are read directly
from $\alpha_T$; the corrected triangle and line-side package are
assembled later from these projected data together with the scoped
comparison theorems:
\begin{center}
\small
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{lll}
\textbf{Projection} & \textbf{Structure} & \textbf{Section} \\
\hline
closed face
  & boundary $\Ainf$ algebra $\Bbound$
  & \S\ref{sec:axioms-Ainfty-chiral} \\[2pt]
open face
  & open-colour line-sector operations on $\cC_{\mathrm{line}}$
  & \S\ref{sec:line_operators} \\[2pt]
arity-$(2,0)$ ordered collision projection
  & spectral $R$-matrix, YBE
  & \S\ref{sec:spectral_braiding} \\[2pt]
arity-$(1,1)$ mixed
  & bulk-to-boundary comparison map
  & \S\ref{sec:ht-bulk-boundary-line} \\[2pt]
closed genus expansion
  & $\Theta_\cA$ (Vol~I): Polyakov programme
  & \S\ref{subsec:rosetta-3d-mc} \\[2pt]
closed cohomology
  & PVA $\{-_\lambda-\}$
  & \S\ref{sec:PVA_descent}
\end{tabular}
\end{center}
\noindent
The scoped identifications
$\cC_{\mathrm{line}} \simeq \cA^!_{\mathrm{line}}\text{-mod}$ and
$\Abulk \simeq \Zder(\Bbound) \simeq
\HH^\bullet_{\mathrm{ch}}(\cA^!_{\mathrm{line}})$ are the later
comparison theorems that read off these direct faces.

\medskip
\noindent
Two further consequences of this projected data are proved in the
frontier chapters:
the complete strictification theorem
(Theorem~\ref{thm:complete-strictification}) shows that the open-face
spectral quasi-factorization package strictifies once constructed
(via root multiplicity one), and the general half-space BV
theorem (Theorem~\ref{thm:general-half-space-bv}) shows every PVA
with inner translation deformation-quantizes to a boundary vertex
algebra (via the doubling construction).

\medskip
\noindent\textbf{Colour decomposition.}\enspace
Write $\alpha_T = \alpha_T^{\mathrm{cl}}
+ \alpha_T^{\mathrm{mix}} + \alpha_T^{\mathrm{op}}$
by the colour grading of the Swiss-cheese operad.  The
MC equation decomposes:
\begin{alignat*}{2}
&\text{pure closed:}\quad
&&D\alpha_T^{\mathrm{cl}}
  + \tfrac{1}{2}[\alpha_T^{\mathrm{cl}},
  \alpha_T^{\mathrm{cl}}] = 0, \\
&\text{mixed:}\quad
&&d_{\Theta}\,\alpha_T^{\mathrm{mix}} = 0, \\
&\text{pure open:}\quad
&&d_{\Theta}\,\alpha_T^{\mathrm{op}}
  + [\alpha_T^{\mathrm{mix}},
  \alpha_T^{\mathrm{op}}]_{\mathrm{op}}
  + \tfrac{1}{2}[\alpha_T^{\mathrm{op}},
  \alpha_T^{\mathrm{op}}]_{\mathrm{op}}
  + \tfrac{1}{2}[\alpha_T^{\mathrm{mix}},
  \alpha_T^{\mathrm{mix}}]_{\mathrm{op}} = 0,
\end{alignat*}
where $d_\Theta = D + [\Theta_\cA, -]$ and
$\Theta_\cA := \alpha_T^{\mathrm{cl}}$.  The first is the Vol~I
MC equation.  The second says that the $r$-matrix and the PVA
bracket are $\Theta_\cA$-twisted cocycles.  The third is the full
open MC equation; in the examples below (genus~$0$), $\alpha_T^{\mathrm{op}} = 0$
and the equation reduces to $\tfrac{1}{2}[\alpha_T^{\mathrm{mix}},
\alpha_T^{\mathrm{mix}}]_{\mathrm{op}} = 0$, which closes the
line-category structure.

\medskip
\noindent\textbf{Heisenberg.}\enspace
$\gSC_T$ is abelian.
$\alpha_{\cH_k} = m_2 + \hbar\,k\cdot\eta\otimes\Lambda$.
No mixed contributions and trivial open-colour higher
operations.  All six projections:
$m_2(J,J;\lambda) = k\lambda$;
trivial open-colour line-sector operations on the Fock category;
$R(z) = e^{k\hbar/z}$;
$\Abulk = \cH_k$;
$\Theta_{\cH_k} = k\cdot\eta\otimes\Lambda$;
$\{J_\lambda J\} = k\lambda$.

\medskip
\noindent\textbf{3d gravity.}\enspace
$\alpha_{\mathrm{grav}} \in \mc(\gSC_T)$ for $SL(2,\R)$ CS at
$k = \ell/(4G)$ with $\Bbound = \mathrm{Vir}_{6k}$.  Closed face:
infinite-depth $\Ainf$ Virasoro.  Open face:
line-sector operations.  $R$-matrix: line-side genus-$0$ braiding.
Genus tower: after effective scalar specialization, the effective scalar genus tower,
read in the gravitational interpretation as the effective scalar perturbation series,
with $\kappa_{\mathrm{eff}} = 3k-13$ and quartic contact
$\mathfrak{Q} = 10/[6k(30k+22)]$.  Later expected realization:
the abstract line-side package modeled by modules for the
dual-central-charge Virasoro algebra, with Virasoro fusion kernel as the expected
braiding realization.
BTZ black holes are MC elements of the gravitational
Swiss-cheese algebra (Definition~\ref{def:btz-deformation}); the
two-sided Cardy asymptotic entropy formula expected from the
Virasoro bar-complex argument is conjectural for the BTZ states identified via
this MC dictionary, and the BTZ
Bekenstein--Hawking entropy formula would then follow from that
two-sided Cardy asymptotic entropy formula
(Theorems~\ref{prop:cardy-mc} and~\ref{thm:gravity-btz-entropy},
\S\ref{sec:3d-gravity}).
At $k = 13/3$ (equivalently $c = 26$), the chiral dual
$\mathrm{Vir}_{26-c} = \mathrm{Vir}_0$ is uncurved: the dual bar
complex is a genuine cochain complex, $\kappa(\mathrm{Vir}_0) = 0$.
This is the bosonic string critical dimension:
the unique value at which the effective scalar genus tower vanishes.


\section*{IX.\quad Koszulness}

The prototype for Koszul duality as convolution is the Chriss--Ginzburg theorem: the Borel--Moore homology of the Steinberg variety $\widetilde{\mathcal{N}} \times_{\fg} \widetilde{\mathcal{N}}$ (where $\widetilde{\mathcal{N}}$ is the Springer resolution of the nilpotent cone) recovers the Hecke algebra $H_W$ of the Weyl group:
$H_*^{BM}(\widetilde{\mathcal{N}} \times_{\fg}
\widetilde{\mathcal{N}}) \cong H_W$.  The Hecke algebra is
a convolution algebra.  The bar complex $\barBch(\cA)$ on
$\FM_n(\C) \times \Conf_n^{<}(\R)$ is the convolution algebra
of chiral algebra: the bar differential is convolution on
$\FM_n(\C)$; the coproduct is restriction on $\Conf_n^{<}(\R)$;
the six projections recover the algebra, the open-colour
line-sector operations, the $R$-matrix, the genus tower, and the
PVA\@; the later chirally-Koszul comparison theorems recover the
open-colour Koszul dual and line-category package.
For the affine lineage, on the reduced evaluation comparison surface
the convolution yields the corresponding quantum-group line package:
$\cC_{\mathrm{line}}^{\mathrm{red}}|_{\mathrm{eval}} \simeq
\mathrm{Rep}_q(\fg)$ and the colored Jones
polynomial is recovered from the monodromy of the twisting morphism classified by the bar complex (Theorem~\ref{thm:affine-monodromy-identification},
Corollary~\ref{cor:jones-polynomial}).

The parallel is precise:
\begin{center}
\small
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{ll}
\textbf{Chriss--Ginzburg} & \textbf{Chiral bar complex} \\ \hline
Springer resolution $\widetilde{\cN} \to \fg$
  & Lagrangian embedding $\cL_\cA \hookrightarrow \cM$ \\[2pt]
Steinberg $\widetilde{\cN} \times_{\fg} \widetilde{\cN}$
  & $\barBch(\cA)$ on $\FM_k(\C) \times \Conf_k^{<}(\R)$ \\[2pt]
Convolution $\Rightarrow$ Hecke algebra $H_W$
  & Convolution $\Rightarrow$ $\SCchtop$-algebra \\[2pt]
Springer correspondence
  & bulk-boundary-line triangle \\[2pt]
Kazhdan--Lusztig basis
  & MC element $\alpha_T$ \\[2pt]
$H_W$ at $q=1$ $\Rightarrow$ $\C[W]$
  & classical limit $\Rightarrow$ PVA \\[2pt]
KL polynomials
  & shadow obstruction tower \\[2pt]
Hecke eigenvalues
  & spectral $R$-matrix, Jones polynomial
\end{tabular}
\end{center}
\smallskip

\emph{Koszulness is the condition that this presentation is
concentrated.}

When the bar cohomology is concentrated ($H^{p,q}(\barBch(\cA)) = 0$
for $q \neq 0$), the derived self-intersection
$\Steinb_b := \barBch(\cA) \times_\cA^h \barBch(\cA)$
is \emph{clean}: the excess Tor sheaf
$\cT\!\mathit{or}^{\cO_\cA}_q(\cO_{\barBch},\cO_{\barBch})$
vanishes for $q \neq 0$, the Borel--Moore homology is
one-dimensional per stratum, and every invariant projects as
a scalar.  Non-concentration means excess $\cT\!\mathit{or}_q
\neq 0$: the self-intersection carries higher derived structure,
$\Ainf$ operations proliferate, and curvature and braiding
entangle.  In this sense, chiral Koszulness is a transversality
condition on the Lagrangian self-intersection.

\medskip\noindent\textbf{The bar complex as a bigraded object.}\enspace
The bar complex carries two gradings: the \emph{internal degree}
(cohomological degree within~$\cA$) and the \emph{bar degree}
(number of tensor factors minus one).  The bar differential mixes
these gradings: it lowers bar degree by one and raises internal degree
by the pole order of the OPE.  The resulting spectral sequence, the
PBW spectral sequence of Volume~I, converges to the bar cohomology.

\medskip\noindent\textbf{The concentration condition.}\enspace
The bar cohomology $H^{p,q}(\barBch(\cA))$ is bigraded.  Say $\cA$
is \emph{chirally Koszul} if this cohomology is concentrated in a
single line:
\[
H^{p,q}\bigl(\barBch(\cA)\bigr) = 0 \quad \text{for } q \neq 0.
\]
This is a single condition.  Twelve consequences follow from it,
each expressing a different face of the same concentration.

\medskip\noindent\textbf{The twelve faces.}\enspace
The Koszul dodecahedron of Volume~I:
\begin{enumerate}[label=\textup{(\roman*)},leftmargin=2em,itemsep=2pt]
\item \emph{Concentration}: bar cohomology in one degree.
\item \emph{Spectral collapse}: the PBW spectral sequence degenerates
  at~$E_2$.  The associated graded $\gr_F \cA$ is classically Koszul
  (Priddy--BGS), and the chiral deformation introduces no corrections.
\item \emph{Formality}: $m_k = 0$ for $k \geq 3$.  One-loop
  exactness: multi-particle collisions on $\FM_k(\C)$ contribute no
  new cohomology beyond what the pairwise OPE already sees.
\item \emph{Small resolution}: the bar-cobar counit
  $\Omegach(\barBch(\cA)) \xrightarrow{\sim} \cA$ is a
  quasi-isomorphism.  Verdier duality on the bar coalgebra gives
  $\mathbb{D}_{\operatorname{Ran}} \barBch(\cA) \simeq
  \barBch(\cA^!)$, identifying the Verdier dual with the bar of the
  Koszul dual algebra (Theorem~A intertwining).
  This identification exists unconditionally for every~$\cA$.  On
  the Koszul locus, $\barBch(\cA^!)$ is formal and its
  cohomology is~$\cA^!$.  Off the Koszul locus,
  $\barBch(\cA^!)$ carries genuinely higher operations.
\item \emph{Ext diagonal}: $\Ext^{p,q}_\cA(\omega_X, \omega_X) = 0$
  for $p \neq q$ (the BGS criterion chiralized).
\item \emph{Hochschild concentration}:
  $\mathrm{ChirHoch}^n(\cA) = 0$ for $n \notin \{0, 1, 2\}$.
\item \emph{Factorization homology}: $\int_{\Sigma_g}\cA$ is
  concentrated in degree~$0$ for all genera.
\item \emph{Barr--Beck--Lurie}: the comparison functor for the
  bar-cobar monad is an equivalence on the fiber; abstract monadicity
  applies without correction.
\item \emph{Kac--Shapovalov regularity}: $\det G_h \neq 0$ in the
  bar-relevant range; no singular vectors intrude to create unexpected
  cohomology classes.
\item \emph{Stratum acyclicity}: restriction to every FM boundary
  stratum is acyclic outside degree~$0$.
\item \emph{Lagrangian transversality}: the deformation and
  obstruction complexes $\mathbf{Q}_g(\cA)$ and $\mathbf{Q}_g(\cA^!)$
  are complementary Lagrangians
  (Vol~I, Theorem~C, the Lagrangian splitting of genus-$g$ cohomology) (conditional on perfectness).
\item \emph{Hodge purity}: $\barB_n(\cA)$ is pure of weight~$n$
  as a mixed Hodge module on $\FM_n(X)$ (forward implication proved; converse open).
\end{enumerate}

\medskip\noindent\textbf{Classical specialization.}\enspace
When $X = \mathrm{pt}$, the chiral tensor product is the ordinary
tensor product, the FM compactification is the Stasheff
associahedron, and all twelve criteria collapse to the classical
Koszul theory of Priddy--BGG--BGS--Loday--Vallette.  The chiral theory
is not a generalization that weakens these conditions; it is a
\emph{lift} that reveals their geometric origin.  Concentration of
bar cohomology, in the classical setting, is a combinatorial miracle.
In the chiral setting, it is a consequence of Arnold cancellation on
FM compactifications: the boundary strata produce exact sequences,
and Koszulness is the statement that these exact sequences have no
higher cohomology.  The ``miracle'' of classical Koszulness is the
shadow of the geometry of configuration spaces.

\medskip\noindent\textbf{The examples.}\enspace
The Heisenberg is Koszul: $m_k = 0$ for $k \geq 3$
(Section~V), the PBW spectral sequence has nothing to degenerate, and
$\cH_k^! = \mathrm{Sym}^{\mathrm{ch}}(V^*)$ is the chiral symmetric algebra on the dual.

Kac--Moody $\widehat{\fg}_k$ is Koszul: the PBW spectral sequence
degenerates at~$E_2$ because $\gr_F \widehat{\fg}_k$ is a polynomial
algebra (classically Koszul by Priddy).  The chiral Koszul dual is
$\widehat{\fg}_{-k-2h^\vee}$ at the dual level.  The big and small
resolutions agree.

Virasoro is chirally Koszul (the PBW spectral sequence concentrates),
but its Swiss-cheese structure is \emph{non-formal}: the transferred
operations $m_k^{\mathrm{SC}} \neq 0$ for all $k \geq 3$
(Section~VII).  The big resolution $(\mathrm{Vir}_c)^!_\infty$
carries the infinite tower of wheel diagrams; the small Koszul dual
$\mathrm{Vir}_{26-c}$ is its shadow.  The boundary between formal
and non-formal Swiss-cheese structure is controlled by the pole
structure of the OPE: simple poles give formal algebras;
higher-order poles create the higher operations that break formality.

Shadow depth and Koszulness are independent invariants.
Four depth classes emerge, each with a distinct
self-intersection geometry
(\S\ref{sec:shadow-tower-atlas}): class~$\mathbf{G}$ (clean
intersection, depth~$2$: Heisenberg, lattice VOAs),
class~$\mathbf{L}$ (Lie-transverse, depth~$3$: affine
$V_k(\fg)$, where Jacobi kills the quartic),
class~$\mathbf{C}$ (contact, depth~$4$: $\beta\gamma$), and
class~$\mathbf{M}$ (infinite excess tower, depth~$\infty$:
Virasoro, $\cW_N$).  Depth classifies the \emph{complexity}
of the shadow obstruction tower, not the existence of a small resolution.

\medskip\noindent\textbf{Homotopy-Koszulity of the operad.}\enspace
Individual algebras may have formal or non-formal
Swiss-cheese structure; the \emph{operad} $\SCchtop$
is always homotopy-Koszul.  Homotopy-Koszulity
(Theorem~\ref{thm:homotopy-Koszul}) means the bar-cobar adjunction
on $\SCchtop$-algebras is a Quillen equivalence, a property of the
categorical framework, not of any particular algebra.  It is what
makes the bar-cobar framework of the 3d MC element~$\alpha_T$ of
Section~VIII available: $\mathbf{B}(\SCchtop)$ has $\partial^2 = 0$
because the operad is homotopy-Koszul, while the six projections of
$\alpha_T$ are the closed/open/mixed/cohomological/genus components
coming from the colour and arity decomposition of the Swiss-cheese
convolution algebra
\textup{(}Definition~\textup{\ref{def:sc-convolution}},
Theorem~\textup{\ref{thm:3d-universal-mc}}\textup{)}.

\medskip\noindent\textbf{Koszulness and PVA descent.}\enspace
On the Koszul locus (formality, criterion~(iii)), the cohomological
shadow of the bar complex is exact: the higher $m_k$ vanish on
cohomology, and $m_2$ alone survives.  The PVA structure of the
next section is therefore the \emph{complete} invariant of a Koszul
chiral algebra at the cohomological level.  For algebras with
non-formal Swiss-cheese structure
(Virasoro, $\mathcal{W}_3$), the PVA is only the leading term of an
infinite tower: the higher transferred $m_k$ carry information that the PVA does
not see, and quantization must lift the entire tower, not just the
PVA bracket.


\section*{X.\quad PVA descent}
\label{sec:preface-pva-descent}

The $\Ainf$ operations $m_k$ are chain-level data.  On cohomology
$H^\bullet(A, Q)$, the higher operations $m_{k \ge 3}$ vanish (they
are $Q$-exact by the homotopy-transfer argument: each $m_k$ for
$k \ge 3$ arises from a chain of dimension $\ge 1$ on
$\Conf_k^<(\R)$, which is contractible, so $m_k = Q(h_{k-1})$).
The binary operation $m_2$ survives, because its topological chain
is zero-dimensional (a point in a contractible space).

\subsection*{Decomposition of $m_2$}

The surviving operation $m_2(a,b;\zeta)$ is a formal Laurent
series in the collision variable $\zeta = z_1 - z_2$.  Decompose
it into regular and singular parts:
\[
m_2(a,b;\zeta)
\;=\;
\underbrace{m_2^{\mathrm{reg}}(a,b;\zeta)}_{\text{no pole at }
\zeta = 0}
\;+\;
\underbrace{\sum_{n \ge 0}
\frac{a_{(n)}b}{\zeta^{n+1}}}_{\text{singular: the OPE}}.
\]
The regular part at $\zeta = 0$ is a commutative product:
\[
\mu(a,b) \;:=\; m_2^{\mathrm{reg}}(a,b;0).
\]
The singular part, after Borel transform
$\zeta^{-n-1} \mapsto \lambda^{(n)}$, is a $\lambda$-bracket:
\[
\{a_\lambda b\}
\;:=\;
\sum_{n \ge 0} \lambda^{(n)}\,a_{(n)}b.
\]
Together, $(\mu, \{-_\lambda-\})$ form a $(-1)$-shifted Poisson
vertex algebra on $H^\bullet(A, Q)$.

\subsection*{The Jacobi identity from $\FM_3(\C)$}

The Jacobi identity for $\{-_\lambda-\}$ is the $n = 3$ Stasheff
relation~\eqref{eq:preface2-stasheff} projected to cohomology.
Write it out.  The boundary $\partial\FM_3(\C)$ has three
codimension-$1$ strata:
\[
D_{12} \;=\; \{z_1 \to z_2\}, \qquad
D_{23} \;=\; \{z_2 \to z_3\}, \qquad
D_{13} \;=\; \{z_1 \to z_3\}.
\]
Each stratum contributes one term to the $n = 3$ relation:
\begin{align*}
D_{12}&\colon\quad
m_2\bigl(m_2(a,b;\zeta_{12}),\, c;\, \zeta_{13}\bigr),
\\
D_{23}&\colon\quad
m_2\bigl(a,\, m_2(b,c;\zeta_{23});\, \zeta_{12}\bigr),
\\
D_{13}&\colon\quad
-\,m_2\bigl(m_2(a,c;\zeta_{13}),\, b;\, \zeta_{12}\bigr).
\end{align*}
The sign on $D_{13}$ is the Koszul sign from permuting $b$ past
$c$.  Stokes' theorem on $\FM_3(\C)$ gives
$\sum_S \Res_{D_S}(\omega) = 0$ for any closed form~$\omega$ on
$\FM_3(\C)$.  Applying this to the form built from three OPE
kernels, projecting to the singular parts, and performing the Borel
transform gives the $\lambda$-bracket Jacobi identity:
\[
\{a_\lambda \{b_\mu c\}\}
\;-\;
\{b_\mu \{a_\lambda c\}\}
\;=\;
\{\{a_\lambda b\}_{\lambda+\mu} c\}.
\]
The three boundary faces of $\FM_3(\C)$ are three collisions; the
cancellation of the integral over $\partial\FM_3(\C)$ is the Jacobi
identity.

\subsection*{Leibniz, skew-symmetry, and the exchange cylinder}

Leibniz ($\{a_\lambda bc\} = \{a_\lambda b\}c +
b\{a_\lambda c\}$) follows from the factorisation of the regular
part~$\mu$ through the collision stratum: when $z_2 \to z_3$ while
$z_1$ is far away, $m_2(a, m_2^{\mathrm{reg}}(b,c;0); \zeta_{12})$
decomposes into the product of $m_2(a,b;\zeta_{12})$ with~$c$ plus
$b$ with $m_2(a,c;\zeta_{13})$.

Skew-symmetry ($\{a_\lambda b\} = -\{b_{-\lambda-\partial} a\}$)
follows from the exchange automorphism $z_1 \leftrightarrow z_2$ on
$\FM_2(\C)$, which reverses the sign of $\zeta = z_1 - z_2$ and
introduces $\partial$ from the Jacobian of the coordinate change.

\subsection*{Heisenberg and Kac--Moody PVAs}

For the Heisenberg: $m_2^{\mathrm{reg}} = 0$ (no regular part), so
$\mu = 0$ and the product is trivial.  The $\lambda$-bracket
$\{J_\lambda J\} = k\lambda$ is the entire PVA\@.  Jacobi is
vacuous (single generator).

For Kac--Moody: $m_2^{\mathrm{reg}}(J^a, J^b; 0) = 0$
(the structure constants are in the singular part).  The
$\lambda$-bracket $\{J^a_\lambda J^b\} = f^{ab}{}_c J^c +
k\delta^{ab}\lambda$ recovers the affine PVA\@.  Jacobi is the
Lie-algebra Jacobi identity for $f^{ab}{}_c$, which is the
residue of the three-face Stokes identity on $\FM_3(\C)$ evaluated
on the $\widehat{\fg}_k$ OPE.


\section*{XI.\quad The bulk-boundary-line triangle}

A holomorphic--topological theory on $\C \times \R_{\ge 0}$
produces three algebraic objects: \emph{bulk} local operators
$\Abulk$ (interior), \emph{boundary} local operators $\Bbound$
(on $\{0\} \times \C$), and \emph{line} operators
$\cC_{\mathrm{line}}$ (along $\R_+ \times \{0\}$).
On the chirally Koszul locus (boundary-linear exact sector),
the strongest relation is
\[
\Abulk
\;\simeq\;
\Zder(\Bbound),
\qquad
\cC_{\mathrm{line}} \simeq \cA^!_{\mathrm{line}}\text{-mod}.
\]
Unconditionally, bulk identifies with chiral Hochschild cochains
of the boundary algebra; the derived-center and line-module
identifications retain the scoped hypotheses above.

The \emph{bulk} vertex is the derived centre
$\Zder(\Bbound) :=
\RHom_{\Bbound\text{-}\Bbound}(\Bbound,\,\Bbound)$.
For Heisenberg: $\Zder(\cH_k) = \cH_k$.
For Kac--Moody at $k = -h^\vee$: the Feigin--Frenkel centre
$\mathfrak{z}(\widehat{\fg}_{-h^\vee})$, the algebra of
${}^L\!G$-opers.  The \emph{line} vertex is modeled, on the
chirally Koszul locus, by $\cA^!_{\mathrm{line}}$-modules:
for Heisenberg,
$\cA^!_{\mathrm{line}} = Y(\mathfrak{u}(1)) \simeq \cH_{-k}$
(Fock modules); for Kac--Moody,
$\cA^!_{\mathrm{line}} = \Ydg(\fg)$.

\subsection*{The spectral $R$-matrix and Yang--Baxter}

Two parallel line operators $L_1(z_1)$, $L_2(z_2)$ interact
through the bulk, producing a meromorphic braiding
$R_{L_1, L_2}(z) \colon L_1 \otimes L_2
\xrightarrow{\sim}
L_2 \otimes L_1$ in $z = z_1 - z_2$.
Three lines braided in two orders give the Yang--Baxter equation
\[
R_{12}(z_{12})\,R_{13}(z_{13})\,R_{23}(z_{23})
\;=\;
R_{23}(z_{23})\,R_{13}(z_{13})\,R_{12}(z_{12}),
\]
from the same $\FM_3(\C)$ Stokes mechanism as the PVA Jacobi
identity: the Jacobi identity is the closed-colour projection,
the Yang--Baxter equation is the open-colour projection, and both
are shadows of the single $n = 3$ Stasheff identity.

For Heisenberg: $R(z) = e^{k\hbar/z}$ (scalar, YBE trivial).
For Kac--Moody on evaluation modules: the spectral $R(z)$ agrees
with the quantum group $R$-matrix of $U_\hbar(\fg)$, with
$r(z) = \Omega/z$ as classical limit.


\section*{XII.\quad PVA quantization: from classical to quantum}

The PVA $\{-_\lambda -\}$ on $H^\bullet(A, Q)$ is the
\emph{classical} shadow of the $\Ainf$ chiral algebra.  The
quantization problem is the reverse: given a PVA, construct the
$\Ainf$ chiral algebra whose cohomological descent recovers it.

This is a lifting problem.  Start with a PVA
$(V, \cdot, \{-_\lambda -\})$.  The bracket
$\{a_\lambda b\} = \sum_{n \ge 0} \lambda^{(n)}\,a_{(n)}b$
encodes the singular part of the OPE; the product
$a \cdot b$ encodes the regular part.  Together they determine
$m_2(a,b;\lambda)$ on cohomology.  The quantization question: does
there exist an $\Ainf$ chiral algebra~$A$ with $H^\bullet(A) = V$
and $m_2|_{H^\bullet} = \mu + \{-_\lambda-\}$?

The obstruction to lifting lives in the Hochschild cohomology
$\HH^3_{\mathrm{ch}}(V)$: the failure of the Jacobi identity
to hold at the chain level, after the first-order lift, is a
cocycle whose class is the obstruction.  When the obstruction
vanishes, the lift exists and is controlled by a Maurer--Cartan
element in the chiral deformation complex
$\mathrm{Def}_{\mathrm{ch}}(V)$.

\medskip
\noindent\textbf{The Heisenberg quantization.}\enspace
The PVA $\{J_\lambda J\} = k\lambda$ lifts to the Heisenberg
$\cH_k$ with $m_2(J,J;\lambda) = k\lambda$ and $m_{k \ge 3} = 0$.
The obstruction vanishes: $\HH^3_{\mathrm{ch}}(\C[J]) = 0$
(the PVA is freely generated).  The quantization is unique.

\medskip
\noindent\textbf{The Kac--Moody quantization.}\enspace
The affine PVA $\{J^a_\lambda J^b\} = f^{ab}{}_c J^c +
k\delta^{ab}\lambda$ lifts to $\widehat{\fg}_k$.  The
obstruction at $m_3$ is the Jacobiator, which is a coboundary:
$m_3$ exists and equals the homotopy-transfer correction from
$\FM_3(\C)$.  Higher obstructions vanish by dimension.

\medskip
\noindent\textbf{The Khan--Zeng 3d sigma model.}\enspace
Khan--Zeng construct a 3d holomorphic--topological sigma model
from any PVA target.  The boundary algebra of this theory is the
quantization of the PVA\@.  The BV-BRST complex of the sigma model
on $\C \times \R_{\ge 0}$, with the PVA as target space data,
produces the $\Ainf$ operations $m_k$ by Feynman-diagram evaluation
on $\FM_k(\C)$: each $k$-ary operation is a sum of tree Feynman
diagrams with $k$ external legs and vertices weighted by the PVA
brackets.  The Stasheff identity is Stokes' theorem on the boundary
of the Feynman-integral compactification, the same boundary
cancellation that gives the $\Ainf$ relations from $\FM_k(\C)$.

This sigma-model construction gives the physical proof of the
quantization theorem: the PVA data determine a consistent field
theory, and the field theory produces the $\Ainf$ algebra.  The
mathematical proof is the bar-cobar correspondence of
Theorem~\ref{thm:homotopy-Koszul}: the Quillen equivalence
guarantees essential uniqueness of the $\SCchtop$-algebra structure,
hence the quantization is unique up to contractible ambiguity.


\section*{XIII.\quad The holographic programme}

A three-dimensional holomorphic--topological quantum field theory on
$\R_+ \times \C$ has bulk observables on the interior, boundary
observables on $\{0\} \times \C$, and line operators along
$\R_+ \times \{0\}$.

\subsection*{Twisted holography}

A 3d $\cN = 2$ gauge theory on $\R_+ \times \C$ with gauge
group~$G$ and Chern--Simons level~$k$ has an action
\[
S \;=\; \frac{k}{4\pi}\int_{\R_+ \times \C}
\operatorname{tr}\!\bigl(
  A \wedge dA + \tfrac{2}{3} A \wedge A \wedge A
\bigr)
\;+\; S_{\mathrm{matter}}.
\]
The holomorphic--topological twist selects the supercharge
$Q = Q_+ + \bar{Q}_-$, under which the gauge field decomposes into
a holomorphic component $A_z \,dz$ and a topological component
$A_t \,dt$.  The BV-BRST complex of the twisted theory on
$\R_+ \times \C$ has:
\begin{itemize}
\item \emph{Boundary observables} ($t = 0$): the chiral algebra
  $\Bbound$ on~$\C$.  For pure CS: $\Bbound = \widehat{\fg}_k$.
\item \emph{Bulk observables} ($t > 0$): the BRST cohomology
  $\Abulk = H^\bullet(C_{\mathrm{BV}}, Q)$.
  By Kontsevich's theorem on holomorphic factorisation of CS,
  $\Abulk \simeq \Zder(\Bbound)$.
\item \emph{Line operators} ($z = 0$, extended along~$\R_+$):
  modules for the open-colour Koszul dual
  $\cA^!_{\mathrm{line}}$.  For pure CS at level~$k$, the
  chiral dual is $\cA^!_{\mathrm{ch}} =
  \mathrm{CE}^{\mathrm{ch}}(\widehat{\fg}_{-k-2h^\vee})$, while on the
  chirally Koszul affine locus the open-colour dual is the
  dg-shifted Yangian $\cA^!_{\mathrm{line}} = \Ydg(\fg)$,
  whose modules model the line category.
\end{itemize}
On evaluation modules in the affine lineage, the reduced HT
spectral $R$-matrix agrees with the quantum group $R$-matrix of
$U_q(\fg)$ at $q = e^{i\pi/(k+h^\vee)}$
\textup{(}Theorem~\textup{\ref{thm:affine-monodromy-identification}}\textup{)}.
Thus the line category is modeled by modules for the
open-colour Koszul dual, which on the chirally Koszul affine
locus carries the dg-shifted Yangian package.

\medskip
\noindent\textbf{Abelian Chern--Simons.}\enspace
$G = U(1)$: $\Bbound = \cH_k$,
$R(z) = e^{k\hbar/z}$, $\Abulk = \cH_k$ (bulk $\simeq$ boundary);
$\alpha_T = m_2 + \hbar k \eta \otimes \Lambda$.

\medskip
\noindent\textbf{Non-abelian Chern--Simons.}\enspace
$G = SL_2$: $\Bbound = \widehat{\mathfrak{sl}}_2{}_k$,
$\cA^!_{\mathrm{ch}} = \widehat{\mathfrak{sl}}_2{}_{-k-4}$,
$r(z) = \Omega/z$.  The KZ connection is the arity-$(2,0)$
projection of $\alpha_T$; on evaluation modules, the reduced HT
spectral $R$-matrix agrees with the quantum-group $R$-matrix of
$U_q(\mathfrak{sl}_2)$ at $q = e^{i\pi/(k+2)}$
(Theorem~\ref{thm:affine-monodromy-identification}).
Line category:
$\cC_{\mathrm{line}}^{\mathrm{red}}|_{\mathrm{eval}}
\simeq \operatorname{Rep}_q(\mathfrak{sl}_2)$
(Corollary~\ref{cor:affine-line-category});
colored Jones polynomial from bar-complex monodromy
(Corollary~\ref{cor:jones-polynomial}).

\subsection*{The holographic modular Koszul datum}

The 3d MC element $\alpha_T$ packages the holographic system:
closed sector $\to$ boundary algebra $\Bbound$ with genus tower
$\Theta_{\Bbound}$; open sector $\to$ line operations on
$\cC_{\mathrm{line}}$ (modeled by
$\cA^!_{\mathrm{line}}$-modules on the chirally Koszul locus);
mixed sector $\to$ spectral braiding $R(z)$.  The
holographic modular Koszul datum
\[
\mathcal{H}(T)
\;=\;
(\Bbound,\; \cA^!_{\mathrm{line}},\; \cC_{\mathrm{line}},\; R(z),\;
\Theta_{\Bbound},\; \nabla^{\mathrm{hol}})
\]
packages these faces with the scoped comparison identifications.

\subsection*{Anomaly completion}

At genus $g \ge 1$, the bar differential is curved:
$d^2 = \kappa(\Bbound) \cdot \omega_g$ (Vol~I, Theorem~D).
The \emph{transgression algebra} absorbs this curvature: adjoin
$\eta$ of degree~$1$ with
$\eta b = (-1)^{|b|} b \eta$ and
$d\eta = \Theta = \kappa \cdot \omega_1$, keeping $\eta^2 \ne 0$:
\[
B_\Theta
\;=\;
\frac{B * \Bbbk\langle\eta\rangle}
{\langle \eta b - (-1)^{|b|} b\eta \rangle},
\qquad
d\eta = \Theta.
\]
A $B_\Theta$-module is a $B$-module with a neutralisation
$h_M$ satisfying $dh_M = \Theta_M$: the datum extending
genus-$0$ structure through genus~$1$.

The \emph{secondary anomaly} $u := \eta^2$ is closed and central,
governing the genus-$g$ Clifford completion
$\mathfrak G_g(B_\Theta)$ (handle operators with
$\alpha_i \beta_j + \beta_j \alpha_i = \delta_{ij} \eta^2$,
encoding the intersection form on $H_1(\Sigma_g, \Z)$):
\begin{itemize}
\item Away from $u = 0$:
  $\mathfrak G_g[\eta^{-2}]
  \cong \operatorname{Mat}_{2^g}(B_\Theta[\eta^{-2}])$;
  higher genus is \emph{Morita-invisible}.
\item On $u = 0$:
  $\mathfrak G_g / (\eta^2) \cong B/(\Theta) \otimes
  \Lambda^{2g+1}$; exterior-algebra degeneration.
\end{itemize}
The full development is in
Chapter~\ref{app:anomaly-completed-topological-holography}.

\subsection*{3d gravity as $SL(2,\R)$ Chern--Simons}

Three-dimensional gravity with $\Lambda = -1/\ell^2$ is
$SL(2,\R) \times SL(2,\R)$ Chern--Simons at levels
$k_\pm = \ell/(4G)$ (Achucarro--Townsend, Witten), with
$A_\pm^a = \omega^a \pm e^a/\ell$.  The HT twist selects
one chiral half: $\Bbound = \mathrm{Vir}_c$ at
$c = 6k = 3\ell/(2G)$ (Brown--Henneaux).
The $\Ainf$ structure has
$m_2(T,T;\lambda) = (c/12)\lambda^3 + 2T\lambda + \partial T$ and
$m_k \ne 0$ for all $k \ge 3$: infinite-depth, the mixed
archetype of Vol~I.

\medskip
\noindent\textbf{Koszul dual and line operators.}\enspace
$\cA^!_{\mathrm{ch}} = \mathrm{Vir}_{26-c}$ (Vol~I); no separate
Yangian reduction.  Self-duality at $c = 13$.
The expected line category is $\mathrm{Vir}_{26-c}$-mod, with the
Virasoro fusion kernel (the $6j$-symbol on $V_{h_1} \otimes
V_{h_2}$) as the spectral braiding $R(z)$: semiclassically
($c \to \infty$), it reduces to the Liouville reflection
coefficient, encoding graviton exchange between conical
defects, with YBE as three-graviton consistency.
For $c \gg 1$, $c' = 26 - c \ll 0$: the algebraic
manifestation of UV/IR mixing in AdS$_3$/CFT$_2$.

\medskip
\noindent\textbf{The shadow obstruction tower.}\enspace
The Virasoro shadow obstruction tower reads as the perturbative expansion of
3d gravity.  Arity~$2$:
$\kappa_{\mathrm{eff}} = (c-26)/2$ (one-loop anomaly).
Arity~$4$: the quartic contact
\[
\mathfrak{Q}^{\mathrm{contact}}_{\mathrm{Vir}}
\;=\; \frac{10}{c(5c+22)}
\]
(four-graviton interaction; poles at $c = 0$ and $c = -22/5$).
The tower does not truncate ($o_5 \ne 0$): 3d gravity has
infinitely many quantum corrections.  Completion entropy
$h_K(\mathrm{Vir}) \approx 0.567$.

\medskip
\noindent\textbf{The gravity MC element.}\enspace
The specialization
$\alpha_{\mathrm{grav}} = \sum_g \hbar^g
\alpha_{\mathrm{grav}}^{(g)}$ reads as the gravitational loop
expansion: genus~$0$ = tree-level, genus~$1$ = one-loop
(Brown--Henneaux), genus~$2$ = two-loop (first modular
interference).  The restriction
$\alpha_{\mathrm{grav}}|_{\text{closed}} =
\Theta_{\mathrm{Vir}_c}$ recovers the Vol~I Virasoro MC element.

\medskip
\noindent\textbf{BTZ black holes.}\enspace
A BTZ black hole of mass~$M$, angular momentum~$J$ is an MC element
$\alpha_{\mathrm{BTZ}} \in \mc(\gSC_T)$
(Definition~\ref{def:btz-deformation}), with boundary data
$|h,\bar h\rangle$, $h = (M\ell + J)/2$, $\bar h = (M\ell - J)/2$.
The conjectured Cardy entropy
$S_{\mathrm{Cardy}} = 2\pi\sqrt{ch/6} +
2\pi\sqrt{c\bar h/6}$ yields the BTZ Bekenstein--Hawking formula
(Theorems~\ref{prop:cardy-mc} and~\ref{thm:gravity-btz-entropy}).


\subsection*{The M2-brane example}

$T = $ M2-brane worldvolume on $\R_+ \times \C$:
$\Bbound = A_{M2,\infty}$ (11d supergravity twist, Costello),
freely generated at rank~$K$ with $K^2(w{+}1)$ generators at
weight~$w{+}1$; $\kappa(A_{M2,\infty})$ open.  The six
projections:
\begin{center}
\small
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{ll}
closed face & $A_{M2,\infty}$; $\kappa$ open \\
open face & perturbative M2 line-sector operations \\
$R$-matrix & M2-brane exchange kernel \\
bulk-to-boundary & $\Zder(A_{M2,\infty})$
  (11d supergravity observables) \\
genus face & $\Theta_{M2} = \kappa_{M2}\,\eta\otimes\Lambda
  + \cdots$; $\kappa_{M2}$ open \\
PVA & classical M2 Poisson vertex algebra
\end{tabular}
\end{center}

\subsection*{Celestial holography}

The celestial programme (Costello--Paquette, Costello--Gaiotto)
translates 4d scattering amplitudes into correlators of a
celestial chiral algebra $\Bbound^{\mathrm{cel}}$ on $\C P^1$,
obtained as the boundary algebra of a 4d $\to$ 3d reduction.
In the bar-cobar framework, celestial OPE data are the disk-level
boundary MC package; the celestial $R$-matrix is the spectral
braiding from the arity-$(2,0)$ projection of $\alpha_T$; in the
scattering reading, $r(z) \sim S_0/z + S_1/z^2 + \cdots$ with
$S_0$ the Weinberg soft factor and $S_1$ subleading; and
collinear splitting is the OPE coefficient extracted from the
collision residue defining the bar differential.

\subsection*{The deformation-quantization bridge}

The deformation-quantization bridge
$Q_{\mathrm{HT}}$ lifts the classical PVA on $H^\bullet(\Bbound)$
to the quantum $\Ainf$ algebra via
PVA $\to$ 3d HT sigma model (Khan--Zeng) $\to$ BV-BRST
quantization $\to$ $\Ainf$ operations on $\FM_k(\C)$.
For Virasoro, the first obstruction is the quartic contact class
$\mathfrak{Q}^{\mathrm{contact}}_{\mathrm{Vir}} =
10/[c(5c+22)]$: it diverges at $c = 0$ and $c = -22/5$, so the
bridge has genuine obstructions controlled by the shadow obstruction
tower.  For $\mathcal{W}_3$, the shadow obstruction tower never terminates:
quantization requires all $m_k$, with the cumulant coalgebra
$\operatorname{Cum}_c(\mathcal{W}_3)$ as kinematic substrate.

\medskip
\noindent\textbf{The half-space BV theorem.}\enspace
For affine CS on $\R_+ \times \C$, the BV complex is
quasi-isomorphic to $\barBch(\widehat{\fg}_k)$
(Chapter~\ref{chap:affine-half-space-bv}).
The BV master equation on the half-space is the modular quantum MC
equation $D\alpha_T + \tfrac{1}{2}[\alpha_T, \alpha_T] +
\hbar\Delta\alpha_T = 0$.

\subsection*{From the half-space to the doubling principle}

The affine theorem localises on wheel graphs (linear Poisson).
For nonlinear PVAs (Virasoro, $\mathcal{W}_3$), the
\emph{doubling theorem} (Theorem~\ref{thm:doubling-rwi})
embeds the half-space into its double
$X_{\mathrm{dbl}} = \C \times \R$ via the reflection
$\sigma(z,t) = (\bar z, -t)$:
\[
K^{\mathrm{refl}}(p_i, p_j)
\;=\;
K(p_i, p_j) \;+\; K(p_i, \sigma(p_j)).
\]
The reflected weight identity is the $\sigma$-invariant
restriction of the whole-space Stokes identity.  The chain:
\[
\text{logarithmic $\SCchtop$-algebra}
\;\xRightarrow{\;\text{double}\;}
\text{reflected weight identity}
\;\xRightarrow{\;\text{operadic Stokes}\;}
\text{boundary vertex algebra}.
\]
Every logarithmic $\SCchtop$-algebra has a boundary vertex algebra
(Theorem~\ref{thm:general-half-space-bv}).  The affine PV sigma
model is logarithmic $\SCchtop$
(Proposition~\ref{prop:affine-is-log-SC}); DS reduction preserves
the structure (Proposition~\ref{prop:ds-preserves-SC}), so
Virasoro and $\mathcal{W}_N$ inherit it
(Corollary~\ref{cor:virasoro-wn-are-SC}).  The standard landscape
enters through these two gates: the affine bridge and DS transport.

\subsection*{The modular bar complex as genus expansion}

The modular bar differential
$D = \sum_{g \ge 0} \hbar^g D^{(g)}$, with $D^{(g)}$
a sum over stable graphs of genus~$g$ weighted by
$1/|\Aut(\Gamma)|$, is the Feynman transform of the modular
operad $\{\overline{\cM}_{g,n}\}$
(Theorem~\ref{thm:formal-genus-expansion}).  The canonical MC
element $\delta_\cA = D - D^{(0)}$ is the total positive-genus
correction (Theorem~\ref{thm:coderivation-MC-synthesis}).  The
combinatorial structure (stable graphs, automorphism weights,
genus bookkeeping) is identical to the 't~Hooft expansion; the
identification $\hbar = 1/N$ for a specific holographic model
is the principal open problem
(Conjecture~\ref{conj:thooft-gl-n}).

\subsection*{The relative holographic deformation complex}

The holographic lifting problem is controlled by the
\emph{relative fibre}
$\mathfrak{h}_T^\Theta :=
\operatorname{fib}(p\colon\gSC_T\to\gAmod)_{\alpha_T}$
over the fixed boundary datum $\Theta_\cA$.  Its graded
obstruction tower
$[\mathfrak{o}_r(\beta)] \in
H^2(\operatorname{gr}^F_r\mathfrak{h}_T^\Theta)$
measures the order-by-order cost of extension; the
\emph{relative quartic obstruction}
$\mathfrak{R}_4^{\mathrm{rel}}(T)$ is the first invariant
detecting when a boundary datum cannot arise from a
planar holographic realisation.

\subsection*{Cyclic trace and the literal 't~Hooft
expansion}

A cyclic trace~$\tau$ on the open $E_1$-colour turns
each vertex into a ribbon vertex (Vol~I, Cyclicity-Ribbon
Theorem); a matrix realisation
$O_N = \operatorname{Mat}_N \otimes O_0$ gives every face
a factor of~$N$, yielding the literal 't~Hooft weight
$N^{V-E+F} = N^{\chi(\Sigma_\Gamma)}$ (Vol~I, Exact
$N^{\chi}$-Weighting Theorem).  The correct language is
\emph{ribbonised modular Swiss-cheese theory}
$\mathrm{RSC}_{\mathrm{tr}}^{\mathrm{mod}}$.
The \emph{Loop--Connes principle}
$\beta_{\mathrm{der}}^*(\Delta_{\mathrm{open}}) = \Lambda_P$:
closed genus creation is the image of the open BV operator
under the derived-centre map.

\subsection*{The arithmetic face (Vol~I)}

The shadow obstruction tower carries arithmetic content through the flat
\emph{arithmetic packet connection}
$\nabla_\cA^{\mathrm{arith}} = d - \bigoplus_\chi
d\log\Lambda_\chi(s)\cdot(\mathrm{id}+N_\chi)\,ds$,
with semisimple skeleton
$\mathrm{Ask}(\cA) = M_\cA^{\mathrm{ss}}$ and
unipotent defect module
$\mathrm{Def}_{\mathrm{alg}}(\cA) = \bigoplus_\chi
N_\chi M_\chi$.  The principle: \emph{arithmetic is
semisimple; homotopy defect is unipotent.}
Three closure conjectures form a strict hierarchy:
quartic closure $\Rightarrow$ Beilinson functional
$\Rightarrow$ clutching closure.

\section*{XIV.\quad The fourteen theorems}

\begin{enumerate}[label=\textbf{(\Alph*$'$)},leftmargin=2.5em]
\item \textbf{Swiss-cheese identification}
  (Theorem~\ref{thm:rosetta-swiss-cheese}).
  $(\barBch(\cA), D_\cA, \Delta)$ is an $\SCchtop$-coalgebra:
  the bar differential is the closed-colour co-structure, the
  deconcatenation coproduct is the open-colour structure.

\item \textbf{Homotopy-Koszulity and Quillen equivalence}
  (Theorem~\ref{thm:homotopy-Koszul}).
  $\Omega\mathbf{B}(\SCchtop) \xrightarrow{\sim} \SCchtop$
  via Kontsevich formality + classical Swiss-cheese Koszulity
  + bar-cobar transfer.  The bar-cobar adjunction on
  $\SCchtop$-algebras is a Quillen equivalence.

\item \textbf{PVA descent} (Theorem~\ref{thm:cohomology_PVA}).
  The regular/singular decomposition of $m_2$ on cohomology
  produces a $(-1)$-shifted PVA\@.  Jacobi from three-face Stokes
  on $\FM_3(\C)$.  Leibniz from regular-part factorisation.
  Skew-symmetry from the exchange automorphism.  Axioms D2--D6
  proved unconditionally.

\item \textbf{Yang--Baxter equation} (Theorem~\ref{thm:YBE}).
  The spectral braiding $R(z)$ on line operators satisfies YBE
  from $W$-coherence on $\FM_3(\C)$: the two orderings of three
  lines match because both codimension-$1$ degenerations of
  $\FM_3(\C)$ contribute and cancel.

\item \textbf{Bulk-boundary-line triangle}
  (Theorems~\ref{thm:boundary-linear-bulk-boundary}
  and~\ref{thm:lines_as_modules}).
  $\Abulk \simeq \Zder(\Bbound)$ (bulk $=$ derived centre of
  boundary).  On the chirally Koszul locus,
  $\cC_{\mathrm{line}} \simeq
  \cA^!_{\mathrm{line}}\text{-mod}$
  (lines modeled by Koszul-dual modules).  Explicit for Heisenberg
  (abelian Fock sector) and Kac--Moody (Feigin--Frenkel centre);
  for Virasoro, the corresponding dual-central-charge module
  realization is expected rather than isolated as a theorem on the
  live surface.

\item \textbf{3d universal MC element}
  (Theorem~\ref{thm:3d-universal-mc}).
  $\alpha_T \in \mc(\gSC_T)$ exists with two-coloured graph
  expansion.  Six projections recover (A$'$)--(E$'$) and the
  PVA\@.  Restriction:
  $\alpha_T|_{\text{closed, all genera}} = \Theta_\cA$ (Vol~I).

\item \textbf{3d gravity}
  (Theorem~\ref{thm:gravity-mc}).
  $SL(2,\R)$ CS at level $k = \ell/(4G)$:
  $\Bbound = \mathrm{Vir}_{6k}$,
  $\kappa_{\mathrm{eff}} = 3k-13$, quartic contact
  $\mathfrak{Q} = 10/[6k(30k+22)]$.
  BTZ black holes are MC elements
  (Definition~\ref{def:btz-deformation});
  conjectured Cardy entropy yields BTZ Bekenstein--Hawking
  (Theorems~\ref{prop:cardy-mc}
  and~\ref{thm:gravity-btz-entropy}).
  Expected: line category modeled by
  $\mathrm{Vir}_{26-6k}$-modules with fusion-kernel braiding.
\item \textbf{Half-space BV quantization}
  (Theorems~\ref{thm:affine-half-space-bv}
  and~\ref{thm:general-half-space-bv}).
  The affine PV sigma model is a logarithmic
  $\SCchtop$-algebra
  (Proposition~\ref{prop:affine-is-log-SC}); the doubling
  theorem (Theorem~\ref{thm:doubling-rwi}) proves the reflected
  weight identity for every admissible graph; the boundary
  factorization algebra is a vertex algebra.  The affine case is
  one-loop exact with level shift $K_{\mathrm{eff}} = k +
  \hbar\delta_\fg$; DS reduction transports to Virasoro and
  $\cW_3$ (Corollary~\ref{cor:virasoro-wn-are-SC}).

\item \textbf{Bulk--boundary--line factorization}
  (Theorem~\ref{thm:bulk-boundary-line-factorization}).
  For any logarithmic $\SCchtop$-algebra: bulk observables form
  an $\SCchtop$-factorization algebra on $\C \times \R$;
  bulk $\simeq$ chiral Hochschild cochains; the stronger
  derived-center identification is proved in the boundary-linear
  exact sector; the line side is modeled by
  $\cA^!_{\mathrm{line}}$-modules on the chirally Koszul locus;
  spectral $R$-matrix from the
  Laplace-transformed $\lambda$-bracket.  Five components,
  one scoped theorem.

\item \textbf{Modular PVA quantization}
  (Theorem~\ref{thm:modular-bar}).
  The stable-graph modular bar coalgebra carries a filtered
  deformation dg~Lie algebra whose MC elements lift the genus-$0$
  PVA to all genera.  The genus expansion $D = \sum_g \hbar^g
  D^{(g)}$ is the Feynman transform of the modular operad
  (Theorem~\ref{thm:formal-genus-expansion}); the canonical MC
  element $\delta_\cA = D - D^{(0)}$ lives in the positive-genus
  filtration.  Universal obstruction $\mathrm{Ob}_g$ at
  genus~$g$; variational modular class $[\varpi_g(\cA)]$.
  The planted-forest $L_\infty$ model on $\FM_3(X)$ isolates the
  first nonlinear obstruction.

\item \textbf{Anomaly-completed Koszul duality}
  (Section~\ref{app:anomaly-completed-topological-holography};
  strict algebraic core proved here, physical/topological
  interpretation formulated as
  Principle~\ref{prin:tholog-anomaly-completed-topological-holography}).
  Transgression algebra $B_\Theta$ with secondary anomaly
  $u = \eta^2$; genus-$g$ Clifford completion
  $\mathfrak{G}_g(B_\Theta)$.  Away from $u = 0$: higher genus is
  Morita-invisible.  On $u = 0$: exterior $H_1(\Sigma_g)$.

\item \textbf{Logarithmic HT monodromy}
  (Theorem~\ref{thm:synthesis}).
  Shifted KZ connection extends to an $\Ainf$ superconnection;
  monodromy is the spectral $R$-matrix.  HT associator satisfies
  the pentagon identity; genus tower realised as analytic object.

\item \textbf{Yang--Mills boundary theory}
  (Sections~\ref{sec:ym-boundary-bar-cobar-core}
  and~\ref{sec:ym-instanton-ir-frontier}).
  Instanton-completed visible centre; screening Laplacian.
  A conditional mass-gap problem is reduced to a screening
  domination estimate.

\item \textbf{Celestial holography.}
  KK reduction $6$d $\to$ $3$d produces celestial chiral algebra;
  disk-level celestial OPE data are encoded by the boundary
  Maurer--Cartan package; in the celestial scattering reading, the
  leading spectral terms of $r(z)$ are expected to reproduce the
  corresponding soft factors.
\end{enumerate}

\noindent
Results (A$'$)--(F$'$) and (H$'$)--(L$'$) are unconditional at the level of the framework.
Within (E$'$) and (I$'$), the stronger derived-center and line-module
identifications carry internal scope: the derived-center identification in (I$'$) is proved
in the boundary-linear exact sector; the line-module identification in (E$'$) holds on the
chirally Koszul locus.  The opening claims of both (bulk $\simeq$ chiral Hochschild cochains;
$R(z)$ from the Laplace-transformed $\lambda$-bracket) are unconditional.
Result (G$'$) applies the framework to $SL(2,\R)$ CS via the
bridge theorem (Theorem~\ref{thm:physics-bridge}).
Results (M$'$) and (N$'$) contain conditional or conjectural components as noted in their entries.

\medskip\noindent\textbf{Holographic reconstruction.}\enspace
The chiral algebra fixes a formal local bulk model.  The Hochschild--Kostant--Rosenberg theorem for
Lagrangian embeddings gives $\HH^\bullet(\cA) \simeq \cO(T^*[-1]\cL_\cA)$, and the
shifted cotangent $T^*[-1]\cL_\cA$ is the underlying local model on which the
formal Darboux theorem builds a $(-2)$-shifted ambient once a compatible closed
$1$-form is supplied (Theorem~\ref{thm:holographic-reconstruction}).  The boundary
therefore determines the shifted-cotangent side of the bulk, while the full formal
ambient still requires the Darboux datum that identifies the actual bulk geometry.

\medskip\noindent\textbf{The completeness question.}\enspace
The holographic Lagrangian $\cL_\cA^{(0)}$ is constructed from genus-$0$ data: the bar-cobar adjunction on $\FM_k(\C) \times \Conf_k^<(\R)$.  \emph{Is the holographic Lagrangian complete?}  That is, does $\cL_\cA^{(0)}$ extend to a family $\cL_\cA^{(g)} \hookrightarrow \cM_{\mathrm{vac}}$ over the moduli $\overline{\cM}_{g,n}$, compatible with gluing along boundary strata?  For algebras with clean self-intersection (classes~$\mathbf{G}$ and~$\mathbf{L}$: Heisenberg, Kac--Moody), the Lagrangian meets itself transversally and the scalar curvature $d^2 = \kappa(\cA) \cdot \omega_g$ extends automatically.  For algebras with excess self-intersection (class~$\mathbf{M}$: Virasoro, $\mathcal{W}_N$), the derived self-intersection carries higher Tor, the Swiss-cheese operations $m_k$ are successive obstruction classes, and the question is open.  (Classes~$\mathbf{G}$, $\mathbf{L}$, $\mathbf{C}$ are chirally Koszul: the PBW spectral sequence concentrates and bar cohomology is concentrated.  Class~$\mathbf{M}$ has infinite shadow depth and non-formal Swiss-cheese structure: the fourth-order pole forces nonvanishing transferred operations $m_k^{\mathrm{SC}} \neq 0$ for all $k \geq 3$, though the algebra is chirally Koszul (PBW spectral sequence concentrates) and well-defined Koszul duals exist.  The difficulty for class~$\mathbf{M}$ is the non-formality: beyond the global self-intersection geometry, the big Koszul dual carries higher $\Ainf$ operations.)  This is the central problem of the modular programme: existence, compatibility, and computation of the higher-genus Lagrangian, developed in Part~V.

\medskip\noindent\textbf{The factorization foundation.}\enspace
Three chapters develop the factorization-primary hierarchy that
underlies the operadic treatment.  The factorization Swiss-cheese
chapter (Chapter~\ref{ch:factorization-swiss-cheese}) constructs
the Swiss-cheese structure directly from factorizable
$\mathcal D$-modules on $\operatorname{Ran}(\Sigma_g) \times
\operatorname{Ran}(\mathbb R)$, without passing through operads.
The modular Swiss-cheese operad chapter
(Chapter~\ref{ch:modular-sc-operad}) constructs the local operadic
model $\mathsf{SC}^{\mathrm{ch,top}}_{\mathrm{mod}}$ and proves
modular homotopy-Koszulity, honest about its scope: the curvature
$\dfib^{\,2} = \kappa \cdot \omega_g$ is invisible to the local
model.  The relative Feynman transform chapter
(Chapter~\ref{ch:relative-feynman}) identifies the algebraic
skeleton $\mathrm{FT}_{\mathsf{Com}_{\mathrm{mod}} /
\mathsf{SC}^{\mathrm{ch,top}}}$ shared by both approaches and
proves its homotopy-involutivity.

\medskip\noindent\textbf{The single open problem.}\enspace
Everything proved in both volumes begins from finite-order projections
of the universal MC element $\Theta_\cA$ (Volume~I) and its $3$d lift
$\alpha_T$ (this volume), together with the later comparison theorems
that assemble those projections into the corrected bulk-boundary-line
and modular packages.  Everything open is a question about the
ambient geometry of the modular convolution algebra
$\mathfrak{g}^{\mathrm{mod}}_\cA$: the $(-2)$-shifted symplectic
stack $\cM_{\mathrm{vac}}(\cA)$ in which the chiral algebra sits as a
Lagrangian.  Volume~I computes the Taylor jets of the Lagrangian
embedding.  This volume identifies the local composition law.  What
remains is the global geometry of the stack: the derived symplectic
structure, the moduli of Lagrangians, and the higher-categorical
descent.  The completeness question (does the genus-$0$ Lagrangian
$\cL_\cA^{(0)}$ extend over $\overline{\cM}_{g,n}$?) is the
central problem, and the shadow depth classification (classes
$\mathbf{G}$/$\mathbf{L}$/$\mathbf{C}$ extend automatically;
class~$\mathbf{M}$ carries higher-Tor obstructions from non-formal
Swiss-cheese structure) is the structural answer this volume provides.

\medskip\noindent\textbf{The monograph computes the local geometry at a point.}\enspace
A chiral algebra is a point in the derived symplectic category: a Lagrangian
$\cL_\cA \hookrightarrow \cM_{\mathrm{vac}}(\cA)$.  The bar complex is the
endomorphism algebra of this point.  The Swiss-cheese operad is the local
composition law.  Koszul duality is the symplectic complement.  The genus tower
is the deformation of the point over the moduli of curves.  The holographic
principle is the theorem that a point determines its ambient.  The entire content
of both volumes is the local geometry of the derived symplectic category at this
single point.  Volume~III asks how this local geometry arises from
Calabi--Yau categories: for $d = 2$ the cyclic $\Ainf$-structure of
a CY category determines an $E_2$-chiral algebra whose shadow
obstruction tower is the automorphic correction of a generalized
Borcherds--Kac--Moody superalgebra; for $K3 \times E$ the
denominator identity is the Igusa cusp form~$\Delta_5$, with modular
characteristic $\kappa = 3 = \dim_\C$ and a shadow--Siegel gap
theorem identifying four structural obstructions.
