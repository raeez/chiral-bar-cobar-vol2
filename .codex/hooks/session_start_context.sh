#!/bin/bash

jq -n '{
  "hookSpecificOutput": {
    "hookEventName": "SessionStart",
    "additionalContext": "Vol II repo mode: mathematical truth lives on the live surface (`main.tex`, active inputs, dirty diff, build/test logs, compute/tests). Use repo skills under `.agents/skills` for rectification, verification, propagation, build triage, and frontier work; use hooks as guardrails, not as substitutes for proof."
  }
}'
