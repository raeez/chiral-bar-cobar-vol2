"""compute package.

Marker file so that `from compute.lib.xxx import ...` resolves. This file
is new (Vol II historically used lib.xxx via conftest path manipulation).
Both import styles now work; existing code is unaffected.
"""
