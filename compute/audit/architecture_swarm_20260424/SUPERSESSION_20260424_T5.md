# 2026-04-24 T5 Supersession

This note supersedes the T5-related downgrade recommendations in the
2026-04-24 architecture swarm reports.

The reports remain useful as adversarial audit artifacts, but their
recommendation to keep the principal `W_N` ladder for `N >= 4` and the
generic `W_infty` endpoint conditional on an unproved higher-spin
antighost axiom is no longer the live mathematical status.

Live status:

- `thm:casimir-antighost-commutativity` proves antighost
  BRST-commutativity for principal Casimir `W_N` towers and for generic
  `W_infty[mu]` in bounded conformal-weight windows.
- `prop:closed-normal-ordered-antighost-homotopies` gives the finite
  raw normal-ordered homotopies `H^(n,m)` by the Euler Koszul
  contraction and locally nilpotent screened perturbation series.
- T5 remains an abstract axiom only for arbitrary non-Casimir
  higher-spin stress towers.

Therefore, do not use `agent05_topologisation_ladder.md`,
`agent12_register_status.md`, or `SYNTHESIS.md` to demote the principal
Casimir `W_N` or generic `W_infty[mu]` endpoint after the 2026-04-24
T5 repair.
